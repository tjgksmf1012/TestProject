# AGENTS.md — 이 저장소에서 일하는 법

TeamFlow AI. 회의에서 정한 일이 칸반과 GitHub 을 거쳐 기여 기록까지
이어지게 하는 도구. 제품 설명은 `docs/00-이-프로그램은-무엇인가.md`.

---

## 빌드·검증 (이것부터)

```bash
# 백엔드 — 150초쯤 걸립니다
.venv/bin/python -m pytest backend/tests -q
.venv/bin/ruff check backend

# 프런트 — 설치 없이 돕니다
npm --prefix frontend test
npm --prefix frontend run typecheck
npm --prefix frontend run build      # build:demo + build:css + 데스크톱 셸

# 리디자인 SPA (webapp/, /app 마운트) — 이쪽은 설치가 필요합니다
npm --prefix webapp run typecheck
npm --prefix webapp run build        # dist/ 를 만들고, dist 는 커밋합니다

# 데스크톱 셸 (Electron) — 서버를 먼저 띄워야 합니다
TEAMFLOW_SERVER_URL=http://127.0.0.1:8811/home.html \
  npm --prefix frontend run desktop

# 화면 스모크 — 서버를 **스스로** 띄우고 21화면을 보통·고대비로 걷습니다
npm --prefix frontend run smoke:screens
#   A 화면이 서로 다른 화면으로 열리는가 · B 5xx·콘솔 오류
#   C 잘려서 안 보이는 것  · D 고대비에서 뜻이 뭉개지는가
#   ⚠️ 못 보는 것: 눌러야 나오는 것 · 글이 참인가 · 확대/큰 글자/터치 ·
#      키보드 · 양이 늘 때  (자를 만들었으면 못 보는 것도 적습니다)
#   `SMOKE_DEBUG=1` 로 자가 무엇을 보고 있는지 찍습니다

# 화면을 눈으로 보려면 (아래 §"디자인은 눈으로" 참고)
bash <스크래치패드>/serve.sh          # uvicorn 8811, demo.db
```

⚠️ **이 일곱을 push·PR 마다 CI 가 돕니다** (`.github/workflows/checks.yml`).
오래도록 CI 가 없어서 「초록입니다」가 전부 **누가 돌리고 그렇게 말한 것**
이었습니다 — `docs/24` 「처음 보는 사람의 눈으로」.

⚠️ **복합 명령에서 `cd frontend` 를 쓰지 마십시오.** 이 환경은 cwd 가
남아서 다음 명령이 엉뚱한 곳에서 돕니다. `npm --prefix frontend` 나
절대 경로를 쓰세요. 이 실수를 세 번 했고, 한 번은 pytest 가 실행조차
안 됐는데 "통과" 라고 커밋 메시지에 적었습니다.

---

## 절대 어기면 안 되는 것 — 기여도 불변식

이 제품은 사람의 기여를 다룹니다. 잘못 만들면 팀을 망가뜨립니다.
네 가지는 **테스트가 지키고 있고**, 어기면 빌드가 깨집니다.

1. **순위·리더보드 금지.** 목록은 이름 순. 점수 순 정렬 금지.
   ⚠️ 값을 **같은 축 위에 세로로** 그리는 것도 금지입니다 — 막대그래프는
   곧 순위표입니다. 이 규칙을 두 번 어겼습니다(기여도 막대의 절대 위치 ·
   카테고리 막대). **값은 글자로, 그림은 폭이나 개수만.**
2. **단일 점수 금지.** 구간 + 신뢰도 + 사유 + 근거 건수.
3. **측정 불가 ≠ 0점.** 못 잰 것은 모르는 것입니다.
   → 화면에서 **결측은 빨강이 아니라 흙빛(`--gap`)** 입니다. 빨강은
   "네가 뭘 잘못했다" 로 읽힙니다.
4. **시스템은 판정하지 않습니다.** 계산값은 참고값이고, 팀이 합의해
   확정하며, 다르게 정했다면 이유를 함께 남깁니다.

---

## 이 저장소가 반복해서 당한 실패 셋

### ① 만들어 놓고 아무도 안 부름

함수·토큰·API 필드를 만들고 화면에 잇지 않는 것. 오류가 안 나서 안
보입니다. `guards.test.ts` 가 `lib/` 의 export 를 화면이 실제로
부르는지 봅니다.

### ② 두 벌이 있으면 한쪽만 고쳐진다

같은 판단이 두 곳에 있으면 반드시 갈라집니다. 두 번째로 필요해지는
순간 공용으로 올리세요 (`app.css` · `lib/`).

### ③ 할 일을 알려 주고 그 일을 할 자리를 안 줌

"트랙이 온전한지 확인하세요" 라고 보내 놓고 확인할 화면이 없던 것.
"근거 #5" 라고 적어 놓고 원문을 볼 방법이 없던 것.

---

## ⚠️ 측정 방법이 틀리면 헛다리를 짚는다

**새 검사 도구를 쓸 때마다 그 도구부터 틀렸습니다.** 실제 사례:

- 표면 색을 대비비로 잰 것 (대비비는 방향이 없습니다)
- 다크 모드 주장을 밝은 모드 값으로 잰 것
- CSS 가드가 첫 미디어 쿼리만 본 것
- 화면 파일을 세는 곳이 `.ts` 로 하드코딩돼 `.tsx` 를 못 본 것
- 위반을 **심는 방법**이 틀려서 "0건" 이 나온 것 (두 번: 선택자가
  없는 곳에 심음 · 한글 이름이 `\w` 에 안 걸림)
- 심은 것을 **되돌리지 못했는데 확인을 안 한 것** — `git checkout` 이
  추적 안 되는 파일에서 pathspec 오류로 **명령 전체를 중단**했고, 두 번째
  심기부터는 아무것도 재고 있지 않았습니다
- **되돌리기가 「고침」까지 지운 것** — 같은 `git checkout`, 반대 방향입니다.
  심은 것을 되돌리려고 파일을 통째로 HEAD 로 되돌렸는데 그 파일에 **아직
  커밋 안 한 이번 고침**이 들어 있었습니다. 심은 것과 함께 고침도 사라져
  **뒤이어 심은 둘은 아무것도 재고 있지 않았습니다**(결함 284).
  ⚠️ **커밋 전에는 `git checkout` 으로 되돌리지 마십시오** — 심은 것만
  정확히 되돌리고, 되돌린 **뒤에 `grep` 으로 고침이 살아 있는지** 보십시오
- `color-mix()` 를 `getComputedStyle` 로 읽은 것 — Chromium 은
  `color(srgb 0.46 0.35 0.22)` 라는 **0~1 실수**로 답합니다. 0~255 로 읽으면
  전부 검정에 가깝게 잡힙니다. 캔버스에 칠해 픽셀을 읽으십시오
- Playwright 의 `locale` 로 날짜 표기를 잰 것 — 그 옵션은
  `navigator.language` 만 바꾸고, `<input type="date">` 는 **브라우저 UI
  언어**를 따릅니다(`--lang=ko-KR`). 없는 결함을 만들 뻔했습니다
- **잴 칸을 이름으로 집었는데 그 이름에 임자가 있던 것** — `.health` 로
  잡았더니 GitHub 연결 진단 칸이 잡혔고, **남의 칸을 재 놓고** 새 구역이
  빨갛다고 적을 뻔했습니다. 화면이 열셋이라 흔한 이름은 대개 임자가
  있습니다. 잴 구역에는 **그 구역만의 이름**을 붙이고 재십시오.
  ⚠️ **지을 때도 같습니다** — 타임라인 목록을 `.tl` 로 지었더니 그 이름은
  운행도표의 자리였고, 발화 목록이 22px 남색 눈금 막대로 그려졌습니다
  (결함 174 — 렌더해서 잡음)
- **순서를 재면서 두 정렬 기준이 같은 데이터로 잰 것** — 이름 순을
  지키는지 보는데 명단이 `김민수(1)·이하늘(2)` 였습니다. **이름 순과
  번호 순이 같아** 번호 순으로 나가도 통과했습니다. 두 기준이 **갈라지는**
  데이터로 재십시오(`강보람` — 이름 맨 앞, 번호 맨 뒤)
- **폰을 화면 폭으로만 흉내 낸 것** — 390px 로 줄여도 `(pointer: fine)` 은
  그대로라 터치 전용 규칙이 하나도 안 걸립니다. `hasTouch: true` 를 켜야
  합니다. 안 켜고 재면 없는 결함을 만들거나 진짜 결함을 놓칩니다
- **서버가 답해야 켜지는 체크박스를 `check()` 로 누른 것** — controlled
  입력이라 누른 직후에는 안 켜져 있고, Playwright 는 그걸 "클릭이 안 먹었다"
  로 적습니다. **요청은 이미 나갔기 때문에** 검사는 실패인데 데이터는
  바뀌어 있습니다. `click()` 하고 **화면에 나타난 글자**로 확인하십시오
- **가짜 입력이 실기와 다른 모양이던 것** — 청크 보관소를 `ArrayBuffer` 만
  받게 짜고 검사 29개가 전부 통과했는데, 실제 청크는 **`Blob`** 입니다.
  냈으면 **디스크에 한 개도 안 적히는데 화면은 "보관 중"** 이었을 것입니다
  (결함 171). 통과는 **넣은 값에 대해서만** 참입니다
- **심어 놓고 결과 줄을 안 읽은 것** — `grep` 이 `# Subtest:` 만 잡아
  통과인지 실패인지 안 보이는 출력을 세 번 보고 "잡혔다" 고 적을 뻔했습니다.
  `ok` / `not ok` 줄을 잡으십시오
- **「덮는가」만 재고 「맞는 칸인가」를 안 잰 것** — 회의 상태를 세 갈래로
  나누는 표에 검사가 붙어 있었는데, 「어디에도 안 떨어지는가」만 봤습니다.
  `pending`(녹음 전·녹음 중)이 **「처리를 마친 것」**에 들어가 있어도
  초록이었고, 녹음도 안 한 회의의 회의록이 「처리를 마쳤습니다」로
  나갔습니다(결함 289). ⚠️ 게다가 그 표에 **회의 상태가 아닌 값**이 넷
  섞여 있어 세 집합이 **꽉 찬 것처럼** 보였습니다 — 분류표를 잴 때는
  **없는 값이 섞였는지**도 같이 보십시오
- **재 놓고 판정에 안 넣은 것** — 생존율 검사가 coverage 를 **출력만** 하고
  OK/⚠️ 판정에 안 넣어, 85.9%(결함 173 — 꼬리 5초 유실) 옆에 OK 가 떠
  있었습니다. 숫자를 눈으로 읽어 잡았지 검사가 잡은 것이 아닙니다.
  **찍는 값은 판정에 넣으십시오**
- **옛 서버가 포트를 물고 있는데 새로 띄웠다고 믿은 것** — `serve.sh` 가 DB 를
  지우고 다시 만든 뒤 `uvicorn` 이 `address already in use` 로 죽었는데,
  `curl` 은 (앞선 서버가 답해서) 200 을 줬습니다. 그 서버는 **지워진 DB** 를
  들고 있어 로그인이 500 이었고, **열두 주소가 전부 로그인 화면**이었습니다.
  감사는 로그인 화면을 열두 번 재고 "0건" 이라고 답했습니다(결함 182).
  포트가 200 을 주는 것은 "내 서버가 떴다" 가 아니라 **"누가 거기 있다"** 입니다.
  화면마다 **요소 수를 찍어** 다른 화면인지 확인하십시오
- **`opacity` 를 색으로 잰 것** — `opacity` 는 글자와 채움을 **같이** 끌어당기므로
  색 토큰은 그대로입니다. `getComputedStyle().color` 를 읽는 대비 감사는
  **구조적으로 못 봅니다** — 2.57:1 짜리 버튼 옆에서 0건이 떴습니다(결함 180)
- **넘침을 페이지 폭으로만 잰 것** — `document.scrollWidth > clientWidth` 는
  `overflow-x: hidden` 이 걸린 화면에서 **영원히 0** 입니다. 잘려서 안 보이는
  것이 제일 나쁜데 그것만 못 봅니다 — 요소 상자를 직접 재십시오(결함 186)
- **잘림을 「조상」만 보고 잰 것** — `h1.appbar__title` 은 **자기 자신이**
  `overflow-x: hidden` 이라 넘친 글자를 **자기 상자 안에서** 자릅니다.
  조상 사슬에는 아무 일도 안 일어나므로, 조상만 보는 자는 머리줄에 **400자를
  심어도 0건**입니다(결함 285). 자를 고치니 `5536>1265` 로 잡혔습니다.
  요소가 **자기가 자르는 상자**면 `el.scrollWidth > el.clientWidth` 를 먼저
  보고, 그다음에 조상 밖으로 나갔는지 보십시오
- **넘침을 심었는데 아무 일도 안 일어난 것** — 칸반의 카드에 900px 을 심었는데
  `.board` 가 `overflow-x: auto` 라 그 안에서 스크롤했습니다. **심은 자리가
  애초에 넘칠 수 없는 자리**였습니다. 스크롤 상자 **밖**에 심으십시오
- **「조상 중 아무나 스크롤하면 넘어가기」 로 짠 것** — 확대 감사를 그렇게
  짰더니 화면 여덟이 전부 「0건」이었습니다. `.pane__body` 가
  `overflow-x: auto` 라 **그 아래 전부**를 삼킨 것입니다. 잘림은
  **가장 가까운 「가로를 막는」 조상**으로 판정하십시오 — 그게
  `hidden` 이면 잘린 것이고 `auto/scroll` 이면 스크롤로 닿습니다.
  ⚠️ 이 둘을 안 가르면 **스크롤 상자 안의 `hidden` 상자**가 통째로
  안 보입니다
- **Playwright 가 `aria-disabled="true"` 를 안 눌러 준 것** — "enabled 아님" 으로
  보고 거부합니다(`force: true` 필요). 그건 도구 사정이고 **사람은 누릅니다** —
  모르고 재면 없는 결함을 만듭니다
- **Playwright 가 `window.confirm` 을 **자동으로 닫아** 버린 것** — 같은 부류의
  반대 방향입니다. 「내 녹음과 성문 지우기」를 눌렀는데 요청도 대화상자도
  글자 변화도 **없었습니다**. 도구가 네이티브 대화상자를 기본으로 **거절**
  하므로 `confirm()` 이 거짓을 돌려주고 `mutate()` 가 아예 안 돕니다.
  하마터면 **이 제품에서 법적으로 제일 민감한 단추가 죽었다**고 적을
  뻔했습니다. `page.on('dialog', d => d.accept())` 를 걸고 다시 누르십시오
  — 걸었더니 `200 POST /api/projects/1/me/data` 가 정상으로 나갔습니다.
  ⚠️ **거절도 한 번 해 보십시오** — 거절했는데 요청이 나가면 그게 진짜 결함입니다
- **HTML 주석 속 「나쁜 예」를 진짜 태그로 문 것** — 이 저장소의 마크업은
  "예전에는 `<button id="consent">` 였고 왜 바꿨다" 를 주석에 적어 둡니다.
  태그를 찾는 정규식이 그 주석을 **진짜 태그보다 먼저** 물어, `<a>` 를
  `<button>` 이라고 답했습니다 — 심어 놓고 「0건」이 나왔습니다(결함 238).
  마크업을 잴 때는 **주석부터 걷으십시오**. 코드에서 두 번 당한 함정이
  마크업에서 세 번째로 걸린 것입니다
- **`aria-disabled` 버튼을 `.disabled` 로 기다린 것** — 그 값은 **언제나
  false** 라 기다림이 즉시 참이 되고, 다음 줄이 30초 뒤에 죽습니다.
  기다리는 축을 **화면이 그리는 축**과 맞추십시오(결함 238)
- **DB 만 지우고 오디오 뿌리를 안 지운 것** — id 가 다시 1부터 나므로 앞
  회차의 조각 파일이 같은 `meetings/N/tracks/M` 에 남아, 서버가 「받은 적
  없는 조각을 가지고 있다」고 답합니다. 커버리지가 **거짓으로 100%** 가
  됩니다(결함 244 에서 그럴 뻔했습니다). 지우는 것은 **둘 다**입니다
- **부르는 곳을 세는 정규식이 「선언」까지 센 것** — `finishRecording()` 은
  `async function finishRecording(): …` 안에도 들어 있습니다. 부르는 자리를
  통째로 지웠는데 2가 나와 가드가 통과했습니다(결함 240). 부르는 자리만
  세려면 `void finishRecording()` 처럼 **호출 문법**으로 좁히십시오

- **페르소나가 데이터를 바꾸는데 다음 페르소나가 그대로 잰 것** — 키보드
  페르소나가 "60번 눌러도 담당자 칸에 못 닿는다" 고 했습니다. 앞 페르소나가
  후보 셋을 **다 처리해 버려서** 그 화면에 카드가 0개였던 것입니다. 큰 결함을
  없는 채로 적을 뻔했습니다 — **페르소나마다 데이터를 다시 심으십시오**(결함 196)
- **클래스만 붙이고 색을 안 재 본 것** — 녹음 화면의 `#blockers li, #warnings li`
  가 `color` 를 정하고 있어서 `li.critical` 같은 톤 규칙이 **다섯 개 전부**
  특성도에서 지고 있었습니다(0,1,1 < 1,0,1). 화면은 심각도를 꼬박꼬박
  클래스로 붙였는데 **아무 색도 안 나갔습니다**(결함 250). 오류도 안 나고
  글자는 그대로라 **글로 읽어서는 안 보입니다** — 칠해서 픽셀을 읽으십시오
- **인자 개수를 `Function.length` 로 센 것** — **기본값이 붙은 인자는 그 수에
  안 들어갑니다.** 「리본이 확신도 말고 다른 값을 받으면 안 된다」는 가드에
  `(c, share = 0)` 을 심었더니 통과했습니다(결함 247). 선언을 읽어 세십시오
- **숨은 조상을 안 본 것** — 녹음 화면의 `#copy` 를 `el.hidden ||
  display === 'none'` 으로만 재고 「보이는데 안 눌린다」고 적을 뻔했습니다.
  그 단추는 `<section id="debug" hidden>` **안**에 있습니다. 보임을 잴 때는
  **조상 사슬 전체**를 보십시오
- **탭 전환을 `bringToFront()` 로 흉내 낸 것** — 그 호출은
  `document.visibilityState` 를 **안 바꿉니다.** 두 탭이 모두 `visible` 이라
  `visibilitychange` 가 아예 안 나고, 그 이벤트에 매달린 배선은 **한 줄도
  안 돕니다** — 「동의하고 돌아와도 화면이 안 바뀐다」는 없는 결함을 만들
  뻔했습니다. `hidden` → `visible` 을 손으로 흘려보내십시오
- **증상을 원인으로 읽은 것** — 동시 녹음에서 `database is locked` 를 보고
  SQLite 를 WAL 로 켜고 `busy_timeout` 을 올렸더니 **더 나빠졌습니다**
  (오류 대신 기다리게 되어 24건 전부 무응답). 진짜 원인은 `async def`
  엔드포인트가 **이벤트 루프에서 막는 일**을 하던 것이었고, 잠금은 그
  병의 증상이었습니다(결함 279). ⚠️ **고친 뒤에는 앞서 넣은 것을 하나씩
  꺼서 다시 재십시오** — 안 꺼 보면 필요 없는 설정이 「원인」으로 남습니다
- **한 사람으로만 재 본 것** — 이 제품의 기본 상황은 **둘 이상이 같이
  녹음하는 것**인데, 그 상황을 여태 한 번도 안 만들어 봤습니다. 순차로는
  한 번에 10ms 인 요청이 동시에 열둘이면 열이 무응답이었습니다. **동시에
  두드려 보십시오** — 순차 검사는 이 부류를 **구조적으로** 못 봅니다
- **끊는 폭이 너무 넓어 무엇을 재는지 흐려진 것** — 오프라인을 재려고
  **연결을 통째로** 끊었더니 `/app/` 이 브라우저 HTTP 캐시에서 뜰 때도
  있고 `ERR_INTERNET_DISCONNECTED` 로 아예 안 뜰 때도 있어 **결과가
  흔들렸습니다**(결함 282). `/api/**` 만 막으니 화면은 뜨고 그 갈래만
  실패해서 정확히 짚혔습니다. **재려는 것만 끊으십시오**
- **없는 주소를 재고 「0건」이라고 적을 뻔한 것** — SPA 라우트는
  `/project/:projectId/kanban` 인데 `app/kanban` 을 열었습니다. `*` 가
  홈으로 되돌려서 **네 화면을 잰다고 해 놓고 홈을 다섯 번** 쟀습니다.
  요소 수가 넷 다 122로 똑같은 것을 보고 알았습니다 — 화면마다 **요소
  수를 찍어** 다른 화면인지 확인하십시오 (결함 182 와 같은 부류)
- **`deviceScaleFactor: 2` 로 확대를 흉내 낸 것** — 그건 **DPI** 만 바꾸고
  CSS 픽셀 수는 그대로라 레이아웃이 하나도 안 달라집니다. 확대 200% 는
  **CSS 뷰포트를 반으로** 줄이는 것입니다 (1440×900 → 720×450)
- **넘침만 재고 「자라는 것」을 못 본 것** — 하단 탭바가 프로젝트 수만큼
  높아져 창의 47% 를 먹고 있었는데, 아무것도 **잘리지는 않아서** 넘침
  감사는 0건이었습니다(결함 281). 캡처를 눈으로 보고 잡았습니다 —
  **감사가 0건이어도 캡처는 보십시오**
- **한 축만 재고 다른 축이 죽은 것을 못 본 것** — 대화상자를 키보드로
  고치고 나서 초점 가두기·Escape·되돌리기가 전부 통과했는데, 렌더해서
  **눌러 보니** 상자가 `(0, 678)` 에 앉고 덮개가 그 위를 덮어 「만들기」가
  **마우스로 안 눌렸습니다**(결함 280). 키보드로 고쳤으면 마우스로도,
  마우스로 고쳤으면 키보드로도 눌러 보십시오
- **Radix 가 여는 순간 초점을 가져가는 것** — 달력에 격자 키보드를 붙였는데
  여전히 `‹` 버튼에 서 있었습니다. `useEffect` 로 옮겨도 Radix 의 자동 초점이
  **뒤에** 와서 도로 뺏어 갑니다. `onOpenAutoFocus` 를 막으십시오

- **흔들리지 않게 만들려다 아무것도 안 재게 된 것** — 회의 시각 검사가
  「`T` 도 `Z` 도 없고 길이가 0 이 아니다」만 봤습니다. CI 시간대에 안
  흔들리게 하려던 것인데, 그 자는 **브라우저 달력이든 팀 달력이든 똑같이
  통과**합니다. 실제로 그 함수는 오래도록 `toLocaleString` 을 시간대 없이
  부르고 있었고 검사는 초록이었습니다(결함 287). 흔들림을 없애는 방법은
  **재는 것을 줄이는 게 아니라 갈라지는 데이터를 고르는 것**입니다
- **「메시지가 없다」를 본문 길이로 판정한 것** — 「역할 저장」을 눌러도
  아무 말이 없다고 적을 뻔했습니다. `document.body.innerText` 의 **길이를
  비교**했는데, 그 메시지는 글자를 늘리는 게 아니라 **바꾸는** 것이라 그
  자에 안 걸립니다(결함 293 회차). `#role-message` 칸을 직접 읽으니
  「합이 1 이어야 합니다 (지금 10)」이 처음부터 거기 있었습니다 —
  **재는 칸을 이름으로 집으십시오**
- **검사가 반대를 말하는데 내 판단을 밀어붙일 뻔한 것** — 기여도 화면이
  활동 0인 사람에게 `0%` 를 적길래 불변식 위반이라 보고 고쳤더니, 결함
  191 이 **검사로 못 박아 둔 결정**(「쟀는데 0건인 것은 그대로 `0%` 다」)이
  빨갛게 떴습니다. 근거를 적어 가며 내린 결정을 조용히 뒤집을 뻔했습니다 —
  **검사가 반대를 말하면 내 판단부터 의심하고, 두 답이 다 방어 가능하면
  고르지 말고 「결정이 필요한 자리」로 적으십시오**
- **짝 검사가 「키 집합」만 보고 있던 것** — 서버 어휘 ↔ 화면 이름표 짝
  검사가 넷 있었는데 **키가 같은가**만 봤습니다. 서버에 한국어 이름이
  **아예 없는** 상태는 그 자로 절대 안 걸리고, 그래서 최종 보고서가
  역할을 `developer`, 빠진 영역을 `document, schedule, peer` 라고 적었습니다
  (결함 291). 짝을 잴 때는 **양쪽이 같은 글자를 내는가**까지 보십시오
- **같은 사실을 말하는 두 자리를 안 나란히 놓은 것** — 회의록은 「일시
  2026-09-08 **10:00**」, 홈 화면은 같은 회의를 「09-08 **19:00**」이라고
  했습니다(결함 290). 문서만 읽으면 10:00 이 그럴듯하고 화면만 보면 19:00
  이 그럴듯합니다 — **둘을 같이 놓아야** 어긋난 것이 보이고, 틀린 쪽이
  밖으로 나가는 문서였습니다. ⚠️ 서버에 팀 달력(`clock.team_zone`)이 이미
  있었는데 `local_date` 가 **날짜만** 답해서 시각을 물을 자리가 없었고,
  그래서 보고서는 안 묻고 직접 찍었습니다
- **버튼은 눌러 봤는데 나온 것을 안 읽은 것** — 「최종 보고서 만들기」는
  201 을 주고 화면에도 잘 그려집니다. 그런데 그 문서 안에 「처리된 회의
  **0건**」과 「그 회의의 발언은 기여도에 안 들어갔습니다」가 적혀 있었고,
  같은 제품의 기여도 화면은 그 사람의 회의 근거를 11건이라고 세고
  있었습니다(결함 288). **숫자는 눌러서는 안 보이고 읽어야 보입니다**
- **검사 씨앗이 실기가 안 만드는 값을 만든 것** — 보고서 검사가
  `m.Meeting(status="done")` 이었습니다. `"done"` 은 **업무** 상태라 실기의
  회의는 절대 그 값이 안 되는데, 서비스도 `== "done"` 으로 세고 있어서
  **검사 안에서만 맞아떨어졌습니다**(결함 288). 씨앗을 만들 때는 그 값이
  **실기에서 나올 수 있는 값인지** 보십시오 — 상태 어휘가 있으면 글자
  대신 그 어휘를 쓰고, 없는 값을 적으면 잡는 검사를 두십시오
- **자리를 바꿔 놓고 초점을 잃은 것** — 로비의 「이름 고치기」는 그 단추를
  **없애고** 입력칸을 그 자리에 만듭니다. 초점을 쥐고 있던 단추가 사라지니
  초점이 `body` 로 떨어졌고, 「저장」·「취소」도 같았습니다 — **여닫이
  셋 다**(결함 303). ⚠️ **여는 쪽만 고치지 말고 닫는 갈래도 세십시오** —
  닫으면 눌렀던 자리로 되돌려야 합니다(결함 280 이 대화상자에서 적어 둔
  그것입니다)
- **글은 옮겨 놓고 초점은 안 데려간 것** — 채팅의 「고치기」는 메시지 글을
  아래 작성칸으로 옮기고 「고치는 중」이라고 말까지 하는데, 초점은 누른
  단추에 남습니다. 키보드만 쓰는 사람은 **Tab 을 31번**(답글은 32번)
  눌러야 그 칸에 닿습니다 — 메시지가 쌓일수록 멀어집니다(결함 302).
  ⚠️ **무언가를 다른 칸으로 옮겼으면 초점도 같이 옮기고, 캐럿은 글 끝에
  두십시오** — 앞에 두면 이어 쓰려다 글머리에 끼워 넣습니다
- **한 갈래만 고치고 옆 갈래를 그대로 둔 것** — 달력에서 일정 **무르기**의
  실패 문구를 서버 문장으로 고쳐 놓고(결함 298), 바로 위 **잡기**는 그대로
  뒀습니다. 다음 회차에 가드가 잡았습니다(결함 301). ⚠️ **같은 화면에서
  같은 모양을 몇 곳이나 쓰는지 세고 고치십시오** — 한 곳만 고치는 것이
  이 저장소에서 제일 흔한 재발 모양입니다(실패 ②)
- **내 가드의 창이 좁아 내 고침을 잡은 것** — 「`detailText` 를 거쳤는가」를
  앞 200자만 보고 판정했더니, 주석이 긴 자리(결함 298 의 달력)가 거짓
  양성으로 걸렸습니다. 예외 이름도 하나 빠뜨렸습니다(`setFeedFailure`).
  ⚠️ **가드가 여럿을 잡으면 하나씩 진짜인지 보십시오** — 「잡혔다」는
  「맞다」가 아닙니다
- **상태 코드 하나에 뜻을 하나로 가정한 것** — 어휘가 409 를 「다른 사람이
  먼저 처리했습니다」로 때우고 있었습니다. 이 제품의 409 를 **전부 세어
  보니 다섯인데 하나도 그 뜻이 아니었습니다** — 전부 「조건이 안 맞는다」
  이고 새로고침해도 안 바뀝니다(결함 300). 서버는 그때마다 정확한 문장을
  보내는데(`ApiError.detail`) 화면 여덟이 `status` 만 꺼내 쓰고 버리고
  있었습니다. ⚠️ **코드에 뜻을 붙이기 전에 그 코드를 내보내는 자리를
  전부 세어 보십시오** — 그리고 서버가 사람에게 쓴 문장이 있으면 그것이
  일반론보다 언제나 낫습니다
- **가드의 자가 「끝에 올 때」만 재고 있던 것** — 결함 285 의 「회의를
  번호로 부르지 마라」 가드가 `` /`회의 ${'${id}'}`/ `` 로 **끝에 백틱**을
  달고 있어, 뒤에 글자가 더 붙은 `` `회의 ${'${meetingId}'} · 팀원 …` `` 를
  통째로 놓쳤습니다(결함 299). 걷는 자리는 맞았습니다 — 결함 286 은
  **걷는 자리**가, 이번은 **자 자체**가 좁았습니다. ⚠️ 가드를 고칠 때는
  **넓힌 자 + 옛 코드**와 **옛 자 + 옛 코드**를 둘 다 돌려 보십시오 —
  뒤엣것이 0건이어야 자가 구멍이었다는 증거가 됩니다
- **만드는 자리만 있고 무르는 자리가 없던 것** — 일정을 잘못 잡으면
  달력·홈·회의 목록에 **영영** 남았습니다. `DELETE /api/scheduled-meetings/
  {id}` 는 검사까지 붙은 채로 서버에 있었고 **부르는 곳만 0곳**이었습니다
  (결함 298 — 실패 ①과 ③이 겹친 자리). ⚠️ **만드는 단추를 봤으면 무르는
  단추를 같이 찾으십시오** — 서버의 갈래마다 부르는 곳이 있는지 세는
  가드가 낱말을 세는 것보다 낫습니다
- **내가 고친 것을 다시 안 써 본 것** — 활동 기록의 식별자를 사람 이름으로
  바꿔 놓고(결함 293), **씨앗 데이터에 있던 넷만** 채웠습니다. 「업무 후보
  승인」을 실제로 눌러 보니 다섯째가 그대로 식별자였고, 세어 보니 여덟 중
  넷이 이름 없이 나가고 있었습니다(결함 297). ⚠️ **고친 뒤에 그 기능을
  일으키는 행동을 하나 더 해 보십시오** — 「기능을 한 번 쓰고 다른 화면을
  열기」는 제 고침에도 그대로 적용됩니다. 그리고 목록을 늘리는 대신
  **짝을 재는 가드**를 두십시오(쓰는 쪽 ↔ 읽는 쪽, 양쪽 방향으로)
- **한 번만 눌러 보고 넘어간 것** — 「이번 주 보고서 만들기」는 한 번
  누르면 멀쩡합니다. **날을 달리해 세 번** 누르니 주간 보고서가 세 벌
  생겼고 그중 둘은 제목까지 같았습니다(결함 296). 화면이 「지난 7일」이라는
  **굴러가는 창**을 보내서 유일 제약의 열쇠가 날마다 달라진 것입니다.
  ⚠️ **만드는 단추는 여러 번 눌러 보십시오** — 쌓이는 것은 한 번으로는
  구조적으로 안 보입니다. 그리고 단추가 「이번 주」라고 적혀 있으면
  그 낱말이 이 제품에서 이미 무슨 뜻인지 세어 보십시오(여기서는 셋이었습니다)
- **막는 길을 하나만 막은 것** — 결함 246 가드는 `new Date(iso).getMonth()` 를
  막았는데, 달력을 갈라놓는 **세 번째 길**이 열려 있었습니다:
  `instant.slice(0, 10)` 은 UTC 달력일입니다. `time/calendar.ts` 가 「쓰지
  마세요」라고 적어 둔 바로 그것을 `reports/view.ts` 가 하고 있었고, 같은
  보고서 한 줄이 두 주를 말했습니다(결함 295). ⚠️ **가드를 쓸 때는 「이
  요구를 어기는 다른 길이 있나」를 같이 세십시오** — 낱말 하나를 막는 것은
  요구를 재는 것이 아닙니다
- **눌린 적이 없는데 「누른 뒤」를 잰 것** — 팀원 내보내기를 재려고 화면
  열하나를 전/후로 찍었는데, 단추를 못 찾아 **클릭이 아예 안 됐습니다.**
  그런데도 「내보낸 뒤」 표를 다 찍고 비교까지 했습니다 — 눌렸는지를
  `눌렀나=false` 로 **찍어 두지 않았으면** 「내보내도 아무 문제 없다」고
  적었을 것입니다(결함 308 회차). ⚠️ 못 찾은 이유도 같은 부류였습니다:
  `li` 중 「박지원」으로 시작하는 것을 집었더니 **「지금 맡고 있는 일」
  줄**(`"박지원\n0"`)이 잡혔습니다 — 팀원 줄은 `.mout` 를 **가진** 줄입니다.
  ⚠️ 게다가 식별자 감지기가 `사용자\s*\d` 라 **`사용자 #3`** 을 못 봤고
  (가운데 `#`), 열한 화면이 전부 「0건」이었습니다. 결함은 **글을 눈으로
  읽어** 잡았습니다. **세 번 다 「없는 결함 0건」 쪽으로 틀렸습니다** —
  조용히 넘어가는 방향입니다
- **불변식을 주석에 적어 놓고 바로 다음 줄에서 어긴 것** — 갓 만든 빈
  프로젝트에서 기여도 카드는 `—` 와 「0 이라는 뜻이 아니라 연결이 없다는
  뜻입니다」를 그리는데, **여섯 줄 아래 확정 줄이 「시스템 0.0%」**였습니다.
  누르면 201 이 떨어지고 최종 보고서가 「측정하지 못했습니다」 두 줄 아래에서
  「팀 확정 0%」라고 적었습니다 — 불변식 ③이 **팀 밖으로 나가는 문서**에서
  깨진 것입니다(결함 307). 원인은 `(systemValues.get(id) ?? 0)` 한 줄이고,
  서버도 같은 모양인데 **주석에는 정답이 적혀 있었습니다**(「그건 "안 했다"
  가 아니라 "이 계산에 잡힌 게 없다" 다」). ⚠️ **결함 191 과 다른 경우입니다**
  — 191 은 「**쟀는데** 0건」이고 이쪽은 「**안 잰**」 것입니다. 처음 쓴 검사가
  그 함정에 빠져 통과했습니다: 팀에 활동이 하나라도 있으면 **모두**가 개수 0
  짜리 범주 칸을 받아 191 의 경우가 됩니다. 씨앗을 「아무도 아무것도 안 잰
  프로젝트」로 바꿔야 잡힙니다. ⚠️ **고치다가 낸 것은 렌더해서 잡았습니다** —
  안 잰 사람의 시스템 값이 0 이라 팀이 **일부러 적은 0** 이 `sameValue(0,0)`
  으로 「안 건드렸다」에 접혀 400 이 났습니다. 검사만 봤으면 못 봤습니다
- **서버 갈래에 부르는 곳이 있는지 한 번도 안 세어 본 것** — 결함 298 을
  적으면서 「서버의 갈래마다 부르는 곳이 있는지 세는 가드가 낱말을 세는
  것보다 낫습니다」라고 써 놓고 여태 안 했습니다. 세어 보니 라우트 81개
  중 하나가 진짜 고아였습니다 — `POST /api/meetings/{id}/minutes` 는
  검사까지 붙은 채로 있고 **부르는 곳만 0곳**인데, 보고서 화면은 사람에게
  「회의 로비에서 회의록을 만드세요」라고 말하고 있었습니다. 로비에 그
  단추가 없었습니다(결함 306 — 실패 ③). ⚠️ **그런데 「0곳」 일곱 중 셋은
  제 자가 틀린 것이었습니다** — 녹음 클라이언트는 주소를
  `${trackUrl}/chunks/${seq}` 처럼 **조각으로** 만들어서 전체 경로를 글자로
  찾는 자에 안 걸립니다. 그대로 적었으면 없는 결함 셋을 만들 뻔했습니다.
  ⚠️ **예외를 둘 때는 「왜 예외인가」를 적고, 예외가 낡는 것도 같이
  재십시오** — 이제 제대로 불리는데 예외에 남아 있으면 다음 사람이 속습니다
- **화면을 만들고 그 화면에 「들어갈 문」을 안 준 것** — 채팅·일정·알림·
  활동 기록·찾기·보고서, **여섯 화면이 제품 어디에서도 안 닿았습니다**
  (결함 305). 주소를 손으로 치는 것 말고는 방법이 없었습니다. `navLinks`
  는 그 여섯으로 가는 링크를 **정확히 만들고** 있었고 그리는 코드도
  멀쩡했는데, 그릴 자리(`<nav id="nav">`)가 열두 화면 어디에도 없었습니다 —
  석 달 전 탭바로 바꾸면서 지운 자리에 그 뒤 만든 여섯이 등록된 것입니다.
  ⚠️ **「부르는가」를 세는 가드는 이걸 못 잡습니다** — 부르기는 제대로
  부릅니다. 안 보고 있던 것은 **그린 것이 갈 곳**입니다. 화면을 만들었으면
  **로그인해서 홈부터 링크를 따라 걸어** 그 화면에 닿는지 보십시오 —
  화면 낱개를 아무리 눌러 봐도 이 부류는 구조적으로 안 보입니다.
  ⚠️ 걸을 때 **크롤러가 `<a href>` 만 따라가는지** 보십시오. 단추로 넘어가는
  화면을 놓치면 없는 결함을 만듭니다 — 소스에서 「그 화면을 가리키는 곳」을
  같이 세어 교차 확인하십시오
- **빈 상자가 「내가 안 본 것」을 단언한 것** — 활동 화면이 「기록은 누가
  무언가를 바꿀 때 쌓입니다 — **아직 아무도 안 바꿨습니다**」라고 적었는데,
  같은 순간 그 팀에는 회의 다섯·업무 카드 넷·세 사람의 기여도 근거가
  있었습니다(결함 304). 그 감사 기록에 남는 것은 **열넷뿐**이고 회의를
  열거나 녹음하거나 이야기 나눈 것은 애초에 안 쌓입니다 — 화면은 자기 목록
  하나가 빈 것으로 **팀 전체**를 말했습니다. ⚠️ **고치면서 반대 방향으로
  같은 실수를 했습니다** — 「사람의 **숫자**를 건드린 일만 쌓입니다」라고
  썼는데 열넷 중 숫자는 둘뿐(`score_adjusted`·`weights_changed`)이라, 업무를
  완료로 옮긴 사람에게는 그것도 거짓말이었습니다. **범위를 말할 때는 세어
  보고 쓰십시오** — 문서의 숫자를 안 믿는 규칙이 방금 내가 쓰는 문장에도
  적용됩니다. ⚠️ 같은 회차에 쓸었더니 채팅의 빈 채널이 「`#공지` 채널이
  **방금 만들어졌습니다**」라고 하고 있었는데, 채널 목록은 `{id, kind, name,
  position}` 뿐이라 화면은 **그 시각을 받지도 않습니다.** 빈 상자를 지을
  때는 **그 문장을 뒷받침하는 값이 화면에 실제로 와 있는지** 보십시오
- **빈 상자의 「왜」와 「다음에 뭘」을 상수로 박아 둔 것** — 일정 화면이
  격자에 9월 초 뱃지를 넷 그려 놓고 바로 아래에서 「이 달에는 잡힌 일이
  없습니다 — **칸반에서 업무에 마감일을 주세요**」라고 했습니다(결함 294).
  「이 달에는 없다」는 참이고, 거짓말은 **뒤의 두 줄**이었습니다. 그 둘은
  「이 프로젝트에는 아무것도 없다」를 전제로 쓰였는데 화면은 그걸 확인한
  적이 없습니다. ⚠️ **빈 상자를 지을 때는 「무엇이 비었나」와 「무엇이
  있나」를 갈라 보십시오** — 화면이 이미 쥐고 있는 것으로 정할 수 있습니다
- **「아무도 안 써 봤다」를 안 세어 보고 단정한 것** — 겸직(역할 비중)을
  「씨앗에 없는 갈래」로 보고 들어갔는데, 씨앗의 다른 두 사람이 **이미
  겸직**이었습니다(개발 60%·디자인 40% 등). 관점을 고를 때도 **세어 보고
  고르십시오** — 다행히 그 갈래는 설정→기여도→보고서까지 전부 맞았고
  검증 넷도 정확했습니다(결함 없음). ⚠️ 그중 `-1 / 2 / 0` 이 중요합니다:
  **합만 보는 자는 통과**시킵니다(-1+2=1). 합이 아니라 값을 보십시오
- **마감이 지난 상태를 아무도 안 재 본 것** — 씨앗의 마감은 전부 미래라
  **「지연」 갈래가 한 번도 안 그려졌습니다.** 마감을 과거로 옮기니 카드
  표면이 「기여도에 반영 안 됨」이었고, 담당자가 둘인 업무에도 붙었습니다
  (결함 319). 서버를 확인하니 **거짓**입니다 — 늦게 끝낸 업무도
  `TASK_COMPLETED` 를 그대로 받고, 늦음이 바꾸는 것은 일정 준수 범주뿐
  입니다. ⚠️ 원인은 `warnings.length > 0` 하나로 **종류를 안 보고** 한
  문장을 붙인 것 — **여러 뜻을 한 글자로 뭉개면 그중 하나만 참입니다**
- **결측 표시를 다른 뜻에 재사용할 뻔한 것** — `.gapmark` 의 점은 「못 잰
  자리」의 표시라고 그 규칙 옆에 적혀 있습니다. 마감 지남에 같은 모양을
  쓰면 **사실이 결측으로 읽힙니다.** 색도 빨강이 아닙니다 — 늦은 것은
  사실이지 「네가 뭘 잘못했다」가 아닙니다(결함 319)
- **로케일을 줬다고 믿은 것** — `--lang=ko-KR` 을 주고도
  `navigator.language` 가 **`en-US@posix`** 였습니다. 그래서 보인
  `mm/dd/yyyy` 는 제품이 아니라 **브라우저가 영어인 것**입니다. 하마터면
  고쳐 둔 결함(F7)이 살아 있다고 적을 뻔했습니다 — **플래그를 줬으면
  먹었는지 재십시오**(결함 319 회차)
- **씨앗이 실기가 만들 수 없는 값을 만드는데 검사가 그걸 못 박고 있던 것** —
  시연 칸반 다섯 중 셋이 출처 없는 업무입니다. 씨앗 주석은 「회의에서
  나오지 않은 것도 칸반에는 있습니다」이고, 제품은 「승인 없이 tasks 에
  쓰는 경로는 없다 — 그게 불변식이다」입니다(결함 318). 고치려는데
  `test_demo_path.py` 가 「손으로 만든 업무**도 있어야** 합니다」를 못
  박고 있었습니다. ⚠️ **두 기록이 서로 어긋나면 고르지 말고 「결정이
  필요한 자리」로 적으십시오** — 그리고 **세 자리가 서로를 가리키게**
  해서 다음 사람이 한쪽만 보고 조용히 「고치지」 않게 하십시오
- **없는 이유를 또 붙일 뻔한 것** — 317 에서 「출처 기록이 없습니다 —
  **기록이 지워졌거나 옛 자료입니다**」라고 적었는데, 시연이 그 상태를
  일부러 만들고 있으니 둘 다 아닙니다(결함 318). 결함 311 과 같은 부류 —
  **결과를 옮겨 적는 자리에서 이유를 지어내지 마십시오.** 아는 것만
  적으면 어느 쪽으로 결정되든 참입니다
- **사용자가 읽는 문서를 제품과 나란히 놓아 본 적이 없는 것** —
  `docs/00-이-프로그램은-무엇인가.md` 는 비개발자가 이 제품을 이해하려고
  읽는 문서인데, 한 줄씩 세어 보니 화면 수·역할 가중치·본인만 수정·캡처
  아홉은 전부 맞았고 **하나가 거짓**이었습니다(결함 317). 「문서에 적힌
  숫자를 세어 보라」는 규칙을 **그 문서 자신에게** 적용하십시오
- **같은 거짓말이 몇 벌인지 세면서 「화면」만 센 것** — 결함 313 에서 빈
  상자와 머리줄을 고쳤는데 세어 보니 넷이었습니다: 카드 서랍(**같은 파일
  안**)과 SPA 카드 힌트가 남아 있었고, 사용자 문서에도 있었습니다(결함 317).
  ⚠️ **자를 두 번 넓혔습니다** — `직접 만들 수` → `직접 만[든들]` →
  `(직접|손으로|수동으로)\s*만[든들]`. 넓히자마자 SPA 자리가 그 자리에서
  잡혔습니다. **낱말을 막지 말고 뜻을 막으십시오**
- **가드가 화면만 걷고 사용자 문서를 안 걸은 것** — 결함 286 의 「가드가
  걷는 자리가 한쪽뿐인지 보십시오」에는 **문서판**이 있습니다. 화면을
  고치고 `docs/00` 을 안 고치면 사람이 읽는 약속은 그대로 거짓입니다
- **소유자로만 재 본 것** — 시연 계정(김민수)은 소유자라 권한 부류가 안
  보입니다. 초대 코드로 막 들어온 사람으로 걸으니 관리 단추 셋이 **열린
  채로** 그려졌고, 누르면 403 인데 화면은 「HTTP 403」이라고 적었습니다
  (결함 316). 판단(`manageBlockedBecause`)은 `@lib` 에 **이미 있었고 그
  주석이 그 단추를 예로 듭니다** — 부르는 곳이 SPA 셋 · 레거시 0 이었습니다
- **가드가 「낱말」을 막고 요구를 안 잰 것** — 결함 301 가드는
  `describeHttpStatus(response.status)` 를 막는데, 요구를 어기는 **다른
  길**(문장에 `HTTP ${'${status}'}` 를 직접 박기)이 열려 있어 **여섯 곳**이
  서버 문장을 버리고 있었습니다(결함 316). 결함 295 가 적어 둔 그것입니다 —
  **낱말 하나를 막는 것은 요구를 재는 것이 아닙니다.** 옛 자 + 옛 코드가
  0건인 것까지 확인해 자가 구멍이었음을 증명하십시오
- **「메시지가 없다」를 또 좁은 자로 판정할 뻔한 것** — 화면이 「못**했**습니다」
  라고 적었는데 제 정규식은 `없습니다|실패|오류` 였습니다(결함 316 회차).
  결함 293 과 같은 함정입니다 — **재는 칸을 이름으로 집어** 읽으십시오
- **꽉 찬 프로젝트를 한 번도 안 만들어 본 것** — 씨앗은 회의 5개뿐이고
  **채널은 아예 0개**라 (⚠️ 이 줄은 오래도록 「채널 2개」라고 적혀
  있었습니다 — 세어 보니 씨앗은 `Channel` 을 하나도 안 만듭니다)
  **양이 늘 때 무너지는 것**은 아무도 안 재고 있습니다. 채널 20개·
  메시지 60개를 심었더니 채팅의 채널 목록이 `top: -3400px` 로 화면 밖에
  나갔고(결함 314), 앞의 메시지 열 줄은 닿을 길이 아예 없었습니다
  (결함 315). ⚠️ **빈 쪽과 꽉 찬 쪽은 다른 관점입니다** — 311~313 은 빈
  쪽에서, 314~315 는 꽉 찬 쪽에서 나왔습니다
- **`offsetParent === null` 로 보임을 잰 것** — `position: fixed` 는
  **언제나 null** 이라 고정 요소를 전부 걸러냅니다. 「고정 요소가 창을
  먹는가」를 재는 감사가 그 필터를 달고 있어서, **결함 281(하단 탭바가
  창의 47%)을 구조적으로 못 보는 자**였습니다(결함 314 회차). 심어 보고
  잡았습니다 — 조상 사슬을 직접 걸으십시오
- **라우트는 불리는데 「인자」가 안 불린 것** — 결함 306 이 만든 「서버
  갈래마다 부르는 곳이 있는가」 가드는 `GET /api/channels/{id}/messages`
  를 초록으로 봅니다. 그 라우트는 불립니다. 안 불린 것은 **`before_id`**
  였고, 그래서 대화의 앞부분이 제품에서 사라졌습니다(결함 315).
  ⚠️ **라우트를 세는 것과 요구를 재는 것은 다릅니다** — 서버가 받을 수
  있는 인자에 화면이 닿는지도 세고, 양쪽 상수(`MAX_PAGE` ↔ `MESSAGE_PAGE`)
  는 **값으로** 짝을 지으십시오
- **「지금 안 보인다」와 「닿을 수 없다」를 안 가른 것** — 같은 회차에 왼쪽
  회의 목록도 아래가 잘려 보여 결함으로 적을 뻔했는데, `div.chan` 이
  `overflow-y: auto` 라 **스크롤해서 끝 회의에 닿았습니다**(재현 안 됨).
  화면 밖에 있는 것을 봤으면 **실제로 굴려서 닿는지** 해 보십시오
- **빈 상자의 문장을 「읽기만」 하고 참인지 안 재 본 것** — 앞 회차에 칸반의
  빈 상자를 읽고 「정직합니다」로 넘겼는데, 그 상자는 「업무를 **직접 만들
  수도 있습니다**」라고 하고 있었습니다(결함 313). 세어 보니 화면의 컨트롤
  열셋 중 만드는 것이 0개, 서버의 업무 생성 라우트도 0개였고, `m.Task(` 를
  쓰는 유일한 자리 옆에는 **「승인 없이 tasks 에 쓰는 경로는 없다 — 그게
  불변식이다」**라고 적혀 있었습니다. ⚠️ **312 보다 나쁜 쪽입니다** — 312 는
  「아직 없는 곳」을, 이것은 **일부러 막아 둔 것**을 하라고 시킵니다.
  **화면이 「할 수 있다」고 말하면 그 자리를 세어 보십시오**
- **빈 상자만 고치고 바로 위 머리줄을 놓칠 뻔한 것** — 「회의에서 승인된
  업무와 **직접 만든 업무**가…」가 그대로 있었습니다. 그 줄은 **비어 있지
  않을 때도 늘** 보이니 더 자주 읽힙니다 — 렌더해서 눈으로 잡았습니다.
  ⚠️ 게다가 **제 첫 가드가 그것을 못 잡았습니다**: 자가 `직접 만들 수` 인데
  머리줄은 「직접 만**든**」이었습니다. 결함 299 대로 **넓힌 자 + 옛 코드**와
  **옛 자 + 옛 코드**를 둘 다 돌려 자가 구멍이었음을 확인했습니다
- **껍질이 격자 항목으로 들어가 규칙이 안 닿은 것** — `.empty-state` 의
  `grid-column: 1 / -1` 이 **껍질이 아니라 안쪽 상자**에 붙어 있어서,
  `RawHtml` 이 만든 껍질이 `#board`(grid) 의 항목이 되는 순간 닿지
  않았습니다. 빈 상자가 1024px 중 **241px 한 칸**만 먹고 한 줄에 한글
  열넷씩 일곱 줄로 접혔습니다(결함 313). 그 규칙 주석은 「한 칸만 먹으면
  옆에 빈 칸 둘이 남습니다」 — **막으려던 바로 그 증상**이 나고 있었습니다.
  ⚠️ 저장소는 이미 스켈레톤에서 `.sk-wrap { display: contents }` 로 같은
  문제를 풀어 뒀고 `RawHtml` 껍질에만 없었습니다. **규칙이 붙은 곳과
  격자 항목이 되는 곳이 같은지 보십시오** — 사이에 껍질이 하나 끼면
  오류 없이 조용히 안 먹습니다
- **결과를 옮겨 적는 자리에서 없는 이유를 붙인 것** — 보고서가 「**가중치가
  0이라** 계산에서 빠진 영역: 업무, 코드, …」라고 했는데, 그 목록을 만드는
  자는 `skipped = [c for c in Category if team_totals[c] <= 0]` 입니다 —
  재는 것은 가중치가 아니라 **팀의 활동량**입니다(결함 311). 갓 만든
  프로젝트에서 여섯이 전부 실렸고 그중 코드(35%)·업무(30%)는 그 사람의
  **가장 큰 가중치**였습니다. ⚠️ 같은 목록을 **화면은 이유 없이** 「이번
  계산에서 빠졌습니다」라고만 적고 있었습니다 — **같은 사실을 말하는 두
  자리를 나란히 놓으십시오**(결함 290). 그리고 「가중치 0」은 **일부러 안
  세기로 했다**는 뜻이라 불변식 ③ 과 반대 방향입니다
- **빈 상자가 「다음에 뭘」을 상수로 뱉는데 그걸 정할 값이 화면에 아예 안
  와 있던 것** — 회의가 0개인 팀에게 「회의 로비에서 회의록을 만드세요」라고
  했습니다(결함 312). 294(달력)·306(로비)에 이은 **세 번째**입니다. 그런데
  294 가 「화면이 이미 쥐고 있는 것으로 정할 수 있습니다」라고 적어 둔 것과
  달리, 이 화면은 `/reports` 하나만 받아 와서 **쥐고 있지도 않았습니다** —
  받아 오게 고쳐야 했습니다. ⚠️ **상태만 만들고 안 채우면 영영 「모름」이고,
  「모름」 갈래는 대개 고치기 전과 같은 글자입니다** — 「부르는가」만 재는
  가드는 그 구멍을 못 봅니다. **채우는 곳까지 같이 재십시오**
- **갓 만든 빈 프로젝트에서 화면을 다 열어 보는 관점** — 304·307·311·312 가
  전부 여기서 나왔습니다. 씨앗 데이터는 언제나 꽉 차 있어서 **빈 상태는
  아무도 안 재고 있습니다.** ⚠️ 그 회차에 홈에 같은 이름 카드가 둘 보여
  중복 렌더인 줄 알았는데, 앞서 **크래시한 제 스크립트**가 이미 하나를
  만들고 죽은 것이었습니다 — **화면을 의심하기 전에 서버에 물어보십시오**
- **한 패널이 현재형과 과거형으로 스스로 모순된 것** — 끝난 회의의 동의
  칸이 세 사람을 「응답 **대기 중**」이라 하고, 두 줄 아래에서 「3명은
  응답하지 않은 **채였습니다**」라고 했습니다(결함 310). 시작 전 회의와
  실패한 회의의 동의 단추가 **글자도 클래스도 색도 같았습니다** —
  `btn btn--primary` · `rgb(61,58,174)` · 「동의합니다」, 레거시·SPA 넷 다.
  ⚠️ **여기서 서버 문을 닫을 뻔했는데, 그 문은 근거를 적어 열어 둔
  것이었습니다**(결함 251 + `test_late_consent_is_still_accepted_on_purpose`).
  251 은 자기가 **말만** 고쳤다고 문장으로 적어 뒀고, 그 말은 버튼 **아래**
  줄이었습니다. 고칠 것은 버튼 자신이었습니다 — **막는 게 아니라 그 문이
  어디로 나는지 적는 것**입니다
- **내가 고치니 재는 자가 낡은 것** — 라벨을 바꿨는데 측정 스크립트는
  옛 글자로 찾아서 「(없음)」이 나왔습니다. 「고치니 버튼이 사라졌다」로
  적을 뻔했습니다(결함 310). ⚠️ **고친 뒤에는 재는 자도 같이 고치십시오** —
  이건 결함 286 의 「화면을 옮기면 가드가 눈을 감는다」와 같은 부류이고,
  이번에는 **내가 그 자리를 만들었습니다**
- **앞 회차에 내가 누른 것이 다음 측정을 바꾼 것** — 「동의합니다」를
  눌러 보고 그대로 다시 쟀더니 청록이 사라져 「끝난 회의에서는 청록이
  아니다」로 읽힐 뻔했습니다. 제가 동의해서 `iAgreed` 가 참이 된 것뿐입니다
  (결함 196 이 페르소나에서 적어 둔 그것이 **내 손**에서 났습니다).
  씨앗을 다시 심고 재니 청록이었습니다
- **막혀 있던 것이 우연이던 것** — 끝난 회의의 로비에서 「녹음 화면으로」가
  안 눌리길래 국면을 본다고 여겼는데, **마침 동의가 안 모여** 눌리지 않은
  것뿐이었습니다(결함 309). 전원이 동의한 채로 실패한 회의였다면 청록
  단추가 멀쩡히 눌렸습니다. ⚠️ **「지금 눌리는가」가 아니라 「무엇을 보고
  정하는가」를 재십시오** — 씨앗이 마침 막아 주는 값이면 눌러 봐도 초록입니다.
  같은 회차에 그 화면의 「동의합니다」는 실제로 눌렸고 **서버가 200을
  줬습니다**(결함 310) — 녹음이 끝난 뒤에 받은 「사전 동의」입니다
- **만든 기능을 한 번도 안 쓰고 다음 화면을 본 것** — 「회의 일정 잡기」는
  201 을 주고 달력에도 잘 그려집니다. 그런데 그 기능이 만든 데이터를
  **처음 보는 다른 화면**(회의 목록)이 500 으로 죽었고, 홈이 「회의 6개」
  라고 적은 채 「회의를 열면 여기에 나옵니다」를 띄웠습니다(결함 287).
  ⚠️ **씨앗 데이터에 없는 상태는 아무도 안 재고 있습니다** — 기능을 한 번
  쓰고 **나머지 화면을 다시 여십시오**
- **라우트가 「있는지」만 보고 「부르면 되는지」를 안 본 것** — 새 갈래를
  `app.routes` 로 훑어 「등록됐다」를 확인하고 됐다고 했는데, 본문이
  `select(m.Track)` 이었습니다. **그런 클래스가 없습니다**(`MeetingTrack`).
  라우트는 등록돼 있으니 목록에는 멀쩡히 뜨고, **한 번도 불러 보지 않아서**
  안 보였습니다(결함 320). ⚠️ **만든 갈래는 한 번 두드려 보십시오** —
  「있다」와 「돈다」는 다릅니다
- **가드가 두 뿌리를 한 자루에 담아 센 것** — 「이 서버 갈래를 부르는 화면이
  있는가」 가드가 `frontend/src` 와 `webapp/src` 를 **합쳐서** 셉니다.
  한쪽에서만 불려도 초록이라, 결함 306 을 **레거시만** 고친 것이 그대로
  통과했습니다 — `/app` 으로 들어온 사람에게 「회의 로비에서 회의록을
  만드세요」는 여전히 갈 곳 없는 안내였습니다(결함 321). 같은 자리에서
  **세 번째**입니다(231 · 306 · 320). ⚠️ **뿌리마다 따로 세십시오** —
  결함 286 이 「걷는 자리가 한쪽뿐인지」로 적어 둔 것의 반대 방향입니다
- **되돌릴 수 없는 단추의 색이 `:hover` 안에만 있던 것** — `.linkish.danger`
  가 `@media (hover: hover)` 안에서만 색을 정해서, **쉬고 있을 때**는
  평범한 링크와 글자색이 `rgb(102,109,128)` 로 **똑같았습니다.** 즉 위험
  신호가 마우스 쓰는 사람에게만 보이고 키보드·터치 사용자에게는 평생
  안 보입니다 — 「강제 종료」가 이미 그랬습니다(결함 322). 결함 239 가
  「쉬고 있는 상태만 재면 안 보인다」고 적은 것의 **거울상**입니다.
  ⚠️ 그리고 SPA 에는 `btn--danger-quiet` 가 **이미 있었는데** 안 쓰고
  `btn--secondary` 를 썼습니다 — **있는 것을 찾아보고 만드십시오**
- **가드의 자가 이름의 「앞부분」만 물어 심은 것을 놓친 것** —
  `\.linkish\.danger` 는 `.linkish.dangerXX` **도 통과**시킵니다. 규칙
  이름을 바꿔 심었는데 초록이 떴습니다(결함 322 회차). 자를 조일 때는
  **이름 끝을 못 박으십시오**(`(?![-\w])`). ⚠️ 이건 결함 299 와 반대
  방향입니다 — 그때는 자가 좁아 놓쳤고, 이번은 **넓어서** 놓쳤습니다
- **지운 것을 다시 물어본 것** — 회의를 지우고 `invalidate` 를 걸었더니,
  홈으로 옮겨 가기 전에 로비가 아직 붙어 있어서 `GET /api/meetings/9`·
  `/consent`·`/tracks` 가 다시 나가 **404 가 셋** 찍혔습니다. **화면에는
  아무 표시도 안 났습니다** — 네트워크를 읽어서 잡았습니다(결함 320).
  지운 것은 `invalidate` 가 아니라 `remove` 입니다
- **내가 방금 찍은 그림인지 확인 안 한 것** — 고치고 다시 찍었는데 그림이
  그대로여서 「안 먹었다」고 볼 뻔했습니다. 두 번째 실행에서 `SP=` 를
  빼먹어 그림이 `undefined/` 로 나갔고, **제가 연 것은 첫 회차 파일**
  이었습니다(결함 320 회차). ⚠️ **캡처를 볼 때는 파일 시각부터 보십시오** —
  「재는 자가 낡은 것」(결함 310)의 사촌이고, 이쪽은 **보는 것**이 낡습니다
- **감사 기록의 이름을 「지금 DB」에서만 찾은 것** — `_target_labels` 는 살아
  있는 행만 찾습니다. 그래서 **행 자체가 사라지는** 기록(무른 회의)은 영영
  못 찾아 화면에 `meetings/8` 이 그대로 나갔습니다 — 결함 293·297 이 고친
  바로 그 모양입니다. 지울 때 `before` 에 적어 둔 이름을 쓰면 됩니다.
  ⚠️ **지어내지는 마십시오** — 적어 둔 게 없으면 `target` 그대로가 정직한
  답입니다(지운 업무에서 이미 내린 결정입니다)
- **검사의 이름과 docstring 은 짝을 잰다는데 실제로는 부정 하나만 보던 것** —
  「보고서와 화면이 같은 말을 하는가」 검사가 실제로 본 것은 **「화면이
  `가중치` 라고 적지 않는가」** 뿐이었습니다. 그래서 화면이 이유를
  **하나도 안 붙인** 상태가 그대로 초록이었습니다(결함 323). 결함 291 의
  「짝 검사가 키 집합만 보고 있던 것」과 같은 모양입니다 — ⚠️ **짝을 잴
  때는 양쪽이 같은 글자를 내는가를 보고, 주석이 아니라 실제로 내보내는
  줄에서 찾으십시오.** 323 자체가 「주석에 적어 두고 안 고친」 경우였습니다
- **앞 결함이 「다음 사람이 하라」고 적어 둔 것을 아무도 안 한 것** — 결함
  311 이 `period.py` 주석에 「화면은 이유를 안 붙입니다 — 두 자리가 같은
  사실을 말해야 합니다」라고 적고 갔습니다. **기록된 결정이 아니라 안 한
  일**이므로 고치는 것이 뒤집는 것이 아닙니다 — ⚠️ 둘을 가르십시오:
  「근거를 적어 내린 결정」(251·191)은 건드리지 말고, 「적어만 두고 간
  숙제」는 하십시오
- **빈 쪽과 꽉 찬 쪽만 보고 「중간」을 안 본 것** — 기여도 화면의 이유 없는
  「빠졌습니다」는 **활동이 있는 팀에서만** 드러납니다. 빈 팀에서는 바로 위에
  「아직 잰 활동이 없습니다」가 떠서 맥락이 채워지기 때문입니다(결함 323).
  311~313 은 빈 쪽에서, 314~315 는 꽉 찬 쪽에서 나왔고, 이건 **중간**에서
  나왔습니다 — ⚠️ **셋은 서로 다른 관점입니다**
- **첫 글자만 그리는 자리를 이름으로 센 것** — 프로젝트 레일은 이름의 **첫
  글자**(`T`·`두`·`세`)만 그립니다. 본문에서 프로젝트 **이름**을 세고
  「SPA 는 프로젝트 하나만 보여 준다」고 적을 뻔했습니다. ⚠️ 접근 이름
  (`title`·`aria-label`)이 따로 있는지 보고, **캡처를 보십시오**
- **접힌 것을 「없다」고 읽은 것** — SPA 의 경고 문장이 `innerText` 에 안
  잡혀 「SPA 에는 그 안내가 없다」고 볼 뻔했습니다. `Why` **팝오버 안**에
  있었고, 그건 「지우지 않고 한 겹 아래로」라고 근거를 적어 둔 결정입니다.
  ⚠️ **눌러서 확인하십시오** — `<details>` 만 열어 보는 것으로는 부족합니다
  (이건 Radix Popover 라 `details` 가 0개였습니다)
- **심은 것이 파일을 깨뜨렸는데 「심었다」고 믿은 것** — 500 을 심으려고
  `raise` 를 넣었는데 4칸 들여쓰기가 `if` 블록에 안 맞아 **파일이 아예 안
  열렸습니다**(`IndentationError`). 서버가 안 뜨는 것을 보고 알았습니다 —
  ⚠️ **심은 뒤에는 그것이 실제로 심겼는지부터 보십시오**(`ast.parse` 나
  응답 코드). 안 보면 「심었는데 안 잡힌다」를 **자의 문제**로 읽습니다
- **내 호출이 틀린 것을 제품 결함으로 읽을 뻔한 것** — 백필을 손으로
  `fetch` 했더니 **422**(본문 없음)였습니다. 제품이 부르는 자리는
  `body: JSON.stringify({})` 를 제대로 보내고 409 를 받습니다. ⚠️ **내 자로
  잰 값과 제품이 받는 값을 헷갈리지 마십시오** — 제품의 경로를 그대로 밟아야
  합니다
- **버튼만 누르는 자는 링크와 입력칸을 구조적으로 못 보는 것** — 화면 18개
  ·버튼 111개를 눌러 5xx 0건을 확인했지만, 그 자는 `<a>` 를 한 번도 안
  건드립니다. 결함 305(여섯 화면이 제품 어디에서도 안 닿음)는 **링크 쪽**
  이라 이 방법으로는 영영 안 보입니다 — ⚠️ **자를 하나 만들었으면 그 자가
  못 보는 것이 무엇인지 같이 적으십시오**
- **답을 `slice()` 로 잘라 버린 것** — 화면에서 찾은 문장을 `slice(0, 3)` 으로
  줄였는데, 그 세 자리를 **원래부터 있던 경고 셋**이 차지해서 새로 뜬 검증
  문구가 밀려났습니다. 「화면이 아무 말도 안 한다」로 적을 뻔했습니다.
  ⚠️ **누른 앞뒤를 비교해 「새로 생긴 줄」을 찾으십시오** — 화면에 원래 글이
  많은 자리에서는 「있는 것 중에 고르기」가 구조적으로 답을 가립니다
- **메시지 상자의 이름을 지어낸 것** — `#title-message`·`#meeting-message`·
  `#channel-message` 를 찍었는데 셋 다 **없는 이름**이라 전부 「(칸 없음)」이
  나왔습니다. 결함 293 이 「재는 칸을 이름으로 집으십시오」라고 적어 둔 것의
  **반대 함정**입니다 — ⚠️ 이름으로 집되 **그 이름이 실제로 있는지** 보십시오
- **자를 늘리기만 하고 상태를 안 늘린 것** — 버튼·입력칸·링크를 차례로
  훑어 세 회차 연속 0건이 나왔습니다. 그런데 **어휘가 허용하는 상태**와
  **씨앗이 만드는 상태**를 나란히 놓아 보니 셋이 한 번도 안 그려져
  있었습니다: 업무 `review`(0건 — **나중에 재 봤고 초록입니다**: 칸반 두
  뿌리·분석 진행률·보고서·감사·기여 이벤트가 전부 맞았습니다) ·
  우선순위 `0·1·3`(전부 `2` → 결함 348) · 권한 `admin`(0건 → 362·373). ⚠️ **결함은 자를 바꿀 때가 아니라 상태를 만들 때
  나왔습니다** — 빈 프로젝트(311·312) · 꽉 찬 쪽(314·315) · 중간(323) ·
  마감이 지난 것(319) · 소유자가 아닌 사람(316). 수확이 줄면 **자가 아니라
  상태를 늘리십시오**
- **절이 나뉜 화면을 겉만 열고 「없다」고 한 것** — SPA 설정을 `/settings`
  로 열면 `role` 절로 되돌려집니다. 그것만 보고 「단추가 15개뿐, 권한을
  안 가른다」로 적을 뻔했습니다 — `members`·`danger` 는 따로 열어야
  보입니다. ⚠️ **되돌려지는 주소인지 보고, 절이 있으면 절마다 여십시오**
- **결함 182 를 막으라고 둔 자가 조용히 눈을 감은 것** — 로그인이 500 이라
  제품 결함인 줄 알았는데, `serve.sh` 가 DB 를 지운 뒤 새 uvicorn 이
  `address already in use` 로 죽고 **옛 서버가 지워진 DB 를 들고** 답하고
  있었습니다(`readonly database`). ⚠️ 그런데 제 신선도 검사
  `grep -c 'address already in use'` 가 **0** 을 줬습니다 — 로그에 바이너리
  바이트가 섞이면 grep 이 「Binary file matches」로 넘어가고 `-c` 는 0 을
  답합니다. **`grep -ac` 로 세십시오.** 결함 182 를 막으려고 둔 자가 낡아
  있었습니다(결함 324 회차)
- **200 을 「내가 시킨 대로 됐다」로 읽은 것** — 담당자를 `assignee_ids` 로
  보냈는데 서버는 `user_ids` 를 받습니다. Pydantic 이 모르는 키를 **버리고**
  기본값 `[]` 를 써서 **200 을 주며 담당자를 전부 지웠습니다.** 「알림이 안
  온다」를 제품 결함으로 적을 뻔했습니다 — ⚠️ **바꾼 뒤에는 서버 상태를 다시
  읽어 「내가 시킨 값이 들어갔는지」를 보십시오.** 모르는 키를 조용히 버리는
  스키마에서는 200 이 아무것도 보장하지 않습니다
- **`details` 를 펴 놓고 「같은 tick 에」 글자를 읽은 것** — 레거시 검토
  화면의 화자 표시를 세는데 `d.open = true` 를 켜자마자 `innerText` 를
  읽어서 **0** 이 나왔습니다. 캡처에는 발화마다 또렷이 찍혀 있었고, 갈라
  재 보니 `같은 tick 0 / 레이아웃 뒤 5 / DOM 에 실제로 5` 였습니다.
  `innerText` 는 **레이아웃을 보고 답하는** 값이라 접혔던 것이 아직
  안 펴진 상태로 잡힙니다. ⚠️ **접힌 것을 펴고 읽을 때는 한 프레임
  기다리거나 `textContent`(DOM)로 세십시오** — 그리고 이것도 「감사가
  0건이어도 캡처는 보십시오」(결함 281)가 살린 자리입니다
- **씨앗을 손으로 만들면서 짝을 안 맞춘 것** — 화자 추정 갈래를 보려고
  `speaker_source` 를 `voiceprint` 로 바꿨는데 `speaker_confidence` 를
  **1.000 그대로** 뒀습니다. 화면이 「목소리로 추정한 화자입니다 (유사도
  **100%**)」라고 그렸습니다 — 실기가 절대 안 만드는 값입니다(결함 288 이
  적어 둔 그 함정). ⚠️ **한 칸을 바꾸면 그 칸과 같이 움직이는 칸을 세어
  보십시오**
- **어휘는 셋인데 문구가 둘이던 것** — 삭제 사유 문구표에 `consent_refused`
  만 없어서 「(사유 미기록)」으로 떨어졌습니다. **사유는 같은 응답의 바로
  옆 칸에 있었습니다**(결함 326). 검사도 나란히 셋뿐이었고 그 하나만
  빠져 있었습니다 — 결함 289 는 「분류표에 **없는 값**이 섞였나」였고
  이번은 반대로 **표에서 빠진 값**입니다. ⚠️ **낱개를 늘리지 말고 짝을
  세십시오** — 어휘의 값 집합과 문구의 키 집합이 같은가. 그러면 넷째가
  생길 때 그 문장을 쓰기 전까지 빌드가 깨집니다(결함 297 의 방법)
- **씨앗이 「거부」를 한 번도 안 만든 것** — 이 제품의 동의는 세 단계인데
  씨앗은 `recording` 하나를 `True` 로만 만듭니다. 즉 **법적으로 제일
  민감한 갈래**가 한 번도 안 그려져 있었고, 결함 326 은 거부한 뒤에야
  보였습니다. ⚠️ **불변식·법적 요구가 걸린 자리는 「지킬 때」가 아니라
  「어길 때」를 만들어 보십시오**
- **🔴 를 보고 「가드가 잡았다」로 읽은 것** — 고침을 빼고 검사를 돌렸더니
  빨갛길래 심기 검증이 끝난 줄 알았는데, 읽어 보니 **제 헬퍼가
  `SourceKind.GITHUB` 로 터진 것**이었습니다(그런 값이 없습니다). 가드는
  아무것도 안 재고 있었습니다(결함 327 회차). ⚠️ **왜 빨간지 읽으십시오** —
  「심었더니 🔴」는 「가드가 잡았다」가 아닙니다
- **씨앗에 범주가 하나뿐이라 가중치를 못 잰 것** — 「나간 사람의 역할
  비중이 안 바뀌는가」를 재는데 씨앗이 `task` 하나만 만들었습니다.
  **범주가 하나면 가중치가 무엇이든 1.0 으로 정규화**되어 어떤 프로파일도
  같은 값을 냅니다 — 고침을 빼도 초록이었습니다(결함 327). `code` 를
  더하니 잡혔습니다. 「이름 순과 번호 순이 같은 명단으로 정렬을 잰 것」과
  같은 함정입니다 — **갈라지는 데이터로 재십시오**
- **어휘 목록을 모델 주석에 적어 둔 것** — `AuditLog.action` 위에 여섯이
  적혀 있었는데 코드가 쓰는 행동은 **열다섯 가지**였고, 적힌 것 중 둘은
  **쓰는 곳이 0곳**이었습니다(결함 328). 주석은 아무것도 안 막습니다 —
  ⚠️ **어휘의 원본은 한 곳이고, 짝 가드가 「쓰는 것 == 이름표가 있는 것」을
  세게 하십시오.** 결함 297 이 `target` 종류에 둔 그 가드가 `action` 에는
  없었습니다 — **같은 표의 옆 칸은 따로 재야 합니다**.
  ⚠️ 그런데 `action` 가드는 **이미 있었습니다**(`test_requirements_map.py`) —
  「덮는가」만 보고 있었을 뿐입니다. 새로 만들었다가 지우고 기존 것을
  넓혔습니다. **가드를 만들기 전에 이미 있는지부터 세십시오**(실패 ②)
- **손으로 고른 목록을 한 번도 안 재 본 것** — 「사람의 기여 숫자를 바꾼
  행동」 집합에 삭제 기록 둘이 빠져 있었습니다. 재 보니 그 삭제 하나가
  **팀 전원의 몫**을 움직입니다(+2.30 · −1.45 · −0.85%p) — 이 저장소에서
  한 번에 제일 크게 움직이는 기록이었습니다(결함 329). ⚠️ **「이건 중요한
  것들 목록」 같은 집합을 보면 그 기준으로 전부 재 보십시오** — 손으로
  고른 것은 대개 만들 때 있던 것만 들어 있습니다
- **주석을 근거로 목록에 넣을 뻔한 것** — `github_login_changed` 옆에는
  「이 한 줄이 바뀌면 기여도가 **통째로** 바뀝니다」가 적혀 있었는데,
  지우고 다시 계산하니 **세 사람 다 한 자도 안 움직였습니다**(수집 시점에만
  쓰입니다). ⚠️ **주석이 단언해도 재 보십시오** — 결함 307·326 은 주석이
  맞고 코드가 틀린 경우였고, 이번은 반대입니다
- **같은 세션에서 두 번 계산해 원인을 가른 것** — 「지운 뒤」와 「안
  지웠다면」을 한 세션에서 계산하면 시간 감쇠가 안 섞여 **무엇 때문에
  움직였나**가 깨끗합니다(결함 327·329). 몫처럼 시간에 흔들리는 값을 잴
  때 쓰십시오
- **한글 질의를 인코딩 안 하고 curl 로 보낸 것** — 서버가 **400**
  (「Invalid HTTP request received」)을 주는데 그것을 「검색 0건」으로
  읽었습니다(결함 330 회차). `curl -G --data-urlencode` 를 쓰십시오 —
  안 그러면 **격리가 깨끗하다**는 결론이 아무것도 안 재고 나옵니다
- **응답 「길이」가 같은 것을 「같은 데이터」로 읽은 것** — 두 프로젝트의
  응답이 똑같길래 `project_id` 를 무시하나 했는데, 셋 다 **같은 오류**
  (422·500·404)를 돌려주고 있었습니다. ⚠️ **길이가 아니라 내용을
  읽으십시오** — 다만 그 의심을 파고들다 결함 330(달력 500)을 찾았습니다
- **막으려고 만든 함수를 한쪽 피연산자에만 건 것** — `clock.as_utc` 의
  docstring 은 `TypeError: can't compare offset-naive and offset-aware
  datetimes` 를 **인용해 두고** 있는데, 달력은 그 함수를 DB 값에만 걸고
  요청값에는 안 걸어 500 이 났습니다(결함 330). 바로 위 주석은 「한쪽으로
  맞춥니다」였습니다 — ⚠️ **비교를 고칠 때는 피연산자를 세십시오**
- **화면도 검사도 같은 모양만 보내던 것** — 달력 범위를 화면은 `Z`,
  검사는 `+00:00` 로 보내서 **시간대 없는 갈래가 한 번도 안 그려졌습니다.**
  검사 씨앗이 화면과 같은 값만 만들면 그 검사는 화면이 안 하는 일을
  구조적으로 못 봅니다
- **한 축에 두 뜻을 실어 정렬한 것** — 알림의 `at` 은 저장된 것에서는
  「일어난 때」, 마감 알림에서는 **「마감일」**인데 한 축에 놓고 내림차순
  정렬했습니다. 아직 안 온 마감이 맨 위, 이미 지난 마감이 채팅 호출보다
  아래였습니다(결함 331). ⚠️ 그리고 **화면은 그 축을 한 글자도 안 그리고
  있었습니다** — 축이 안 보이면 사람은 그냥 「새것부터」로 읽습니다.
  값에 **이름을 붙이면**(「마감 08-25」) 두 뜻이 보입니다
- **재기 전에 씨앗을 새로 안 심은 것** — 「담당자를 바꿨는데 알림이 안
  간다」를 파고드는데, **제 앞선 API 호출이 이미 그 표를 바꿔 놔서**
  스크래치 DB 가 아무것도 안 재고 있었습니다(결함 196 의 함정이 제 손에서).
  씨앗을 새로 심으니 「원래 그 사람 담당이었다」가 보였습니다 —
  ⚠️ **재현 안 됨을 적기 전에 씨앗부터 다시 심으십시오**
- **씨앗 표의 튜플을 user_id 로 읽은 것** — `("개발 환경 문서 정리", (1,), …)`
  의 `1` 을 사람 번호로 읽고 「알림이 안 갔다」고 적을 뻔했습니다.
  ⚠️ **씨앗을 근거로 쓸 때는 그 값이 무엇의 번호인지 확인하십시오**
- **한 문서 안에서 축이 갈린 것** — 주간 보고서의 「이 기간에 일어난 일」은
  기간으로 걸러 오는데 「사람별 기여」는 **프로젝트 전체**였습니다. 아무 일도
  없던 주에 세 사람의 몫이 최종 보고서와 똑같이 붙었습니다(결함 332).
  ⚠️ **한 문서가 두 범위를 말하면 각 범위에 이름을 붙이십시오** — 결함 331
  이 알림의 시각에서 만난 것과 같은 모양입니다
- **가드를 넓혀 놓고 내가 그 구멍으로 들어간 것** — 결함 292 의 마크다운
  가드가 보고서를 **`FINAL` 하나로만** 지어서, 제가 넣은 주간 전용 문단의
  별표를 못 봤습니다(초록). 결함 286 의 「걷는 자리가 한쪽뿐인지」 그대로
  입니다. ⚠️ **종류가 여럿인 것을 만들 때는 가드가 그 종류를 다 짓는지
  보십시오** — 옛 자 0건 → 넓힌 자 🔴 로 구멍을 증명하고 넓혔습니다
- **`force: true` 로 눌러 놓고 「클릭이 먹었다」고 본 것** — 그 단추는 진짜
  `disabled` 라 **아무 일도 안 일어났습니다**(결함 333 회차). ⚠️ `force` 는
  **도구가 거부하는 것**(`aria-disabled`)을 뚫을 뿐, **브라우저가 막는
  것**(`disabled`)은 못 뚫습니다 — 둘을 가르고, 누른 뒤 **상태가 바뀌었는지**
  를 보십시오
- **`@lib` 에 있는 것이 두 뿌리에 다 배선됐는지 안 센 것** — 결함 217 이
  만든 초안 보존 모듈이 **SPA 에만** 있었고 레거시는 새로고침 한 번에
  잃었습니다(결함 333). 231·306·320·321 에 이어 **네 번째**입니다.
  ⚠️ **`@lib` 에 무언가를 만들었으면 `grep -rl` 로 뿌리마다 부르는 파일
  수를 세십시오** — 「만들었다」와 「두 곳에서 쓴다」는 다릅니다
- **가드의 구멍이 하나가 아니었던 것** — 결함 246 의 「브라우저 달력 금지」
  가드가 (1) `getMonth()` 류 **낱말만** 막고 (2) `.ts` 만 걷고 있었습니다.
  그래서 `toLocaleString` 을 시간대 없이 부르는 자리 둘이 `.tsx` 와 `@lib`
  에서 살아 있었고, 확정 시각이 서울 `9. 2.` · 뉴욕 `9. 1.` 로 갈렸습니다
  (결함 334). ⚠️ **구멍을 하나 막았으면 그 자가 못 보는 다른 축도
  세십시오** — 자(무엇을 찾나)와 걷는 자리(어디서 찾나)는 **따로**
  낡습니다
- **심기 검증이 「초록」 쪽으로 헛돈 것** — `-k` 로 고른 검사가 **그
  이름의 검사가 아니었습니다.** 심어도 초록이라 「가드가 못 잡는다」로
  읽을 뻔했습니다(결함 334 회차). 결함 327 은 🔴 이 가드가 아니라 제
  헬퍼가 터진 것이었고 이번은 반대입니다 — ⚠️ **심은 뒤 그 검사가 실제로
  돌았는지**(이름·개수)를 보십시오
- **`position: fixed` 를 「조상 밖으로 나갔다」로 센 것** — 잘림 감지기가
  왼쪽 레일을 네 화면에서 잡았는데, 고정 요소는 **설계상** 조상의 클리핑을
  벗어납니다(본문이 `margin-left` 로 자리를 비워 둡니다). ⚠️ 위의
  「`offsetParent === null` 로 보임을 잰 것」과 **거울상**입니다 — 그때는
  고정 요소를 **빠뜨렸고** 이번엔 **잘못 넣었습니다.** 잘림을 잴 때는
  `position` 을 보고 고정 요소를 가르십시오
- **`fullPage` 캡처가 만든 겹침을 결함으로 읽을 뻔한 것** — 확대 200% 의
  전체 캡처에서 하단 탭바가 문서 한가운데에 그려져 경고 상자를 반으로
  자르고 있었습니다. `fullPage` 는 고정 요소를 **첫 뷰포트 위치에 한 번**
  합성합니다 — 뷰포트만 찍으니 제자리(창의 13%)였습니다. ⚠️ 결함 281 은
  「감사가 0건이어도 캡처는 보십시오」였고 이것은 그 반대입니다 —
  **캡처가 보여 주는 것이 캡처가 만든 것일 수 있습니다.** 고정 요소는
  뷰포트 캡처로 확인하십시오
- **두 뿌리를 나란히 놓고 「무슨 말을 하는가」를 안 읽어 본 것** — 로비의
  ②③ 동의를 레거시는 「거부해도 됩니다 — 트랙별 녹음이라 화자는 이미
  확정입니다」라고, SPA 는 「다음 회의에서 화자를 더 잘 알아봅니다」라고
  했습니다. `/app` 은 **동의하면 얻는 것**만 말하고 `webapp/src` 전체에서
  「거부해도」가 **0곳**이었는데, `docs/07` §2.3 은 ②③ 에 대해 「거부해도
  서비스 이용 가능」을 요구합니다(결함 335). ⚠️ 게다가 SPA 의 ③ 은 이
  제품이 **하지 않는 일**이었습니다 — 파이프라인이 `speaker_source` 에 쓰는
  값은 `"track"` 한 곳뿐이고 `Voiceprint(` 를 만드는 코드는 0곳입니다.
  ⚠️ **닫아 둔 결정을 뒤집은 것이 아닙니다** — docs/17 의 「「목소리 특징
  저장」 동의 — 결함이 아니었습니다」는 ③ 을 **묻는 것**이 옳다고 적으면서
  근거로 「화면도 이미 그렇게 말합니다」를 들었고, **그 전제가 한 뿌리에서
  깨져 있던 것**입니다. 낱개 화면을 아무리 눌러도 이 부류는 안 보입니다 —
  **한쪽만 보면 그쪽 말이 언제나 그럴듯**하기 때문입니다(결함 290).
  그리고 갈라진 자리를 찾았으면 **어느 쪽이 맞는지는 문서에서** 찾으십시오
- **가드가 「글자 자리」를 재고 있어서 내가 공용으로 올리자 터진 것** —
  「②③ 을 묻고 보낸다」 가드가 `id="keep-audio"` 라는 **글자**를 찾고
  있었습니다. 글을 `@lib` 으로 올리자 화면에서 그 글자가 사라져, 요구는
  하나도 안 바뀌었는데 자만 빨개졌습니다(결함 335). ⚠️ **같은 가드가 걷는
  자리도 레거시뿐이었습니다** — 뿌리가 갈라지는 자리에서 **일곱 번째**
  입니다(231 · 306 · 320 · 321 · 333 · 334 · 335). 자(무엇을 찾나)와 걷는
  자리(어디서 찾나)는 따로 낡습니다
- **「칸이 없다」를 값을 적기 전에 판정한 것** — 기여도 확정 줄의 입력칸이
  레거시 6개(값+이유) · SPA 3개(값만)라 불변식 ④ 위반으로 적을 뻔했습니다.
  SPA 는 **시스템 값과 다른 값을 적은 사람에게만** 이유 칸을 폅니다 —
  하나 적으니 1개, 셋 적으니 3개였고 비운 채 누르면 막습니다(재현 안 됨).
  ⚠️ **조건부로 나타나는 칸은 그 조건을 만들고 세십시오** — 「눌린 적이
  없는데 「누른 뒤」를 잰 것」이 입력칸에서 되풀이된 것입니다
- **`aria-label` 이 눈보다 많이 알고 있던 것** — 홈의 리본 옆에 `80%` 만
  적혀 있고 축 이름(「녹음 커버리지」)은 `aria-label` 에만 있었습니다.
  같은 요소에서 **귀가 눈보다 많이 아는 상태**입니다 — 그 목록의 옆 줄들이
  「처리 중」·「검토 필요」라 80% 까지 찬 막대는 **처리 진행률**로 읽힙니다
  (결함 336). 형제 자리인 기여도는 `ribbonReading()` 으로 「확신 45% ·
  모름 55%」를 눈에도 적고 있었고 **홈만 그 함수가 없었습니다.**
  ⚠️ 접근성 검사는 보통 「`aria-label` 이 **있는가**」를 보는데 이건 반대로
  **있어서** 결함이 그 뒤에 숨은 경우입니다 — **같은 요소의 두 채널을
  나란히 찍어** 보십시오. 그리고 값을 두 곳에서 따로 조립하고 있으면
  (`${'${Math.round(c*100)}'}%` 가 칸에 하나, 라벨에 하나) 한쪽에만 이름이
  빠져도 아무도 못 봅니다 — **이름 붙이는 일은 `@lib` 한 곳에서**
- **같은 그림이 자리마다 다른 뜻인데 그 뜻을 세어 본 적이 없는 것** —
  `docs/22` 는 리본이 크기마다 다른 뜻이라고 정해 뒀습니다(LG 시간축 ·
  MD 커버리지 · SM 확신도). 그런데 **그 뜻이 화면에 글로 있는지**는
  아무도 안 쟀습니다. 셋을 렌더해 나란히 놓으니 하나만 비어 있었습니다.
  ⚠️ **문서가 「이건 자리마다 다르다」고 적어 둔 것을 보면 자리를 전부
  세어 보십시오** — 다른 것이 규칙이면 빠진 것은 규칙 위반이 아니라
  **안 보입니다**
- **`aria-hidden` 을 「닿을 수 없다」로 읽은 것** — 로그인 화면의 왼쪽
  삽화가 `aria-hidden="true"` 인데 그 안에 진짜 `<button>` 이 있었고,
  **첫 Tab 이 거기**였습니다(결함 337). `aria-hidden` 은 **낭독기에게만
  하는 말**이고 초점 순서·마우스는 그대로입니다 — 낭독기에는 「없는
  것」이라고 해 놓고 키보드로는 제일 먼저 닿았습니다. 게다가 눌러도
  0바이트·0요소·0요청이었고(`onOpen={() => {}}`), `EvidenceChip.tsx`
  **자신의 첫 줄**이 「칩은 언제나 원문으로 이어져야 합니다」라고 적어
  둔 것을 어기고 있었습니다. ⚠️ **주석이 금지한 사용법이 같은 저장소
  안에 있는지 세어 보십시오** — 주석은 아무것도 안 막습니다
- **하나를 재러 갔다가 그 옆을 눌러 본 것** — 뒤로가기 관점으로 걸었고
  뒤로가기는 **깨끗했습니다**(주소·요소 수가 다섯 화면 다 짝이 맞고,
  로그아웃 뒤 뒤로 눌러도 앞사람 데이터가 안 보임). 결함은 그 화면에서
  **Tab 을 눌러서** 나왔습니다 — **관점이 빈손이어도 그 자리에서 다른
  자를 하나 더 대 보십시오**
- **글자만 커지는 축을 한 번도 안 잰 것** — 좁은 폭·확대 200%·다크·터치는
  다 쟀는데, **브라우저 기본 글자**를 키운 사람은 아무도 안 봤습니다.
  확대는 CSS 뷰포트를 줄이지만 이쪽은 **뷰포트를 그대로 둔 채 `rem` 만
  두 배**로 만들어서, **폭으로 접는 규칙이 하나도 안 걸립니다.** 32px 로
  키우니 SPA 홈의 회의 제목 다섯이 폭 0, 주 단추 다섯이 화면 밖
  (1624 > 1440)이었습니다 — 결함 213 이 **좁은 창**에서 고친 무너짐이
  **넓은 창**에서 그대로 다시 난 것입니다(결함 338). ⚠️ 원인은 그 고침이
  `@media (max-width: 1023.5px)` 라 **창 폭** 축이었던 것 — 「막는 길을
  하나만 막은 것」입니다. **접는 폭은 `rem` 으로 적으십시오**: 미디어
  쿼리의 `rem` 은 브라우저 기본 글자를 보므로 그게 「한 줄이 안 들어가는가」를
  실제로 재는 축입니다. ⚠️ 단위를 뿌리마다 세어 보십시오 — SPA 는 px 넷 ·
  rem 둘, 레거시는 px 하나 · rem 여섯이었고 레거시 화면들은 멀쩡했습니다
- **「잘림」을 재면서 낭독기 전용 글을 걸러내지 않은 것** — `.visually-hidden`
  은 **설계상** 자기 상자 안에서 잘립니다(1×1). 「자기 상자 안에서
  잘리는가」만 보면 그것이 전부 걸려 진짜 잘림이 묻힙니다
- **짝을 재기 전에 그 둘이 정말 짝인지 안 본 것** — `GithubEventKind` 를
  `search/view.ts` 의 `KIND_LABEL` 과 맞춰 보고 「셋이 영어로 찍힙니다」가
  나왔습니다. 그 표는 **검색 결과 종류**(업무·회의·회의 내용·GitHub)이지
  GitHub 이벤트 종류가 아닙니다 — 이름이 같다고 짝이 아닙니다(「재는 칸에
  임자가 있던 것」의 사촌). ⚠️ 그리고 **서버가 글자를 실어 보내면 두 벌이
  아닙니다** — `REACTION_LABEL`·`GITHUB_EVENT_LABEL` 은 화면에 사본이
  없어서 갈라질 수가 없습니다. 짝 가드에 넣을 것은 **양쪽에 사본이 있는 것**뿐
- **색을 빼고 읽어 본 적이 없는 것** — `forced-colors: active`(고대비)를
  대 보니 리본이 **통째로 사라지는데** 옆의 「확신 45% · 모름 55%」와
  못 잰 칸의 `—`+점선이 남아 뜻이 살았습니다. 「값은 글자로, 그림은 폭이나
  개수만」이 정확히 이걸 막고 있었습니다(재현 안 됨). ⚠️ 다만 **결함 336 을
  고치기 전이었다면 홈은 못 넘었습니다** — 리본이 사라지고 이름 없는 `80%`
  만 남았을 것입니다. **불변식이 왜 있는지는 그 불변식을 깨는 상황을
  만들어 봐야 보입니다**
- **탐지기가 답을 보내는데 화면이 안 읽은 것** — `decision_conflict` 의
  「왜」가 「무엇」을 그대로 되풀이해서 화면에 **같은 말이 두 줄**이었습니다
  (「앞의 결정을 뒤집었습니다」/「회의에서 앞의 결정을 뒤집었습니다」).
  탐지기는 `superseded_decision_id`·`decision_ids` 를 보내고 있었고 그
  주석이 「안 달면 **어느 결정인지 볼 방법이 없습니다**」라고 적어 뒀는데,
  세어 보니 그 필드를 읽는 화면 파일이 **두 뿌리 다 0개**였습니다 —
  다른 탐지기의 값은 전부 읽힙니다(결함 339). ⚠️ **「왜」 칸을 차지하면서
  아무 말도 안 하는 것**은 「지어내지 않는다」 규칙을 어기는 또 다른
  길입니다. 그리고 고칠 때는 **번호가 아니라 글**을 보내십시오(결함 293)
- **「한 번도 안 그려진 화면」은 어휘와 DB 를 나란히 놓으면 셀 수 있는 것** —
  `db/vocab.py` 의 허용값과 씨앗의 실제 값을 표로 놓으니 `meeting_events`
  여섯 중 셋이 0건이었습니다. ⚠️ **씨앗에 있는 값이 곧 실기가 내는 값은
  아닙니다** — 씨앗의 `unanswered_question` 은 탐지기가 아니라 LLM 쪽에서
  옵니다. 진짜 생산자를 **직접 돌려** 무엇이 나오는지 보십시오
- **발화를 심어 놓고 기여도가 움직이길 기다린 것** — `speaker_source` 를
  `diarization` ↔ `track` 으로 바꿔 가며 다시 계산했는데 근거 수가
  `11/10/3` 으로 **똑같았습니다.** 기여도는 `utterances` 가 아니라
  **`contribution_events`** 를 읽고 그 표는 회의 처리 때 채워집니다 —
  발화를 손으로 넣는 것만으로는 그 갈래가 **구조적으로** 안 움직입니다.
  ⚠️ **값이 안 바뀌면 「그 갈래가 원래 그런가」보다 「내가 그 갈래를
  건드리기는 했나」를 먼저 보십시오**
- **필드 이름을 모르고 찍어 놓고 값을 읽을 뻔한 것** — `MemberScore` 에
  `low`·`range` 가 있는 줄 알고 두 번 터졌고, `range_low` 를 몫인 양
  백분율로 찍어 **2990~5448%** 를 봤습니다(정규화 **전** 값입니다).
  **이상한 숫자를 보면 제 자를 먼저 보십시오**
- **같은 파일의 옆 칸에는 있는 가드가 정작 그 파일이 생긴 칸에는 없던 것** —
  `db/vocab.py` 는 「만들어진다고 적은 값에 진짜 만드는 코드가 있는가」를
  재려고 `speaker_source` 때문에 생긴 파일인데, 그 검사가 **옆 칸
  (`MeetingEventType`)에만** 있었습니다. 세어 보니 `STORED` 는 넷을
  주장하는데 쓰는 자리는 `track` 하나였고, `manual` 은 **`CERTAIN`**
  (확정으로 셈)에 들어 있으면서 사람이 지정하는 자리가 서버·화면 다
  0곳이었습니다(결함 340). 결함 328 의 「**같은 표의 옆 칸은 따로 재야
  합니다**」가 같은 파일 안에서 되풀이된 것입니다. ⚠️ 다만 **없는 기능을
  만들지 마십시오** — `docs/07` §2.3 이 「멀티트랙이면 애초에 불필요」라고
  적어 뒀으므로 고친 것은 **저장소가 스스로에 대해 하는 말**입니다
- **옆 칸의 자를 그대로 가져다 쓴 것** — 옆 칸은 「글자가 파일 어딘가
  보이는가」로 충분했는데, 이 칸은 `confidence.py` 가 **주석에** 어느 값이
  확정인지 적어 두고 있어서 그 자로는 안 쓰는 값이 「쓰인다」로 잡힙니다.
  **자는 칸마다 따로 정하십시오** — 주석을 걷고 `speaker_source=` **대입**만
  세야 맞습니다(결함 238 이 마크업에서 겪은 함정의 파이썬판)
- **갈라짐을 막으려고 만든 파일이 자기 자신과 갈라져 있던 것** —
  `db/vocab.py` 484줄이 「`GITHUB` 은 아직 만드는 코드가 0곳입니다.
  웹훅에서 부를 자리를 아직 안 잡았습니다」인데, **서른여덟 줄 아래**
  522줄은 「`_tell_the_assignee` 가 만듭니다」였습니다. 맞는 쪽은 아래고,
  웹훅 엔드포인트가 실제로 부르고 있었습니다(결함 341). ⚠️ **집합은
  맞았고 검사도 그 집합을 보고 있었습니다** — 안 보고 있던 것은 주석뿐
  입니다. 「주석은 아무것도 안 막습니다」가 **어휘 파일 자신에게서** 난
  것입니다. ⚠️ 값별 주장을 글로 적지 말고 **집합 하나만 가리키게** 하고,
  글로 적을 거면 **집합에도 넣으십시오** — 그 둘이 어긋나면 🔴 이 되게
  가드를 두었습니다

- **화면이 「갈 수 없습니다」라고 적은 것을 세어 본 적이 없는 것** — 알림
  화면의 링크 줄이 「회의를 지정하지 않아 로비·검토 화면으로 **갈 수
  없습니다**」라고 적고 있었는데, 같은 화면 왼쪽 열에
  `/lobby.html?meeting=N` 이 **여섯 개** 있었습니다(결함 343). 그 문장을
  만드는 함수의 주석에는 왜 필요한지가 적혀 있었고 그 말은 맞았습니다 —
  **셸에 회의 목록이 생기기 전**의 이야기였을 뿐입니다. ⚠️ **화면이 「할
  수 있다」고 말하면 그 자리를 세라**(결함 313)는 규칙에는 **반대 방향**이
  있습니다: **「할 수 없다」고 말해도 세십시오.** 그리고 그런 말은 상태를
  전부 만들어 봐야 보입니다 — 여기서는 넷이었습니다(회의 6개 · 0개 ·
  맥락 없음 × **창 폭** 1440/900). ⚠️ 대신 적을 말을 고를 때는 **폭과
  상관없이 참인 문**을 고르십시오(여기서는 「홈」) — 그리고 결함 306 대로
  **가라고 적기 전에 그 자리를 세십시오**

- **팀 하나를 잰 값을 사람 이름 밑에 그려 놓고 범위를 안 적은 것** —
  최종 보고서의 김민수 항목이 「근거 11건」 세 줄 아래에서 「녹음이 끊긴
  트랙이 있습니다 — **해당 팀원의** 발언량은 측정할 수 없습니다」라고
  적었습니다. 김민수의 커버리지는 1.0 이고 끊긴 트랙의 주인은 다른
  사람입니다(결함 344). 원인은 `compute_confidence` 가 **사람 루프 밖**에서
  한 번 불리고 그 한 벌이 사람 수만큼 복사되는 것 — 세 사람의 값이
  소수점까지 같습니다. ⚠️ **한 값이 사람마다 같은지 세어 보십시오** —
  같으면 그건 팀 값이고, 사람 이름 밑에 놓을 때는 **범위를 적어야** 합니다.
  두 화면은 이미 적고 있었고(「팀 신뢰도」·「팀 전체」) **팀 밖으로 나가는
  보고서만** 안 적었습니다(결함 290). ⚠️ 그리고 팀 문장 안에서 「해당
  팀원」처럼 **사람을 가리키면 받는 이가 없어** 카드 주인을 가리킨다고
  읽힙니다
- **씨앗이 그 기능을 한 번도 못 켜는 것** — 「내 녹음과 성문 지우기」는
  이 제품에서 법적으로 제일 민감한 단추인데, 씨앗의 `audio_assets`·
  `voiceprints`·`track_chunks` 가 **전부 0건**이라 시연에서는 언제나
  「지울 녹음이 없습니다」입니다. 눌러도 200 이 오고 아무 일도 안 일어
  납니다 — **눌렀는데 값이 안 바뀌면 「그 갈래가 원래 그런가」보다 「내가
  그 갈래를 건드리기는 했나」를 먼저 보십시오**. 손으로 심을 때는 실기가
  만드는 값이어야 합니다(`kind="raw"` — `"master"` 같은 값은 없습니다)
- **`pkill -f "uvicorn …"` 이 자기 명령줄을 잡은 것** — 셸이 스스로를 죽여
  exit 144 가 나고, 그 뒤 명령이 통째로 안 돕니다. `pkill -f "[u]vicorn …"`
  로 쓰십시오

- **씨앗이 언제나 서로 다른 값을 써서 「겹치면?」을 아무도 안 잰 것** —
  팀원 둘의 이름을 같게 만드니 설정 화면에 **되돌릴 수 없는 「내보내기」가
  글자까지 같은 줄 둘**에 붙었고, 기여도 확정 칸의 `aria-label` 도
  「이하늘 확정값」으로 같았습니다(결함 345). 이 제품은 사람을 이름으로
  부르기로 했고(결함 293·297) 그건 옳은데, 그 뒤로 **이름이 유일한
  손잡이**가 됐습니다. ⚠️ **제품이 이미 막고 있는 충돌을 찾으십시오** —
  설정 화면은 GitHub 아이디에 대해 「같은 아이디를 둘이 쓰면 남의 PR이 내
  기여가 됩니다」라고 적어 두고 **막고** 있었는데, 같은 위험이 이름에는
  그대로 열려 있었습니다. 그래서 손잡이도 거기 있었습니다(`github_login`
  은 프로젝트 안에서 유일하고 화면이 이미 받아 옵니다 — 결함 294).
  ⚠️ **겹칠 때만, 그리고 양쪽 다** 붙이십시오 — 한쪽만 붙이면 나머지가
  「이름표 없는 쪽」이 되어 소거법으로 읽어야 합니다. ⚠️ 붙여도 못 가르는
  경우(둘 다 미연결)에는 **그 사실을 적되 단추를 막지는 마십시오**
- **동명이인을 「나」와 겹치게 만들어 못 볼 뻔한 것** — 설정 화면은 자기
  줄에 「나」를 붙이므로 그 조합은 구분됩니다. **남 둘**이 겹쳐야 드러
  납니다 — 겹침을 잴 때는 **구분 표시가 이미 붙는 자리를 피해서** 만드십시오
- **판단을 두 벌 둬야 할 때 「알고 두는」 방법** — 보고서는 **기록**이라
  만든 순간의 글자를 저장하므로 이름표를 화면이 아니라 서버가 붙여야
  합니다. 그러면 같은 규칙이 파이썬·TS 두 벌이 되는데, **공용 사례
  파일**(`label_cases.json`)을 두 검사가 같이 읽게 하면 한쪽만 고칠 때
  **양쪽 다** 빨개집니다. 심어서 확인했습니다 — 그리고 「사례가 비어 있으면
  둘 다 조용히 통과한다」를 막는 검사를 양쪽에 같이 두십시오
- **앞 결함이 고친 「표」에 한 자리가 빠져 있던 것** — 결함 325 가
  「줄에 서 있는 것과 하고 있는 것은 다릅니다」로 서버·홈·로비 **셋**을
  갈라놓으면서 **검토 화면을 그 표에 안 넣었습니다.** 그래서 큐에 걸린
  회의를 열면 홈은 「처리 대기 — 아직 시작하지 않았습니다」, 검토는
  「처리하는 중 … 잠시 뒤에 새로고침하세요」였습니다(결함 346). ⚠️ **결함이
  고친 자리를 표로 적어 두면, 다음 사람이 그 표에 빠진 칸을 셀 수 있습니다** —
  325 는 표를 적어 뒀고 거기 검토 화면이 없다는 것이 단서였습니다
- **판단이 화면 파일 안에 있어서 조용히 낡은 것** — `emptyReviewState` 가
  `demo/review.tsx` 안에 있었습니다. 화면 코드에는 자동 검사가 없으므로
  옆에서 같은 판단이 고쳐지는 동안 이것만 묶인 채로 남았습니다.
  **`@lib` 로 옮기면 그 자리에 검사가 붙습니다**
- **같은 표를 두 모양으로 내보내는 자리에는 「묶음이 같은가」를 재십시오** —
  `reviewPhase`(SPA)와 `reviewEmptyState`(레거시)가 같은 상태표의 두
  투영입니다. 「같은 상태 집합을 가르는가」를 재자마자 **두 번째 결함**이
  나왔습니다: `pending` 이 한쪽에서만 갈라져 있어, **녹음도 안 한 회의**가
  「처리는 끝났는데 뽑을 만한 발언이 없었습니다」를 받고 있었습니다
  (결함 289 의 모양). ⚠️ 낱말이 아니라 **묶음**을 비교하십시오
- **한 화면의 빈 상자를 세십시오** — 검토 화면은 셋이었고 결함 284 가
  고친 것은 둘이었습니다. 세 번째(타임라인)는 상태를 안 받고 양다리
  문장을 돌려줬는데, 처리에 **실패한** 회의에게는 그 양다리가 「기다리면
  온다」로 읽힙니다(결함 346)
- **같은 사실을 옮기는 자리가 둘인데 한쪽만 어휘표를 안 거친 것** —
  활동 기록은 「PR 병합 · 박지원」인데 찾기는 `pull_request.merged ·
  jiwon-db` 였습니다(결함 347). `vocab.py` 는 「서버가 라벨을 만들어
  내려보냅니다 — 화면에 두 번째 표를 만들지 마십시오」라고 적어 뒀는데,
  **두 번째 표가 아니라 아예 안 거치는 곳**이 있었습니다. ⚠️ 「두 벌인가」
  만 세지 말고 **「거치기는 하는가」**도 세십시오
- **갈래를 둘로 적어 두고 셋째가 있던 것** — `who = user_name or
  actor_login` 의 주석은 「팀원이면 이름, 아니면 로그인」 둘만 적었습니다.
  셋째는 **로그인조차 없는** 경우(지워진 GitHub 계정 → `"user": null` →
  `.get("login","")`)이고, 그때 화면의 칸이 **통째로 사라집니다.** 빈 칸은
  「없음」이 아니라 「고장」으로 읽힙니다 — 지어내지 말고 **모른다고
  적으십시오**(결함 297)
- **`a ⊆ b` 짝 검사가 빈손으로 통과한 것** — 「두 화면이 같은 말을 하는가」를
  부분집합으로 썼는데, 한쪽이 비면 거저 참입니다. 심어도 안 잡혀서
  알았습니다(결함 347) — **양쪽이 실제로 그 데이터를 봤는지 먼저 세십시오**
- **이름표를 「영문이 없는가」로 잰 것** — 「PR 병합」의 `PR` 에 걸려
  빨개졌습니다. 한국어에서 그대로 쓰는 말입니다. 요구는 **내부 식별자가
  안 새는 것**이므로 `_`·`.` 과 「한국어가 있는가」로 재십시오
- **씨앗이 넷 다 같은 값이라 그 갈래가 한 번도 안 그려진 것** — 업무
  우선순위는 어휘가 넷(`0 긴급`~`3 낮음`)인데 씨앗은 **넷 다 `2`** 였습니다.
  실기 경로로 `0·1·3` 을 만들고 두 칸반을 나란히 놓으니 SPA 는 배지 셋을
  그리는데 **레거시에는 우선순위라는 글자가 한 자도 없었습니다**(결함 348).
  판단은 `@lib` 에 다 있었고(`priority.ts` — 어휘·배지 규칙·메뉴 항목)
  부르는 화면이 SPA 하나뿐이었습니다. 「한쪽 뿌리만」의 **열 번째**입니다
  (231·306·320·321·333·334·335·337·345). ⚠️ **라우트를 세는 가드는 이걸
  못 봅니다**(결함 315) — `PATCH /tasks/{id}` 는 옮기기가 이미 부르고 있어
  초록입니다. 안 불린 것은 **`priority` 라는 칸**이었습니다
- **마우스로 고쳐 놓고 키보드로 안 눌러 본 것** — 348 을 붙이고 키보드로
  누르니 값은 바뀌는데 **초점이 `body`** 였고, 내가 붙인 자리만이 아니라
  담당자·옮기기도 같았습니다(결함 349). 요청이 도는 동안 컨트롤에
  `disabled` 가 붙는데, 브라우저는 **초점을 쥔 요소가 `disabled` 가 되면
  초점을 버리고** 다시 `enabled` 가 돼도 안 돌려줍니다 — 카드를 한 칸
  옮길 때마다 문서 맨 앞에서 다시 Tab 입니다. ⚠️ **마우스로는 아무 일도
  안 일어나므로 눈으로는 구조적으로 안 보입니다.** 결함 280·303 의 세 번째
  자리입니다
- **두 갈래만 고치고 한 번 재 봐서 세 번째를 잡은 것** — 담당자·우선순위는
  초점이 돌아왔는데 **「옮기기」는 그대로 `body`** 였습니다. 그 버튼은
  카드가 다른 열로 가면서 **통째로 사라져** 되돌릴 데가 없었던 것입니다
  (결함 349). ⚠️ **다 고친 뒤에 한 번만 재면 이 갈래는 안 보입니다** —
  고치는 중간에 재십시오. 「한 갈래만 고치고 옆 갈래를 그대로 둔 것」
  (결함 298·301)이 될 뻔했습니다
- **되돌릴 때 「사람이 그 사이 누른 곳」을 뺏을 뻔한 것** — 초점을 언제나
  되돌리면 요청이 도는 동안 사람이 옮겨 간 자리를 가로챕니다. 되돌리는
  것은 **셋이 다 참일 때만**입니다: 기억한 자리가 있고 · 지금 아무도 안
  쥐고 있고(`body`나 `null`) · 그 자리가 아직 문서에 있다. ⚠️ `body` 와
  `null` 을 **같이** 보십시오 — 브라우저마다 초점을 어디로 떨어뜨리는지가
  다릅니다
- **붙여 놓고 렌더해 보니 카드마다 줄이 하나 는 것** — 여닫이를 세로로
  쌓았더니 44px 짜리 줄이 카드마다 하나 더 생겼고, **그 비용은 바로 옆
  주석이 이미 적어 둔 것**이었습니다(`.whoedit` — 「데스크톱에서는 높이를
  걷습니다」). 둘을 한 줄에 세워 되돌렸습니다. ⚠️ 밑금을 `8rem` 으로 줬다가
  카드 폭(≈12.5rem)에 둘이 못 들어가 **그대로 두 줄**이었던 것도 렌더해서
  잡았습니다 — `flex` 밑금은 눈으로 확인하십시오
- **심는 방법이 자보다 먼저 틀린 것** — 「기여도가 우선순위를 읽는가」
  가드에 `PLANTED_PRIORITY = 0` 을 심었는데 자가 `\bpriority\s*=` 라
  **대문자에 안 걸렸습니다.** `task.priority` 로 다시 심으니 잡혔습니다 —
  「안 잡힌다」를 **자의 문제**로 읽을 뻔했습니다

- **주석이 「눈으로만 읽히는 표시」라고 적어 두고 눈이 뭘 읽는지는 아무
  데서도 안 정한 것** — 회의 레일의 알약은 `3` 한 글자였고 낭독기만
  「업무 후보 3건 검토 대기」를 들었습니다(결함 350). 결함 336 과 같은
  모양인데, 여기서는 **읽힐 다른 뜻이 이미 정해져 있었습니다** — 이 셸은
  일부러 메신저를 본떴고(`docs/19`) 그 세계에서 채널 이름 옆의 둥근 알약은
  **안 읽은 개수**입니다. 이 제품에도 안 읽은 알림이 있고 같은 모양으로
  그립니다. ⚠️ **관습을 빌려 오면 그 관습이 붙여 놓은 뜻도 같이 옵니다**
- **취향과 결함을 「세어서」 갈라야 하는 것** — 「배지에 축 이름이 없다」는
  낱개로 보면 취향입니다. 두 뿌리의 세는 알약을 **전부 모아** 축 이름이
  어디 있는지 세니 여섯 중 다섯이 달고 있었고, 안 다는 것은 레일 하나
  뿐이었습니다(결함 350). ⚠️ 반대로 **우선순위 배지는 안 적었습니다** —
  이 제품의 칩은 전부 맨몸이라(`.avatar`·`.n`·`.kcard__marker`) 하나만
  골라 이름을 붙이면 **결함을 고치는 게 아니라 제품의 규칙을 나 혼자
  바꾸는 것**입니다. 「저 혼자 예외인가」가 가르는 자입니다
- **적을 것과 안 적을 것을 가른 뒤 「안 적은 것」도 적어 둔 것** — 레일의
  상태 점에도 눈에는 범례가 없습니다. 알약과 달리 **틀린 값을 믿게 만들지는
  않아서**(점은 값이 아니라 장식) 안 적었고, 그 이유를 결함 기록에 남겼습니다.
  ⚠️ **판단을 적어 두면 다음 사람이 다시 셀 수 있습니다** — 조용히 넘어가면
  다음 사람은 「아무도 안 봤다」와 「보고 넘어갔다」를 못 가릅니다

- **어휘가 「없는 능력」으로 등급을 가른 것** — `ProjectRole` 의 값 옆에
  「소유자는 넘겨줄 수는 있어도」·「관리자는 프로젝트를 지우지는 못함」이
  적혀 있었는데, 눌러 보니 **403**(소유자를 주는 길이 아무에게도 없음)과
  **405**(라우트가 없어 아무도 못 지움)였습니다(결함 351). 즉 뒤엣것은
  관리자를 소유자와 **갈라 주지 않습니다.** ⚠️ **할 수 있는 일의 원본은
  한 곳**(`permissions._ALLOWED`)이고, 값 옆에 다시 쓰면 두 벌이 됩니다 —
  결함 341 이 **같은 파일 자신**에서 겪은 그것입니다
- **「부르는 곳을 세는」 자를 라우트에만 대 본 것** — 결함 306 이 만든 그
  자를 **권한 어휘**에 대니 일곱 중 하나(`DELETE_PROJECT`)가 0곳이었습니다
  (결함 351). 게다가 그 값에는 꼼꼼한 주석이 붙어 있어 **읽으면 있는
  기능처럼 보입니다.** ⚠️ **자를 하나 만들었으면 다른 어휘에도 대 보십시오** —
  라우트·행동·상태·종류, 「집합인데 쓰는 곳이 있는가」를 물을 자리는 여럿입니다
- **내가 방금 넣은 글이 내 자를 망가뜨린 것** — 「값 옆에 능력을 적지 마라」
  가드를 `class …:.*?\n\n` 로 썼는데, **제가 방금 넣은 docstring 안에 빈
  줄이 있어서** 블록이 값 줄까지 못 갔습니다. 옛 문장을 그대로 심어도
  초록이었습니다(결함 351 회차). ⚠️ **자는 「몇 개를 봤는지」까지 세게
  하십시오** — 값 줄 개수가 어휘 개수와 같은지 확인하니 그 자체가 실패로
  잡힙니다. 결함 285·299 는 자가 좁아서, 이번은 **내 고침이 자를 비켜 가서** 였습니다

- **씨앗을 심고 응답 코드를 안 찍어 세 번을 헛돈 것** — 자정 넘김을
  재려고 일정을 심었는데 필드가 `starts_at` 이 아니라 `at` 이라 **422**
  였습니다. 그런데 세 시간대를 그대로 다 쟀고, 값이 셋 다 같길래
  하마터면 「팀 달력이 잘 돈다」고 적을 뻔했습니다 — **없는 회의를 세 번
  잰 것**입니다. 「눌린 적이 없는데 「누른 뒤」를 잰 것」이 씨앗에서
  되풀이됐습니다. ⚠️ **심었으면 응답을 찍고, 그다음에 재십시오**
- **이 문서가 「이걸 잴 때는 …」이라고 적어 둔 자리는 숙제입니다** —
  기록된 결정이 아닙니다. 훑어서 안 한 것을 하십시오. 자정 넘김이 그랬고,
  하고 나니 **초록**이라 다음 사람이 다시 안 재도 됩니다. ⚠️ **초록도
  적어 두십시오** — 안 적으면 다음 사람은 「아무도 안 봤다」와 「보고
  넘어갔다」를 못 가릅니다

- **두 화면의 「네트워크」를 나란히 안 놓아 본 것** — 같은 회의를 두
  검토 화면으로 열고 요청을 나란히 찍으니, SPA 가 `utterance-types`
  (REVIEW-005)와 `speaking`(AI-AUDIO-005)을 **아예 안 부르고** 있었습니다
  (결함 352). 화면 글자를 읽는 것으로는 「그 판이 원래 없는 건가」와
  「빠진 건가」를 못 가릅니다 — **요청을 세면 갈립니다**
- **주석이 「접었다」고 적어 두고 실제로는 없앤 것** — 그 판의 머리 주석은
  「통계·요약은 **접고** 전사가 주인공」인데, 요약만 접혀 있고 통계는 아예
  없었습니다. 「없앴다」가 아니라 「접었다」로 적혀 있으면 다음 사람은
  **어딘가 접혀 있다고 읽습니다**(결함 352)
- **「부르는 곳을 세는」 가드가 두 뿌리를 한 자루에 담고 있던 것** —
  결함 306 의 라우트 가드가 `frontend/src` 와 `webapp/src` 를 합쳐서
  세어, 한쪽만 불러도 초록이었습니다(결함 321 과 같은 모양 · 「한쪽 뿌리만」
  열한 번째). ⚠️ **그렇다고 뿌리마다 가르면 안 됩니다** — 라우트 82개 중
  레거시만 부르는 것이 28개인데 대부분은 **SPA 에 없는 화면**의 것입니다.
  **같은 화면이 두 뿌리에 다 있는 자리**만 좁혀서 재고, 기존 가드에는
  **자기가 못 보는 것**을 적어 그 검사를 가리키게 하십시오
- **내가 지어낸 예외를 내 낡음 검사가 잡은 것** — 새 검사에 「`timeline`
  은 SPA 만 부른다」고 적었는데 **레거시도 부릅니다.** 다만 여닫이를 펴야
  부르는 **늦은 호출**이라 네트워크에 안 찍혔던 것입니다(결함 352 회차).
  ⚠️ **예외는 재 보고 적으십시오** — 그리고 결함 306 의 「예외가 낡는 것도
  재라」는 그 예외를 **쓰는 사람 자신**에게도 걸립니다
- **네트워크에 안 찍혔다고 「안 부른다」로 읽은 것** — 늦게 부르는 자리
  (여닫이를 펴야 · 스크롤해야)는 첫 몇 초에 안 나옵니다. **소스에서도
  같이 세십시오** — 둘이 다르면 그 차이가 곧 「언제 부르는가」입니다

- **같은 이름의 자가 두 뜻을 겸한 것** — `atText` 가 **글자까지 똑같이**
  두 파일에 있었고 둘 다 `0` 을 「모른다」로 떨어뜨렸습니다. 사건·미해결
  사안에는 맞는 규칙입니다(근거가 없으면 구간이 `0~0`). 그런데 발화 한
  줄이 그 자를 써서, 회의 **첫 발화의 시각이 빈칸**으로 나갔습니다
  (결함 353). 발화에는 「모르는 시각」이 없고 `0` 은 **값**입니다.
  ⚠️ **두 벌을 지울 때는 「둘이 정말 같은 것을 재는가」를 먼저 보십시오** —
  이름이 같다고 뜻이 같은 것이 아니고, 뜻이 다르면 **이름을 갈라야**
  다음 사람이 아무거나 안 가져다 씁니다
- **씨앗이 경계값을 한 번도 안 만든 것** — 씨앗 회의는 전부 `0:01` 부터라
  `start_ms = 0` 이 여태 안 나왔습니다. 그런데 실기의 `to_segments` 는
  `start * hop_ms` 이고 `start` 는 0부터라, **녹음 버튼을 누르며 바로
  말하면** 첫 구간이 정확히 `0` 입니다(결함 353). ⚠️ **씨앗이 0·빈 문자열·
  최댓값을 만드는지 세어 보십시오** — 「빈 쪽 / 꽉 찬 쪽 / 중간」에 이어
  **경계값**이 또 하나의 관점입니다
- **탐지기가 0건일 때 탐지기를 의심한 것** — 세 갈래를 만들려다 두 번
  0건이 나왔는데 **둘 다 내 입력**이었습니다: `overlap_surge` 는 회의가
  6분 이상이어야 하는데 57초를 줬고, `decision_conflict` 는 `decisions`
  표를 읽는데 발화만 넣었으며, 다시 넣은 뒤에도 `content_words` 가 조사를
  걷어내 겹치는 낱말이 하나뿐이었습니다(결함 353 회차). ⚠️ **생산자를
  돌리기 전에 그 생산자가 요구하는 최소 조건을 읽으십시오**
- **처음으로 값을 그린 판을 눈으로 확인한 것** — 「누가 얼마나 말했나」는
  씨앗 회의가 전부 짧아 늘 「회의가 짧아 비중을 말하기 어렵습니다」였습니다.
  8분 회의를 만드니 처음으로 값이 나왔고, 이름 순·막대 없음·합 100% 를
  그때서야 실제로 확인했습니다. ⚠️ **「못 잰다」 갈래만 그려지는 판은
  아무도 검수한 적이 없는 판입니다**

- **씨앗이 구멍을 가려 준 것** — `/contributions` 는 **번호 순**으로
  내려오는데 시연 데이터에서 그 번호 순이 하필 `42.19 · 32.95 · 24.85`
  **완벽한 내림차순**입니다. 화면이 `orderForDisplay` 를 빼먹으면 그대로
  리더보드가 그려지는데 그것을 재는 검사가 없었습니다. ⚠️ **판단 함수에
  검사가 있는 것과 「화면이 그 함수를 거치는가」는 다른 질문입니다** —
  `@lib` 도 보고서도 검사가 있었고 그 사이만 비어 있었습니다
- **「테스트 전용」이라고 적힌 것이 쓰는 테스트가 0개이던 것** —
  `ranked_ids()`(점수 내림차순)의 docstring 은 「내부 검증·테스트 전용 ·
  UI에 순위를 노출하지 말 것」인데 **부르는 곳이 0곳**이었습니다. 그 말
  자체가 거짓이고, 불변식 ①을 지키는 것은 주석 한 줄이었습니다.
  ⚠️ **지우는 대신 제 일을 시켰습니다** — `docs/05` 가 그 함수를 쓸
  검증(Spearman 순위상관)을 적어 뒀으므로, 그것으로 「내려보내는 순서가
  순위가 아니다」를 증명하게 하고 짝 검사로 제품 호출 0곳을 못 박았습니다
- **템플릿 문자열 안에 백틱을 넣어 검사 파일이 아예 안 돈 것** —
  ``it(`⭐ ${'${rootName}'} — … **`orderForDisplay`** …`)`` 로 적었더니
  `ERR_INVALID_TYPESCRIPT_SYNTAX` 로 **파일 하나가 통째로** 실패했습니다.
  「심었는데 안 잡힌다」가 아니라 **자가 안 돌고 있었습니다** — 실패 줄을
  읽으십시오(결함 327 의 「왜 빨간지 읽으십시오」와 같은 부류)
- **「그 낱말이 파일에 있는가」로 배선을 잰 것** — 레거시는
  `orderForDisplay` 를 두 곳에서 부릅니다(초안 · 그리는 목록). 그리는
  쪽만 빼도 낱말은 남아 **심어도 초록**이었습니다. ⚠️ 배선을 잴 때는
  **그린 목록이 그 함수에서 나왔는가**로 좁히십시오 — 그러면서 자가 두
  번 더 틀렸습니다: `=` 뒤 창을 넓게 둬 `;` 를 넘어 **다음 문장의 호출을
  앞 변수 것으로** 붙였고, `.map(` 을 전부 세어 값 표를 만드는
  `new Map(….map(…))` 까지 「그린다」로 잡았습니다(JSX 안 `{x.map(` 만)

- **끊을 규칙은 있는데 「끊을 자리」가 없던 것** — `app.css` 는 `body` 에
  `overflow-wrap: break-word` 를 걸고 「긴 URL 은 끊어야 가로로 안
  밀립니다」라고 적어 뒀는데, 자기소개 칸이 **플렉스 항목**이라
  기본값 `min-width: auto` 로 **내용 최소 크기 아래로 안 줄어들었습니다.**
  끊을 필요가 생기기 전에 상자가 먼저 자라, 긴 주소 하나가 문서를
  1849px 로 늘리고 `overflow-x: hidden` 뒤로 넘긴 만큼을 영영 숨겼습니다
  (결함 354). ⚠️ **같은 파일이 「모든 자식에 `min-width: 0` 이 필요합니다」
  라고 적고 네 곳에 적용해 뒀는데 이 한 곳만 빠졌습니다** — 규칙을 적어
  두는 것과 **빠진 자리를 세는 것**은 다른 일입니다
- **가드의 「전제」를 같이 안 재면 조용히 뚫리는 것** — `min-width: 0` 만
  재면, 끊어 주는 `overflow-wrap` 이 사라졌을 때 **초록인 채로** 다시
  밀립니다. ⚠️ **가드가 기대는 다른 규칙도 같이 잽니다**
- **경계값을 「자」가 아니라 「상수」에서 찾은 것** — `@lib` 에서 화면이
  갈리는 상수를 전부 모아(`MAX_BIO` · `EVIDENCE_CHIPS_SHOWN` ·
  `LOW_CONFIDENCE` · `MIN_USABLE_COVERAGE` …) **씨앗이 그 경계를 넘는지**
  봤더니 `MAX_BIO` 가 나왔습니다 — 씨앗의 `bio` 는 전부 `null` 이라
  자기소개가 **한 번도 안 그려져** 있었습니다. ⚠️ 어휘 표(값 집합)에
  이은 **두 번째 표**입니다: 화면이 갈리는 **숫자**와 씨앗이 만드는 값
- **「담아 준 것」과 「고친 것」을 가른 것** — 같은 결함이 SPA 에서는
  `.pane__body`(`overflow-x: auto`)에 담겨 스크롤로 닿았습니다. 닿기는
  하니 결함으로 안 셀 뻔했는데, **원인은 같고 담아 준 조상만 다릅니다** —
  둘 다 고쳤습니다

- **씨앗이 만드는 「개수」를 세어 본 적이 없는 것** — 값 집합(어휘)·
  경계값(상수)에 이어 **세 번째 표**입니다: 씨앗이 각 종류를 **몇 개**
  만드는가. 세어 보니 프로젝트가 **하나**였고, 그래서 「어느 프로젝트인가」로
  갈리는 코드는 여태 전부 미검증이었습니다. 둘 만들고 링크를 세니 홈의
  「칸반 보기」가 `/kanban.html?meeting=6` 이라 **프로젝트 1의 보드**가
  열렸습니다(결함 355). ⚠️ **기본값이 「언제나 맞는」 상태를 조심하십시오** —
  레거시 칸반은 `params.get('project') ?? '1'` 이고, 프로젝트가 하나면 그
  기본값이 **항상 정답**입니다(결함 327 의 「범주가 하나면 가중치가 무엇이든
  1.0」과 같은 함정). ⚠️ 그리고 `nav/links.ts` 머리말은 **바로 이것을
  금지하고 있었습니다** — 같은 판단이 `home/next.ts`·`review/phase.ts` 에도
  있었고 그 둘만 안 지켰습니다(실패 ②). 칸반 주소를 만드는 열두 곳 중
  정확히 그 둘입니다
- **한 뿌리가 다른 뿌리의 구멍을 덮고 있던 것** — SPA 는 `spaHref` 가
  `/kanban.html…` 을 `/project/${'${projectId}'}/kanban` 으로 **갈아 끼우면서**
  프로젝트를 다시 붙여 멀쩡해 보였습니다. `@lib` 의 구멍은 그대로였고
  레거시로 들어온 사람만 당했습니다 — 결함 354 의 「담아 준 것이지 고친
  것은 아닙니다」와 같은 모양입니다. ⚠️ **한쪽이 초록이면 그쪽이 덮고
  있는 것인지 보십시오**
- **필요 없는 값을 인자로 끌고 다닐 뻔한 것** — `nextStepFor` 가 프로젝트를
  받게 되자 목록을 가르는 `sectionMeetings` 까지 그 값을 끌고 다녀야
  했습니다. 목록을 가르는 일에는 프로젝트가 아무 상관이 없습니다 —
  그럴 때는 **작은 판단을 이름 붙여 떼어 내고**(`waitsForPeople`) 원래
  함수도 그것을 부르게 하십시오. 안 그러면 다음 사람이 아무 숫자나 넣고,
  베끼면 판단이 두 벌이 됩니다(실패 ②)

- **정지 화면의 링크만 세고 걸어 보지 않은 것** — 링크 감사는 「지금
  화면의 `href` 가 맞는가」만 봅니다. 눌러서 걸으니 SPA 에서 프로젝트 2 로
  옮긴 뒤 「홈」을 누를 때 **1번으로 떨어지고**, 그때부터 탭 셋이 전부 1번을
  가리켰습니다(결함 356). 원인은 셸이 탭 주소를 **손으로 적은 것** —
  `appRailHref` 가 처음부터 `/?project=N` 을 만들고 있었고 그 머리말이
  「SPA 의 홈은 프로젝트 하나의 계기판입니다」라고 적어 뒀는데, **레일만
  그것을 부르고 탭은 사본**이었습니다(실패 ①+②가 한 자리에). 넷 중 셋은
  우연히 같은 글자라 **홈 하나만 달랐습니다**. ⚠️ **결함 305 의 「걸어
  보십시오」에는 두 축이 있습니다** — 닿는가, 그리고 **맥락을 들고 가는가**
- **가드에 어휘를 다시 적을 뻔한 것** — 「셸이 쓰는 화면 이름이 진짜
  있는가」를 재려는데 `ScreenId` 는 **타입이라 실행 시점에 없습니다.**
  목록을 가드에 베끼면 그 순간 두 벌입니다 — **원본 파일의 유니언을 읽어서**
  쓰면 화면이 늘 때 가드도 같이 자랍니다. ⚠️ 이 이름을 오타 내면
  `appRailHref` 가 조용히 **칸반으로** 떨어뜨립니다(`STAYS` 에 없는 화면의
  기본 동작) — 「기여도」를 눌렀는데 칸반이 열리고 오류는 안 납니다

- **「한쪽 뿌리만」처럼 생긴 것이 기록된 결정이던 것** — `/app/` 부터
  `<a href>` 를 따라 걸으니 주소 스물여섯에 닿는데 채팅·일정·알림·활동
  기록·찾기·보고서는 **하나도 없었습니다.** `navLinks` 는 그 여섯으로 가는
  링크를 정확히 만들고 부르는 곳은 레거시 하나 — 결함 305 와 **같은
  모양**입니다. 그런데 `docs/22` §R8 이 「레거시 6화면은 라우트만 유지 ·
  **내비에 새로 노출하지 않았습니다**」라고 적어 둔 자리였습니다.
  ⚠️ **부르는 곳이 한 곳이면 「왜 한 곳인지」가 적혀 있는지부터
  찾으십시오** — 없으면 결함이고, 있으면 두 기록이 어긋나는 자리이니
  고르지 말고 세 자리가 서로를 가리키게 하십시오(결함 318 의 방법).
  ⚠️ 가르는 자는 **「가라고 말하는가」** 입니다 — SPA 화면 열다섯을 훑어
  「보고서에서 …」 같은 안내가 0건인 것을 확인했습니다. 하나라도 있으면
  그때는 실패 ③ 이고 결함입니다(결함 306·312)

- **같은 사실을 옮기는 자리를 「둘」로 세고 셋째를 놓친 것** — 결함 347 이
  `github/presenting.py` 를 만들면서 「활동 기록 · 찾기」 둘을 맞췄는데,
  옮기는 자리는 **셋**이었습니다. 알림만 문장을 손으로 적어 같은 PR 병합을
  「연결된 PR **상태가 바뀌었습니다**」라고 불렀습니다(결함 357).
  ⚠️ **어휘표를 만들었으면 그 표를 거쳐야 할 자리를 전부 세십시오** —
  「두 벌인가」가 아니라 **「몇 벌인가」** 입니다
- **알림 행에 사건 번호가 없어 뭉뚱그린 것 — 집합이 하나면 말할 수
  있습니다** — `notifications` 는 일부러 글자를 저장하지 않아 「어느
  GitHub 사건인가」가 없습니다. 그런데 그 알림을 만들 수 있는 사건이
  **하나뿐**이면(`vocab.LINKED_TO_TASKS`) 그 하나의 이름을 정확히 말할 수
  있습니다. ⚠️ **집합에 기댈 때는 「집합이 늘면 어떻게 되는가」를 같이
  짜고 가드로 못 박으십시오** — 늘어난 뒤에도 이름을 말하면 리뷰가 붙었을
  뿐인데 병합됐다고 합니다
- **내 가드가 헬퍼만 재고 「사람이 읽는 글자」를 안 잰 것** — 옛 문장을
  그대로 심었는데 **검사 열여섯이 전부 초록**이었습니다. 헬퍼는 맞는 말을
  돌려주는데 그 헬퍼를 **안 부르면** 아무 일도 안 일어납니다 — 대표
  실패 ①이 제 가드에서 났습니다. ⚠️ **문장을 재는 검사는 그 문장을
  내보내는 함수까지 가서 재십시오**
- **씨앗이 어떤 종류를 0건으로 두면 그 화면은 검수된 적이 없는 것** —
  알림 여섯이 전부 0건이라 알림 화면은 **한 번도 알림을 그린 적이
  없었습니다.** 여섯을 진짜 생산자로 만들자마자 결함이 나왔고, 동시에
  결함 331 이 고쳐 둔 「축 이름」을 처음으로 눈으로 확인했습니다.
  ⚠️ 갈래가 「저장하는 것 / 지금 상태에서 만드는 것」으로 나뉘어 있으면
  **그 갈래대로** 만드십시오 — 파생은 DB 를 뒤져도 영영 0건입니다

- **불변식이 쓰는 말을 아는 값에 붙인 것** — 달력에서 잡은 회의의
  회의록이 「일시 **못 쟀습니다**」라고 적었습니다. 시각은 `scheduled_at`
  으로 **알고 있고**, 홈은 같은 회의를 「예정 09-15 10:00」이라고
  부릅니다(결함 358). 「못 쟀습니다」는 불변식 ③(**측정 불가 ≠ 0점**)이
  쓰는 말이라 아는 값에 붙이면 **그 말이 닳습니다** — 결함 319 가 결측
  표시에서 적어 둔 것의 문장판이고, 하필 **팀 밖으로 나가는 문서**였습니다
- **결함 287 이 만든 「한 벌」 표에 서버 쪽이 안 들어가 있던 것** —
  `meetingWhen` 의 주석은 「화면마다 `started_at ?? scheduled_at` 를 적으면
  한쪽만 고쳐집니다」인데, 보고서 builder 는 그 표 밖에서 `started_at`
  하나만 봤습니다. ⚠️ **판단을 한 벌로 모았으면 그 판단이 필요한 자리를
  언어 너머까지 세십시오** — 보고서는 기록이라 서버가 문장을 만듭니다
- **두 벌을 「알고」 둘 때는 글자가 아니라 판단을 맞추십시오** — 홈은
  「예정 09-15 10:00」(월-일), 회의록은 「예정 2026-09-15 10:00」(전체
  날짜)라 **형식이 다릅니다.** 공용 사례 파일에는 `at`·`planned` 같은
  **판단**만 담고 형식은 각자 재게 하십시오(결함 345 의 방법 + 결함 358).
  ⚠️ 그리고 사례에 **갈라지는 값이 있는지 재는 검사**를 같이 두십시오 —
  옛 코드도 통과하는 사례만 모으면 짝 검사가 아무것도 안 잽니다
- **막아 둔 자리 옆에 안 막은 길이 있던 것** — `cancel_meeting` 은 「행을
  지우면 그것들이 허공에 뜹니다」라며 **연** 회의를 막아 뒀는데, 안 연
  회의에도 딸리는 것이 하나 있었습니다(회의록). 무르면 회의는 사라지는데
  회의록만 보고서 목록에 남았고 **지울 방법이 0곳**이었습니다(결함 359).
  ⚠️ **「무엇을 막았나」를 읽었으면 「그 이유가 다른 데도 걸리나」를
  세십시오**
- **심은 것이 롤백된 줄 모르고 잰 것** — 회의 상태를 바꾸는 스크립트가
  `session_scope` **안에서** 예외로 죽어 통째로 롤백됐는데, 그 결과를
  「다시 만들어도 안 바뀐다」는 결함으로 적을 뻔했습니다. **심은 뒤에는
  세션을 새로 열어 다시 읽으십시오** — 「심었는지 확인」은 같은 트랜잭션
  안에서 찍은 값으로는 안 됩니다

- **어휘가 「화면에서 만들 수 있다」고 단언하는데 만드는 자리가 0곳이던 것** —
  `channels.kind` 는 둘인데 채팅 화면의 만드는 폼은 이름칸 하나뿐이고
  `kind: 'text'` 가 박혀 있었습니다. 서버는 `{kind:'voice'}` 를 201 로
  받고 화면은 음성 채널을 **제대로 그립니다**(`voiceChannelNote`) —
  읽는 쪽은 다 되어 있고 배선 하나가 없었습니다(결함 360). ⚠️ `vocab.py`
  는 「두 종류 다 **화면에서 만들 수 있고**」, `docs/20` 은 CHANNEL-002 를
  **✅** 라고 적어 뒀습니다 — **주석과 대조표가 코드보다 앞서 있었습니다**
  (결함 341 과 같은 파일에서 또). ⚠️ **「어휘에 있는데 만드는 코드가
  0곳」(결함 340)과 「만드는 코드는 있는데 부를 자리가 0곳」(실패 ①)을
  가르십시오** — 서버에 넣어 보고 201 이 나오면 뒤엣것이고, 고칠 것은
  어휘가 아니라 배선입니다
- **공용 규칙과 싸운 것** — `.ckind label { display: grid }` 를 줬는데
  안 먹었습니다. `app.css` 의 `label:has(> input[type='radio'])` 가
  (0,2,1)로 `.ckind label`(0,1,1)을 **이깁니다** — 렌더해서 보니 「음성
  채널」이 「음성 / 채널」 두 줄로 쪼개져 있었습니다(결함 250 의 부류).
  그 공용 규칙은 줄 전체를 44px 접촉면으로 만들려고 **일부러** 있는
  것이라, 뒤집는 대신 `flex-wrap` 만 더했습니다. ⚠️ **안 먹는 규칙을
  보면 「누가 이기나」부터 세고, 이긴 쪽이 왜 있는지 읽으십시오**
- **출력을 `| head` 로 잘라 캡처가 안 찍힌 것** — node 가 SIGPIPE 로 죽어
  스크린샷을 찍기 전에 끝났고, 제가 연 그림은 **8분 전 것**이었습니다.
  「고쳤는데 그림이 그대로」를 세 번 보고서야 파일 시각을 봤습니다 —
  ⚠️ **캡처를 만드는 명령은 파이프로 자르지 마십시오**(결함 320 회차의
  「내가 방금 찍은 그림인지 확인 안 한 것」이 다른 원인으로 되풀이됐습니다)

- **「흐리게」가 있던 색을 그대로 다시 칠하고 있던 것** — SPA 의 0점 발언
  유형 규칙이 `.tally--zero { color: var(--c-ink-muted) }` 인데, 그 줄이
  담긴 `.disc__body` 가 **이미 `--c-ink-muted`** 였습니다. 클래스도 붙고
  규칙도 있고 주석까지 「흐리게」라고 적혀 있는데 열세 줄이 두 테마에서
  전부 같은 색이었습니다(결함 361). 결함 164 의 「심었는데 아무 일도 안
  일어난 것」이 CSS 에서 난 것입니다 — ⚠️ **색을 정할 때는 그 자리가
  **물려받는 색**을 먼저 재십시오.** 그리고 가드는 「그 클래스가 있는가」가
  아니라 **「물려받는 색과 다른 색을 내는가」**를 재야 합니다
- **한 축을 고치고 다른 축에서 다시 죽은 것** — 색을 고쳐 밝게·다크 둘 다
  갈라지는 것을 보고 끝낼 뻔했는데, `forced-colors: active`(고대비)로 재니
  **다시 열세 줄이 같은 검정**이었습니다. 고대비는 색을 통째로 덮습니다.
  레거시는 숫자의 **무게**(600/400)까지 떨어뜨려 살아남고 있었습니다 —
  ⚠️ **뜻을 색 하나에 실었으면 고대비로 재 보십시오.** 무게·모양은 안
  덮입니다. 결함 280 의 「한 축만 재고 다른 축이 죽은 것」이 색에서 난 것입니다
- **씨앗이 0점 갈래를 한 줄만 만들어 눈으로 가릴 수 없던 것** — 발언 유형
  열셋 중 씨앗이 여덟만 만들어 `social`(맞장구)이 0건이었고, 0점 줄이
  `기타` 하나뿐이면 **「이게 흐린 건지」를 눈으로 못 가릅니다.** 분류기를
  직접 돌리니 다섯을 다 만들 수 있었습니다 — ⚠️ **없던 것이 생산자인지
  씨앗인지 가르십시오**(앞은 결함 340, 뒤는 그냥 안 그려 본 것). 그리고
  「갈라지는 데이터로 재라」는 규칙이 **색에도** 걸립니다
- **판정에 넣을 문자열이 출력 끝에 있다고 가정한 것** — 레거시 빌드의
  성공 표시(「화면을 빌드했고」)는 `build:demo` 가 찍고 그 **뒤에**
  tailwind 출력이 더 붙습니다. `tail -3 | grep` 이 그것을 잘라 **성공한
  빌드를 「실패」로** 읽었습니다 — ⚠️ 판정은 **출력 전체**에 거십시오
  (결함 299 의 「자가 끝에 올 때만 재고 있던 것」이 셸에서 난 것입니다)

- **고를 것이 하나뿐인 선택칸을 그린 것** — 관리자가 팀원을 볼 때 등급
  `select` 의 옵션이 `["팀원"]` 하나였습니다. 그 사람은 **이미 팀원**이라
  눌러도 아무 일도 안 나고 요청도 안 갑니다(결함 362). 원인은
  `assignableRoles('admin')`(내가 **줄 수 있는** 등급)을 **상대가 지금
  무엇인지 보지 않고** 그대로 그린 것 — ⚠️ **「내가 줄 수 있는 것」과
  「이 사람에게 지금과 다른 것을 줄 수 있는가」는 다른 질문입니다.**
  고를 것이 없으면 칸을 그리지 마십시오(등급은 옆에 글자로 남습니다).
  ⚠️ 결함 316 의 「버튼을 지우지 말고 이유를 말하라」와 **다른 경우**입니다
  — 저기는 할 수 있는 일이 막힌 것이고 이쪽은 **처음부터 없는** 것입니다
- **컨트롤을 `<button>` 으로만 센 것** — 관리자 화면을 훑으면서 버튼만
  세고 「등급을 바꿀 자리가 없다」로 적을 뻔했습니다. 그건 `<select>`
  입니다(결함 362 회차). ⚠️ **자를 하나 만들었으면 그 자가 못 보는 것을
  같이 적으십시오** — `select,input,[role=combobox]` 까지 세야 합니다
- **메뉴 항목을 「잎 요소」로 세어 글자가 조각난 것** — 칸반 카드 메뉴를
  자식 없는 요소로 셌더니 `["1","2","4"]` 라는 맨 숫자가 나와 「라벨이
  없다」로 읽힐 뻔했습니다. `[role=menuitem]` 로 세니 「← 할 일로1」로
  멀쩡했습니다(숫자는 열 번호 뱃지). ⚠️ **한 항목이 여러 요소면 잎만
  세는 자는 그것을 조각냅니다** — 역할로 집으십시오
- **씨앗이 권한 3단계의 가운데를 한 번도 안 만든 것** — 소유자 하나·팀원
  둘뿐이라 `admin` 으로 로그인해 본 적이 없었고, 결함 362 는 그 등급으로
  걸어야만 보입니다. ⚠️ **등급·상태가 여러 단계면 가운데를 만들어
  보십시오** — 양 끝만 만들면 가운데 갈래는 전부 미검증입니다

- **React controlled 입력에 `.value =` 를 넣고 「눌렀다」고 본 것** — 찾기
  칸에 글자를 넣고 「찾기」를 눌렀는데 **요청이 0건**이었습니다. React 가
  값을 쥐고 있어서 `input` 이벤트를 손으로 흘려도 상태가 안 바뀝니다.
  `fill()` 로 바꾸니 `?q=…` 가 나갔습니다 — ⚠️ **입력을 넣었으면 요청을
  찍어 확인하십시오.** 「서버가 답해야 켜지는 체크박스를 `check()` 로
  누른 것」의 입력칸판입니다
- **목록 항목을 「그 글자가 든 아무 요소」로 집어 못 누른 것** — 보고서
  목록에서 누를 것은 `button.ritem` 이었는데 `li` 를 눌렀습니다.
  `눌렸나=false` 를 찍어 둬서 「보고서를 열어도 아무것도 안 나온다」를
  안 적었습니다 — ⚠️ **누른 뒤를 재기 전에 눌렸는지를 찍으십시오**
- **재처리 순서를 안 밟고 「멱등이 아니다」로 읽을 뻔한 것** —
  `record_utterance_events` 를 그냥 불렀더니 `IntegrityError` 로 배치가
  통째로 죽었습니다. 실기의 재처리 경로는 **먼저 `forget_meeting_events`
  로 지우고** 다시 만들고, 그 주석이 이유까지 적어 뒀습니다(「안 지우면
  재처리할 때마다 점수가 누적된다」). ⚠️ **제품의 경로를 그대로
  밟으십시오** — 중간 함수 하나만 부르면 그 함수가 기대는 전제가 빠집니다
- **두 벌인데 결함으로 세지 않은 것 — 그리고 그 판단을 적어 둔 것** —
  `search_service.py` 가 `started_at or scheduled_at` 을 직접 적습니다.
  결함 358 이 그 판단을 `meeting_when` 으로 모았으니 **파이썬에 두
  벌**입니다. 그런데 두 값이 지금 같고 검색의 `at` 은 **어느 갈래도
  화면에 안 그려집니다** — 틀린 값을 재현할 수 없으므로 결함으로 안
  셌습니다. ⚠️ **두 벌인 것과 틀린 것은 다릅니다** — 세되, 재현 못 하면
  「결함」이 아니라 **「갈라질 수 있는 자리」**로 적고 조건(검색이 `at` 을
  그리기 시작하면)까지 남기십시오

- **「저 혼자 예외인가」는 유죄도 무죄도 냅니다** — 결함 362 는 그 자로
  **1개 중 1개**가 나와 결함이 됐고, 「강등된 뒤에도 화면이 옛 등급을
  적는다」는 **23개 중 0개**(`useMutation` 스물셋이 전부 `onSuccess`
  에서만 다시 받아 옵니다)라 **관습으로 판명**됐습니다. 하나만 고치면
  제품 규칙을 나 혼자 바꾸는 것이라 안 고쳤습니다. ⚠️ **세어 보기
  전에는 어느 쪽인지 알 수 없습니다** — 그리고 뒤집고 싶으면 스물셋을
  다 놓고 정하십시오
- **권한이 바뀌는 순간은 「위험한 방향」만 재면 됩니다** — 승격 중에 탭이
  열려 있는 것은 「새 능력이 안 보인다」뿐이라 해가 없습니다. 되돌릴 수
  없는 쪽은 **강등**이고, 옛 화면에서 누른 되돌릴 수 없는 작업을 **서버가
  막는지**를 봐야 합니다. ⚠️ **403 을 보고 넘기지 말고 목록을 다시 읽어**
  그 사람이 남아 있는지 세십시오 — 화면 글자가 아니라 데이터로 확인합니다

- **부르는 자리를 세고 「만들어지는 것」이라고 읽은 것** — 「씨앗이 회의를
  몇 개 만드나」를 재려고 대본의 `m.Meeting(` 을 셌더니 **2** 였습니다.
  그중 하나가 **루프 안**이라 실제로는 5행을 만듭니다. ⚠️ **개수를 재려면
  행을 세십시오** — 씨앗을 실제로 돌리는 파일(`test_demo_path.py`)에 두면
  `session.query(Model).count()` 로 잴 수 있습니다. 결함 306 의 「부르는
  곳을 세는」 자는 **있는가**를 재는 것이지 **몇 개인가**가 아닙니다
- **「칸을 잘못 집어 0」과 「진짜 0」을 못 가른 것** — 어휘 전수 대조에서
  `channels.kind` 가 0건으로 나왔습니다. 이름을 틀린 다른 여섯
  (`consents`→`recording_consents`, 반응→`message_reactions.mark`)과
  **같은 모양**인데, 이것은 정말 0행이었습니다. ⚠️ **표를 열어 행 수를
  찍어야** 갈립니다 — 그리고 `PresenceStatus` 처럼 **칸이 아예 없는**
  (계산해서 내려보내는) 것도 있습니다
- **문서가 스스로에 대해 틀린 것** — `AGENTS.md` 가 「씨앗은 회의 5개·
  **채널 2개**」라고 적고 있었는데 씨앗은 채널을 **하나도** 안 만듭니다
  (`channels` 0행 · API 도 `[]`). 「문서에 적힌 숫자를 그대로 믿지 말고
  세어 보세요」가 **적혀 있는 파일**이 틀린 숫자를 들고 있었습니다
  (결함 341 의 모양). ⚠️ 제품 동작이 아니라 문서라 번호는 안 붙였고,
  `test_demo_path.py` 에 **씨앗 행 수 ↔ 문서 주장** 짝 가드를 뒀습니다
- **없는 헬퍼 이름을 지어내 부른 것** — 가드에 `_strip_python_comments`
  를 썼는데 그런 함수가 없습니다. ⚠️ **있는지부터 보고 쓰십시오**

- **가드가 「한 파일을 읽는 것」을 「전부 본다」로 믿은 것** —
  `MIN_USABLE_COVERAGE` 가 **세 벌**이었습니다(서버 · `recording/
  timeline.ts` · `lobby/room.ts`). 그 셋을 맞추라고 만든 짝 가드는
  `FRONT_CONSTANTS` **한 파일**만 읽고, 그 옆 주석은 「프런트 파일은
  전부 여기입니다」라고 적고 있었습니다 — **사실이 아니었습니다**.
  로비의 값을 0.5 로 바꾸고 **번들까지 다시 만든 뒤** 돌리니 pytest
  2145 · 프런트 1969 이 전부 초록이었습니다(결함 363). 「한쪽 뿌리만」의
  **상수판**입니다 — 여태는 화면 뿌리가 갈렸고 이번은 **같은 뿌리 안에서
  파일이** 갈렸습니다. ⚠️ **가드를 넓히는 것보다 사본을 없애는 것이
  낫습니다** — 넓히면 갈라짐을 **감지**할 뿐이고, 없애면 **못 갈라집니다**
- **사본을 되살릴 때 값을 「같게」 두고도 잡히는지 보십시오** — 값이
  같아도 잡혀야 **갈라지기 전에** 막는 자입니다. 값을 갈라 놓아야만
  잡히는 자는 이미 한 번 틀린 뒤에야 웁니다(결함 363)
- **심을 때 번들까지 다시 만드십시오** — 소스만 바꾸고 돌렸더니 프런트가
  3건 실패했는데 셋 다 **낡은 번들** 검사였고 값에 대한 것은 하나도
  없었습니다. 그걸 「잡혔다」로 읽었으면 결함을 놓쳤을 것입니다
  (결함 327 의 「왜 빨간지 읽으십시오」)
- **`export` 를 요구하면 모듈 전용 상수를 놓칩니다** — 정의를 세는 자를
  `export const` 로 썼다가 짝 상수 셋 중 둘이 「0곳」으로 나왔습니다.
  그 둘은 파일 안에서만 쓰는 `const` 입니다(결함 363 회차)
- **이름이 같다고 사본은 아닙니다** — `LOW_CONFIDENCE` 가 두 곳에 다른
  값(0.6 · 0.7)으로 있는데, **서로 다른 것을 재는 서로 다른 문턱**입니다
  (기여도 신뢰도 ↔ 후보 확신도). 한쪽을 고쳐도 다른 쪽이 안 틀려지므로
  사본이 아닙니다. ⚠️ **모으지 마십시오** — 모으는 순간 「같은 값이어야
  한다」는 없던 요구가 생깁니다

- **한 종류의 개수를 보고 화면 전체로 일반화한 것** — 「후보의 근거가
  1~2건이니 이 화면은 접을 일이 없다」고 적어 두고 다음 회차에 확인하니,
  같은 화면의 `repeated_discussion` 이 근거를 **11건** 들고 있어 접는
  갈래는 **씨앗 그대로도 이미 그려지고** 있었습니다. ⚠️ **화면 하나에
  값을 넣는 생산자가 여럿이면 그 전부를 세십시오** — 하나만 세고 화면을
  말하면 다음 사람이 없는 것을 찾으러 갑니다
- **「내가 안 넣은 값」이 결과에 섞인 것이 「남의 칸을 재고 있다」는
  신호입니다** — 근거 12건을 심고 화면에서 `+6` 을 보고 off-by-one 으로
  적을 뻔했는데, 펴 보니 `#13`·**`#14`** 가 있었습니다. 심지 않은
  번호입니다. 그 묶음은 제 후보가 아니라 다른 항목의 것이었고 그쪽
  기준으로는 `+6` 이 맞습니다. ⚠️ **심은 값과 나온 값을 나란히 놓으면
  갈립니다** — 「그 이름에 임자가 있던 것」의 알아채는 법입니다

- **넓은 창에서만 보고 「괜찮다」로 넘긴 것** — 근거가 열둘인 후보 카드가
  1440px 에서는 한 줄(494×22)인데 **창 반쪽(720px)에서는 네 줄**(228×78)로
  제목 바로 아래를 덮었습니다(결함 364). `overflow` 도 없고 잘리지도 않아
  **넘침 감사는 0건**입니다 — 아무것도 잘못되지 않았는데 자리만 먹습니다
  (결함 281 의 「넘침만 재고 「자라는 것」을 못 본 것」). ⚠️ **접는 판단이
  `@lib` 에 있으면 그리는 자리를 전부 세십시오** — `splitEvidenceChips` 는
  그 해악을 머리말에 적어 두고도 **부르는 곳이 하나**였습니다
- **선택자를 못 찾으면 소스를 읽으십시오** — 앞 회차에 후보 카드를
  선택자로 세 번 헛짚고 「못 본 자리」로 남겼는데, `grep` 한 번으로
  찾았습니다. 어디서 그리는지 알면 **무엇을 재야 하는지도** 정해집니다
- **가드가 「맞는 자리」까지 세어 고쳐 놓고도 빨간 것** — 「낱개 칩을
  늘어놓지 마라」 가드가 **접는 래퍼 자신의 `map`** 까지 셌습니다. 그건
  맞는 자리입니다. 「왜 빨간지」를 읽어서 알았습니다(결함 327) — ⚠️ 자를
  만들 때 **예외로 두어야 할 자리**를 같이 정하십시오

- **막는 길을 세지 않고 문장을 하나만 둔 것** — 레거시 검토의 「제출」이
  막히는 길은 둘인데(`blocked > 0` · 「아직 아무것도 안 정함」) 화면은
  앞엣것만 적고 뒤엣것에는 **빈 문자열**을 그렸습니다. 뒤엣것이 검토
  화면을 연 사람이 **맨 처음 보는 상태**라, 후보 열둘을 앞에 두고 단추가
  죽어 있는데 왜인지가 화면 어디에도 없었습니다(결함 365). 결함 326 의
  「어휘는 셋인데 문구가 둘이던 것」과 같은 모양입니다 — ⚠️ **조건식이
  거짓이 되는 길을 전부 세고, 그 개수만큼 문장을 두십시오.** 그리고
  가드는 낱개 사례를 늘리지 말고 **길을 전수로 만들어** 「하나도 빈
  문자열이 아닌가」를 재게 하십시오
- **진짜 `disabled` 는 「닿을 수 없다」입니다** — 그 단추는 Tab 으로
  문서를 **한 바퀴(146번)** 돌아도 한 번도 안 닿았고, 닿지 못하니
  `aria-describedby` 로 사유를 들려줄 수도 없습니다. 결함 234 가 설정의
  「저장」 넷에서 이미 고치고 `lib/ui/save.ts` 머리말에 규칙까지 적어
  뒀는데, 검토 화면이 옛 모양으로 남아 있었습니다 — ⚠️ **「기록된 결정」이
  아니라 「적어만 두고 간 숙제」입니다.** 규칙을 적었으면 **그 규칙이
  걸리는 자리를 세십시오**
- **안 고친 것을 세어 적으면 다음 사람이 셀 수 있습니다** — 레거시에서
  조건으로 막는 진짜 `disabled` 는 **열 곳**입니다. 하나만 고쳤고, 고른
  이유(사유를 적을 칸이 이미 있는데 한 갈래에만 비어 있었다)와 안 잰
  아홉을 결함 기록에 적었습니다. ⚠️ **「저 혼자 예외인가」가 무죄를 낼
  때도 세어야 합니다** — 세어 보지 않고 하나만 고치면 제품 규칙을 나
  혼자 바꾸는 것이고(결함 350), 세어 보고 이유를 적으면 숙제가 남습니다
- **잠금을 옮겼더니 남의 가드가 「글자 자리」를 재고 있던 것** — 결함 89 의
  「누르는 동안 잠근다」 가드가 `disabled={…flag…}` 라는 **글자**만 봤습니다.
  잠금을 `aria-disabled` + `onClick` 의 되돌아가기로 옮기자 **요구는 하나도
  안 바뀌었는데** 그 자만 빨개졌습니다(결함 335 와 같은 부류 · 이번 세션
  네 번째). 자를 요구 쪽으로 넓혔습니다 — **그리는가**와 **누르는 손이 그
  값을 보고 되돌아가는가**를 **둘 다** 봅니다. ⚠️ 그리기만 하고 안
  되돌아가면 `aria-disabled` 는 **낭독기에게 하는 말일 뿐**이고 요청은 두
  번 나갑니다(결함 280 이 모달에서 적어 둔 그것)
- **뜻이 다른 두 규칙을 「같아 보인다」고 합칠 뻔한 것** — SPA 의 「검토
  끝내기」는 「하나도 남기지 말고 다 처리하라」이고 레거시의 「제출」은
  「정한 것만 올리고 나머지는 나중에」입니다(그래서 카드마다 「나중에
  검토」가 있습니다). 함수를 하나로 합치면 **없던 요구가 생깁니다** —
  ⚠️ 이름을 갈라 두고(`whyCannotSubmitBatch`) **왜 갈랐는지**를 머리말에
  적으십시오(결함 353 · 363 의 `LOW_CONFIDENCE` 와 같은 판단)
- **한쪽 뿌리에만 없는 것이 결함이 아닐 수도 있는 것** — `splitEvidenceChips`
  를 부르는 곳은 레거시에 **0곳**입니다. 그런데 레거시는 근거를 칩이
  아니라 **쉼표로 이은 한 줄**로 그려서 열둘이어도 20px 입니다 — 결함
  364 의 해악(자란다)이 **애초에 없는 모양**입니다. ⚠️ **「부르는 곳이
  0곳」을 보면 그 자리가 같은 해악을 겪는지 재 보십시오** — 결함 354 의
  「담아 준 것이지 고친 것은 아닙니다」와 **반대 방향**입니다
- **카드 제목을 `textContent` 로 찾은 것** — 레거시 후보 카드의 제목은
  고칠 수 있는 `<input value>` 라 `textContent` 에 **안 잡힙니다.**
  「못 찾음」이 나와서 알았습니다 — ⚠️ 값을 읽을 때는 그 칸이 **입력칸인지**
  보십시오(「컨트롤을 `<button>` 으로만 센 것」의 사촌)
- **같은 카드를 열두 번 누른 것** — `.first()` 로 잡아 놓고 반복해서
  「12건에서 11건으로 가고 멈춘다」를 결함으로 적을 뻔했습니다. **요청이
  0건**인 것을 보고 알았습니다(초안 모델이라 제출 전에는 요청이 안
  나갑니다). ⚠️ **누른 뒤가 안 바뀌면 「같은 것을 또 눌렀나」부터
  보십시오** — 목록을 훑을 때는 `.nth(i)` 로 자리를 옮기십시오
- **`pkill -f "[u]vicorn …"` 을 다른 명령과 같은 줄에 둔 것** — `[u]` 로
  써도 **같은 명령줄 안에 `uvicorn …` 이 또 있으면** 셸이 스스로를 죽여
  exit 144 가 납니다. 이 문서가 적어 둔 함정에 **낱개 명령으로 떼어
  놓지 않아서** 다시 걸렸습니다 — ⚠️ 죽이는 명령은 **그 줄에 그것만**

- **빈 목록이 「왜 비었나」를 이미 아는데 옆자리가 안 물어본 것** — 후보가
  처음부터 0건인 회의에서 머리줄은 「**결정한 후보가 없습니다**」, 스무 줄
  아래 빈 상자는 「이 회의에서는 업무 후보가 나오지 않았습니다」였습니다
  (결함 366). 같은 사실인데 **위엣것은 사람을 탓합니다** — 결정을 안 한
  게 아니라 결정할 것이 없었습니다. `reviewPhase` 가 그 갈래를 **이미
  정확히 갈라 두고** 있었고 옆자리만 안 물었습니다(결함 290). ⚠️ 결함 232
  는 `confirmed` 하나만 갈랐고 `needs_review` + 0건은 **일반 문장으로
  떨어졌습니다** — **갈래를 하나 갈랐으면 나머지가 어디로 떨어지는지
  보십시오**
- **「막힌 것」과 「처음부터 없는 것」을 또 안 가른 것** — 그 회의는 서버가
  **일부러** `confirmed` 로 안 옮기므로(결함 84) 「검토 끝내기」는 영영
  안 열립니다. 결함 316 은 「버튼을 지우지 말고 이유를 말하라」이지만 그건
  **할 수 있는 일이 막힌 것**이고, 이쪽은 결함 362 쪽입니다 — **끝낼 검토가
  없으면 단추를 안 그립니다.** 말은 빈 상자에 그대로 남습니다
- **말을 고치니 같은 문장이 두 번 그려진 것** — ①(문장)만 고쳤더니 같은
  22자가 머리줄과 빈 상자에 **두 번** 나왔습니다. **고치기 전에는 서로
  다른 문장이라 안 보이던 것**입니다 — ⚠️ **문장을 맞춘 뒤에는 렌더해서
  「이제 두 번 말하지 않는가」를 보십시오**
- **씨앗이 「가장 흔한 회의」를 안 만든 것** — 회의에서 아무도 일을 맡지
  않으면 후보가 0건입니다. 씨앗의 0건짜리 넷은 전부 다른 국면
  (`pending`·`processing`·`confirmed`·`failed`)이라 **`needs_review` +
  0건**만 비어 있었고, 그게 실기에서 제일 흔합니다. ⚠️ **어휘 × 개수로
  표를 만들 때 「상태 × 개수」 조합도 세십시오** — 상태만 세면 0건짜리가
  이미 넷이라 채워 보입니다
- **가드가 「문장을 안 적었는가」만 보고 「부르기는 하는가」를 안 본 것** —
  결함 232 의 가드가 `!/결정한 후보가 없습니다/.test(화면) || /describeReviewDone\(/`
  였습니다. 화면이 그 판단을 **직접 짜는 동안** 조용했습니다(결함 366).
  ⚠️ **금지형 가드(`!/…/`)를 쓸 때는 「그럼 무엇을 부르는가」를 같이
  요구하십시오** — 안 그러면 화면이 그냥 다른 글자로 같은 일을 합니다

- **한 함수가 「지금 소식」을 만드는데 국면을 안 본 것** — `roomStatus().message`
  의 마지막 갈래 「전원 종료했습니다. **회의 처리가 시작됩니다**」는 녹음이
  방금 끝난 회의에게만 참인데, 레거시 로비가 국면과 상관없이 언제나
  그렸습니다. 씨앗의 회의 1(`needs_review`, 후보 셋 대기)에서 그대로
  재현됩니다 — 바로 아래 「업무 후보 검토」 버튼이 있는데 같은 화면이
  「이제 시작한다」고 합니다(결함 367). SPA 는 `phase.canStart` 로 이미
  가르고 있었고 주석까지 적혀 있었습니다. ⚠️ **문장을 만드는 함수가
  「언제 참인가」를 인자로 받고 있는지 보십시오** — 받고도 안 쓰면
  화면마다 다시 가려야 합니다
- **`@lib` 이 국면 문장을 들고 있는데 한 뿌리가 한 곳에서도 안 그린 것** —
  `lobbyPhase().note` 를 레거시는 **0곳**에서 그렸습니다(`canStart` 만
  꺼내 씀). 그래서 홈이 「트랙이 온전한지 확인하세요」라고 보낸 회의의
  로비에 그 말이 이어지지 않았습니다 — 결함 214 가 SPA 에서 고쳐 둔
  사슬입니다. ⚠️ **함수가 돌려주는 값마다 「그리는 곳이 있는가」를
  뿌리마다 세십시오** — 「부르는가」만 세면 `canStart` 하나로 초록입니다
- **심은 상태를 보러 갔다가 옆엣것을 본 것** — 「전사가 빈 회의」를 만들어
  그 회의가 나오는 화면을 전부 열었는데, 결함은 **씨앗의 회의 1** 에서
  났습니다(결함 367). ⚠️ **상태를 하나 만들면 그 상태만 보지 말고 같은
  화면을 다른 회의로도 여십시오** — 비교할 것이 생겨야 이상한 것이 보입니다
- **고칠 데이터가 화면에 안 와 있으면 「다음 실마리」로 적는 것** — 홈의
  「회의에서 업무가 나오지 않았습니다」는 발화가 0건인 회의에게 헛다리를
  짚게 하는데, 홈이 받는 payload 에 **발화 수가 없습니다**. 같은 제품이
  검토 화면에서는 그 말을 이미 합니다. 서버 칸을 늘려야 하므로 그
  회차에서 안 하고 **조건까지 적어** 뒀습니다 — ⚠️ **반쯤 고치지 말고,
  무엇이 있으면 고칠 수 있는지 적으십시오**(결함 312 의 모양)

- **한 문장이 두 사실을 뭉갠 것 — 다음에 할 일이 정반대인데** — 홈이
  「검토할 업무 후보가 없습니다 — 회의에서 업무가 나오지 않았습니다」를
  **소리가 하나도 안 잡힌 회의**에게도 글자까지 같이 했습니다(결함 368).
  앞은 넘어가면 되고 뒤는 **다시 녹음해야** 합니다. ⚠️ **같은 제품이
  다른 화면에서 이미 그 말을 하고 있으면**(검토 화면은 「기록된 발화가
  없습니다」라고 정확히 말합니다) 못 하는 쪽은 대개 **값을 못 받고 있는
  것**입니다 — 화면 글자를 고치기 전에 payload 를 세어 보십시오
- **서버에 칸을 더할 때 타입이 여럿이면 전부 세십시오** — `utterance_count`
  를 SPA 응답 타입에만 안 넣을 뻔했습니다. 값은 JSON 이라 **런타임에는
  흘러가고 타입만 거짓말**을 합니다 — 오류도 안 나고 화면도 멀쩡해서
  다음 사람이 그 칸이 없다고 믿습니다. ⚠️ **응답의 칸 집합을 세 자리
  (서버 · 화면 타입 · `@lib` 입력)에서 맞추는 짝 가드**를 두십시오 —
  화면 타입은 서버와 **같아야** 하고 `@lib` 은 그 **부분집합**입니다
- **응답을 통째로 안 넘기고 칸을 골라 넘기면 조용히 낡습니다** — 화면이
  `nextStepFor({meeting_id, status, …})` 처럼 손으로 조립하면 서버에
  칸이 하나 늘 때 **그 화면만** 옛 갈래로 떨어집니다. 오류도 글자
  변화도 없습니다 — 넘기는 **모양**을 재는 가드를 두십시오
- **못 받은 칸을 `?? 0` 으로 읽지 마십시오** — 옛 응답을 읽는 자리가
  전부 「0건」이 되어 **없는 사고를 만듭니다.** 불변식 ③(측정 불가 ≠
  0점)이 payload 에도 그대로 걸립니다 — `undefined` 는 「아무 말도 안
  하는 쪽」으로 두십시오
- **검사를 늘리면 살아 있는 문서가 낡습니다** — 검사 둘을 더하니
  `docs/13` 의 「(56)」이 틀려졌고 가드가 잡았습니다. ⚠️ **문서의 숫자를
  세는 가드는 내가 검사를 늘릴 때도 웁니다** — 그게 제 몫입니다

- **밖으로 나가는 문서가 「알 수 없는 것」을 단언한 것** — 소리가 하나도
  안 잡힌 회의의 회의록이 「미해결로 남은 사안이 **없습니다**」라고
  했습니다(결함 369). 읽는 사람에게는 평범한 회의록이고, 진실은 「다시
  녹음해야 한다」입니다. ⚠️ **「처리를 마쳤다」에는 두 얼굴이 있습니다** —
  들었고 결과가 이런 것과, 아무것도 못 들은 것. 갈래를 세고 그 개수만큼
  문장을 두십시오(결함 326·365 와 같은 모양)
- **밖으로 나가는 문서는 「무엇을 근거로 썼는지」를 밝히십시오** — 사실
  줄 하나(「기록된 발화 14건」)면 됩니다. 0 이면 그 자리에서 스스로
  드러납니다. ⚠️ **`gap=True` 를 주지 마십시오** — 0 은 못 잰 값이 아니라
  **잰 0** 이고, gap 을 주면 「글자로 복사」가 그 줄을 「못 쟀습니다」로
  바꿔 내보냅니다. 복사 단추를 눌러 클립보드를 읽어 확인하십시오
- **가드 셋을 썼는데 그중 하나가 아무것도 안 재고 있던 것** — builder 가
  갈래를 제대로 가르는지만 재고, **서비스가 그 값을 먹여 주는지**는 안
  봤습니다. 서비스에서 인자를 빼고 심었더니 **전부 초록**이었습니다
  (결함 369). 대표 실패 ①이 제 가드에서 난 것입니다 — ⚠️ **문장을 재는
  검사는 그 문장을 내보내는 자리(API·화면)까지 가서 재십시오**
- **눌러 놓고 나온 것을 읽었더니 결함이 나온 것** — 「회의록 만들기」는
  201 을 주고 화면에도 잘 그려집니다. **문서를 읽어야** 보였습니다
  (결함 288 이 최종 보고서에서 적어 둔 그것). ⚠️ 그리고 **정상인 것과
  나란히 놓으십시오** — 한 벌만 읽으면 「별 소득 없는 회의였구나」로
  읽고 넘어갑니다(결함 290)

- **「나머지」를 한 덩어리로 센 것** — 최종 보고서가 「처리된 회의 3건 ·
  **2건은 아직 처리 전**」이라고 했는데, 그 둘 중 하나는 `failed`
  입니다(결함 370). 「아직 처리 전」은 **기다리면** 들어오고 실패한 것은
  **사람이 다시 돌려야** 들어옵니다 — 읽는 사람은 기다립니다. ⚠️ **같은
  패키지의 옆 파일이 이미 셋으로 가르고 이유까지 적어 두고 있었습니다**
  (`minutes.py` 의 `state_of`) — `total - processed` 처럼 **빼서 만든
  집합**을 보면 그 안에 몇 갈래가 들어 있는지 세어 보십시오
- **갈래를 갈랐으면 「다음에 할 일」도 갈라 적으십시오** — 「못 잰 것」
  목록에서 기다리는 것은 그대로 두고, 실패한 것에는 **「다시 처리해야
  들어갑니다」**를 붙였습니다. 그것이 두 갈래의 진짜 차이입니다
- **짝 가드는 낱개 사례보다 낫습니다** — 「두 builder 가 상태를 같은
  셋으로 가르는가」를 `MeetingStatus` **전수**로 재면, 새 상태가 생겼을
  때 한쪽만 고치는 순간 빨개집니다(결함 370). 사례를 늘리는 것은 이미
  아는 것만 지킵니다
- **앞 회차에서 배운 것을 바로 쓰십시오** — 결함 369 에서 builder 만
  재다가 「서비스가 값을 안 넘기는」 심기를 놓쳤습니다. 370 에서는
  처음부터 **API 를 통과한 뒤**를 재는 검사를 같이 썼고, 심어 보니 그
  하나만 🔴 이었습니다 — 없었으면 또 놓쳤습니다

- **한 문서 안에서 축이 갈린 것** — 주간 보고서의 「이 기간에 일어난 일」은
  기간으로 걸러 오는데 「사람별 기여」는 **프로젝트 전체**였습니다. 아무 일도
  없던 주에 세 사람의 몫이 최종 보고서와 똑같이 붙었습니다(결함 332).
  ⚠️ **한 문서가 두 범위를 말하면 각 범위에 이름을 붙이십시오** — 결함 331
  이 알림의 시각에서 만난 것과 같은 모양입니다
- **이름표를 붙였으면 「그 범위인 줄」을 전부 세십시오** — 332 는 사람별
  기여 판에 이름표를 붙였는데, 같은 문서의 사실 칸에 있는 「남은 업무」도
  기간을 안 보는 줄이었습니다(결함 371). 넷이 0건인 주에 그 줄만 3건이라
  「이번 주에 밀렸다」로 읽힙니다
- **「안 변한다」를 잴 때 「0이라 안 변한다」와 가르십시오** — 창을 비웠다
  넓혔다 하며 「값이 안 변하는 줄」을 찾는 가드를 썼는데, 검사 씨앗에서
  **양쪽 다 0건**인 줄은 자의 눈 밖이었습니다(결함 371 회차, 두 번).
  ⚠️ 자에 **「내가 못 보는 줄이 있는가」**를 먼저 세게 하고, 있으면 씨앗을
  늘리십시오 — 없으면 심기가 조용히 통과합니다

- **기록된 결정이 한쪽 뿌리에만 적용돼 있던 것** — 레거시 기여도의 「이 값으로
  확정」이 `disabled={saving}` 한 줄이라 **빈 폼을 한 번 누르면** 팀 전원의
  몫이 계산값으로 확정되고 그 밑에 사람 이름이 적혔습니다(결함 372 · 불변식
  ④). SPA 는 같은 순간 「3칸 남음」으로 막고 있었고, v2 F1-4 가 「빈 칸은
  0이 아니라 **아직 안 정함**이고 다 정해야 확정이 열린다」를 이미 정해
  뒀습니다. ⚠️ **판단이 SPA 화면 파일 안에 있어서 옮겨 갈 몸이 없었습니다** —
  레거시가 베낄 것도 가드가 잴 것도 없습니다. 「한쪽 뿌리만」의 **열네 번째**
  입니다. `@lib` 로 올리고 **막는 길을 세어 그 개수만큼 문장**을 뒀습니다
- **「전수로 돈다」고 적어 놓고 손으로 적은 배열이던 것** — 막는 길 다섯을
  도는 검사를 만들고 「여섯째가 생기면 빌드가 깨집니다」라고 문서에 썼는데,
  목록이 **손으로 적은 다섯 줄**이라 여섯째를 더해도 초록이었습니다(결함
  329 의 모양이 제 검사에서). 소스에서 갈래 수를 세어 목록 길이와 맞추니
  「막는 길이 6 인데 5 개만 재고 있습니다」로 잡혔습니다. ⚠️ **문서에 쓸
  문장이 검사보다 강하면 문서를 약하게 하지 말고 검사를 그 문장까지
  올리십시오**
- **내 고침을 확인하러 갔다가 앞 검사가 바꿔 둔 데이터를 잰 것** — 고친 뒤
  레거시를 다시 열어 「채움이 진한 남색이고 사유 칸이 비었다」를 보고
  **고침이 안 먹은 줄 알았습니다.** 같은 회차의 성공 경로 검사가 이미 확정을
  넣어 둔 프로젝트였고, 그건 **확정이 끝나 열린 단추**의 정상 모습입니다.
  ⚠️ 결함 196 이 페르소나에서 적어 둔 그것이 **내 고침을 확인하는 자리**에서
  났습니다 — **재현 안 됨을 적기 전에도, 고침을 확인하기 전에도 씨앗부터
  다시 심으십시오**
- **절 목록을 손으로 적어 화면의 절반을 안 연 것** — SPA 설정에서 팀원이
  막히는 단추를 세는데 `role`·`members`·`danger` **셋만** 열고 「SPA 는
  0건」이라는 답을 받았습니다. 소스의 절은 **일곱**이고, 막힌 셋은 제가 안
  연 `repo`·`general` 에 있었습니다 — 그대로 적었으면 **없는 결함**을
  만들었을 것입니다. ⚠️ 게다가 처음엔 `#role` 처럼 **해시**로 열어 셋이 전부
  같은 화면이었습니다(els=88·컨트롤 18로 똑같아서 알았습니다 — 결함 182).
  **절 목록은 손으로 적지 말고 소스에서 가져오고, 열었으면 요소 수를
  찍으십시오**
- **캡처가 바이트까지 같아서 의심했더니 그게 맞았던 것** — 두 사람으로 찍은
  기여도 화면이 `md5` 까지 같길래 결함 182 인 줄 알았는데, `/api/auth/me` 로
  확인하니 김민수·이하늘로 **정말 다른 두 세션**이었고 그 화면에는 「나」
  표시가 없어 같은 그림이 나오는 것이 맞았습니다. ⚠️ **같아 보이면 확인하고
  넘어가되, 확인하는 주소도 지어내지 마십시오** — 처음에 `/api/me` 라는
  **없는 주소**를 찍어 404 를 받고 「이름을 안 준다」로 읽을 뻔했습니다
  (「메시지 상자의 이름을 지어낸 것」의 라우트판)
- **자 하나가 세 방향으로 헐거웠던 것** — 주소를 찾는 자는 **뒤**(더 긴
  형제 갈래) · **가운데**(역추적) · **앞**(남의 파일 경로 안에서 걸림) 셋
  다 막아야 합니다. `GET /health` 가 `../lib/github/health.ts` 라는
  **import 경로**에 걸려 오래 「불린다」였습니다(결함 379). ⚠️ **자를 고칠
  때는 「이 요구를 어기는 다른 길이 있나」를 세 방향으로 보십시오** —
  결함 295 가 적어 둔 것의 정규식판입니다
- **메서드를 안 보는 자 — 다만 `GET` 은 예외로 두는 것** — 쓰기 갈래는
  화면이 메서드를 **글자로 적어야** 부를 수 있어 재기 쉽습니다(재 보니
  0건). `GET` 은 `fetch(url)`·`get(url)`·`<audio src>` 처럼 적는 방식이
  여럿이라 **거짓 양성만** 나옵니다 — 넷을 하나씩 확인해서 갈랐습니다
- **심기가 한 뿌리에서만 이뤄져 초록이 나온 것** — `PATCH` 를 `GET` 으로
  바꿔 심었는데 SPA 의 `api.patch(…)` 가 같은 주소를 부르고 있어 자는
  정직하게 초록이었습니다. ⚠️ **두 뿌리에 다 심어야 재는 것이 됩니다** —
  「왜 초록인지 읽으십시오」(결함 327 의 반대 방향)
- **주소를 찾는 자가 「끝을 안 막아」 형제 갈래에 속은 것** — 결함 306 의
  라우트 가드가 `rx.search` 로만 재서, 화면이 `/api/channels/${'${id}'}/messages` 를
  부르는 것만으로 `PATCH /api/channels/{id}` 까지 「불린다」가 됐습니다.
  채널 이름 변경·삭제는 **화면에 컨트롤이 0개**인 채 오래 초록이었습니다
  (결함 378). ⚠️ **끝을 막을 때 두 가지를 같이 하십시오**: 뒤에 경로가
  더 이어지지 않는지(`(?![A-Za-z0-9_/-])`), 그리고 `{id}` 자리를
  **원자 그룹**으로 둘 것 — 그냥 `+` 면 역추적이 끝 막기를 무력화합니다
  (`${'${channelId}'}` 를 `${'${channelI'}` 까지 줄여 가며 맞추다 `}` 앞에서 성공)
- **끝을 너무 세게 막아 멀쩡한 자리를 잡은 것** — 「뒤에 아무 글자도 오면
  안 된다」로 썼더니 `?q=` 가 붙는 자리(찾기·달력) 다섯이 전부 「안
  불린다」가 됐습니다. 막을 것은 **경로가 더 이어지는 것**뿐입니다
- **정규식 문자열을 손으로 고치다 `\s` 를 `\\s` 로 만든 것** — 문자군이
  **글자 `s` 를 제외**하게 되어 멀쩡한 갈래 일곱이 고아로 나왔습니다.
  ⚠️ **이스케이프를 손으로 깁지 말고 함수를 통째로 다시 쓰십시오**
- **✅ 를 세기 전에 ✅ 를 지키는 자를 본 것** — 대조표의 ✅ 71개를 한 줄씩
  세려다가, 근거의 존재·호출은 **둘 다 0건**(초록)이고 대신 **자**가 헐거운
  것을 찾았습니다. 377 이 만든 짝 가드 덕분에 예외에 번호만 적으니 거짓 ✅
  두 줄이 **자동으로** 잡혔습니다 — **자를 고치는 것이 줄을 세는 것보다
  멀리 갑니다**
- **「부르는 곳이 0곳」을 적어 둔 자리와 「✅」를 적는 자리가 서로를 안 본 것** —
  라우트 가드의 예외 표는 「CHANNEL-005 채널 순서 — 서버만 있고 화면에 아직
  안 이었습니다」라고 적는데, `docs/20` 은 같은 줄을 **✅** 로 적고 있었습니다
  (결함 377). 채팅 화면에 순서를 바꾸는 컨트롤은 0개입니다. ⚠️ **예외 표를
  한 벌로 올리고 대조표와 짝을 재십시오** — 사본을 두면 「서버만 있다」와
  「다 됐다」가 또 갈라집니다(결함 363 의 방법)
- **주석이 「잇습니다」라고 단언하는데 이은 적이 0건인 것** —
  `meetings.channel_id` 는 칸도 있고 `ScheduleIn` 도 받고 응답도 돌려주는데
  **화면이 한 번도 안 보냅니다**(회의 6건 중 0건). 결함 315 의 「라우트는
  불리는데 인자가 안 불린 것」과 같은 모양입니다. ⚠️ **이을 자리가 둘 이상
  필요하면(고르는 칸 + 보여 주는 자리) 그건 「없는 기능」입니다** — 만들지
  말고 **문서가 사실을 말하게** 고치고 조건을 적으십시오(결함 317·312 의 방법)
- **고친 것이 다음 관점을 열어 준 것** — 376 을 고치기 전에는 어떤 채널도
  `voice` 가 아니라, 음성 채널 화면이 **구조적으로** 안 그려졌습니다.
  고치고 나서야 그 갈래를 처음 봤고 초록이었습니다. ⚠️ **고친 뒤에 그
  갈래로 열리는 화면을 다시 여십시오**
- **`useCallback` 이 안 쥔 state 가 조용히 옛 값으로 나간 것** — 채널
  만들기의 deps 가 `[newName, loadChannels]` 라 `newKind` 가 빠져 있었고,
  콜백은 **이름이 바뀔 때만** 다시 만들어집니다. 이름칸이 라디오보다
  **위**에 있어 사람은 이름을 먼저 적으므로, 실사용 순서에서 「음성 채널」은
  **사실상 만들 수 없었습니다** — 서버는 201 을 주고 화면에는 오류가
  안 납니다(결함 376). ⚠️ **바꾼 뒤에는 서버 상태를 다시 읽으십시오** —
  DB 를 안 열었으면 「만들 수 있다」로 적고 넘어갔을 것입니다.
  ⚠️ 같은 UI 상태에서 **행동 순서를 바꿔** 보면 원인이 못 박힙니다
  (이름먼저 `text` · 종류먼저 `voice`)
- **가드가 URL 템플릿의 `${'${'}` 를 본문 시작으로 문 것** — `sendJson` 인자에서
  **첫 `{`** 를 찾으면 `` `/api/projects/${'${projectId}'}/channels` `` 의 중괄호가
  먼저 걸리고, 경로 글자(`channels`·`messages`)가 state 이름과 겹쳐
  **맞는 자리**를 잡습니다. 최상위 쉼표로 인자를 갈라 **세 번째 인자만**
  보십시오 — 「잡혔다」는 「맞다」가 아닙니다
- **「옆 빈 칸이 이유를 말한다」를 빈 칸에서만 재 본 것** — 앞 회차가 소스만
  읽고 「나머지 게이트는 옆 칸이 스스로 말한다」로 넘겼는데, 렌더해 보니
  **반만 맞았습니다.** placeholder(「두 글자 이상」)는 **빈 칸에서만** 말하고,
  한 글자를 적으면 사라지는데 단추는 그대로 막혀 있습니다 — 그때 이유가
  화면에서 통째로 없어집니다(결함 375). ⚠️ **게이트를 잴 때는 「빈 칸」과
  「다 채운 칸」 사이의 중간 상태를 만드십시오** — 빈 쪽/꽉 찬 쪽/중간이
  서로 다른 관점이라는 것이 입력칸에서도 그대로입니다(결함 323)
- **같은 규칙이 두 화면에 있는데 한쪽만 말하고 있던 것** — `@lib` 의
  `blockedReason` 은 주석에 「⚠️ 버튼만 흐려 두면 사람은 **왜 안 되는지**
  모른 채 계속 누릅니다」라고 적어 두고 있었는데, 채팅은 그 함수를 안 부르고
  `query.trim().length < 2` 를 손으로 적고 있었습니다(대표 실패 ②).
  ⚠️ **문장을 재사용할 때는 「그 문장이 시키는 것이 이 화면에 있는가」를
  보십시오** — 찾기 화면의 「담당자·상태를 고르세요」는 채팅에 **없는 칸**
  입니다. `filters` 를 `null` 로 받게 해 「이 화면에는 거를 칸이 없다」를
  표현하고 문장을 갈랐습니다(결함 313 의 모양을 피함)
- **앞 사람이 「안 쟀다」고 세어 두고 간 목록을 실제로 센 것** — 결함 365 가
  「진짜 `disabled` 가 열 곳인데 하나만 고쳤다 · 나머지 아홉은 안 쟀다」로
  끝냈습니다. 세어서 갈라 보니 대부분은 **잠깐 지나가는 것**(`busy`·
  `sending`)이거나 **옆 빈 칸이 이유를 스스로 말하는 것**이었고, **혼자
  다른 것이 로비**였습니다(`!affordance.enabled` — 옆에 채울 칸이 없는
  계산된 국면). 씨앗의 **다섯 회의 전부**에서 막혀 있어 Tab 으로는 어느
  회의에서도 안 닿았습니다(결함 374). ⚠️ **「저 혼자 예외인가」가 1/9 로
  유죄를 냈습니다** — 세어 보기 전에는 몰랐고, 세고 나니 정확히 하나였습니다
- **막는 이유가 「라벨 자신」이면 `aria-describedby` 를 붙이지 않는 것** —
  로비의 단추는 라벨이 「전원 동의 후 시작할 수 있습니다」입니다. 373 처럼
  사유 칸을 가리키면 **같은 말을 두 번** 합니다(결함 366). 닿기만 하면
  들립니다 — ⚠️ **고칠 때 옆 자리에 이미 그 말이 있는지 보십시오**
- **손을 고쳤으면 열리는 쪽도 눌러 본 것** — 막힌 갈래만 재고 끝내면
  `return` 하나가 열린 갈래까지 막아도 안 보입니다. 세 사람이 제품의
  「동의합니다」를 눌러 동의를 모으니 라벨이 바뀌고 주소가 넘어갔습니다
  (결함 374). ⚠️ 그 전에 동의를 **손으로 `fetch`** 했다가 **422** 를 받고
  「안 열린다」로 읽을 뻔했습니다 — **제품의 경로를 그대로 밟으십시오**
- **진짜 `disabled` 를 「막았다」로만 읽은 것** — 검토의 「업무로 등록」이
  `disabled={blockers.length > 0}` 이라, Tab 으로 문서를 **한 바퀴(56번)**
  돌아도 막힌 카드의 그 단추에는 **한 번도 안 닿았습니다.** 사유 글은 눈에
  있었지만 `aria-describedby` 가 없어 이어지지도 않았습니다(결함 373).
  SPA 는 같은 화면에서 이미 제대로 하고 있었습니다 — 「한쪽 뿌리만」의
  **열다섯 번째**. ⚠️ **막는 것을 봤으면 Tab 으로 닿는지 세어 보십시오** —
  「안 눌린다」와 「있는지도 모른다」는 다릅니다
- **`aria-disabled` 로 바꿨더니 눌리는 것과 똑같이 보인 것** — `app.css` 의
  `button[aria-disabled="true"]`(0,1,1)가 「덜 채운 모양」을 정하는데, 화면
  `<style>` 의 `.acts .approve`(0,2,0)가 **이깁니다.** 대비만 재면 8.70:1 로
  멀쩡해서 **렌더해서 눈으로** 잡았습니다(결함 250 과 같은 자리 · 결함 373).
  ⚠️ **`disabled` → `aria-disabled` 로 옮길 때는 그 단추를 덧칠하는 규칙이
  있는지 세십시오** — 이긴 쪽이 왜 있는지 읽고, 뒤집지 말고 막힌 갈래만
  같은 자리에서 다시 정하십시오
- **가드가 「맞는 자리」를 두 번 잡은 것** — 「화면이 칸 이름을 손으로
  고르는가」를 `effectiveAssignee(…) === null ?` 로 쟀더니 Radix `Picker` 의
  `value` 가 걸렸고, `'assignee'` 글자로 바꿨더니 **CSS 클래스 이름**이
  걸렸습니다(결함 373 회차). ⚠️ **「잡혔다」는 「맞다」가 아닙니다** — 재는
  자리를 **그 동작이 사는 블록**(여기서는 막힌 단추의 `onClick`)으로
  좁히십시오
- **가드의 창을 짐작으로 잡은 것** — 등록 단추를 찾는 창이 1400자였는데
  SPA 는 여는 태그에서 라벨까지 **1500자**라 「단추를 못 찾았습니다」가
  떴습니다. ⚠️ **창은 재서 정하십시오** — 세어 보면 한 줄입니다
- **경로 상수를 지어낸 것** — `join(ROOT, '..', 'public')` 이라고 써서
  `ENOENT` 로 터졌습니다. 그 파일에는 `PUBLIC` 이 **이미 있었습니다**
  (「메시지 상자의 이름을 지어낸 것」의 상수판). ⚠️ 그리고 고치면서
  `replace(…, 1)` 이 **남의 줄**을 먼저 물어 다른 검사를 건드렸습니다 —
  바꾼 뒤 `git diff` 로 **내 줄만 바뀌었는지** 보십시오
- **등급이 셋인데 가운데를 안 만들어 본 것** — 씨앗은 소유자 하나·팀원
  둘이라 `admin` 이 한 번도 안 그려져 있었습니다. 만들어서 칸반·검토·채팅을
  세 등급으로 훑으니 **컨트롤 수도 막힌 수도 셋 다 같았습니다** — 그건
  `permissions._ALLOWED` 의 결정대로라 초록입니다(결함 373 회차).
  ⚠️ **초록이어도 적어 두십시오** — 그리고 그 표를 만들다가 옆에서 결함이
  나왔습니다(「하나를 재러 갔다가 그 옆을 눌러 본 것」)
- **파생값을 저장값이 가려 낡은 것** — 막힌 이유를 `setMessage` 로 담았더니,
  사유를 적어 게이트가 **열린 뒤에도** 「이유를 적어야 합니다」가 열린 단추
  밑에 그대로 남았습니다(결함 372 회차 · 결함 310 의 모양). 그 칸은 막혀
  있는 동안 이미 게이트 값을 **그리고** 있었으므로 담을 필요가 없었고,
  담는 순간 낡습니다. ⚠️ **「저 혼자 예외인가」로 갈랐습니다** — 메시지를
  안 지우는 레거시 화면이 **셋 중 셋**이라 「안 지운다」는 관습이고, 이
  자리만 다른 이유는 **그 글이 게이트 문장 자신**이라 게이트가 열리는
  순간 거짓이 된다는 것입니다. 관습 쪽은 안 고치고 세어서 적었습니다
- **알려 주기를 고치다 갈 곳을 없앤 것** — 낡은 메시지를 빼고 나니 막힌
  단추를 눌러도 **아무 일도 안 났습니다**(대표 실패 ③). 옆 뿌리는 이미
  데려가고 있었으므로 그 판단을 `@lib` 으로 올려 둘이 같이 쓰게 했고,
  그러면서 **세 번째 사본**이 보였습니다 — 「이 조정에 사유가 필요한가」를
  「무엇이 문제인가」와 「어디로 데려갈까」가 각자 적고 있었습니다. 갈라지면
  **「이유를 적으라」고 해 놓고 엉뚱한 칸으로 데려갑니다.** 하나로 모으고
  **경계값에서 두 자가 같은 답을 내는지**를 재십시오
- **판단과 글자를 갈라 두면 타입이 짝을 재 줍니다** — 갈래를
  `confirmBlockOf`(판단)와 `whyCannotConfirm`(문장)으로 나누니, 한 화면은
  문장을 쓰고 다른 화면은 사유 문단의 **id** 를 씁니다. id 표를
  `Record<갈래, …>` 로 두면 갈래가 늘 때 **컴파일이 막습니다** — 런타임
  가드보다 낫습니다(심어서 확인: `error TS2741`)
- **잠금을 옮기면 「글자를 세는」 가드가 또 웁니다** — 결함 89 의 「누르는
  동안 잠근다」 가드가 `disabled={…flag…}` 라는 글자를 봅니다. `aria-disabled`
  로 옮기자 요구는 한 자도 안 바뀌었는데 그 자만 빨개졌습니다 — **이번
  세션에서 다섯 번째**입니다(결함 335 의 부류). 자를 요구 쪽으로 넓히되
  **되돌아가기가 `{ … return }` 블록 형태**인 것도 보게 하고, 「옛 자 + 옛
  코드 = 0건」으로 구멍이었음을 증명하십시오

- **심었더니 「옛 자도 빨갛다」 — 읽어 보니 다른 갈래였던 것** — 형제
  글자 구멍을 증명하려고 진짜 호출 다섯을 딴 주소로 돌렸더니 **옛 자도
  빨갰습니다.** 「구멍이 아니었다」로 읽을 뻔했는데 **무엇이 잡혔는지**
  읽으니 옛 자는 `PATCH` 만 잡고 `GET` 은 초록이었습니다 — 구멍이던 것은
  정확히 `GET` 쪽입니다. 결함 327 의 「왜 빨간지 읽으십시오」가
  **초록이 아니라 빨강에서** 걸린 것입니다. ⚠️ **「옛 자 + 옛 코드 = 0건」
  으로 구멍을 증명할 때, 0건이 아니면 「구멍이 아니다」가 아니라
  「내가 잰 갈래가 그것이 맞나」부터 보십시오** — 심기 하나가 여러 갈래를
  건드리면 옛 자도 **딴 이유로** 빨개집니다
- **`{id}` 자리는 형제 갈래의 「글자 그대로」도 삼키는 것** — 라우트 가드의
  `{project_id}` 는 아무 글자나 채우므로, 화면이 형제인
  `/api/projects/join` 을 부르는 것만으로 `GET /api/projects/{project_id}`
  가 초록이 됩니다(`…/members/me` 도 같습니다). 지금은 둘 다 진짜 호출이
  따로 있어 **거짓 초록 0건**이지만, 그 호출이 사라지는 날 아무도 못
  봅니다. ⚠️ **자가 「아무 글자」를 허용하는 자리를 보면, 그 자리에 진짜로
  글자를 박은 형제가 있는지 세십시오** — 결함 378·379 가 뒤·가운데·앞을
  막았고 이것은 **가운데를 채우는 값** 쪽입니다

- **화면이 「누르면 된다」고 약속하는데 서버는 그때 이미 못 한다는 것을
  알고 있던 것** — 설정의 경고 줄이 「아래 '지난 활동 가져오기'를 누르면
  채웁니다」로 끝나고 단추도 그려지는데, 눌러 보니 **409** 였습니다
  (결함 380). 화면이 못 판단한 이유는 `canBackfill` 이 **배달 수와 백필
  이력만** 봤기 때문이고, 서버는 거기에 **자격 증명과 App 설치**를 더
  봅니다. ⚠️ 그 필드의 주석은 「눌러도 아무 일이 없고, 사람은 그게 고장인
  줄 압니다」라고 **이 해악을 정확히 적어 두고** 한쪽 길만 막고
  있었습니다 — 결함 295 의 「막는 길을 하나만 막은 것」입니다.
  ⚠️ **결함 300 과 다른 자리입니다**: 300 은 **누른 뒤에 나오는 말**을
  고쳤고(그 기록이 재현으로 든 것이 바로 이 버튼입니다) 이것은 **누르기
  전**입니다 — 결함 251 의 「말만 고치고 단추는 그대로」가 되풀이된 것.
  ⚠️ **「할 수 있다」고 말하는 자리를 세라**(결함 313)에는 **약속판**이
  있습니다: 화면이 「누르면 ~된다」고 하면 **그 버튼을 실제로
  누르십시오**
- **검사가 결함을 검사로 못 박고 있던 것** — 고치니 검사 둘이 반대를
  말했는데, 읽어 보니 **둘 다 씨앗이 낡은 것**이었습니다. 하나는
  `installation_present` 를 안 채워 **실기가 안 만드는 상태**였고
  (배달이 와서 `verified_at` 이 찍힌 프로젝트는 설치 id 도 있습니다 —
  결함 36), 다른 하나는 **검사 환경에 자격 증명이 없는데** 「가져오기라고
  말해야 한다」를 못 박고 있었습니다. ⚠️ **검사가 반대를 말하면 내 판단부터
  의심하되**(결함 191), 그 검사의 **씨앗이 실기가 만드는 값인지**도
  같이 보십시오 — 씨앗이 낡으면 검사는 결함을 지키는 자가 됩니다
- **「글씨가 너무 많다」를 요소당 글자 수로 세면 읽어 볼 화면이 골라지는
  것** — 낱개로 보면 취향인데, SPA 열둘을 렌더해 **글자÷요소**로 세우니
  하나가 두 배로 튀었고(저장소 연결 5.83 · 다음이 3.67) 그 화면을 읽다가
  결함 380 이 나왔습니다. ⚠️ **자로 「어디부터 눈으로 볼지」를 정하고,
  판단은 눈으로 하십시오** — 그리고 전사처럼 **글자가 곧 내용**인 화면은
  많은 것이 맞으니 자만 보고 고치면 안 됩니다

- **소스를 읽고 「늘 그렇다」고 적었다가 돌려 보니 아니었던 것** —
  경고와 `coverage` 가 「둘 다 켜지면 늘 같은 말을 두 번 한다」고
  적었는데, 네 조합을 **실제로 찍어 보니** 하나만 중복이고 나머지는
  날짜를 더하거나 아예 다른 말이었습니다. ⚠️ **「읽어서 안 것」과
  「돌려서 안 것」을 가르십시오** — 앞엣것은 결함 기록에 적기 전에
  뒤엣것으로 바꿔야 합니다. 그리고 좁은 중복 하나 때문에 고치면
  **흔한 경우에서 연결 날짜를 잃습니다** — 안 고치고 이유를 적었습니다
- **`itertools.product` 로 축을 곱하면 실기가 안 만드는 조합이 섞이는
  것** — `backfilled_to` 만 있고 `backfilled_at` 이 없는 상태를 만들어
  놓고 「이 갈래는 모순된다」고 읽을 뻔했습니다. ⚠️ **「씨앗이 실기가
  만드는 값인가」(결함 288)는 내가 손으로 곱한 조합에도 걸립니다** —
  전수로 도는 것은 좋지만, 나온 조합이 **실기에 있는 것인지** 같이
  세십시오
- **접힌 것을 「없다」고 읽을 뻔한 것 — 자가 두 번 틀렸습니다** — SPA
  기여도가 범위를 안 밝힌다고 적을 뻔했는데 `Why` 팝오버 **안**에
  있었습니다. ⚠️ 자가 두 곳에서 틀렸습니다: ① `(?)` 를 `?|왜|why` 로
  짐작해 **하나도 못 찾았고**(실제 `aria-label` 은 「김민수 — 이 숫자를
  읽기 전에 — 이유 5가지 보기」), ② 누른 **뒤에 Escape 를 먼저 눌러**
  읽어서 또 0건이었습니다. **누른 앞뒤를 비교해 「새로 생긴 줄」을
  찾으십시오** — 그제야 보였습니다

- **주석을 「지워서」 줄 번호를 센 것** — 감사 기록 가드가 docstring·주석을
  `re.sub(…, "")` 로 걷고 그 텍스트에서 `count('\n')` 을 셌더니, 가드가
  **23줄 어긋난 곳**을 가리켰습니다. 개행만 맞춰도 모자랍니다 —
  ⚠️ `^\s*#` 은 `\s` 가 **개행을 먹어 앞줄까지** 지웁니다. **같은 길이의
  공백으로 덮으십시오**(`_blanked()`) — offset 이 원본과 정확히 같아집니다.
  가드가 엉뚱한 줄을 가리키면 다음 사람이 그 줄을 보고 「없는데?」 합니다
- **선언을 막으려다 「점」까지 막은 것** — 「성문을 만드는 코드가 있나」를
  `Voiceprint\(` 로 셌더니 `class Voiceprint(Base)` 가 잡혔고(결함 240),
  그걸 막으려 `(?<![A-Za-z_.])` 를 달았더니 이번엔 **`.` 를 막아** 이
  저장소가 실제로 쓰는 `m.Voiceprint(` 를 못 봤습니다 — **심어도
  초록**이었습니다. ⚠️ **좁힐 때는 「무엇을 빼려는지」만 정확히 빼십시오** —
  막을 것은 `class` 뿐이었습니다. 결함 299(좁아서 놓침)와 322(넓어서
  놓침)가 **한 자에서 연달아** 났습니다
- **내 자가 무엇을 가리키는지 모르고 「틀렸다」고 읽을 뻔한 것** — 가드가
  `task_service.py:560` 을 짚었는데 `grep` 은 `action=` 을 563 에서
  찾아, 3줄 어긋난 줄 알고 자를 또 고칠 뻔했습니다. 560 은 `m.AuditLog(`
  **만드는 줄**이고 그게 맞는 자리입니다 — ⚠️ **판정 전에 내 자가 어느
  칸을 가리키는지 확인하십시오**
- **「범위를 말하는 문장」은 조각마다 따로 재야 하는 것** — 활동 화면의
  「이 기록에는 **사람이 손으로 내린 결정만** 쌓입니다 — … 회의를 열거나
  녹음하거나 이야기 나눈 것은 여기 안 남습니다」에 가드가 **이미
  있었는데**, 재고 있던 것은 **뒷부분뿐**이었습니다. 앞부분(누가 썼나)은
  아무도 안 보고 있었고, 그것은 지금 **두 우연** 위에 서 있었습니다
  (`project_id=None` 이라 목록에서 걸러지는 것 · 갈래가 죽어 있는 것).
  ⚠️ 결함 328 의 「**같은 표의 옆 칸은 따로 재야 합니다**」가 **한 문장
  안에서** 난 것입니다 — 문장을 조각으로 갈라 조각마다 자를 대십시오
- **「0건」이 나왔을 때 심기를 의심하기 전에 왜 0인지 읽은 것** — 정기
  작업이 쓰는 감사 기록을 진짜 생산자로 만들었는데 활동 API 가 0줄이라
  「내 심기가 실패했다」로 읽을 뻔했습니다. 읽어 보니 그 줄은
  `project_id=None` 이라 **질의가 구조적으로 거릅니다** — 즉 화면 문장이
  참인 이유였습니다. ⚠️ **0 을 보면 심기와 자와 「진짜 0」 셋을
  가르십시오**

- **표를 지키는 자가 스스로 경고한 일을 스스로 당한 것** — 짝 상수 표의
  낡음 검사가 「이 검사가 없으면 다음 사람이 상수를 하나 더 만들면서 같은
  주석을 달고, 그건 아무도 안 지킵니다」라고 적어 두고, 정작 **파이썬만**
  걷고 **하드코딩한 파일 둘**만 읽고 **「와 같은 값」이라는 표현 하나**만
  찾았습니다. 프런트에 적힌 「서버 … 와 **같아야 한다**」 다섯 줄이 통째로
  눈 밖이었고, 짝 넷이 표 밖에 있었습니다(`CODE_LENGTH`·`MAX_BIO`·
  `MAX_BODY`·`MIN_PASSWORD_LENGTH`). ⚠️ **자는 세 축으로 좁아집니다 —
  언어 · 걷는 자리 · 찾는 표현.** 하나를 넓혔으면 나머지 둘도 세십시오
- **표의 구조가 「넣고 싶어도 못 넣게」 되어 있던 것** — 짝 표가 프런트
  파일을 `FRONT_CONSTANTS` **하나**로 못 박아, `timeline.ts` 밖의 상수는
  구조적으로 표에 못 들어갔습니다. ⚠️ **「빠졌다」를 보면 「빠뜨린 것」과
  「넣을 수 없던 것」을 가르십시오** — 뒤엣것이면 목록이 아니라 **모양**을
  고쳐야 합니다
- **손으로 고른 목록을 「기준」으로 바꾼 것** — 결함 329 가 적어 둔
  「손으로 고른 목록을 그 기준으로 전부 재 보십시오」를 실제로 했습니다.
  기준(「서버와 `@lib` 양쪽에 같은 이름의 숫자 상수」)으로 전수를 세니
  여섯이고 표에는 둘뿐이었습니다. ⚠️ **주석이 없는 짝도 있습니다**
  (`MAX_BODY`) — 「주석이 그렇게 말하는가」만 재면 그 부류를 영영 못 봅니다
- **「옛 자 + 옛 코드 = 0건」을 심을 필요가 없을 때가 있는 것** — 빠진
  넷이 **이미 저장소에 있는데** 직전 커밋에서 pytest 2163 이 초록이었습니다.
  그것이 자가 구멍이었다는 **역사적 증거**라 심기보다 강합니다 —
  ⚠️ 심기 전에 **「지금 상태가 이미 증거인가」**를 보십시오

- **한 칸을 읽는 두 자리가 서로 다른 글자를 보고 있던 것** —
  `projects.status` 를 달력은 `== "done"`, 유지보수 작업은
  `== "finished"` 로 쟀습니다. 앞엣것은 **`TaskStatus` 의 값**이고 같은
  파일 예순네 줄 위의 업무 갈래에서 베껴 온 것입니다(결함 381). 쓰는
  코드가 0곳이라 **둘 다 영원히 거짓**이었고, 그래서 달력의 `isOverdue`
  가 프로젝트에는 구조적으로 안 걸렸습니다 — 그 함수 주석이 「끝냈는데
  빨갛게 남으면 사람은 아직 할 일이 있다고 읽습니다」라고 **그 해악을
  적어 두고** 있었습니다. ⚠️ **「쓰는 곳이 있나」(결함 340)만 세면 이
  부류를 놓칩니다** — 갈린 것은 **읽는 쪽끼리**입니다. 읽는 자리가
  여럿이면 **글자를 나란히 놓으십시오**
- **가드가 넷을 잡았는데 진짜는 하나였던 것** — 「상태 칸을 날 글자와
  비교하지 마라」 가드가 넷을 잡길래 전부 결함인 줄 알았는데, 어휘와
  하나씩 대조하니 **셋은 값이 맞고 글자로 적혀 있을 뿐**이었습니다.
  어휘에 **없는** 값은 하나뿐이었습니다. ⚠️ **「잡혔다」는 「맞다」가
  아닙니다** — 그리고 **심각도가 다른 둘을 한 자로 잡으면 세는 것이
  틀립니다**(셋은 고쳤지만 결함으로는 안 셌습니다)
- **씨앗의 값이 무엇의 것인지 확인 안 한 것** — `deadline:
  datetime(2026, 9, 4)` 를 보고 「프로젝트에 마감이 있다」고 읽었는데
  **업무 후보**의 마감이었습니다. `m.Project(` 는 `title` 만 넣습니다.
  ⚠️ **씨앗을 근거로 쓸 때는 그 값이 무엇의 번호·무엇의 날짜인지
  보십시오** — 이 저장소에서 세 번째입니다
- **읽기만 하고 쓰는 곳이 0곳인 칸은 「상태를 늘릴 자리」의 후보** —
  수확이 줄었을 때 늘릴 상태를 고르는 방법입니다. `where(... == "…")`
  같은 **읽는 자리**에서 시작해 **쓰는 곳을 세십시오**. 0곳이면 그
  갈래는 한 번도 안 그려진 것이고, 대개 그 안에 갈라진 것이 있습니다

- **대조표가 ✅ 라는데 그 기능의 「입력」이 영영 안 들어오던 것** —
  `ANALYTICS-005` 가 ✅ 인데, 그 신호가 보는 `task_dependencies` 를
  **만드는 코드가 0곳**이라 계산이 언제나 `None` 이었습니다(결함 382).
  ⚠️ **결함 377 과 다른 축입니다** — 377 은 「서버는 있고 화면이 없다」이고
  이것은 **「입력이 아예 안 들어온다」**입니다. **라우트를 세는 자로는
  구조적으로 안 보입니다** — 라우트는 멀쩡히 있고 계산도 돕니다.
  ⚠️ 세는 법: **모델 전수에서 「그 클래스를 만드는 코드가 0곳」**을 찾고,
  그 표에 기대는 요구가 ✅ 인지 보십시오
- **컬럼 자에는 `_now()` 기본값이라는 거짓 양성이 있는 것** — 「쓰는 곳
  0곳」을 컬럼으로 세면 `recorded_at`·`received_at` 처럼 **컬럼 기본값**으로
  채워지는 것이 전부 걸립니다(여섯 중 넷). ⚠️ **표 단위**로 세면 그
  부류가 사라집니다 — 대신 「표는 채워지는데 그 칸만 안 채워지는」 경우를
  못 봅니다. **두 자는 서로를 못 봅니다**
- **네 개가 걸렸는데 결함은 하나였던 것** — 「만드는 코드가 0곳인 표」
  넷 중 셋은 진짜가 아니었습니다: 하나는 **분모 0이면 계산에서 빠지게
  설계**(`PeerReview`), 하나는 **코드가 그 일을 대신**
  (`ScoringProfileRow`), 하나는 **기록된 결정**(`Voiceprint`,
  `docs/07` §2.3). ⚠️ 가른 자는 **「대조표가 ✅ 라고 주장하는가」**
  입니다 — 비어 있는 것 자체는 죄가 아니고, **비어 있는데 됐다고 말하는
  것**이 죄입니다

- **한 문서가 두 층인데 아래만 고치고 위를 둔 것** — `docs/20` 은 절
  **요약표**와 요구 **상세표** 두 층인데, 결함 377·378 이 상세 셋을
  `⚠️ 서버만` 으로 내리고 요약은 맨몸 `✅` 로 두었습니다(결함 383).
  문서를 펴면 **요약이 먼저** 보이므로 읽는 사람은 아래를 안 봅니다.
  ⚠️ 「한 갈래만 고치고 옆 갈래를 그대로 둔 것」(결함 298·301)이
  **문서에서** 난 것입니다 — **표가 두 층이면 두 층을 나란히 놓으십시오**
- **자가 기호 하나를 몰라 눈을 감고 있던 것** — 표시를 `⛔·⚠️·🟡·✅`
  넷으로만 읽었더니 `❌` 가 「분류 못 함」으로 조용히 흘렀고, 그 상태로는
  **「요약 ✅ + 상세 ❌」 절이 있어도 못 봅니다.** 기호를 더하니 후보가
  하나에서 둘로 늘었습니다. ⚠️ **자에 「못 읽은 것이 있으면 실패」를
  넣으십시오** — 안 넣으면 다음 사람이 기호를 늘릴 때 조용히 새어 나갑니다
- **「빠진 줄이 있는가」를 물으려면 원본이 있어야 하는 것** — 대조표의
  근거인 **요구사항 정의서가 저장소에 없습니다**(`docs/원본자료` 에는
  제안서와 대화록뿐). 그래서 「대조표가 빠뜨린 요구」는 이 저장소 안에서
  **잴 수 없습니다.** ⚠️ **못 재는 것은 「초록」이 아니라 「못 쟀다」로
  적으십시오** — 다음 사람이 원본을 받으면 그때 ID 집합을 맞추면 됩니다

- **낭독기 전용 클래스를 하나만 알고 거른 것** — 잘림 감사에서
  `.visually-hidden` 만 걸러냈는데 이 저장소는 **`.vh` 도** 씁니다
  (`app.css`). 설계상 1×1 인 것 둘이 「자기 상자에서 잘림」으로
  올라왔습니다. ⚠️ **거르는 이름은 세어서 넣으십시오** — 하나만 알면
  그 자는 거짓 양성을 내고, 그 소음에 진짜가 묻힙니다
- **표와 산문이 서로를 가리키면 어긋난 것이 아닌 것** — `REVIEW-005` 의
  표는 `✅` 인데 산문은 「사람별로 안 만들었습니다」였습니다. 어긋난
  줄 알았는데 그 표 줄이 **「아래 이유」로 산문을 가리키고** 있었습니다.
  ⚠️ **두 자리가 다른 말을 하면 「서로를 아는가」부터 보십시오** —
  가리키고 있으면 한 벌입니다

- **「보이는가」를 `rect` 로 판정한 것 — 세 번** — `getBoundingClientRect()` 는
  「자리를 차지하는가」이지 「보이는가」가 아닙니다. ① `.vh`(앞 회차) ②
  `opacity: 0` 으로 **겹쳐 둔** 입력칸(레거시 홈의 「사진 고르기」는 `.btn`
  을 입은 label 위에 투명한 `input[type=file]` 이 덮여 있습니다) ③ **접힌
  `<details>` 의 자식** — Chromium 은 `content-visibility` 로 감추므로
  **상자를 그대로 돌려줍니다.** 셋 다 「잘림 0건」 자리에서 거짓 양성으로
  올라왔습니다. ⚠️ `el.checkVisibility({ contentVisibilityAuto: true,
  opacityProperty: true, visibilityProperty: true })` 하나로 갈립니다
- **가짜 요소로 심은 것** — 「고정 요소가 창을 먹는가」를 심으려고
  `body::after` 에 `position: fixed` 를 줬는데 **아무 일도 안 일어났습니다.**
  `querySelectorAll('*')` 은 가짜 요소를 못 봅니다 — 진짜 DOM 노드로
  심으십시오(결함 164 의 「심었는데 아무 일도 안 일어난 것」의 가짜요소판)
- **글을 비교하면서 잎 요소로 센 것 · 복제해서 잰 것** — 두 뿌리가 같은
  말을 하는지 재는데 SPA 가 홈 3줄 · 칸반 1줄로 나왔습니다. React 가 글을
  조각내서 12자 넘는 줄이 **잎에 거의 없습니다.** 그래서 `cloneNode` 해
  offscreen 에 붙여 `innerText` 를 읽었더니 이번엔 **CSS 가 안 따라와** 또
  적게 나왔습니다. ⚠️ **살아 있는 화면의 `innerText` 를 읽고 내비가 낸
  줄만 빼십시오** — 「잎만 세는 자는 한 항목을 조각냅니다」의 문장판입니다
- **이 저장소의 SPA 는 글을 「지우지 않고 한 겹 아래로」 넣습니다** —
  두 뿌리를 비교하는 자에는 **「?」를 전부 누르는 단계**가 반드시 있어야
  합니다. 안 누르고 쟀더니 「SPA 에 없다」가 셋 나왔는데 **전부 팝오버
  안**이었고, 그중 로비의 커버리지 문구는 오히려 레거시보다 **자세했습니다**
  (끊긴 구간을 시각까지). `<details>` 만 여는 것으로는 부족합니다 — Radix 는
  눌러야 납니다
- **한 자리를 확인하고 「화면 전체」를 말한 것** — 결함 344 의 기록이
  「두 화면은 이미 범위를 적고 있었습니다(레거시 「팀 신뢰도」 · SPA
  「팀 전체」)」라고 적었는데, SPA 의 「팀 전체」는 **머리줄의 팀 리본**이고
  사람 카드의 팝오버는 다른 자리였습니다. 거기서는 「신뢰도 낮음」이라고만
  했고 커버리지 100% 인 사람이 그것을 자기 것으로 읽었습니다(결함 384).
  ⚠️ **「이미 하고 있습니다」를 적을 때는 그 화면에서 그 값을 그리는 자리를
  전부 세십시오** — 한 자리를 보고 화면을 말하면 다음 사람이 그 문장을
  믿고 안 셉니다
- **팀 값과 사람 값을 한 줄에 이은 것** — 「신뢰도 낮음 · 모르는 폭 23%p」
  는 앞이 팀(세 사람이 소수점까지 같음) 뒤가 사람(23·14·18%p)입니다.
  한 줄에 두 범위를 실으면 **이름표가 붙은 쪽으로 둘 다 읽힙니다** —
  결함 331(알림의 `at`)·332(주간 보고서)가 만난 그 모양이 한 **줄** 안에서
  난 것입니다. ⚠️ 그리고 **고친 뒤 다시 이어 붙이는 것**이 정확한 재발
  경로이므로, 가드를 「범위를 적는가」와 **「한 줄에 잇지 않는가」 둘로**
  두십시오 — 심어 보면 서로 다른 하나만 빨개집니다
- **어떤 값을 「그리는 자리」는 손으로 적지 말고 census 하십시오** —
  `confidence_label` 을 **읽는** 파일을 뿌리마다 훑게 하면, 새 화면이 그
  값을 읽기 시작할 때 가드가 그 자리도 같이 셉니다. 손으로 적은 목록은
  만들 때 있던 것만 지킵니다(결함 329). ⚠️ 타입 선언(`이름: string;`)은
  「그리는 것」이 아니니 빼고, **한쪽 뿌리가 0곳이면 그것 자체를 실패로**
  두십시오(결함 286)
- **판단이 화면 파일 안에 있으면 검사가 안 닿습니다 — 글자도 같습니다** —
  384 의 유일한 위반 자리는 SPA 화면 파일 안의 `` `신뢰도 ${label} · …` ``
  였습니다. 레거시는 같은 글을 자기 파일에 손으로 적고 있었고(사본 둘),
  `@lib` 에 `teamConfidenceLine()` 하나를 두고 둘이 부르게 하니 **갈라질
  수가 없어졌습니다**(결함 363 의 「가드를 넓히는 것보다 사본을 없애는 것」)
- **접히는 축은 「값 하나」로 재면 접히는 순간을 못 봅니다** — 기본 글자를
  32px 로만 재고 초록을 받았는데, breakpoint 를 **막 넘는** 값(20·24px)은
  안 댔습니다. 접히다 마는 자리가 거기 있습니다 — 안 잰 것을 적어 둡니다

- **「갈래가 둘 다 있다」와 「값이 갈린다」는 다른 말인 것** — 씨앗에
  `deadline_missed` 를 넣어 양쪽 갈래를 다 그려 놓고 초록을 받았는데,
  세 사람의 일정 준수 점수가 **전부 2.0** 이었습니다. `10 × 비율 ×
  min(1, 건수/5)` 에서 `1.0×0.2` 와 `0.5×0.4` 가 **정확히 상쇄**합니다 —
  그 배치에서는 `missed` 항을 통째로 지워도 씨앗이 아무것도 못 잡습니다
  (결함 385). 「이름 순과 번호 순이 같은 명단으로 정렬을 잰 것」이
  **점수**에서 난 것입니다. ⚠️ **갈래를 만들었으면 그 갈래가 값을
  바꾸는지도 재십시오** — 가드를 「갈래가 있는가」와 「값이 갈리는가」
  둘로 두면 심었을 때 서로 다른 하나만 빨개집니다
- **씨앗이 「짝으로만 나오는 것」을 한쪽만 적은 것** — 실기의
  `task_service.complete` 는 완료 이벤트를 쓴 **바로 다음에** 마감 판정을
  같은 `source_id` 로 씁니다(마감일이 없을 때만 건너뜁니다). 씨앗은 완료만
  넷 적고 판정을 0줄 적었습니다 — **제품이 만들 수 없는 모양**입니다.
  범주가 팀 전체 0건이면 화면이 통째로 빼서 안 보이다가, **누가 카드를
  하나 완료로 옮기는 순간** 범주가 살아나 나머지 사람이 「일정 준수 0건 ·
  팀의 0%」로 그려졌습니다(결함 385). ⚠️ **어떤 이벤트가 언제나 짝으로
  나오면 그 짝을 전수로 재는 가드를 두십시오** — 낱개 사례를 늘리는 것은
  이미 아는 것만 지킵니다
- **범주가 통째로 빠져 있으면 그 안의 거짓말은 안 보이는 것** — 「빈 쪽 /
  꽉 찬 쪽 / 중간」에 이어, **「그 범주가 살아나는 순간」**이 또 하나의
  관점입니다. 살리는 방법은 **제품의 버튼을 한 번 누르는 것**입니다
- **목록에서 `.first()` 로 집어 남의 카드를 누른 것** — 칸반에서
  `button.move[data-to=done]` 의 첫 번째는 내 카드가 아니라 **보드의 첫
  카드**였습니다. 업무 셋이 엉뚱하게 옮겨졌고, 그 상태를 그대로 재서
  「기여 이벤트가 안 생긴다」를 적을 뻔했습니다(그 카드는 담당자가 없어
  원래 안 생깁니다). ⚠️ **목록에서 하나를 누를 때는 그 항목 안으로
  좁히십시오** — 「같은 카드를 열두 번 누른 것」의 거울상입니다
- **모델·칸 이름을 지어낸 것** — `m.ContributionEvent`(실제는
  `ContributionEventRow`) · `.kind`(실제는 `event_type`) · `.kcard`(실제는
  `.moves`·`button.move`). 세 번 다 「없다」가 아니라 **이름이 틀린** 것
  이었습니다. ⚠️ **집기 전에 소스에서 이름을 확인하십시오** — 「메시지
  상자의 이름을 지어낸 것」이 모델·선택자에서 되풀이됐습니다
- **읽기만 하고 만드는 곳이 0곳인 이벤트 종류** — `DOCUMENT_REVISED` ·
  `PEER_RATING` · `BLOCKER_RESOLVED` 는 `scoring.py` 가 **읽기만** 합니다.
  결함 340 의 자(「어휘에 있는데 만드는 코드가 0곳」)를 **이벤트 종류
  표**에 대면 나옵니다 — 라우트·행동·상태·권한에 이어 **또 하나의 어휘**
  입니다. ⚠️ 다만 대조표가 ✅ 라고 말하지 않으면 죄가 아닙니다(결함 382) —
  **안 세어 본 자리**로 적어 두십시오

- **「밟을 수가 없다」가 곧 답이던 것** — 「되돌렸다 다시 완료하면 점수가
  오르는가」를 밟으러 갔는데, 그 조작의 **첫 단추(마감일 미루기)** 자리가
  제품에 0곳이었습니다(결함 386). 서버는 `TaskPatch.deadline` ·
  `_change_deadline` · 변경 이력 표 · 조작 탐지를 다 갖췄는데 **보내는
  화면이 두 뿌리 다 0곳**입니다. ⚠️ **경로를 밟으러 갔다가 못 밟으면
  「재현 안 됨」으로 접지 말고 「왜 못 밟나」를 세십시오** — 거기 결함이
  있습니다
- **라우트는 불리는데 「칸」이 안 불린 것 — 두 번째** — `PATCH /tasks/{id}`
  는 옮기기·우선순위가 이미 부르므로 결함 306 의 라우트 census 는 **언제나
  초록**입니다. 안 불리는 것은 `deadline` 이라는 **칸**이었습니다(결함 315
  가 `before_id` 에서 겪은 그 모양). ⚠️ **서버가 받을 수 있는 칸마다 화면이
  닿는지도 세십시오** — 라우트를 세는 것과 요구를 재는 것은 다릅니다
- **탐지기는 살아 있는데 그 입력이 영영 안 들어오는 것** — 결함 63 이
  「세는 코드만 있고 만드는 코드가 없다」를 고쳐 **만드는 코드**를 붙였는데,
  **그 코드에 닿는 길**이 0곳이라 `frequent_deadline_change` 는 여전히
  영원히 안 뜹니다(결함 386). ⚠️ **「만드는 코드가 있는가」(결함 340) 다음
  질문은 「그 코드가 불릴 수 있는가」입니다** — 셋이 한 줄입니다:
  탐지기 → 생산자 → **생산자를 부르는 화면**
- **내 자가 「내가 방금 쓴 주석」을 문 것** — 화면 글을 재는 가드를 처음
  돌렸더니 방금 고친 `calendar.tsx` 가 위반으로 잡혔습니다. 이 저장소의
  화면 주석은 「예전에는 이렇게 적었고 왜 바꿨다」를 **옛 문장 그대로**
  인용합니다. 파이썬 주석만 걷는 `_blanked` 를 TSX 에 댄 것이 원인이라
  **길이를 지켜 JS 주석을 걷는 `_blanked_ts`** 를 따로 뒀습니다 —
  결함 238 이 마크업에서 겪은 그것의 TSX 판입니다. ⚠️ **글을 재는 자를
  만들 때는 그 언어의 주석부터 걷으십시오**
- **짝 가드는 「양방향」으로 심어야 짝인 것** — 「화면이 못 하는 일을
  문서가 됐다고 적지 않는가」를 한 방향으로만 재면, 나중에 그 기능을
  **만들었을 때** 문서의 단서가 조용히 낡습니다. 세 번 심어 확인했습니다:
  옛 문장 → ①이 빨강, 단서 삭제 → ②가 빨강, **화면이 그 칸을 보내게
  만들면 → ②가 반대 방향으로 빨강**(그때 ①은 초록 — 말이 참이 되므로)
- **「왜 0곳인지」를 찾을 때 비슷한 기록에 속지 말 것** — 마감일 편집이
  0곳인 이유를 찾다가 「담당자·마감일의 **진짜 popover** — 안 만들기로
  했습니다」를 만났습니다. 그건 **승인 화면 위젯의 생김새** 이야기이고
  (네이티브 위젯을 다시 칠할 것인가), 「마감일을 나중에 고칠 수 있는가」와는
  다른 질문입니다. ⚠️ **낱말이 겹치는 기록을 「그 결정」으로 읽지 마십시오** —
  기록이 무엇을 정했는지 문장으로 확인하고, 없으면 「정한 적이 없다」가
  답입니다

- **손으로 고른 집합을 「그 기준으로」 다시 잰 것 — 두 번째** — 결함 329 가
  「사람의 기여 숫자를 바꾼 행동」 집합에 삭제 둘을 넣으면서 **그 기준으로
  전부 재 보라**고 적어 뒀는데, 정작 그 집합을 전수로 잰 적은 없었습니다.
  재 보니 **업무 완료**가 빠져 있었고, 그게 이 저장소에서 제일 크게
  움직이는 행동이었습니다(김민수 +3.38~5.96%p · **담당자도 아닌** 박지원
  −3.07~5.42%p — 범주 몫이 다시 나뉘기 때문). ⚠️ 바로 위 줄이
  「담당자 변경이 여기 있는 이유는 **완료 점수**가 갈 사람이 바뀌기
  때문」이라고 적고 있었습니다 — **근거로 든 것 자체가 집합에 없었습니다**
- **움직임을 재려면 「대조군」이 먼저인 것** — 두 번 재는 사이에 시간이
  흐르므로, **아무것도 안 하고 두 번 재서 0 이 나오는지**부터 봐야
  합니다. 안 넣었으면 ±0.2%p 짜리 움직임을 시간 감쇠와 못 갈랐습니다
  (결함 387). 자를 만들 때 **첫 줄이 대조군**입니다
- **씨앗의 합성 이벤트로 「삭제가 점수를 움직이나」를 잰 것** — 씨앗의
  완료 이벤트는 `source_id` 가 **실제 업무를 안 가리킵니다**(일부러 그렇게
  둔 것이고 그것을 지키는 검사도 있습니다). 그래서 업무를 지워도 아무 일이
  없는 게 당연했습니다 — **완료시킨 뒤 지워야** 재는 것이 됩니다.
  ⚠️ 「안 움직임」이 나오면 **내가 움직일 수 있는 상태를 만들었는지**부터
  보십시오
- **본문 모양을 지어내 422·409 를 받고 「안 움직임」으로 적을 뻔한 것** —
  `{"decisions": …}` 는 없는 모양이고 실제로는 `{"items": …}` 입니다.
  거부당한 요청은 **「안 움직임」이 아니라 「못 쟀음」**입니다. 표에 그렇게
  적으십시오 — 안 그러면 다음 사람이 그 줄을 「쟀는데 0」으로 읽습니다
- **목록에서 하나를 집을 때 「조상을 걸어 제목 찾기」는 판 전체를 뭅니다** —
  지난 회차에 `.first()` 로 남의 카드를 눌러 놓고, 고친 자가 **또**
  틀렸습니다: 버튼에서 위로 걸어 올라가며 제목을 찾으면 결국 **판**이
  걸립니다. 카드에는 `data-id` 가 있었습니다 —
  `article.task[data-id="N"] button.move[data-to="done"]`. ⚠️ **누르기 전에
  「내가 집은 것이 그 카드인가」를 찍으십시오** — 감사 기록에 내가 안 누른
  업무가 찍혀 있어서 알았습니다
- **검사가 결함을 못 박고 있을 때 191 과 가르는 법** — 「업무 완료는
  숫자를 안 건드린다」를 못 박은 검사가 있었습니다. 191(기록된 결정)과
  다른 점은 **이유가 한 줄도 없다**는 것입니다 — 「집합에 없는 아무 행동」이
  필요해서 고른 예였고, 측정이 그 예를 반박합니다. ⚠️ **아닌 쪽 예도
  재서 고르십시오** — 안 그러면 다음 사람이 그 예를 결정으로 읽습니다
- **docstring 의 「제일 큰」 같은 최상급도 낡는 것** — 결함 329 가
  「이 저장소에서 한 번에 제일 크게 움직이는 기록입니다」라고 적어 뒀는데
  387 에서 더 큰 것이 나왔습니다. **문서의 숫자는 세어 보고 쓰라**가
  검사 파일의 docstring 에도 걸립니다

- **내가 방금 쓴 주석이 과장이던 것** — 검사 하나를 만들고 옆 주석에
  「그 기준으로 **전수를** 잽니다」라고 썼는데, 그 검사는 **행동 하나만**
  재고 있었습니다. 결함 304 의 「범위를 말할 때는 세어 보고 쓰십시오」가
  **내가 이번에 쓴 문장**에 걸린 것입니다 — 다음 사람이 그 주석을 믿으면
  나머지를 영영 안 셉니다. ⚠️ **「전수」·「전부」·「언제나」를 쓰기 전에
  세십시오.** 못 재는 것이 있으면 **그것을 이름으로 적으십시오** —
  「안 움직인다」가 아니라 「못 쟀다」입니다
- **짝 가드를 「한 방향만」 단언해야 하는 경우** — 「숫자를 움직이는 행동은
  집합에 있어야 한다」는 참인데, **반대는 아닙니다.** 담당자 변경·AI 결과
  수정은 지금 당장은 안 움직이고 **앞으로 갈 점수의 주인을 바꿉니다** —
  집합 주석이 그것을 「눈에 띄는 쪽으로 틀립니다」로 정해 뒀습니다.
  ⚠️ **양방향 가드가 기록된 결정을 뒤집지 않는지 보십시오** — 뒤집을 것
  같으면 재는 방향을 하나로 좁히고 **왜 좁혔는지** 적으십시오
- **「씨앗이 안 만든다」와 「제품이 안 만든다」는 다른 것** — 「내 녹음과
  성문 지우기」를 켜 보려고 `Voiceprint` 를 심으려다 그만뒀습니다.
  `Voiceprint(` 를 만드는 실기 코드가 **0곳**이고, `docs/07` §2.3 이
  「멀티트랙이면 애초에 불필요」라고 적어 둔 **기록된 결정**입니다.
  앞엣것(씨앗 구멍)은 채워야 하고(결함 385) 뒤엣것은 건드리면 안 됩니다 —
  ⚠️ **심기 전에 「진짜 생산자가 있는가」부터 세십시오**
- **심다가 터진 트랜잭션이 **다른 것까지** 되돌린 것** — 오디오와 성문을
  한 트랜잭션에서 심었는데 성문의 `embedding` 이 **bytes 가 아니라 JSON**
  이라 `TypeError` 가 났고, **오디오까지 같이 롤백**됐습니다. 세션을 새로
  열어 0행인 것을 보고 알았습니다 — 안 봤으면 「심었는데 화면이 그대로」를
  **제품 결함**으로 읽었을 것입니다(결함 358 회차의 그것). ⚠️ **칸의 타입을
  지어내지 말고 모델을 읽으십시오**, 그리고 **심은 뒤에는 새 세션에서
  행 수를 세십시오**

- **「뜻이 사라졌는가」를 「버킷이 1이 됐는가」로 판정한 것** — 고대비에서
  색이 뭉개지는 것을 재는데 「모두 같은 색이 됐는가」로 물었습니다. 레일의
  상태 점은 다섯 색이 **둘**로 줄어드는데 그 자에 안 걸립니다 — 다섯 상태가
  두 덩어리가 되는 것도 뜻이 사라지는 것입니다. ⚠️ **「하나가 됐는가」가
  아니라 「가짓수보다 줄었는가」로 물으십시오** — 뭉개짐은 전부 같아질
  때만 생기는 것이 아닙니다
- **이름표를 요소 자신에서만 찾은 것** — `.chan-dot` 에는 `aria-label` 이
  없고 **줄**(`a.chan-row`)에 「스프린트 2 계획, 녹음 단계」로 붙어
  있습니다. 제 자신만 보고 「이름이 없다」로 읽어, 하마터면 결함 350 의
  전제가 거짓이라고 적을 뻔했습니다. ⚠️ **이름을 잴 때는 조상 사슬을
  걸으십시오** — 「숨은 조상을 안 본 것」이 **보임**에서 겪은 것의
  **이름**판입니다
- **기록된 결정은 그 「전제」를 재고 나서 두는 것** — 결함 350 이
  「점은 장식이고 **귀는 `aria-label` 로 듣습니다**」라고 적어 뒀습니다.
  그 전제를 실제로 재 보니 참이라(줄에 붙어 있습니다) 결정을 그대로
  뒀습니다. ⚠️ **결정을 존중하는 것과 그 근거를 안 재는 것은 다릅니다** —
  재고 나서 두면 다음 사람이 같은 의심을 다시 하지 않습니다. 그리고
  **이번에 처음 잰 수치**(고대비에서 5→2)는 적어 두십시오
- **브라우저가 있어야 도는 자는 검사 묶음에 못 들어가는 것** — 고대비
  대조는 `npm test`(node 전용)에도 pytest(DOM 없음)에도 못 넣습니다.
  ⚠️ **그러면 「가드를 못 만들었다」를 적어 두십시오** — 스크래치패드
  스크립트와 방법을 `docs/24` 에 남겨야 다음 사람이 다시 돌립니다.
  안 적으면 그 축은 이 회차 뒤로 아무도 안 봅니다

- **씨앗의 날짜를 오늘과 나란히 놓아 본 적이 없는 것** — 회의 시각이
  `datetime(2026, 9, 1, ...)` 이라는 **고정 날짜**였는데 오늘은 8월
  25일이었습니다. 달력이 「1일 · **연 회의** · 1주차 정기회의」라고
  그렸고, 그 회의는 `needs_review` 입니다 — `started_at` 은 회의가
  **시작할 때** 찍히므로 미래일 수 없습니다(결함 388). ⚠️ **검사는 이
  부류를 구조적으로 못 봅니다** — 씨앗을 만들고 **그 안에서** 셈하기
  때문에, 고정 날짜끼리 일관되기만 하면 전부 초록입니다. 오늘과 견주는
  자가 하나도 없었습니다. 고정 날짜는 **「시연을 언제 보는가」를 말없이
  전제**하고, 낡을 때 오류도 안 내고 화면도 멀쩡히 그려집니다.
  ⚠️ 상태(빈 쪽·꽉 찬 쪽·중간·경계값)에 이은 또 하나의 축입니다 —
  **시각**. 씨앗이 만드는 날짜를 오늘과 견주십시오
- **고치자 반대쪽이 깨질 뻔한 것** — 회의를 과거로 옮기면 거기서 나온
  마감일도 같이 과거가 되는데, 서버는 **과거 마감을 거절합니다**
  (`DEADLINE_IN_PAST`). 시연의 대표 장면인 「후보 승인」이 통째로 막힐
  뻔했습니다 — **고친 뒤 그 기능을 한 번 더 써 봐서** 잡았습니다
  (결함 297 의 방법). ⚠️ **앵커를 옮길 때는 그 앵커에 매달린 값이
  무엇인지 세십시오** — 이미 승인된 후보는 회의 기준으로, 아직 승인
  대기인 후보는 **앞날**로 갈라야 합니다
- **고친 것이 다음 관점을 열어 준 것 — 두 번째** — 388 을 고치자 씨앗
  회의가 전부 물러나 9월이 **처음으로 비었고**, 그 빈 달을 렌더해서
  결함 389 가 나왔습니다(첫 번째는 결함 376 → 음성 채널 화면).
  ⚠️ **고친 뒤에 그 고침으로 열리는 상태를 다시 여십시오**
- **내가 고친 화면의 옆자리를 안 센 것** — 결함 386 이 일정 화면
  **머리줄**에서 「칸반에서 마감일을 고치면」을 걷어냈는데, 같은 화면
  빈 상자가 500px 아래에서 「칸반에서 업무에 마감일을 **주세요**」라고
  하고 있었습니다(결함 389). 한 화면이 두 곳에서 반대되는 말을 합니다 —
  **「같은 화면에서 같은 모양을 몇 곳이나 쓰는지 세고 고치십시오」**
  (결함 298·301)의 「한 갈래」가 이번에는 **내 고침**이었습니다
- **내 가드의 자가 좁아 내 고침의 옆자리를 놓친 것** — 386 가드가
  `칸반에서[^.]{0,40}마감일을 (고치|바꾸|수정)` 라 「마감일을 **주세요**」
  를 못 봤습니다. 걷는 자리는 맞았습니다 — **자 자체**가 좁았습니다
  (결함 299 의 모양). 넓힌 자는 낱말이 아니라 요구를 봅니다:
  「칸반이 마감일을 **정하는 자리**라고 말하는가」 — 동사를
  `고치|바꾸|수정|주|넣|정하|달` 로 넓히고 **두 낱말의 순서가 뒤집힌
  경우**도 봅니다. 「옛 자 + 옛 코드 = 0건」으로 구멍을 증명했습니다
- **한 갈래의 글자를 못 박는 검사는 옆 갈래를 못 보는 것** — 빈 상자
  검사가 `emptyNote` 의 **한 갈래**만 글자로 못 박고 있었습니다. 다른
  갈래에 같은 주장을 심으니 그 검사는 조용했습니다 — **갈래 전수를 훑어
  요구를 재는 검사**를 따로 두고, 다른 갈래에 심어 **그것만 따로**
  빨개지는 것을 확인하십시오

- **백틱을 정규식으로 짝지어 자가 눈을 감은 것** — 「HTML 을 만드는
  템플릿에서 이스케이프를 안 거친 자리」를 `` `[^`]*<[a-zA-Z/][^`]*` `` 로
  쟀습니다. 백틱을 **앞에서부터 둘씩** 짝지어 가므로, 주석이나 따옴표
  문자열 안에 백틱이 하나라도 있으면 **그 뒤의 짝이 통째로 어긋납니다.**
  `call.ts` 의 `<li>` 템플릿이 아예 안 걸려 그 안의 `${'${capture.tone}'}` 이 눈
  밖이었고, 동시에 코드를 가로질러 **가짜 템플릿**도 만들었습니다
  (`hooks.ts` 33개 → 진짜는 0개). 문자 하나씩 걷는 스캐너로 바꾸니
  293 → 72 였고, 그러자마자 **템플릿 안의 템플릿**이 하나 더 보였습니다.
  ⚠️ **짝을 맞춰야 하는 것(따옴표·괄호·백틱)은 정규식으로 세지 마십시오** —
  합성 소스로 옛 자와 새 자를 나란히 돌려 증명하십시오
- **자를 검증하려고 제품을 약하게 만들 뻔한 것** — 이스케이프를 빼서 심으려
  했는데, 재려는 것은 「**누출이 나면 자가 우는가**」입니다. 화면에서
  `host.innerHTML = payload` 로 **누출 상태를 직접 만들어** 자가 우는 것을
  확인하면 충분합니다. ⚠️ 그러면 「자가 볼 수 있다」만 증명되고 「제품이
  안전한 이유」는 소스 census 가 따로 대야 합니다 — **무엇을 증명했고
  무엇을 안 했는지 갈라 적으십시오**
- **주석을 걷는 헬퍼가 문자열 안의 `//` 를 먹는 것** — `_blanked_ts` 는
  문자열을 안 가려서 `'http://127.0.0.1:8811/home.html'` 을 줄 주석으로
  먹습니다. 세어 보니 두 뿌리에서 아홉 줄이고 여덟은 이미 주석 안,
  진짜는 하나였습니다. ⚠️ **방향이 fail-open 입니다** — 먹힌 코드는 안
  보이므로 가드가 「0건」쪽으로 틀립니다. 이 함수 위에 가드를 얹을 때는
  **「내가 보려는 것이 문자열 안에 있나」를 먼저 세십시오.** 고치려면
  정규식 리터럴까지 갈라야 하는데(`/[&<>"']/g` 가 그 함정) 그 자가 틀리면
  이 함수를 쓰는 가드가 **전부 조용히** 어긋나므로, 잘못 고치는 것이 안
  고치는 것보다 나쁜 자리입니다 — **재서 적어만 두었습니다**
- **제일 무거운 부류를 한 번도 안 세어 본 것** — `lib/html.ts` 는 XSS 를
  **알고** 만들어졌고 머리말이 실제 사고까지 인용하는데, 「그래서 지금
  전부 거치고 있는가」를 세는 자가 없었습니다. 세어 보니 새는 자리 0
  이었고, 페이로드를 제품 경로로 심어 화면 열여섯을 열어도 0 이었습니다.
  ⚠️ **「이 위험은 이미 안다」와 「지금 지켜지고 있다」는 다릅니다** —
  주석이 위험을 정확히 적어 두었을수록 아무도 안 세어 봤을 수 있습니다

- **거르개가 어디까지 걸리는지 화면이 안 말한 것** — 찾기의 「담당자」·
  「업무 상태」는 서버에서 **업무 검색의 조건**입니다(SEARCH-002).
  담당자를 김민수로 고르고 찾으면 업무 묶음은 사라지는데 회의·회의 내용은
  그대로 남고, 그 줄의 사람 칸이 **박지원**입니다 — **고른 사람과 다른
  사람의 이름이 결과에 붙습니다**(결함 390). 서버 설계는 옳고 docstring 도
  정확했고, 안 적은 것은 화면입니다. ⚠️ **옆 칸은 이미 「업무 상태」라고
  자기 범위를 적고 있었습니다** — 같은 폼 안에서 「한 갈래만 고치고 옆
  갈래를 그대로 둔 것」(결함 298·301)입니다. 331·332·344·384 에 이어
  **범위를 말할 때는 이름을 붙이십시오**가 거르개에서 난 것입니다
- **이름을 고치는 것만으로는 모자란 것** — 「업무 담당자」로 고쳐도
  「그럼 회의는 왜 나오지」에는 답이 없습니다. **안 걸린 묶음이 실제로
  화면에 있을 때만** 한 줄을 답니다(결함 294 의 방법) — 업무만 나왔으면,
  안 걸렀으면, 거를 칸이 없는 화면이면 할 말이 없습니다. 네 경우를
  브라우저로 확인했습니다
- **「지금 고른 값」으로 말할 뻔한 것** — 그 파일에는 이미 `asked` 가 있고
  주석이 「입력칸이 아니라 **이것**으로 자릅니다」라고 적어 둡니다.
  거르개도 같은 이유로 `askedFilters` 를 따로 둬야 합니다 — 고르기만 하고
  안 누른 값으로 말하면 **그 줄이 결과보다 앞서 갑니다.**
  ⚠️ **같은 파일이 이미 그 함정을 한 번 피해 뒀으면 그 방법을 그대로
  쓰십시오**
- **결과를 `body.innerText` 의 끝부분으로 읽은 것** — 끝에 있는 것은 왼쪽
  레일이라 무엇을 찾아도 같은 글자가 나왔습니다. 「아무것도 안 나온다」로
  적을 뻔했습니다 — `.kgroup`·`.hit`·`.hwho` 로 **이름을 집어** 읽으니
  그때서야 보였습니다(결함 293 의 모양)
- **내 검사가 「언제나 참인 자리」에서 낱말을 찾은 것** — 「안 걸린 목록에
  업무가 없다」를 재는데 **문장 전체**에서 '업무' 를 찾았습니다. 그 문장은
  「업무에만 걸립니다」로 시작하므로 언제나 참입니다 — **줄표 뒤**만 봐야
  재는 것이 됩니다. ⚠️ 문장 안에 **같은 낱말이 두 뜻으로** 들어 있으면
  자를 그 조각에 대십시오

- **씨앗의 우연이 깨지자 비로소 잴 수 있게 된 것** — 기여도 목록이 이름
  순인지는 **번호 순·이름 순·점수 순이 갈라져야** 확인됩니다. 예전 씨앗은
  서버가 내려보내는 번호 순이 하필 점수 내림차순이었고, 결함 388 이
  앵커를 옮기면서 그 우연이 깨졌습니다. 셋이 갈라진 상태에서 두 뿌리를
  재니 **둘 다 이름 순**이었습니다 — 초록이지만 **이번이 처음**입니다.
  ⚠️ **그 상태를 가드로 못 박으십시오** — 씨앗 이름 하나를 바꿔 심으니
  정확히 그 문장으로 빨개졌습니다. 겹치는 날이 오면 고칠 것은 화면이
  아니라 **씨앗**입니다
- **「한쪽 뿌리만」처럼 생겼는데 그릴 그림이 없던 것** — 결함 336 이 만든
  `coverageReading`·`ribbonReading` 을 부르는 레거시 파일이 0개였습니다.
  렌더해 세어 보니 **레거시 홈에는 리본 요소 자체가 0개**입니다(SPA 는
  14개) — 그림이 없으면 이름을 붙일 것도 없습니다. 결함 365 가
  `splitEvidenceChips` 에서 적어 둔 「부르는 곳이 0곳이면 그 자리가 같은
  해악을 겪는지 재 보십시오」 그대로입니다. ⚠️ **소스에서 「0곳」을 봤으면
  화면을 렌더해 그 그림이 있는지 세십시오**
- **숫자를 「숫자가 든 줄」로 고르고 사람과 못 짝지은 것** — 기여도 카드를
  누구 것인지 가르려는데 **이름 줄에는 숫자가 없어서** 필터가 통째로 걸러
  버렸습니다. 셋이 뒤섞인 채로 「첫 카드가 김민수인데 역할이 다르다」고
  적을 뻔했습니다 — ⚠️ **재는 축과 짝지을 축을 같이 뽑으십시오**

- **짝인 두 머리말 중 하나만 공용을 거친 것** — 보고서의 「팀 공통」은
  `teamReasonsHeading()` 을 거치는데 「이 사람」 쪽은 복사 경로에서
  `'못 잼'` 이라는 **글자로 박혀** 있었습니다. 화면과 복사한 글이 같은
  목록을 다른 이름으로 불렀습니다. 나가는 값이 틀린 것은 아니라 **결함으로
  안 셌지만**, 한쪽만 고치면 갈라지는 자리라 사본을 없앴습니다(결함 363).
  ⚠️ **머리말·이름표가 짝을 이루면 둘 다 같은 곳에서 오는지 세십시오** —
  하나만 공용이면 그 짝은 이미 반쯤 갈라져 있습니다
- **공용 씨앗이 그 갈래를 안 만들어 내 검사가 헛돌 뻔한 것** — 「두 머리말이
  다 나오는가」를 공용 `CONTENT` 로 쟀는데 그 씨앗은 `reasons` 가 **빈
  배열**이라 「팀 공통」 줄이 아예 안 나옵니다. 늘 통과하는 자였습니다 —
  **둘 다 있는 경우를 검사 안에서 만들어야** 재는 것이 됩니다(결함 347 의
  「빈손으로 통과한 짝 검사」)
- **나가는 문서는 눌러서 클립보드까지 읽으십시오** — 화면은 「이 사람」인데
  복사한 글은 「못 잼」이었습니다. 화면만 보면 둘이 다르다는 것을 영영
  모릅니다

- **재는 자와 보여 주는 자의 정밀도가 달랐던 것** — 확정 칸의 「시스템
  36.9%」와 placeholder 는 **한 자리 반올림**인데 「시스템 값과 같은가」는
  `sameValue` 로 **정확히**(1e-9) 쟀습니다. 실제 값이 `36.874` 라, 화면이
  보여 준 값을 그대로 적으면 「다르게 정했다」가 되어 사유를 요구했습니다.
  열리는 값은 **화면 어디에도 없습니다** — 이 제품은 불변식 ②(단일 점수
  금지) 때문에 정확한 값을 안 그리고, 빈 칸으로 두는 길은 결함 372 가
  막았습니다. 그래서 팀은 **하지도 않은 조정에 사유를 지어내야**
  했습니다(결함 391). ⚠️ 불변식 ④가 사유를 요구하는 것은 **실제 조정**에
  근거를 남기라는 뜻인데, 지어낸 사유가 섞이면 그 기록 자체가 값을
  잃습니다. **고침은 재는 자를 보여 주는 자에 맞추는 것**입니다 —
  글자를 두 곳에 적지 말고 표시 함수를 그대로 쓰십시오
- **같은 판단을 부르는 곳이 둘인데 하나만 고칠 뻔한 것** — 「같은가」는
  게이트(`needsReasonFor`)와 **보내는 값**(`toPayload`)이 같이 씁니다.
  한쪽만 고치면 「이유가 필요 없다」고 해 놓고 값은 조정으로 실려 나가
  서버가 400 으로 거절합니다 — 둘을 **따로 심어 각각** 빨개지는 것을
  확인하십시오
- **`aria-label$=`(끝나는)으로 집어 「그 화면에는 없다」고 적을 뻔한 것** —
  SPA 의 라벨은 `김민수 확정값 (%) — 시스템 추정 27~47%` 라 끝이 다릅니다.
  「SPA 에는 확정 자리가 0개」라는 답을 받았습니다 — **소스를 읽고 이름을
  확인한 뒤** `*=` 로 고치니 3개였습니다
- **심기 검증 grep 이 검사 이름의 `⭐` 때문에 안 걸린 것** — `- (이름)` 으로
  찾았는데 검사 이름이 `⭐ 이름` 이라 못 잡았습니다. 「가드가 안 잡는다」로
  읽을 뻔했습니다 — **`not ok` 줄을 통째로 찍으십시오**

- **씨앗이 0건인 표는 그 뒤의 세계 전체가 미검수인 것** —
  `final_contributions` 가 0행이라 **확정 뒤의 보고서·다시 확정·그 감사
  기록**이 한 번도 안 그려져 있었습니다. 셋 다 밟아 보니 초록이었고,
  그 김에 결함 293·297(감사 기록의 사람 이름)이 **씨앗이 안 만드는
  종류**에서도 도는 것을 처음 확인했습니다. ⚠️ **행이 0인 표를 보면 그
  표에 매달린 화면을 세십시오** — 대개 하나가 아닙니다
- **소수 자리가 다른 것을 결함으로 적을 뻔한 것** — 보고서에 「팀 확정
  40%」와 「팀 확정 36.9%」가 나란히 나옵니다. 세어 보니 같은 파일의
  `describeRange` 도 서버가 준 숫자를 그대로 찍습니다 — **저 혼자 예외가
  아니라** 이 모듈의 관습(정밀도는 서버가 정하고 화면은 옮겨 적음)입니다.
  하나만 고치면 제품 규칙을 나 혼자 바꾸는 것입니다(결함 350 의 가르는 자)

- **제일 무거운 쓰기에 문이 하나도 없던 것** — 팀 전원의 확정 기여도를
  쓰는 라우트가 `_require_project_member` 만 봤습니다. 평범한 팀원이 자기
  몫을 90%, 나머지를 5%씩으로 확정했고 `201` 이 떨어졌습니다(결함 392).
  ⚠️ **가른 것은 세어 본 것입니다** — 쓰기 라우트 44개 중 「팀원 누구나」
  이면서 **남의 숫자**를 쓰는 것은 이것 하나였고, 나머지 셋은 경로가
  `/me` 라 남을 못 건드립니다. 그리고 이 저장소는 **더 작은 일**(역할
  비중)을 이미 「자기 것만」으로 정해 두고 그 이유까지 적어 뒀습니다
- **「기록된 결정」처럼 보였지만 조건이 붙어 있던 것** — 그 라우트 주석은
  「지금은 구성원 누구나입니다. 이 저장소에 역할 개념이 아직 없습니다 …
  **역할이 생기면 여기부터 좁혀야 합니다**」였습니다. 그 뒤에 역할이
  생겼습니다 — **조건이 충족된 결정은 뒤집는 것이 아니라 그 문장이
  스스로 시킨 숙제**입니다. ⚠️ 주석을 읽을 때 **「지금은」·「아직」·
  「생기면」** 같은 말을 찾으십시오. 그 결정은 유효기간이 있습니다
- **화면만 막으면 API 가 그대로 열려 있는 것** — 권한은 **어휘와
  라우트**에서 먼저 막고, 화면은 **왜 안 되는지**를 말합니다(결함 316 —
  버튼을 지우지 않습니다). 그리고 그 갈래는 **맨 앞**이어야 합니다:
  확정할 수 없는 사람에게 「3칸 남음」부터 말하면 셋을 다 채우고 눌렀을
  때야 403 을 만납니다
- **가드가 「파일 어딘가 한 곳」만 본 것** — 「게이트에 등급을 넘기는가」를
  파일 전체에서 한 번만 찾았더니, SPA 가 **사유 문단에서도** 같은 함수를
  불러서 **정작 단추를 잠그는 호출에서 빼도 초록**이었습니다. 부르는
  자리를 **전부 세고 하나씩** 보십시오(결함 306 을 한 파일 안에 댄 것)
- **셸이 백틱을 먹은 것** — `python -c "… \`myRole\` …"` 을 그대로 넣었더니
  bash 가 명령 치환으로 읽어 주석 한 조각이 통째로 사라졌습니다.
  **파일에 넣을 글에 백틱이 있으면 heredoc 을 쓰십시오**

- **flex 컨테이너에 심어 놓고 「자가 안 잡는다」로 읽을 뻔한 것** — 넘침
  자를 검증하려고 2200px 짜리 `div` 를 `header.appbar` 에 넣었는데 0건
  이었습니다. 그 헤더는 flex 컨테이너라 **빈 `div` 가 `flex-shrink` 로
  줄어들어 애초에 안 넘쳤습니다.** 같은 자를 `h1`·`main`·`body` 에 심으니
  전부 잡혔습니다 — 결함 164 의 「심었는데 아무 일도 안 일어난 것」의
  flex 판입니다. ⚠️ **넘침을 심을 때는 줄어들 수 없는 것**(글자가 든
  요소 · `flex-shrink: 0` · 스크롤 상자 밖)에 심으십시오
- **세로를 먹는 것을 「높이」로만 센 것** — 고정 요소가 창을 얼마나
  먹는지 재는데 높이만 봤더니 **왼쪽 레일**(창 높이 전체)이 「창의 100%」
  로 잡혔습니다. 세로를 먹는 것은 **가로로 넓고 위·아래에 붙은** 것
  뿐입니다 — 결함 281 을 다시 재려면 축을 갈라야 합니다
- **접히다 마는 값을 안 잰 것** — 결함 338 은 기본 글자 **32px** 하나로
  잡았고, 미디어 쿼리의 `rem` 이 보는 축은 그 사이입니다. 16·20·24·28·32
  × 화면 여섯(30 조합)을 훑으니 **전부 0건**이었고, 고정 크롬이 창을 먹는
  비율은 0%→13% 로 자랐습니다(결함 281 의 47% 와 다른 수준이라 안 셌습니다).
  ⚠️ **한 값에서 고쳤으면 그 사이를 훑으십시오**

- **SPA 라우트를 또 지어내 홈을 여러 번 잰 것** — 검토 화면을
  `/app/project/1/review/1` 로 열었습니다. 실제 라우트는
  `/meeting/:meetingId/review` 이고 `*` 가 **홈으로 되돌립니다.**
  결함 182·356 이 적어 둔 바로 그것인데 **또** 걸렸습니다 — 이번에는
  캡처를 열어 보고 알았습니다. ⚠️ **라우트는 `main.tsx` 에서 읽고**,
  화면마다 **요소 수와 머리글**을 찍으십시오(SPA홈 els=116 · SPA검토
  els=327 로 갈립니다)

- **고대비에서 「채움만으로 그린 그림」이 통째로 사라지는 것** —
  `forced-colors: active` 는 **채움을 시스템 배경색으로 덮습니다.**
  기여도의 불확실성 점 열여섯이 밝은 쪽에서는 흰 바탕에 흰색, 다크에서는
  검은 바탕에 검은색이 되어 하나도 안 보였는데, **옆 문구는 「점 하나가
  4%p」라고 그 그림을 가리키고** 있었습니다(결함 393). ⚠️ 저장소가 그
  함정을 이미 적어 두고 **개수가 0일 때만** 막고 있었습니다 — 고대비는
  「개수는 열여섯인데 안 보이는」 경우입니다(결함 295 의 「막는 길을 하나만
  막은 것」의 색 판). 고침은 `@media (forced-colors: active)` 안에서
  **시스템 색 이름**(`CanvasText`)을 쓰는 것입니다 — 토큰이나 hex 는 그
  모드에서 다시 덮입니다
- **「글자가 그대로인가」로 고대비를 잰 것** — 네 모드의 `innerText` 를
  diff 했더니 여덟 화면 전부 0줄 차이였습니다. 당연합니다 — **글자는 색이
  바뀌어도 그대로**입니다. 이 자는 **그림이 사라지는 것을 구조적으로 못
  봅니다.** 고대비에서 재야 할 것은 「글자가 남았나」가 아니라 **「글자가
  없는 그림이 배경과 같아졌나」**입니다
- **내 어휘로 남의 화면을 잰 것** — 「커버리지·확신·모름이 남아 있는가」로
  쟀더니 레거시 홈·기여도가 **네 모드 전부에서** 빨갰습니다. 모드와 상관
  없이 같은 결과라는 것이 단서였습니다 — 그 낱말들은 **SPA 의 어휘**이고
  레거시는 같은 뜻을 「모르는 폭 20%p」로 적습니다. ⚠️ **모든 조건에서
  똑같이 빨간 자는 그 조건을 안 재고 있는 것입니다**
- **2×2 PNG 의 base64 앞부분으로 색을 견준 것** — 헤더가 앞을 차지해서
  **밝은 모드에서도 「같음」**이 나왔습니다. 고대비에서는
  `getComputedStyle` 이 **덮인 값을 그대로** 돌려주므로 그것을 배경과
  나란히 놓으면 됩니다 — 픽셀을 읽어야 하는 것은 `color-mix()` 쪽입니다

- **값이 `title` 에만 있는 것을 「보이는가」로는 못 보는 것** — 로비의 끊김
  표시는 글자도 초점도 없는 8px 조각인데 `title` 에 「22~23분 · 마이크가
  꺼져 있었습니다」를 달고 있었습니다. 표시는 **그려져 있으므로** 보임·
  잘림·대비 감사가 전부 초록이고, 닿는 길은 **마우스 하나**였습니다
  (키보드는 초점을 못 주고 · 터치는 hover 가 없고 · 고대비에서는 표시
  자체가 사라집니다 — 결함 394). ⚠️ **가른 자는 「본문 글자에 같은 값이
  있는가」입니다** — `\d+~\d+분` 을 본문 전체에서 찾으니 **0줄**이었습니다.
  결함 336 은 `aria-label` 이 눈보다 많이 아는 것이었고 이것은 **`title` 이
  키보드보다 많이 아는 것**입니다 — **같은 값을 나르는 채널을 전부 찍어
  나란히 놓으십시오**(글자 · `title` · `aria-label` · 초점 가능 여부)
- **주석이 한쪽 뿌리를 좁혀 놓은 것** — `describeGap` 의 주석이 「툴팁에
  씁니다」라 레거시가 **그 말대로** `title` 에만 넣었습니다. 함수도 값도
  맞는데 **쓰는 자리를 주석이 정한** 것입니다(결함 394). 「주석은 아무것도
  안 막습니다」의 반대 방향 — ⚠️ **`@lib` 주석에 「어디에 쓴다」를 적을
  때는 그것이 유일한 자리인지 세고 적으십시오.** 고칠 때는 코드와 함께
  **주석도** 고쳐야 다음 사람이 같은 결론을 안 내립니다
- **가드를 「부르는가」로 쓰면 옛 코드도 초록인 것** — 옛 레거시는
  `title={describeGap(…)}` 로 **이미 부르고** 있었습니다. 자를
  **「`title=` 이 아닌 자리에서 부르는가」**로 좁혀야 심기가 잡힙니다 —
  결함 306 의 census 가 이 부류를 구조적으로 못 보는 자리입니다
- **자를 하나 만들었으면 그 자를 전수로 대 보는 것** — 394 가 남긴
  「`title` 을 쓰는 다른 자리는 안 셌습니다」를 실제로 폈더니 하나가 더
  나왔습니다(결함 395). 검토의 확신도 알약이 `34%` 만 그리고 「AI 확신도」는
  `title` 에만 있었고, **접근성 트리에는 그 숫자가 아예 없었습니다**
  (`%` 를 이름으로 가진 노드 0개) — 눈은 맨 숫자, 귀는 아무것도, 키보드·
  터치는 못 닿습니다. ⚠️ **가른 자는 「본문 글자에 같은 값이 있는가」**
  입니다. 결함 329 의 「손으로 고른 목록을 그 기준으로 전부 재라」가
  **내가 방금 만든 자**에 걸린 것입니다
- **취향과 결함을 「세어서」 가른 것 — 이번엔 유죄** — 「배지에 축 이름이
  없다」는 낱개로 보면 취향입니다. 이 제품이 사람에게 보이는 백분율을
  **전부** 모으니 열 자리가 축을 달고(「팀의 32%」·「커버리지 80%」·「모르는
  폭 20%p」·「팀 신뢰도 62%」·「유사도 87%」·「기간 40% 지남」·「확신 45% ·
  모름 55%」) **맨몸은 하나**였습니다. 결함 350 은 같은 자로 **무죄**를
  냈습니다(칩이 전부 맨몸이라 하나만 고치면 제품 규칙을 나 혼자 바꾸는
  것) — ⚠️ **세어 보기 전에는 어느 쪽인지 알 수 없습니다**
- **한쪽 뿌리가 이미 맞게 하고 있으면 그것이 관습이 아니라는 증거인 것** —
  SPA 는 같은 값을 「확신 34%」라고 적고 있었습니다. **같은 제품이 같은
  값을 두 가지로 부르면 그중 하나는 틀린 것**이고, 어느 쪽이 맞는지는
  세어서 정합니다(결함 290 의 「둘을 같이 놓아야 어긋난 것이 보인다」)
- **고칠 때 「고치면 사본이 둘」인지 보는 것** — SPA 는 그 글자를 **자기
  파일에 손으로** 적고 있었습니다. 레거시만 고치면 사본이 둘 남습니다 —
  `@lib` 에 한 벌을 두고 양쪽이 부르게 했습니다(결함 363). ⚠️ 그러면 자도
  **「@lib 를 거치는가」 + 「손으로 조립하지 않는가」** 둘이 되고, 각각
  따로 심어 서로 다른 하나만 빨개지는 것을 확인해야 합니다
- **기록된 결정이 「무엇에 대한」 결정인지 좁혀 읽는 것** — 그 알약 주석은
  「48px `34%` 를 걷어냈습니다 — 확신도는 제목을 읽고 나서 참고하는 값」
  이었습니다. **크기**에 대한 결정이고 **이름**에 대한 것이 아닙니다.
  크기를 줄이면서 축 이름이 `title` 로 내려간 것은 결정이 아니라 부작용
  입니다 — ⚠️ **결정문이 무엇을 정했는지 문장으로 확인하십시오**(「낱말이
  겹치는 기록을 그 결정으로 읽지 마십시오」의 사촌)
- **이름이 겹치는 화면에서 Tab 이 무엇을 짚었는지 안 가른 것** — 레거시 홈의
  막힌 레일 링크를 재는데 Tab 이 「칸반」에 닿았습니다. 그런데 이 셸은
  **하단 탭바에도 같은 이름**이 있습니다 — 레일인지 탭바인지 안 갈랐으므로
  「닿는다」도 「못 닿는다」도 못 적습니다. **다음 실마리로 적었습니다**
  (「재는 칸에 임자가 있던 것」의 부류)
- **기록된 결정의 「전제」를 재 보는 것 — 그리고 깨졌는데도 결함이 아닌 것** —
  칸반의 「근거를 줄로 깔지 않습니다 — 확정과 추정은 이미 **색**이 말하고
  있어서」는 근거를 적은 결정입니다. 그 전제를 고대비에서 재니 **정말
  깨졌습니다**(두 갈래가 색·무게·기울기·밑줄·`::before` 까지 한 자도 안
  다름). 그런데 같은 카드의 `<details>「자세히」`가 그 값을 **글로** 들고
  있었고 Tab 으로 닿았습니다 — SPA 도 「?」 팝오버로 똑같습니다.
  **재현 안 됨.** ⚠️ **전제가 깨지는 것과 값이 사라지는 것은 다릅니다** —
  전제만 재고 멈추면 없는 결함을 만듭니다
- **접힌 것을 찾은 것이 「눌러서」가 아니라 `grep` 이었던 것** — 같은 파일에
  `describePull` 을 그리는 자리가 **둘**이었고, 둘째가 드로어였습니다.
  화면만 봤으면 「없다」로 읽었을 것입니다 — ⚠️ **같은 값을 그리는 자리를
  세면** 이 부류가 막힙니다(결함 313 의 「빈 상자만 고치고 바로 위 머리줄을
  놓칠 뻔한 것」의 반대 방향)
- **씨앗이 한 갈래만 만들면 「둘이 갈라지는가」는 언제나 초록인 것** —
  `.gh-list li` 가 **1개**였고 그것도 `sure` 하나였습니다. 확정/추정을
  비교한다면서 **확정만 두 번 잰 것**이 될 뻔했습니다(결함 327 의 「범주가
  하나면 가중치가 무엇이든 1.0」과 같은 함정). ⚠️ **비교하기 전에 양쪽이
  화면에 몇 개씩 있는지 찍으십시오**
- **없던 갈래를 「실기 경로」로 만드는 법 — 서명한 웹훅** — GitHub 추정
  링크는 `GITHUB_WEBHOOK_SECRET` 를 주고 서버를 띄운 뒤, `TASK-` 표식이
  **없고** 브랜치 이름만 `fix/3-…` 인 PR 배달을 HMAC 서명해 POST 하면
  `link_source=branch` · `relevance=0.6` 으로 생깁니다(`202`). 표에 행을
  손으로 넣는 것보다 **실기가 만드는 값**이라는 것이 보장됩니다(결함 288)
- **앞 가드가 「다른 축」을 재고 있던 것 — 집합 크기 ≠ 두 줄을 가릴 수
  있는가** — 결함 357 이 알림 문장을 고치면서 「이을 수 있는 사건이
  하나뿐이면 그 이름을 말할 수 있다 · ⛔ 둘 이상으로 늘리려면 알림 행이
  사건을 가리키게 만들어야 한다 · **가드가 잡습니다**」로 끝냈습니다.
  가드는 있고 잘 돕니다 — 재는 것이 **낱말이 뭉뚱그려지는가**일 뿐입니다.
  집합은 하나 그대로인데 **같은 종류의 사건이 둘** 오니 알림 두 줄이
  글자·시각·링크까지 한 자도 안 달랐고, 그중 하나는 브랜치로 **추정**한
  연결이었습니다(결함 396). ⚠️ **「무엇이 늘면 깨지는가」를 적을 때는
  「종류가 늘 때」와 「같은 종류가 여럿 올 때」를 갈라 세십시오** — 앞엣것만
  막으면 뒤엣것이 조용히 지나갑니다
- **docstring 이 「생각해 본 것」을 세면 「안 물어본 질문」이 보이는 것** —
  `_tell_the_assignee` 는 ⚠️ 를 넷 답니다(담당자에게만 · 자기 PR 제외 ·
  담당자 없으면 조용 · 여럿이면 전원). 꼼꼼한 목록인데 **「추정도 알릴
  것인가」가 없습니다.** 기록된 결정이 아니라 **안 물어본 질문**이고,
  그 둘을 가르는 것이 「건드리지 마라」와 「숙제다」를 가릅니다
- **「안 잰 것」이 다음 회차의 입구인 것** — 앞 회차가 「추정 링크가
  기여도에 어떻게 들어가는지 안 쟀다」로 끝냈습니다. 재 보니 **안
  들어갑니다**(초록 — PR 작성자로 붙지 업무 연결의 확신도와 무관). 그런데
  **그 경로를 읽는 길에** 결함 396 이 있었습니다. ⚠️ **적어 둔 질문이
  초록이어도 그 질문을 따라가십시오**
- **되돌릴 수 없는 축을 알림 행에 「글자로」 적지 않는 것** — 확정/추정은
  사람이 확인해서 바뀝니다. 알림 행에 「추정」이라고 적어 두면 확인한 뒤에
  그 글자만 옛말이 됩니다 — 그 표가 문장을 저장하지 않는 이유 그대로,
  **가리키는 것만 저장하고 판단은 읽을 때** 합니다(결함 396)
- **`grep -c` 가 남의 줄을 세어 「심기가 안 먹었다」로 읽을 뻔한 것** —
  심기를 확인하려고 `github_event_id=event.id` 를 셌는데 그것은 알림이
  아니라 **`TaskGithubLink` 를 만드는 줄**이었습니다. 심기는 먹었고 검사도
  제 이유로 빨갰습니다 — ⚠️ **세기 전에 그 이름이 그 파일에 몇 뜻으로
  쓰이는지 보십시오**(「내 자가 어느 칸을 가리키는지」의 되풀이)
- **검사 씨앗의 「배달 번호」를 안 갈라 아무것도 안 잰 것** — 웹훅을 둘
  보내는데 `delivery` 기본값이 같아 두 번째가 **중복**으로 걸러졌습니다.
  「두 줄이 다른가」를 재는 검사가 한 줄만 보고 있었습니다 — ⚠️ **멱등을
  막는 열쇠가 있는 API 는 씨앗마다 그 열쇠를 갈라야 합니다**
- **내가 방금 화면에 쓴 말이 「할 수 있는 일」인지 세어 본 것** — 결함 396
  에서 알림에 「(추정 — **확인 필요**)」를 넣고 나서, 그 확인을 할 자리가
  있는지 세었습니다(결함 306 의 「가라고 적기 전에 그 자리를 세십시오」).
  `confirmed_by` 는 **쓰는 코드가 0곳**이라 실패 ③ 처럼 보였는데, 세어
  보니 아니었습니다 — ① `docs/20` 이 그 요구를 ✅ 라고 **안 적고**
  (결함 382 의 가르는 자), ② 「확인」은 사람이 PR 을 보는 것이고 그러라고
  `repo#41` 을 찍으며, ③ **github.com 링크를 안 다는 것이 기록된 결정**
  입니다(「실제 GitHub 에 붙여 본 적이 없어 주소가 **추측**입니다」).
  ⚠️ **내 고침이 만든 문장에도 같은 자를 대십시오** — 그리고 「단추가
  없다」를 봤으면 「그 단추가 있어야 한다고 누가 적었나」부터 세십시오
- **잘못 적어 둔 실마리를 다음 회차에 고친 것** — 처음엔 그 줄을 「다음
  실마리(실패 ③ 의 모양)」로 적었습니다. 세어 보니 셋 다 반대였고, **그
  줄을 지우지 않고 「이렇게 적었다가 세어 보고 고쳤다」로** 남겼습니다.
  ⚠️ **틀린 실마리를 조용히 지우면 다음 사람이 같은 의심을 다시 합니다** —
  결론과 함께 **왜 아닌지**를 남기십시오
- **한 함수 안에서 갈래마다 다르게 하고 있으면 그 자체가 단서인 것** —
  `hrefFor` 는 회의면 `?meeting=N` 으로 **번호를 들고 가는데** 메시지
  갈래만 `message_id` 를 **버리고** `/chat.html?project=N` 만 만듭니다.
  그래서 한 사람이 나를 두 번 부르면 두 줄이 글자·링크까지 똑같았습니다
  (결함 397). ⚠️ 결함 396 과 다른 점은 **가리킬 것이 이미 있었다**는
  것입니다 — 396 은 칸을 더해야 했고 이쪽은 있는 값을 안 쓴 것입니다.
  **옆 갈래가 하는 일을 이 갈래는 왜 안 하는지 물으십시오**
- **문장을 고칠 때 「이 규칙이 이미 어디 사는지」부터 찾는 것** —
  채널 이름에 `#` 을 붙이는 규칙은 `@lib/chat/view.ts` 의 `channelTitle`
  에 있고 주석이 「`#` 은 **여기서** 붙입니다」라고 이유까지 적어 뒀습니다.
  서버가 같은 규칙을 다시 적으면 두 벌입니다 — 그래서 **서버 산문이 이미
  쓰는 모양**(`channel_service` 의 「`{이름}` 채널이 이미 있습니다」)을
  그대로 썼습니다. ⚠️ **두 벌을 만들지 않는 세 번째 길은 「그 언어가 이미
  쓰는 표현을 찾는 것」**입니다
- **200 을 보고 「바뀐 것」과 「원래 그런 것」을 못 가른 것** — 담당자를
  `PATCH /tasks/{id}` 로 보냈더니 405 였고, 고친 뒤 200 이 온 자리에서
  「업무 3 의 담당자가 지워졌다」고 읽을 뻔했습니다. 그 업무는 **원래부터
  담당자가 없었습니다.** ⚠️ **심기 전 값을 찍어 두십시오** — 안 찍으면
  200 뒤의 상태가 내 탓인지 원래인지 못 가릅니다(「바꾼 뒤에는 서버 상태를
  다시 읽으십시오」의 앞 절반)
- **검사 이름이 재는 것과 다르면 그 갈래는 미검증인 것** — 「채널을 못
  찾으면」이라고 이름 붙여 놓고 실제로는 `message_id` 가 없는 행을 쟀습니다
  (결함 397 회차). 다음 사람은 이름을 믿고 그 갈래를 다시 안 봅니다 —
  결함 323 의 「검사의 이름과 docstring 은 짝을 잰다는데 실제로는 부정
  하나만 보던 것」이 **내가 방금 쓴 검사**에서 났습니다
- **한 축에 두 뜻을 실은 채로 「자른」 것** — 결함 331 이 알림의 `at` 에
  두 뜻이 있다고 적어 뒀는데(저장된 것은 「일어난 때」, 마감은 「마감일」),
  그 축으로 정렬한 뒤 `[:MAX_ITEMS]` 까지 했습니다. **지난** 마감은 과거라
  맨 아래로 가라앉고 채팅 50통이면 **통째로 잘려 나갑니다** — 재현했습니다
  (결함 398). ⚠️ **정렬을 재는 것과 자르기를 재는 것은 다릅니다** — 축이
  섞인 목록에 상한이 붙어 있으면 「무엇이 먼저 잘리나」를 따로 세십시오
- **기록된 결정의 「시야」를 문장으로 확인한 것** — 331 은 이 정렬을 보고
  **축에 이름 붙이기**로 풀었습니다. 그래서 정렬은 안 건드렸고, 331 이 안
  본 **자르기**만 고쳤습니다. ⚠️ **기록된 결정을 만나면 「그 결정이 무엇을
  봤나」를 읽으십시오** — 같은 코드라도 내가 든 사실이 그 시야 밖이면
  고칠 자리이고, 안이면 건드리면 안 됩니다
- **「무엇이 남았나」가 아니라 「무엇이 없어졌나」를 본 것** — 꽉 찬 뒤의
  알림 50줄만 보면 멀쩡합니다. **조용할 때와 꽉 찼을 때를 같은 사람으로
  두 번 찍어** 종류별로 세니 한 줄로 갈렸습니다(`overdue 1` → `0`).
  ⚠️ 상한이 있는 목록은 **전후를 나란히** 놓으십시오
- **자리를 먼저 주는 고침에는 「상한을 안 넘는가」를 같이 재야 하는 것** —
  마감에 자리를 주다가 `MAX_ITEMS` 를 넘기면 상한을 둔 이유가 사라집니다.
  심어 보니 그 검사만 따로 빨개졌습니다(결함 398)
- **그리는 순서와 자르는 순서가 다를 수 있는 것** — 그리는 것은 새것부터,
  자르는 것은 **오래 지난 마감부터 남깁니다.** 안 가르면 제일 급한 것이
  잘립니다 — 심어서 그 하나만 우는 것을 확인했습니다
- **검사 한 줄이 우연히 맞은 것** — `all(n["kind"] if isinstance(n, dict)
  else n.kind == "overdue" …)` 는 dict 갈래에서 **글자를 돌려주는** 참값이라
  아무것도 안 잽니다. 지금 데이터가 dataclass 라 우연히 맞았을 뿐입니다 —
  ⚠️ **자는 단순해야 읽힙니다**(`{n.kind for n in got} == {"overdue"}`)
- **고대비가 `transparent` **테두리**까지 덮는 것** — 결함 393 은 **채움**이
  배경색으로 덮여 그림이 사라지는 경우였고, 이번은 반대로 **투명한 테두리가
  보이는 막대로 바뀌어** 갈라짐이 사라졌습니다. 알림의 안 읽음 표시는 왼쪽
  2px 막대 하나뿐인데, `forced-colors: active` 에서 읽음·안읽음·지연이 **셋 다
  `rgb(0,0,0)`** 이 됐습니다 — 바로 위 배지는 「안 읽은 알림 2」라고 그 표시를
  가리킵니다(결함 399). ⚠️ **고칠 때는 「보여야 하는 것」만 정하면 안 됩니다** —
  읽은 줄에도 `Canvas` 를 줘서 **안 보이게** 해야 갈라집니다
- **같은 파일 안에서 한 갈래는 규칙을 지키고 옆 갈래는 안 지킨 것** —
  `.nitem.urgent` 는 색 말고 **무게**도 올려서(결함 324) 고대비에서 살아남는데,
  `.nitem.fresh` 는 색 하나뿐이었습니다. ⚠️ **「뜻을 색 하나에 실었나」를
  물을 때는 옆 갈래가 어떻게 하는지 보십시오** — 지키는 이웃이 있으면 그것이
  이 제품의 규칙이라는 증거입니다(「저 혼자 예외인가」의 CSS 판)
- **앞 실행이 남긴 상태로 재서 아무것도 안 재고 있던 것** — 고친 뒤 다시
  쟀는데 `fresh` 줄이 **0개**였습니다. 앞 실행이 「다 읽음으로」를 눌러 둔
  DB 였습니다 — **재현이 아니라 확인을 할 때도 씨앗부터 새로 심으십시오**
  (결함 196 이 페르소나에서 적어 둔 것이 **고침 확인**에서 또 났습니다)
- **「상태 수식자의 본문이 색뿐인가」를 전수로 센 것** — 399 의 자를 화면
  전체에 대는 census 를 만들었더니 여럿이 걸렸는데, **옆 규칙을 읽어**
  가르니 대부분 무죄였습니다(`.aitem.weighty` 는 옆 줄이 `font-weight` 를
  대고 `.kprio--urgent` 는 배지에 「긴급」이라고 글자가 있습니다). 진짜는
  달력의 `.cell.today`·`.cell.out` 둘이었습니다(결함 400). ⚠️ **자는 규칙
  하나만 봅니다** — 옆 규칙이 대는 무게·글자를 못 보므로 「잡혔다」를
  「맞다」로 읽지 마십시오
- **눈과 귀가 **다른 검사**로 갈리는 것** — 달력의 「오늘」은 CSS(고대비)와
  이름표(`aria-label`) 양쪽에서 사라져 있었고, 고침도 둘입니다. 검사도
  `test_design_tokens.py` 와 `month.test.ts` 로 갈라 두고 **각각** 심어
  빨개지는 것을 확인했습니다 — ⚠️ 한쪽만 두면 다른 쪽이 조용히 낡습니다
- **인자를 필수로 두면 컴파일러가 옛 호출을 잡아 주는 것** — `dayAriaLabel`
  에 `today` 를 **선택**이 아니라 **필수**로 넣으니 옛 호출 자리 둘이 바로
  `TS2554` 로 잡혔습니다(결함 400). 런타임 가드보다 낫습니다(결함 372 의 방법)
- **같은 파일이 「옆 값」에는 이미 이름을 붙이고 있던 것** — 달력은 **고른
  날**에 `aria-current="date"` 를 붙이면서 **오늘**에는 아무것도 안
  붙였습니다. 관습이 없던 것이 아니라 한쪽에만 안 붙은 것입니다 —
  ⚠️ **「이 제품이 이런 걸 하나」를 물을 때는 같은 파일의 옆 값부터
  보십시오**(결함 395 와 같은 가르는 자)
- **자가 「있다」 쪽으로 틀려 그 자리가 영영 안 열릴 뻔한 것** — 색-전용
  census 가 「`forced-colors` 블록: **있음**」이라고 적었는데, `in css` 로
  **문자열만** 본 것이라 걸린 것은 **주석**이었습니다. 문법으로
  (`@media\s*\(\s*forced-colors`) 다시 세니 진짜 블록은 **셋**뿐이고 전부
  레거시 — **SPA 에는 하나도 없었습니다.** ⚠️ **fail-open 인 자는 「0건」이
  아니라 「안 봤다」를 만듭니다** — 낱말이 아니라 문법으로 재십시오
- **「가짓수보다 줄었는가」를 뿌리별로 재는 자** — BEM 수식자가 붙은 요소를
  뿌리 클래스별로 모아, 보통과 고대비에서 **모양 가짓수**(색·배경·테두리·
  무게·기울기·밑줄)를 비교하면 「갈래가 뭉개지는 자리」가 한 줄로 나옵니다.
  ⚠️ 걸린 것을 **하나씩 읽어** 갈라야 합니다 — 갈래마다 글자가 있으면
  (`.status--*` 「검토 필요」) 뭉개져도 사실이 안 없어집니다
- **일부러 고른 「모양」이 고대비가 덮는 바로 그것이던 것** — 이 저장소는
  막힌 단추를 「흐림 대신 **덜 채운 모양**」으로 정했는데(결함 236), 고대비가
  덮는 것이 정확히 그 **채움**입니다. 막힌 「등록」과 열린 「등록」이 한 자도
  안 달랐습니다. **재현했지만 결함으로 안 셌습니다** — 옆에 보이는 사유 칸이
  `● 근거 1 ○ 담당자 ○ 마감` 처럼 **글자와 기호**로 말하고 있어 사실이
  안 없어집니다(393 의 기준). ⚠️ **조건을 적어 두십시오** — 그 사유 칸이
  사라지거나 `.vh` 로 내려가면 그 순간 결함이고, 지금 그것을 잡는 자는 없습니다

- **심었더니 🔴 인데 그 빨강이 「파일이 안 열림」이었던 것** — 검사를 붙이면서
  앵커를 `anchor[:-2]` 로 잘라 `});` 대신 `}` 가 남았고,
  `ERR_INVALID_TYPESCRIPT_SYNTAX` 로 **파일이 통째로** 안 돌았습니다. 그
  상태로 **고침을 넣은 회차와 심은 회차를 둘 다** 쟀습니다 — 둘 다 아무것도
  안 재고 있었는데 「pass 2029 / fail 3」 같은 총계만 보고 넘어갈 뻔했습니다.
  위의 「템플릿 문자열 안에 백틱을 넣어…」와 같은 부류이고 **원인만 다릅니다**.
  ⚠️ **총계가 아니라 실패 줄을 읽으십시오** — 「몇 개가 빨간가」가 아니라
  **「왜 빨간가」**입니다(결함 401 회차)
- **자의 괄호가 안쪽에서 닫힌 것** — `peers\.get\([^)]*\)\?\.close\(\)` 가
  실제 코드 `peers.get(String(action.user_id))?.close()` 에서 **0곳**을
  냈습니다. `[^)]*` 가 `String(` 의 닫는 괄호에 먼저 걸립니다 — 심지도 않았는데
  가드가 빨개져서 알았습니다. ⚠️ **중첩 괄호가 있는 자리를 `[^)]*` 로 세지
  마십시오**(`[\s\S]{0,80}?` 처럼 게으른 창을 쓰거나 최상위 쉼표로 가르십시오)
- **움직이는 값을 아무 때나 캡처한 것** — 마이크 레벨은 100ms 마다 요동쳐서,
  고친 뒤 찍은 그림이 하필 **값이 0인 순간**이었습니다. 「고쳤는데 그대로」로
  읽을 뻔했습니다 — ⚠️ **채움 폭이 어느 값을 넘는 순간을 기다렸다가**
  찍으십시오(결함 402). 「내가 방금 찍은 그림인지 확인 안 한 것」의 사촌이고,
  이쪽은 **그림은 새것인데 값이 바닥**입니다
- **끊어야만 그려지는 갈래는 「재려는 것만」 끊으면 나옵니다** — 통화의
  `disconnected`·`failed` 는 여태 한 번도 안 그려져 있었습니다. WebRTC 미디어는
  **UDP**, API·시그널링 WS 는 **TCP** 이므로 `iptables` 로 UDP 만 끊으면
  (`--dport 1024:65535`) 화면은 멀쩡히 돌면서 그 갈래만 그려집니다 —
  +8초 `disconnected`, +18초 `failed`. 두드리는 내내 API 가 200 인 것을 같이
  찍어 **끊긴 것이 미디어뿐**임을 증명하십시오(결함 282 의 방법 · 결함 401)
- **「나머지」를 한 덩어리로 센 것 — 통화판** — `총원 − 연결된 사람`을 통째로
  「연결 중」이라 불러서, 카드가 「연결 실패 — 네트워크가 막고 있을 수
  있습니다」인데 **같은 화면 머리줄이 「나머지는 연결 중입니다」**였습니다
  (결함 401). 머리줄을 먼저 읽는 사람은 기다립니다 — 결함 370 이 최종
  보고서에서 만난 그 모양입니다. ⚠️ **뺄셈으로 만든 집합을 보면 그 안에 몇
  갈래가 들어 있는지 세고, 갈랐으면 낱말은 옆 칸이 쓰는 말과 맞추십시오**

- **서버 상태 값 하나를 「그 일이 일어나고 있다」로 읽은 것** — 트랙의
  `status = "recording"` 은 녹음 화면을 **열기만 해도** 붙습니다
  (`join_track`). 「참가했다」는 뜻인데 통화 화면이 그것만 보고 초록
  「녹음 중입니다」를 띄웠습니다 — 같은 순간 로비는 팀에게 「2분째 녹음이
  한 조각도 안 왔습니다」라고 말하고 있었습니다(결함 404). ⚠️ **상태 칸이
  「시작됐다」를 뜻하는지 「진행 중이다」를 뜻하는지 세어 보십시오** — 그
  차이를 아는 값(여기서는 `chunk_count`·`silent_ms`)이 **같은 응답에 이미
  실려 있는 경우가 많습니다**
- **판단은 옆 화면이 이미 하고 있었던 것** — 로비의 `verdictOf` 가 그 둘로
  정확히 갈라 두고 문턱(`WARN_GAP_MS`)까지 정해 놨습니다. 베끼지 않고
  **불렀습니다**. ⚠️ **문장은 갈라 두십시오** — 로비는 남에게 하는 말
  (「그 폰에서」)이고 통화 화면은 자기 화면(「녹음 화면을 확인하세요」)
  입니다(결함 344·358)
- **화면의 컨트롤을 `button` 으로만 센 것 — 이번엔 「안 보이는 단계」** —
  녹음 화면의 시작이 영영 안 열리길래 결함으로 적을 뻔했는데,
  **`#permission`(「마이크 권한 허용」)** 을 안 누른 것이었습니다. 브라우저
  권한을 컨텍스트에서 줘도 **제품은 사람이 그 단추를 눌러야** 마이크를
  엽니다. ⚠️ **컨트롤을 셀 때는 `button, a.btn, input, select` 를 전부 세고,
  「막혀 있다」를 적기 전에 그 화면의 **앞 단계 단추**를 찾으십시오**
- **검사 씨앗이 화면과 같은 좁은 모양이라 그 결함이 구조적으로 안 잡힌 것** —
  `describeMyCapture` 검사 다섯이 전부 `{ status }` 만 넘기고 있었습니다.
  **화면이 못 보던 칸이 검사에도 없었습니다.** 씨앗을 서버 응답과 같은
  모양(`TrackHealth`)으로 바꾸니 그때서야 잴 수 있었습니다 — 결함 288 의
  「씨앗이 실기가 만드는 값인가」의 **모양**판입니다

- **주석이 「세 국면」이라는데 코드는 두 갈래였던 것** — 홈의 `pending`
  갈래가 그랬습니다. 둘이 녹음을 마치고 커버리지 100% 가 찍힌 회의에서
  홈은 「동의를 받고 녹음을 시작합니다」였고 로비는 「1명이 참가하지 않아
  회의가 끝나지 않습니다」였습니다(결함 405). ⚠️ **주석이 갈래 수를 말하면
  코드의 `return` 을 세어 보십시오** — 결함 328·341 이 어휘에서 겪은 것의
  분기판입니다
- **값은 이미 응답에 와 있는데 `@lib` 타입만 그 칸을 몰랐던 것** — 서버는
  `coverage` 를 언제나 보내고 SPA 홈은 그걸 「커버리지 100%」로 **그리고
  있었습니다.** 판단 함수의 입력 타입에만 없어서 갈래를 못 갈랐습니다.
  ⚠️ **「그 판단에 필요한 값이 없다」고 적기 전에 응답을 찍어 보십시오**
- **기능을 한 번 쓰고 나머지 화면을 다시 여는 규칙 — 이번엔 「녹음」** —
  결함 287 이 「회의 일정 잡기」에서 적어 둔 그것이 녹음에서 그대로
  났습니다. ⚠️ **씨앗이 결과만 심는 기능은 그 과정을 실제로 밟아야
  중간 상태가 처음 그려집니다** — 이 저장소는 둘이 동시에 녹음한 적이
  한 번도 없었습니다(결함 279 가 적어 둔 축)

- **설명 문장을 `title=` 하나에만 담은 것 — 이번엔 다섯 줄** — SPA 홈의
  「다음에 할 일」이 그랬습니다. 본문에 0곳 · `Why` 0개 · `aria-label`
  없음 · Tab 으로 서도 안 나옴 — **마우스에만** 있었습니다(결함 406).
  결함 394(`title` 이 키보드보다 많이 앎)·395 에 이은 **세 번째**입니다.
  ⚠️ **취향과 결함을 가른 것은 「문서에 적힌 처방」이었습니다** —
  `docs/22` 처방 ③ 이 「문장을 지우지 않고 `?` 한 겹 아래로(`Why`)」라고
  정해 두었고 그 `Why` 는 SPA 화면 넷에서 **열 곳** 쓰이는데 홈만
  예외였습니다. **낱개로 판단하기 전에 저장소가 그 자리에 대해 뭘
  적어 뒀는지 찾으십시오**
- **`title=` 를 전수로 세면 부류가 갈립니다** — 화면 제목 prop · `title` +
  `aria-label` 짝 · 본문에 같은 값이 있는 것 · **혼자 있는 것**. 앞 셋은
  무죄이고 마지막만 결함입니다. ⚠️ 이 저장소는 `Chain.tsx` 에서 이미 그
  짝을 쓰고 있었습니다 — **함정을 아는 코드가 같은 저장소에 있는지
  보십시오**(결함 337 의 「주석이 금지한 사용법이 같은 저장소 안에 있는지」)

- **제품이 글로 하는 약속을 재 본 적이 없던 것 — 이번엔 초록** — 통화
  화면은 「녹음은 통화와 **별개로** 사람마다 따로 남습니다」라고 적어 두고
  있는데 아무도 안 쟀습니다. 둘이 통화 + 녹음을 켠 상태에서 UDP 만 끊으니
  통화는 36초 동안 죽었지만 **청크는 한 번도 안 멈췄고**(3→10 · 2→9) 끝나서
  커버리지 100% · 공백 0 이었습니다. ⚠️ **화면이 약속하는 문장을 모아
  하나씩 재 보십시오** — 「할 수 있다」(결함 313)·「할 수 없다」(결함 343)에
  이은 **세 번째 방향**입니다
- **앞 실험이 남긴 DB 로 다음 실험을 시작한 것 — 이번 세션 세 번째** —
  트랙이 이미 `completed` 라 재참가가 막혔고 「시작이 영영 안 열린다」로
  읽을 뻔했습니다. ⚠️ **막힌 이유(blocker 목록)를 찍어 두었기에 갈렸습니다** —
  안 찍었으면 없는 결함을 만들었을 것입니다
- **고정 대기로 준비 상태를 기다린 것** — 「시작 가능」이 되는 데 4초가
  넘을 때가 있어 3.5초 고정 대기는 흔들립니다. ⚠️ **상태를 폴링**하십시오
- **못 재는 것은 「초록」이 아니라 「못 쟀다」입니다 — 이유까지** —
  「강제 종료 → 처리 → 검토」를 진짜 오디오로 밟으려면 `process_meeting` 이
  `Transcriber`(ASR)·`MeetingAnalyzer`(LLM)를 받아야 하는데 이 저장소는
  비용 0원 · 외부 생성형 API 금지라 그 둘이 없습니다. **가짜를 넣으면
  제품이 하지 않는 일을 재는 것**이라 안 했습니다

- **어휘 × 씨앗 표를 「화면이 보내는 범위」로 만든 것** — 달력을 한 달치로
  물으면 「이 달에 없는 것」과 「아예 없는 것」이 안 갈립니다. **십 년
  범위로 한 번** 부르면 갈립니다(결함 408). ⚠️ 그리고 쿼리 이름을 지어내
  (`start`/`end` — 실제는 `since`/`until`) **422 를 「0건」으로** 읽을
  뻔했습니다 — **재기 전에 상태 코드를 찍으십시오**
- **읽는 곳은 여럿인데 「쓰는 곳」이 0곳인 **칸**** — `tasks.start_date`(읽기 1 ·
  쓰기 0)와 `projects.deadline`(읽기 3 · 쓰기 0)이 그랬고, 그래서 달력의
  두 종류가 한 번도 안 그려졌습니다(결함 408). ⚠️ **결함 382 의 자는
  표 단위라 이 축을 구조적으로 못 봅니다** — 행은 멀쩡히 만들어지고 그
  칸만 안 채워집니다. 382 자신이 「두 자는 서로를 못 본다」고 적어 뒀습니다.
  **모델의 칸마다 「쓰는 코드가 있는가」를 세고, 그 칸에 기대는 요구가
  대조표에서 ✅ 인지 보십시오**
- **어떤 칸이 비었는지 궁금하면 그 칸을 설명하는 주석부터 찾으십시오** —
  408 의 정답은 `db/vocab.py` 에 이미 적혀 있었습니다(「달력의
  `project_due` 는 한 번도 그려진 적이 없습니다」). 같은 저장소가 두 곳에서
  다른 말을 하고 있었습니다(결함 341 의 모양)

- **읽는 쪽만 팀 달력으로 옮기고 쓰는 쪽을 둔 것** — `datetime-local` 은
  **시간대 없는 글자**인데 화면이 `new Date(when).toISOString()` 로
  바꿨습니다. 그건 **브라우저 달력**입니다 — 같은 「10:00」이 팀 달력에
  Seoul 10:00 · UTC 19:00 · New York 23:00 으로 나갔습니다(결함 409).
  결함 246 의 가드는 `getMonth()` 류 **낱말**을, 295 는 `slice(0,10)` 을
  막는데 이 길은 둘 다 안 걸립니다 — **넷째 길**입니다.
  ⚠️ **시간대를 갈라 재야** 「내 환경 탓」과 「제품 결함」이 갈립니다.
  ⚠️ 그리고 **자정을 넘는 값으로** 재십시오 — `10:00` 만 재면 변환을
  통째로 지워도 서울에서는 같은 답이 나옵니다
- **주석이 「하는 일」만 적고 근거가 없으면 기록된 결정이 아닙니다** —
  409 의 옛 주석은 「브라우저의 시간대로 해석해 순간으로 바꿔 보냅니다」
  였고, 같은 줄의 「9시간 어긋납니다」가 **브라우저가 서울에 있다**는
  가정을 드러냅니다. ⚠️ **「왜 그렇게 했나」가 없으면 안 물어본 질문입니다**
- **고침을 확인하면서 앞 실행이 남긴 행을 읽은 것 — 이번 세션 네 번째** —
  같은 제목으로 다시 만들어 `find(x => x.title === t)` 가 옛 행을 집었고,
  「보낸 본문은 고쳐졌는데 저장은 그대로」라는 이상한 표를 봤습니다.
  ⚠️ **고침을 확인할 때도 씨앗부터 다시 심으십시오**

- **인원 수도 「상태」인 것** — 씨앗은 언제나 팀원 셋이라 「셋에서만 맞는
  것」이 조용히 통과합니다. 가입(`/api/auth/signup`)과 초대 코드
  (`/api/projects/join`)로 **20명을 실기 경로로** 만들어 보니, 막 들어와
  활동이 0인 열일곱에게 최종 보고서가 「측정하지 못했습니다」라고 적고
  있었습니다 — 같은 순간 화면은 `0%` 였습니다(결함 410). ⚠️ **이름은
  번호 순과 이름 순이 갈라지도록** 고르십시오
- **같은 판단이 파이썬·TS 두 벌이라 합칠 수 없을 때** — `가드를 넓히는
  것보다 사본을 없애는 것이 낫다`(363)를 못 쓰는 자리입니다. 결함 345 의
  방법으로 **공용 사례 파일**을 두 검사가 같이 읽게 하십시오. ⚠️ 그 파일에
  **자기 검사 둘**을 붙이십시오 — 양쪽 답이 다 나오는가(347 의 빈손 통과),
  그리고 **옛 규칙과 새 규칙이 다른 답을 내는 줄이 있는가**(없으면 옛
  코드도 통과합니다)
- **응답에 없는 필드 이름을 지어내 「초록」을 받은 것** — `/contributions`
  응답에는 `name` 이 **없습니다**(`user_id` 뿐). `m.name` 으로 읽으니
  스무 명이 전부 빈 글자였고 「이름 순인가?」가 `true` 로 나왔습니다 —
  **아무것도 안 재고 초록**입니다. ⚠️ **빈 값이 여럿이면 자를 먼저
  의심하십시오**

- **입력칸 이름을 지어내 「자리가 없다」로 적을 뻔한 것** — 기여도 확정의
  사유 칸을 `aria-label*="사유"` 로 찾아 **0개**가 나왔고, 대표 실패 ③
  (「할 일을 알려 주고 그 일을 할 자리를 안 줌」)으로 적을 뻔했습니다.
  실제 이름은 **「… 조정 이유」**이고 입력칸은 40개(값 20 + 이유 20)가
  멀쩡히 있었습니다. ⚠️ **「0개」가 나오면 자를 먼저 의심하고, 그 화면의
  입력칸을 **통째로 찍어** 보십시오** — 「메시지 상자의 이름을 지어낸 것」의
  입력칸판입니다
- **캡처를 작게 보고 색을 잘못 읽은 것** — 로비의 막힌 「녹음 화면으로」가
  꽉 찬 primary 로 보여 결함 373 의 재발로 적을 뻔했는데, 계산값을 재니
  옅은 `btn--unmet`(밝은 `rgb(236,236,249)`)이었습니다. ⚠️ **눈으로 본
  것과 잰 값이 다르면 잘라서 다시 찍으십시오** — 「캡처가 보여 주는 것이
  캡처가 만든 것일 수 있습니다」의 축소판입니다
- **「저 혼자 예외인가」가 아무 답도 안 낼 때** — 사람 목록 일곱을 세니
  이름 순 셋 · 가입 번호 순 넷이었습니다(3 대 4). 이럴 때는 **고르지 말고**
  세어 둔 표와 **조건**을 적으십시오 — 「사람 목록은 이름 순」이 어딘가에
  적히거나 공용 함수가 생기면 그때 결함입니다

- **중복을 없애려던 검사가 「엉뚱한 사람을 부르는 길」이 된 것** — 멘션을
  뽑는 루프가 `if tail.startswith(name) and name not in found: … break` 였습니다.
  `break` 가 **중복 판정 안**에 있어서, 같은 사람을 두 번 부르면 가장 긴
  후보가 이미 `found` 에 있어 조건이 거짓이 되고 루프가 **더 짧은 이름으로
  흘러내렸습니다.** 팀에 `한동희` 와 `한동` 이 같이 있을 때 `@한동희` 를 두 번
  쓴 글이 **한동을 부른 것**으로 기록되고 알림까지 갔습니다(결함 411).
  그 모듈 머리말은 「아무나 아무에게나 알림을 쏠 수 있게 됩니다 … 그래서
  판정은 **본문에서만** 나옵니다」라며 막으려던 바로 그 일입니다.
  ⚠️ **`break` 나 `continue` 가 「무엇을 고를까」가 아니라 「이미 골랐나」에
  걸려 있으면 의심하십시오** — 고르는 조건과 거르는 조건은 다른 일입니다
- **주석이 「이 배치를 지원한다」고 단언하면 그 배치를 만들어 보십시오** —
  `mentions.py` 머리말은 「`윤식` 과 `윤식은` 이 **둘 다 있으면 긴 쪽이
  이깁니다**」라고 적어 뒀는데, 그 「둘 다 있는 팀」이 정확히 결함 411 의
  전제였습니다. 씨앗의 이름 스물은 전부 세 글자라 **접두 관계가 한 번도
  없었습니다** — 어휘·경계값·개수·시각에 이은 또 하나의 축입니다:
  **이름끼리의 관계**(같은 이름 → 결함 345 · 한쪽이 다른 쪽의 앞부분 → 411)
- **자동완성이 없다는 사실 자체가 다음 관점인 것** — 채팅에 멘션 자동완성이
  없다는 것을 세고(컨트롤 31개 · `datalist` 0 · `@한` 을 쳐도 138→138)
  「없는 기능」으로 넘길 뻔했습니다. 없으니 **사람이 손으로 칩니다** —
  손으로 치면 같은 이름을 여러 번 쓰고, 그 자리에 결함이 있었습니다.
  ⚠️ **「이 기능이 없다」를 적었으면 「없어서 사람이 대신 하는 일」을
  세십시오**

- **접근 이름을 「`title` 이 있는가」로 잰 것** — 설정 탭의 묶음 이름이
  `title=` 에만 있었고 주석은 「`title` 로 남겨 **낭독기와 마우스에는**
  전해집니다」라고 단언했습니다. 링크에 글자가 있으면 접근 이름은 **그
  글자**에서 나오고 `title` 은 이름으로 **안 쓰입니다** — 트리를 찍으니
  `.tabs__group` 이 `none ""` 이었고 「내 설정」은 본문에 0회였습니다
  (결함 412 · 394·395·406 에 이은 네 번째). ⚠️ **접근성 트리는
  `snapshot({ root, interestingOnly: false })` 로 찍으십시오** — 기본값은
  가지를 쳐서 `nav` 아래가 통째로 비어 나옵니다. 「없다」로 읽을 뻔했습니다
- **`title=` 을 세는 자는 부류부터 갈라야 하는 것** — JSX 의 `title` 은
  대개 **컴포넌트 prop**(`<AppShell title=…>`)이고 HTML 속성이 아닙니다.
  17곳 중 아홉이 그것이고 여섯은 `aria-label`·`aria-describedby` 와 짝을
  이룹니다 — 안 가르면 진짜 하나가 묻힙니다(결함 412)
- **고칠 때 「글자를 더하지 않는 길」을 찾은 것** — 바로 위 주석이 「탭
  줄에 머리말을 넣으면 누를 수 없는 글자가 탭처럼 보입니다」라고 이유까지
  적어 둔 기록된 결정이었습니다. `role="group" aria-label` 은 그 결정을
  지키면서 낭독기에만 이름을 줍니다 — 이 저장소가 `activity.tsx` 의
  거르개에서 **이미 쓰는 방법**입니다(결함 322 의 「있는 것을 찾아보고
  만드십시오」)

- **`<a>` 는 `href` 가 없으면 「안 눌린다」가 아니라 「닿을 수조차 없다」인
  것** — 레거시 셸의 막힌 탭 셋이 `href` 도 `tabindex` 도 없어서 **`focus()`
  를 직접 불러도** 초점이 안 잡혔고, 막힌 이유는 `title` 에만 있어 본문
  글자에 0회였습니다(결함 413). 이 저장소가 `aria-disabled` 를 쓰는 이유가
  「초점을 받고 사유를 읽히게 하려고」인데 그 약속이 셋에게만 거짓이었습니다.
  ⚠️ **막힌 것을 봤으면 `focus()` 로 초점이 잡히는지부터 재십시오** —
  Tab 을 세는 것보다 빠르고, 「안 눌린다」와 「있는지도 모른다」를 가릅니다
- **가드를 새로 만들기 전에 이미 있는지 세는 것 — 그리고 걷는 자리만
  넓히는 것** — 결함 219 의 가드가 이 요구를 **이미 재고 있었고**
  `webapp/src` 한 뿌리만 걷고 있었습니다(결함 286). 새로 만들었으면 같은
  요구를 재는 자가 둘이 됩니다. ⚠️ 넓힐 때 **「한 뿌리에서 한 건도 못 보면
  실패」**를 같이 넣으십시오 — 안 넣으면 다음에 그 뿌리가 비어도 조용합니다
- **`replace(…, 1)` 이 남의 코드를 먼저 문 것 — 두 번째** — 가드의 walker
  를 고치려고 첫 occurrence 를 바꿨는데 그것이 **다른 검사(결함 218)의
  walker** 였습니다. 검사 둘이 한꺼번에 빨개져서 알았습니다 —
  ⚠️ **바꾼 뒤 `git diff` 로 내가 고치려던 줄만 바뀌었는지 보십시오**,
  그리고 같은 모양이 파일에 몇 개인지 먼저 세십시오

- **표의 「행 수」를 전수로 세면 「아무도 안 본 화면」이 나오는 것** — DB 의
  표를 전부 세니 0행이 아홉이었고, 여덟은 기록된 결정이거나 이미 적힌
  결함이었고 **하나(`message_reactions`)만 아무도 안 본 것**이었습니다.
  눌러 보니 결함 414 가 나왔습니다. ⚠️ 어휘(값 집합) · 상수(경계값) ·
  개수 · 시각에 이은 **다섯 번째 표**입니다 — 그리고 0행마다 **왜 0인지**를
  적어 두면 다음 사람이 그 목록에서 새것만 고를 수 있습니다
- **「부르기는 부른다」와 「응답을 쓴다」는 다른 것** — 채팅은
  `/api/chat/reactions` 를 제대로 부르고 이름표까지 꺼내 쓰는데, **고를
  것의 집합과 순서**는 자기 배열로 정했습니다(결함 414). 그 엔드포인트는
  「화면이 이 표를 자기 안에 두면 안 됩니다」라고 적으면서 순서까지 못
  박아 두는데 둘 다 버려졌습니다. 결함 315 의 모양이고 **라우트 census 는
  초록**입니다 — ⚠️ **응답의 칸마다 「화면이 그것을 쓰는가」를 세십시오**
- **`AGENTS.md` 자신의 단언이 반만 맞았던 것** — 「`REACTION_LABEL` 은
  화면에 사본이 없어서 갈라질 수가 없습니다」라고 적어 뒀는데, **이름표**는
  사본이 없고 **값 집합**은 `REACTION_MARKS` 라는 사본이 있었습니다.
  결함 328 의 「**같은 표의 옆 칸은 따로 재야 합니다**」가 이 문서 자신의
  문장에서 난 것입니다 — ⚠️ 「사본이 없다」를 적을 때는 **이름표·값 집합·
  순서**를 따로 세십시오
- **주석이 「못 받아도 화면은 돕니다」라고 하면 무엇으로 도는지 물을 것** —
  반응 목록을 못 받아도 화면이 돌던 이유는 **화면이 집합을 지어내고
  있었기** 때문입니다. 같은 파일의 채널 종류는 「못 받으면 **고르는 칸을
  안 그립니다**」라고 정반대로 적혀 있었습니다(결함 360) — 옆 갈래가
  이미 답을 적어 두고 있었습니다

- **뿌리는 몸통에 「보는 사람마다 다른 칸」이 실려 나간 것** — `MessageOut`
  은 **부른 사람 기준**으로 만들어지는데(`my_reaction` = 「내가 단 반응」),
  소켓으로 뿌리는 네 자리 중 **반응 갈래만** 손으로 그 칸을 지우고 있었
  습니다. 글쓴이가 자기 글에 반응을 달고 그 글을 고치면, 지켜보던 사람의
  칩이 `aria-pressed="true"` · 「내가 누름」으로 바뀌었습니다 — 사람의
  행동을 지어내 낭독기에 읽어 준 것입니다(결함 415). ⚠️ **화면 코드를
  읽어서는 구조적으로 안 보입니다** — 화면은 `incoming.my_reaction ?? mine`
  으로 맞게 짜여 있었고 전제가 서버에서만 깨져 있었습니다. **두 세션을
  동시에 띄워야** 보입니다
- **주석이 해악을 정확히 적어 둔 자리를 보면 「옆 갈래도 그렇게 하나」를
  세십시오** — 반응 갈래의 주석이 이 해악을 그대로 적고 손으로 지우고
  있었는데 같은 파일의 `edit`·`delete` 는 그 문장을 안 읽었습니다.
  ⚠️ **손으로 지우는 자리를 보면 그것이 몇 곳인지 세고, 한 곳을 지나게
  고치십시오**(결함 363) — 갈래가 하나 더 생겨도 손으로 다시 적을 일이
  없어집니다
- **응답 스키마에 「내」로 시작하는 칸이 생기면 뿌릴 때 뺄지 정할 것** —
  손으로 고른 목록은 만들 때 있던 것만 지킵니다(결함 329). `my_*` 로
  보이는 새 칸이 생기면 가드가 「넣을지 정하십시오」로 웁니다

- **소켓을 여는 화면을 전수로 안 센 것** — 이 저장소의 WebSocket 은
  둘(통화·채팅)인데 **통화만** `onclose` 를 달고 있었습니다. 채팅은 소켓이
  끊겨도 아무 말이 없어, 서버를 다시 띄운 뒤 오는 글이 **영영 안 오는데**
  화면은 살아 있는 대화 그대로였습니다(결함 416). ⚠️ 통화 쪽에는 그
  해악까지 적혀 있었습니다 — 「⚠️ 조용히 닫히면 화면은 통화 중인 줄
  안다.」 **「저 혼자 예외인가」가 1/2 로 유죄**를 냈고, 지키는 쪽의 주석이
  증거였습니다
- **「안 붙는 것」과 「붙었다가 끊긴 것」은 다른 갈래인 것** — 채팅의
  기록된 결정(「실패를 화면에 적지 않습니다 — 사람이 할 수 있는 것이
  없는데 빨간 줄만 남습니다」)은 **안 붙는** 갈래에만 참입니다. 그때 화면은
  방금 HTTP 로 읽어 와 최신이고, **끊기면 그때부터 낡습니다**(그리고
  새로고침이라는 할 일이 생깁니다). ⚠️ **결정문의 전제를 문장으로 읽고
  내가 든 갈래를 덮는지 보십시오** — 안 덮으면 뒤집는 것이 아니라 숙제입니다
- **닫히는 갈래가 둘이면 가르는 것** — 우리가 닫은 것(채널 이동·화면
  떠남)과 저쪽이 끊은 것. 안 가르면 정상 조작마다 경고가 떠서 **그 말이
  닳습니다.** 판단은 `@lib` 에 두어(`streamClosedNote(onPurpose)`) 검사가
  닿게 하고, 「가르는가」를 따로 심어 재십시오

- **문장은 자리를 말하는데 링크가 딴 데로 데려간 것** — 「**디자인** 채널에서
  나를 불렀습니다」를 눌렀더니 `#공지` 가 열리고 부른 글은 화면에
  없었습니다(결함 417). `Notice.channel_id` 칸은 **처음부터 있었고 채우는
  곳이 0곳**이었습니다(결함 312 의 모양). ⚠️ **채널이 하나뿐이면 이
  부류는 구조적으로 안 보입니다** — 첫 채널이 언제나 정답이기 때문입니다
  (결함 355). 앞 회차에 채널을 둘로 만든 것이 이 회차를 열었습니다
- **앞 결함이 「버린다」고 적어 두고 안 고친 자리를 세십시오** — 결함 397 의
  기록이 「`hrefFor` 는 회의면 번호를 들고 가는데 **메시지 갈래만
  `message_id` 를 버리고**」라고 적고 **문장만** 고쳤습니다. 기록된 결정이
  아니라 **적어만 두고 간 숙제**입니다 — ⚠️ 결함 기록에서 「버린다」·
  「안 씁니다」·「0곳입니다」로 끝나는 문장을 찾으면 그것이 다음 회차입니다
- **「칸이 있는가」와 「채우는가」와 「쓰는가」는 셋 다 다른 것** — 이번에
  깨져 있던 것은 **채우는 곳**과 **응답 스키마**와 **링크** 셋이었고, 어느
  하나만 빠져도 값이 사람에게 안 갑니다. 가드도 셋으로 나눠 각각
  심으십시오. ⚠️ 타입에 **필수**로 넣으면 옛 호출 자리를 컴파일러가
  잡아 줍니다(결함 372 의 방법 — `TS2322` 로 바로 나왔습니다)
- **예외 표의 「사유」도 낡는 것 — 사유 자체가 틀릴 수도 있습니다** —
  라우트 예외가 `/mentions` 를 「멘션 자동완성」이라고 적어 뒀는데 그
  갈래는 `{"mention_total": N}` **숫자 하나**를 줍니다. 이었어도 자동완성은
  못 만듭니다. 결함 306 이 「예외가 낡는 것도 같이 재십시오」라고 적어 둔
  자리에서 **사유가 처음부터 거짓**이었습니다 — ⚠️ 예외를 읽을 때는
  **그 갈래가 실제로 무엇을 돌려주는지** 열어 보십시오

- **라우트 census 가 초록인 자리를 두 번 뚫렸으면 축을 늘리는 것** —
  결함 414(응답의 **집합·순서**를 안 씀)와 417(응답의 **칸**이 비어 있음)이
  둘 다 「부르는가」만 보는 자에는 초록이었습니다. 다음 축은 **응답 모델의
  칸마다 읽는 화면이 있는가** 입니다 — 모델 49개·칸 270개 중 20개가
  0곳이었고, 하나씩 열어 보니 전부 사유가 있었습니다(결함 0건). 그 표를
  가드로 굳혀 **칸이 늘고 안 이으면** 빨개지게 했습니다
  (`test_response_fields.py`). ⚠️ 어휘·상수·개수·시각·표 행 수에 이은
  **여섯 번째 census 축**입니다
- **자를 만들면 「예외의 사유」가 다음 회차의 일감이 되는 것** — 스무 개
  중 일곱은 열어 볼 시간이 없어 **「⚠️ 안 쟀습니다」로 표에 적었습니다.**
  숨기고 그럴듯한 사유를 지어내면 다음 사람이 그것을 믿습니다 — 결함 417
  회차에 라우트 예외의 사유가 **처음부터 거짓**이었던 것이 그 결과입니다

- **근거를 세어 놓고 볼 문을 안 준 것 — 상자의 「모양」이 원인이던 것** —
  칸반 카드가 `evidence_utterance_ids` 를 손에 들고 「근거 발화 1건」이라고
  개수만 적었고, 원문으로 가는 컨트롤은 0개였습니다(결함 418).
  `lib/review/evidence.ts` 머리말이 「이 제품의 **대표 주장**은 숫자에서
  출발해 몇 번째 발언까지 거슬러 올라갈 수 있다는 것 … 오랫동안 화면은
  `근거 #5` 라고 **적기만** 했습니다」라고 그 해악을 적어 두고 있었습니다.
  ⚠️ **상자는 원래 있었습니다** — `mountEvidence(apiBase, meetingId)` 라
  **회의를 붙일 때** 정하는 모양이라, 카드마다 회의가 다른 화면에서는
  **쓸 수가 없었습니다.** 회의를 **부를 때** 받게 바꾸니 두 화면이 같은
  상자를 씁니다
- **「부르는 곳이 하나」를 보면 「둘째 자리가 왜 못 쓰는지」를 보십시오** —
  「왜 한 곳인지가 적혀 있는가」(결함 306)가 첫 질문이고, 안 적혀 있으면
  다음 질문은 **함수의 모양이 둘째 자리를 막고 있는가** 입니다. 막고
  있으면 그것이 실패 ① 의 진짜 원인이고, 고침은 배선이 아니라 **모양**입니다
- **「안 쟀습니다」로 적어 둔 것이 다음 회차를 집어 주는 것** — 응답 칸
  census 의 예외 일곱을 그렇게 적어 뒀고, 다음 회차에 하나씩 열어 여섯은
  사유가 명확했고 **하나에서 결함이 나왔습니다.** 그럴듯한 사유를
  지어냈다면 일곱 다 조용히 닫혔을 것입니다

- **못 찾으면 그 자리를 통째로 비운 것** — 답글의 인용 블록이
  `messages.find(…)` 로 원글을 찾고 **못 찾으면 안 그렸습니다.** 서버는
  최신 50개만 주므로 앞쪽 글에 단 답글은 **평범한 글처럼** 보였고, 답글인
  줄도 모르니 「무엇에 단 거지」를 물을 수조차 없었습니다(결함 419).
  ⚠️ 그 자리의 주석이 해악을 정확히 적어 두고 있었습니다 — 못 찾는
  갈래만 비어 있었습니다. **`find(...)` 뒤에 `!== undefined &&` 가 오면
  「못 찾았을 때 무엇을 그리나」를 물으십시오**
- **앞 결함이 문을 만들어도 「문 너머를 가리키는 것」은 따로인 것** —
  결함 315 가 「이전 대화 더 보기」를 붙였는데, 그 너머의 원글을 가리키는
  답글은 여전히 조용했습니다. ⚠️ **고침이 열어 준 상태를 다시 걸으십시오**
  (「고친 것이 다음 관점을 열어 준다」의 네 번째 사례)
- **없는 기능을 만들지 않고 고치는 법 — 이미 있는 문을 가리키기** —
  원글을 따로 받아 오는 갈래를 새로 파는 대신 화면에 **이미 있는 단추**를
  가리켰습니다. ⚠️ 그러면 가드가 「그 단추가 실제로 있는가」까지 봐야
  합니다 — 문장 속 「」 안의 글자를 뽑아 화면에서 찾게 하면, 단추 글자가
  바뀔 때 같이 빨개집니다(결함 310 의 「재는 자가 낡은 것」을 피하는 쪽)

- **「N건」이라고 적어 놓고 그 번호가 화면에 와 있는지 안 센 것** — 검토
  화면의 회의록이 「배포 방식을 정하지 못했습니다 · **근거 발화 1건**」이라고
  적고 있었는데, 그 1건은 **발화 #5** 이고 **같은 화면 아래** 후보 카드는
  바로 그 #5 를 `근거 #5` 라는 **단추**로 열고 있었습니다(결함 420). 서버
  `UnresolvedIssueOut` 의 docstring 은 번호를 싣는 이유를 「근거 없이 "이게
  미해결입니다" 라고만 하면 사람은 확인할 방법이 없다」로 적어 뒀는데,
  `@lib` 의 `describeIssue` 가 그 번호를 **버리고** 개수 하나만 돌려주고
  있었습니다. ⚠️ **개수를 보면 「번호가 화면까지 오는가」부터 세십시오** —
  오는데 문이 없으면 실패 ③ 이고, 안 오면 서버 칸을 늘려야 하는 **다른
  축**입니다(결함 312). 안 가르면 고칠 수 없는 것을 결함으로 적습니다.
  ⚠️ 가른 자는 **형제 뷰**였습니다 — 같은 패키지 옆 파일의 `FindingView` 는
  처음부터 `evidence: number[]` 를 들고 두 뿌리가 그것을 엽니다. 결함 328 의
  「같은 표의 옆 칸은 따로 재야 합니다」의 **모듈판**입니다
- **가드를 만들면서 그 가드가 막으려던 함정에 그대로 빠진 것** — 「근거를
  세었으면 문도 준다」를 **파일 단위**로 물었습니다. 미해결 사안의 문을
  통째로 걷어내도 **같은 파일 아래 관찰 줄**이 `openEvidence(…, view.evidence,
  …)` 를 부르므로 초록이었습니다(결함 420 · 결함 392 회차의 「파일 어딘가 한
  곳만 본 것」). 심어 보고 알았습니다 — ⚠️ **판정 단위를 「그 값을 그리는
  자리」로 좁히십시오.** 여기서는 `issueViews(...)` 에서 시작해 그 줄을
  그리는 `.map(...)` 의 **몸통만** 잘라 내고, 그 안에서 **그 줄의
  이름**(`param.evidence`)이 문에 들어가는지 봅니다
- **자에 「못 읽은 것이 있으면 실패」를 넣어 두면 눈을 안 감는 것** —
  SPA 의 첫 `issueViews(...)` 는 여닫이 머리말의 `….length` 라 그리는
  자리가 아닙니다. 그것만 집었더니 그리는 자리를 못 찾았는데, **「못 읽은
  것이 있으면 실패」**를 넣어 뒀기에 「0건」이 아니라 **「못 읽었다」**로
  잡혔습니다(결함 420). ⚠️ 그리고 걷는 자리도 좁혔습니다 — `src` 통째로
  걸면 `@lib` 자신이 잡힙니다(그 파일도 자기 안에서 그 함수를 부릅니다).
  **문을 낼 수 있는 자리(화면)만** 걷으십시오
- **칩으로 바꾸면 축 이름이 `aria-label` 로 내려가는 것** — 「· 근거 1건」을
  `EvidenceChips` 로 바꿨더니 눈에는 맨 숫자 `#5` 뿐이고 「근거」는
  `aria-label` 에만 남았습니다(결함 395 의 모양이 **내 고침**에서).
  `· 근거 #5` 로 **축 이름을 눈에도 남겼습니다** — 렌더해서 잡았습니다

- **요구를 만족시키는 길이 여럿인데 「그 길을 안 썼다」를 결함으로 읽을
  뻔한 것** — 채널 60개짜리 채팅에서 글 쓰는 칸까지 **Tab 65번**인 것을
  보고 「건너뛰기 링크가 없어서」라고 읽었습니다. 세니 레거시 화면 열셋에
  `a.skip` 이 **0개**(SPA·정적 둘은 1개씩)라 「한쪽 뿌리만」의 전형처럼
  보였고, `app.css` 에는 `.skip` 규칙이 **아무도 안 쓰는 채로** 있었습니다.
  ⚠️ 그런데 **Tab 순서를 재니 반대**였습니다 — 레거시는 `Tab 1 = 본문 ·
  67 = #nav · 72 = #tabs` 이고 SPA 는 `Tab 1 = 건너뛰기 · 2~6 = 레일 ·
  7 = 본문` 입니다. WCAG 2.4.1 이 요구하는 것은 「되풀이되는 덩어리를
  **지나갈 수 있을 것**」이고 링크는 그 방법 **하나**일 뿐이라, 내비를 본문
  **뒤**에 두는 것도 같은 요구를 만족합니다. 안 재고 고쳤으면 **없는 결함을
  만들면서 모든 키보드 사용자에게 Tab 한 번을 더 물릴 뻔**했습니다
  (결함 421 · 재현 안 됨). 결함 295 의 「막는 길을 하나만 막은 것」의
  **거울상**입니다 — 저기는 길이 여럿인데 하나만 막았고, 이쪽은 길이
  여럿인데 **하나만 인정**했습니다
- **한 자리에 두 축이 섞여 있던 것** — 같은 census(`a.skip` 이 있는가)에
  「건너뛰기 링크」(재현 안 됨)와 「`main` 표지」(결함 421)가 같이 잡혔습니다.
  표지 쪽은 진짜였습니다 — 레거시 홈은 컨트롤 스물 중 **열여섯이 어느
  표지에도 안 들어** 있었고 낭독기의 표지 목록에 본문이 없었습니다.
  ⚠️ **세어서 갈라야** 없는 결함을 안 만들고 진짜를 그 뒤에 안 숨깁니다.
  표지를 세는 자: `main, nav, header, footer, aside, [role=…]` 를 모아
  **각 표지 안의 컨트롤 수**와 **화면 전체 컨트롤 수**를 견주면 차이가
  「어느 표지에도 안 든 것」입니다 — Tab 순서와 **다른 축**이라 둘 다 재십시오
- **껍질을 한 겹 끼울 때는 상자를 전후로 재는 것** — `#app` 은 화면 열셋이
  이름으로 집는 자리라 id 를 못 바꿔 `<main>` 으로 **감쌌습니다.** 결함 313
  이 「껍질이 격자 항목으로 들어가 규칙이 안 닿은 것」을 적어 뒀으므로,
  채팅의 `.side`·`.cols`·`form.cnew`·채널 60개를 전후로 쟀습니다 — **한
  픽셀도** 안 움직였습니다. 재고 나서 두면 다음 사람이 다시 안 셉니다

- **막으라고 둔 가드가 있는데 그 가드가 **한 함수만** 걷고 있던 것** —
  「폰 이야기 금지」를 재는 자(`platform/recording.test.ts`)가 낱말은
  정확히 고르고(`폰`·`홈 화면`·`모바일`) **`describeRecordingSafety`
  하나**만 봤습니다. 그래서 모바일을 범위에서 뺀(2026-08-13) PC 전용
  제품의 홈 화면이 데스크톱에서 「**홈 화면에서** 바로 들어옵니다」와
  「[**홈 화면에 추가**]」를 그리고 있었습니다(결함 422). ⚠️ **자(무엇을
  찾나)는 맞았고 걷는 자리가 좁았습니다**(결함 286) — 자를 만들 때
  **걷는 자리**를 같이 적고, 사람에게 보이는 안내를 새로 만들면 그
  자리에도 같은 자를 대십시오
- **자를 넓히자마자 「한 번도 안 연 화면」이 나온 것** — 마크업까지 걷게
  하니 `offline.html` 이 「올리지 못한 조각은 **폰**에 남아 있고」로
  걸렸습니다. 제 census 가 **로그인이 필요한 화면만** 훑고 있어서 그
  화면은 목록에 아예 없었고, 거기 결함 둘(폰 문구 · 표지 밖 컨트롤)이
  같이 있었습니다. ⚠️ **census 의 목록도 census 하십시오** — 목록을 손으로
  적으면 만들 때 있던 것만 봅니다(결함 329 의 모양)
- **고친 것이 다음 칸을 열어 주는 것 — 표지판** — 결함 421 이 셸에 `main`
  을 만들고 **나서야** 「그럼 표지 밖에 뭐가 남았나」를 셀 수 있었고,
  그것이 결함 423 이었습니다(통화 화면의 주된 컨트롤 셋과 오프라인 화면의
  유일한 컨트롤이 `position: fixed` 막대 안이라 어느 표지에도 안 듦).
  ⚠️ 고칠 때 **자리는 안 옮깁니다** — `role="region" aria-label` 은 배치를
  한 픽셀도 안 바꾸고, 옮기면 고정 막대가 `main` 의 스택 맥락에 들어갑니다
- **로그인한 채로 `/login.html` 을 열면 홈으로 되돌아가는 것** — 요소 수가
  홈과 **똑같이** 나와서 알았습니다(결함 182 의 자). 안 봤으면 「로그인
  화면에 로그아웃 단추가 있다」는 없는 결함을 적을 뻔했습니다

- **화면 목록을 손으로 적어 「로그아웃한 사람」을 한 번도 안 재 본 것** —
  목록을 `public/*.html` 과 `main.tsx` 의 라우트 표에서 **뽑아** 로그아웃
  상태로 전부 여니, 레거시 열넷 중 열셋이 곧장 로그인으로 가는데
  **`search.html` 하나만** 멀쩡한 찾기 화면으로 열렸습니다. 폼을 다 채우고
  「찾기」를 누른 **뒤에야** 튕겼고, 적은 말은 주소에 없으니 돌아와도 빈
  칸입니다(결함 424). 원인은 첫 로드의 `if (response === null ||
  !response.ok) return;` — **401 이 거기서 사라집니다.** ⚠️ 서버는 안
  샜습니다(요청 넷이 전부 401) — 새는 것은 **사람의 시간**입니다
- **자를 두 번 좁혀야 진짜 하나가 남은 것** — 「`get(` 마다 세션을 보는가」로
  재니 **11건**이 걸렸는데 **전부 맞는 자리**였습니다(보조 로드는 앞선
  로드가 이미 보냅니다). 「`!ok` 로 돌아서는 자리」로 좁히니 9건, 다섯은
  그 자리에서 `goToLogin()` 을 부르고 있었습니다. 「**맨 `return`** 으로
  돌아서는 자리」로 좁히니 셋이고 둘은 어휘 로드(결함 414·360), **진짜는
  하나**였습니다. ⚠️ 하나씩 안 읽었으면 **맞는 코드 열 곳을 고칠 뻔**했습니다
- **「아무 말도 안 하는 것」과 「틀린 말을 하는 것」은 다른 자인 것** —
  위 자로는 결함 425 를 못 봅니다. 보고서 화면을 열어 둔 채 세션이 끊기고
  보고서를 누르면 「보고서를 열지 못했습니다 (**HTTP 401**)」이 떴습니다.
  같은 파일의 다른 **세** 자리는 전부 `isSessionExpired` → `goToLogin`
  이고 **열기만** 빠졌습니다(결함 298·301 의 모양이 한 파일 안에서).
  ⚠️ **「실패를 사람에게 말하는 로드 자리는 세션을 먼저 본다」**를 자로
  따로 두십시오
- **기록된 결정의 「시야」에 그 경우가 있었는지 보는 것** — 결함 301 의
  예외는 「불러오기 실패는 **다시 불러오기**가 답이라 서버 문장을 안
  거친다」인데, 그 시야에 **401 은 없었습니다** — 다시 불러와도 401 은
  그대로이고 답은 로그인입니다. ⚠️ **뒤집은 것이 아니라 안 본 자리를
  채운 것**입니다. 결정문이 무엇을 봤는지 문장으로 확인하십시오
- **세션이 죽은 뒤를 재는 법** — `ctx.clearCookies()` 로 화면을 **열어 둔
  채** 끊고 나서 누릅니다. 로그아웃하고 다시 여는 것과 **다른 갈래**입니다 —
  첫 로드는 이미 지나갔고, 그 뒤에 누른 것만 401 을 만납니다

- **「실패를 한 자리에서 받는다」는 그 자리를 지나갈 때만 참인 것** —
  SPA 는 `queryCache`/`mutationCache` 의 `onError` 로 401 을 한 곳에서
  받고(결함 227), 그 결정도 배선도 옳습니다. 그런데 홈의 대화상자가
  `api.post(...)` 를 **직접** 불러서 그 길을 안 탔습니다. 세션이 죽은 채
  「만들기」를 누르면 「로그인이 필요합니다」라고 말하는데 **로그인으로
  가는 자리가 0개**였습니다 — 227 이 고친 그 병입니다(결함 426).
  ⚠️ 대표 실패 ①의 **거울상**입니다: 부르긴 하는데 **그 길로 안 부르는 것**.
  ⚠️ **읽기와 쓰기는 다른 길입니다** — 같은 상태에서 레일로 화면을 옮기면
  곧장 `/app/login` 입니다. 질의만 재고 「SPA 는 괜찮다」로 넘길 뻔했습니다
- **누른 것이 진짜 요청을 내는지 안 본 것** — 세션을 끊고 `main button` 의
  첫 번째를 눌렀더니 넷 중 셋이 **대화상자 열기·팝오버** 같은 로컬 UI 라
  요청이 0건이었고, 「SPA 는 안 튕긴다」로 읽을 뻔했습니다. **네트워크를
  찍어 401 이 실제로 나갔는지** 보십시오
- **자를 넓혔으면 「옛 모양이 아직 잡히는지」도 심어 보는 것** — 결함 218 의
  자(`onError` 인자 · `.isError`)가 `try/catch` 로 말하는 **셋째 길**을
  몰라서, 요구가 한 자도 안 바뀌었는데 빨개졌습니다. 자를 넓힌 뒤
  **「아무 말도 안 하는」 옛 모양을 심어** 아직 우는지 확인했습니다 —
  안 하면 넓히면서 구멍을 낸 것을 모릅니다

- **판단이 공용 헬퍼에 있는데 자를 「부르는 자리마다」로 쓴 것** —
  「쓰기가 세션 만료를 보는가」를 부르는 **블록** 단위로 쟀더니 설정
  화면의 쓰기 **열둘 중 열하나**가 걸렸습니다. 읽어 보니 그 화면은
  `call()` 이라는 공용 헬퍼에서 한 번 보고 있었고, **그게 이 저장소가
  권하는 모양**입니다 — 자가 잘 푼 화면을 벌주고 있었습니다. 자를
  **`trySend(` 를 감싸는 함수**로 바꾸니 거짓 양성이 0 이 되고 진짜 넷만
  남았습니다(결함 427). ⚠️ **고칠 때도 헬퍼에 두십시오** — 부르는 쪽
  다섯에 각각 적으면 이번처럼 셋이 빠집니다(대표 실패 ②)
- **읽기와 쓰기를 뿌리마다 따로 재야 하는 것** — SPA(결함 426)와
  레거시(결함 427)가 **각각 다른 이유로** 갈려 있었습니다. 한쪽을 재고
  「이 축은 봤다」로 넘기면 다른 쪽이 그대로 남습니다. ⚠️ 그런데 남은
  **예외 둘은 두 뿌리가 같았습니다**(로그인·가입 = 비밀번호 틀림 ·
  로그아웃 = 이미 끊김) — 서로 다른 코드가 같은 결론에 닿은 것이 그 둘이
  진짜 예외라는 증거입니다

- **빌드를 돌리는 동안 pytest 를 돌린 것** — pytest 를 백그라운드로 띄워
  두고 그 사이에 `npm run build` 를 했더니 「SPA 홈이 새로고침을 견딘다」가
  실패했습니다. 혼자 돌리니 통과합니다 — 검사가 **빌드 중인 `dist`** 를
  읽은 것입니다. ⚠️ **빌드와 검사를 겹치지 마십시오** — 「왜 빨간지」를 안
  읽었으면 없는 회귀를 쫓았을 것입니다(결함 327)

- **씨앗이 한 번도 못 켜는 기능을 켜 보는 것 — 그리고 씨앗 모양이 또
  틀렸던 것** — 「내 녹음과 성문 지우기」는 씨앗의 `audio_assets` 가 0건이라
  **진짜로 지우는 갈래가 한 번도 안 그려져** 있었습니다. 켜서 끝까지
  걸으니 **전부 초록**이었습니다(청크 파일 4→0 · `deleted_reason=
  'user_request'` · 감사 기록 · 두 뿌리가 같은 문장 · 기여도와 최종
  보고서가 「본인 요청으로 …삭제」를 사람별로 정확히). 불변식 ③ 이 **팀
  밖으로 나가는 문서**에서 지켜집니다. ⚠️ **처음에 파일 경로로 심었는데
  실기는 `store.storage_key(...)` 즉 트랙 **디렉터리**를 넣습니다** —
  잘못된 모양으로도 200 이 나오고 화면도 멀쩡해서 그대로 「지워진다」고
  적을 뻔했습니다(결함 288). 심을 때는 **그 값을 만드는 실기 코드**를
  찾아 그 함수를 그대로 부르십시오
- **접힌 것을 「없다」고 읽을 뻔한 것 — 또** — SPA 의 「기여도는 어떻게
  되나요」 뒤에 아무 글도 없어 보였는데 `<Disclosure>` 안이었습니다.
  `innerText` 에 안 잡힙니다 — **잎 요소를 세어** 확인하십시오

- **분모 0 에 뜻이 둘인데 한 규칙으로 뭉갠 것** — 신뢰도 계산이 「분모가 0 인
  신호는 뺀다」로 되어 있었습니다. 그 규칙은 **모듈 미사용**을 위한 것이고
  주석의 예도 「동료평가를 안 쓰는 팀」입니다. 그런데 `utterances_total == 0`
  은 **측정 실패**일 수도 있어서, 전사 품질을 재는 두 신호(가중치 6.0 중
  **3.0**)가 **바로 그 실패에서만** 통째로 사라졌습니다 — 아무것도 못 들은
  회의가 신뢰도 **1.0 「높음」**, 완벽한 회의(0.73 「보통」)보다 높고 조정 폭은
  **0%p·사유 0건**이었습니다(결함 428). 1.0 은 구간을 **한 점으로** 접으므로
  불변식 ②(단일 점수 금지)가 거기서 깨집니다. ⚠️ **같은 제품이 그 상태를
  이미 두 곳에서 알고 있었습니다**(홈 368 · 회의록 369) — 신뢰도만 몰랐습니다.
  ⚠️ 그리고 **`track_quality` 주석이 이 함정을 이미 적어 두고**(「이 신호가
  없으면 망가진 녹음이 오히려 높은 신뢰도로 보인다」) **40%만 녹음된 경우**
  하나만 막고 있었습니다 — 이번은 **coverage 1.0 인데 결과가 0** 인 길입니다
  (결함 295 의 「막는 길을 하나만 막은 것」). **「빼는 규칙」을 보면 빼는 이유가
  몇 가지인지 세십시오**
- **깎는 고침은 반대 방향으로 번지지 않는지 같이 재는 것** — 428 을 고치면서
  잡담만 한 회의(발화는 있고 점수 대상 0)와 갓 만든 팀(회의 0)이 같이 깎이면
  안 됩니다. 판정을 `utterances_scored` 가 아니라 **`utterances_total`** 로 하고
  `meetings_recorded > 0` 을 달아 갈랐고, **넷을 따로 심어** 서로 다른 검사만
  빨개지는 것을 확인했습니다. ⚠️ 씨앗 둘은 **갈라지는 데이터**여야 합니다 —
  같은 값을 내면 그 검사는 아무것도 안 잽니다(결함 327)
- **씨앗이 프로젝트를 하나만 만들어 「프로젝트를 넘는」 축이 통째로 미검증이던
  것** — 프로젝트를 둘 만들고 초대 코드로 **한쪽에만** 들어온 사람을 실기
  경로로 만들어 재니, 격리는 47갈래 전부 초록이었고(GET 30 · 경로에 프로젝트가
  없는 쓰기 17 — 200 이 **0건**) 결함 101 도 지켜집니다. **초록이지만 이번이
  처음입니다** — 그리고 그 상태를 만들자마자 옆에서 결함 428 이 나왔습니다.
  ⚠️ **수확이 줄면 자가 아니라 상태를 늘리십시오**가 「인원 수」(결함 410)에
  이어 **프로젝트 수**에서도 맞았습니다
- **격리를 재면서 422 다섯을 「초록」으로 적을 뻔한 것** — 본문 모양이 틀려
  **권한 검사에 닿지도 못한** 것입니다. 그대로 적었으면 다섯 줄이 아무것도
  안 잰 채로 남았습니다. 스키마를 읽어 채우니 전부 403 이었습니다
  (`consented` · `items[].approve` · `ended_at` · `X-Client-At-Ms` 헤더).
  ⚠️ **거부당한 요청은 「안 움직임」이 아니라 「못 쟀음」입니다** — 이 문서가
  이미 적어 둔 것이고, 이번에는 **코드를 찍어 뒀기에** 갈렸습니다

- **검사가 4,368개인데 아무것도 안 돌리고 있던 것** — CI 가 없었습니다.
  「초록입니다」는 전부 **누가 돌리고 그렇게 말한 것**이고, 처음 온 사람에게는
  확인할 방법이 0 이었습니다. 이 저장소가 결함마다 「심어서 가드가 잡는지」를
  그렇게 엄격히 요구하면서 **그 확인이 자동으로 재현되지 않았습니다.**
  붙였습니다 — `.github/workflows/checks.yml`, 잡 넷. ⚠️ **「번들이 커밋된
  것과 같은가」를 적기 전에 두 번 빌드해 지문이 같은 것을 쟀습니다** —
  안 재고 적었으면 CI 가 첫날부터 빨갛고 사람은 그 단계를 지웁니다
- **결함의 절반이 사는 층에 그물이 없던 것** — 최근 결함 커밋 73개가 건드린
  층을 세니 **화면 39 · `@lib` 37 · 서버 15** 인데, 화면 파일 23개(레거시 15 ·
  SPA 8)의 자동 검사는 **0개**였습니다(`@lib` 은 81 중 78). 매 회차 사람이
  브라우저로 찾고 그 확인은 아무 데도 안 남아서, **이미 찾은 자리가 다시
  깨지는 것은 아무도 안 보고 있었습니다.** 화면 스모크를 붙였습니다
  (`frontend/screens/smoke.mjs` · `npm --prefix frontend run smoke:screens`)
- **자를 만들면서 자가 네 번 「초록 쪽」으로 틀린 것** — 전부 아무것도 안
  재면서 통과하고 있었습니다: `page.evaluate` 에 **함수 문자열만** 준 것
  (표현식으로 평가되어 함수 자체가 돌아옵니다 — `(${'${자}'})()` 로 호출까지) ·
  baseline 을 대비 루프 **안**에 둬 `undefined` 와 비교한 것 ·
  **`rgba(255,255,255,0)`(알파 0인 흰색)을 투명으로 안 본 것**(Chromium 이
  고대비 밝음에서 그렇게 답합니다 — 글자로 비교하면 `rgb(255,255,255)` 와
  달라 보여 **흰 바탕 위 흰 막대**를 「다르다」로 읽습니다) · **폭 0 인
  테두리**를 색만 보고 센 것. ⚠️ 셋째는 **다른 회차가 자기 자에서 겪고
  적어 둔 것**인데 제 자에도 그대로 있었습니다 — **남의 측정 실수 목록을
  내 자에 대 보십시오**
- **심은 자리가 애초에 아무 일도 안 하는 자리이던 것** — 고대비 뭉개짐을
  BEM 수식자에 심었는데 그 그룹은 **화면당 갈래가 하나뿐**이라 비교 자체가
  안 됩니다(결함 164 의 모양). 갈래가 둘 이상인 자리에 심으십시오
- **「잡혔다」가 「맞다」가 아니라서 자에 규칙을 넣은 것** — 새 자가 10건을
  잡았는데 읽어 보니 대부분 **앞 회차가 이미 「뜻이 안 사라진다」로 판정한
  것**이었습니다. ⚠️ **빨간 채로 CI 에 붙이면 사람이 빨강을 무시하게
  됩니다** — 가르는 규칙을 넣어(글자가 뜻을 나르는가 · 「지금 어디인가」는
  현재↔나머지만) 10 → 4 → 0 으로 줄이고, 남은 하나는 **이유와 낡음 검사를
  붙인 예외**로 뒀습니다(결함 306 의 방법)
- **검사 파일이 제품 CSS 를 바꾼 것** — 화면 스모크를 만들자마자 배포
  번들 `tw.css` 에 `.sr-only` 가 생겼습니다. Tailwind v4 는 **설정 없이
  저장소를 통째로 훑으므로**, 제가 스모크에 적은 **선택자 글자**가 그대로
  유틸리티로 실린 것입니다. 해롭지는 않지만 방향이 나쁩니다 — CI 에
  `git diff --exit-code -- frontend/public` 을 붙이면 **스모크를 고칠
  때마다 엉뚱한 파일이 웁니다.** `@source not "../../screens"` 로 훑는
  자리에서 뺐고, 심어서 양쪽을 증명했습니다(제외 있음 + 심은 것 = 0건 ·
  제외 뺌 + 심은 것 = 1건). ⚠️ **자를 새로 만들었으면 그 자가 제품 산출물을
  건드리는지 보십시오** — 「빌드가 손대는 것」 목록(§`npm run build` 가
  손대는 것)에 **내가 새로 만든 파일이 입력으로 들어갔는지**가 그 질문입니다

**규칙: "0건" 을 보고하기 전에 있는 것을 하나 심어 보고, 잡히는 것을
확인한 뒤에 0건이라고 적으십시오.**

되돌림이 안 깨지면 "내 검사가 다른 이유로 통과 중" 을 먼저 의심하세요.

⚠️ **반대 방향도 있습니다.** 심었는데 **아무 일도 안 일어나면**, 내가 심은
자리가 애초에 아무 일도 안 하고 있던 것입니다. 칸반에 44px 규칙을 적고
`min-height: 0` 을 심었는데 그대로 44px 이 나왔습니다 — `app.css` 가 이미
같은 일을 하고 있었고, 제가 적은 것은 **사본**이었습니다(결함 164).

---

## 디자인은 **눈으로** 봐야 합니다

글로 판단하지 마십시오. 브라우저로 렌더해 캡처를 **실제로 보고**
고치세요. 이 규칙 없이 만든 실험판 네 개가 전부 못 쓰게 나왔습니다.

렌더해야만 나온 결함들: 시간축이 등간격 거짓말 · 한글이 세로로 쪼개짐 ·
아무것도 없는 가로줄 · 폰에서 222px 넘침 · 특성도에 져서 안 먹은 규칙 ·
`display: flex` 가 문장을 흩어 놓은 것 · 마크다운 별표가 그대로 노출.

그리고 **눈으로 본 뒤에도 재십시오.** 입력창 높이가 안 줄어 특성도를
의심했는데, 재 보니 원인은 `line-height` 였습니다.

---

## 색과 간격

⚠️ **새 hex 를 만들지 마십시오.** `frontend/public/tokens.css` 의 값은
전부 계산값이고, 대비 주장 스무 개 남짓을 `backend/tests/test_design_tokens.py`
가 매번 다시 잽니다. 필요하면 `color-mix()` 로 기존 값에서 파생시키세요.

⚠️ **팔레트는 두 벌입니다** — 레거시 `frontend/public/tokens.css` 와 SPA
`webapp/src/app.css`. 같은 파일(`test_design_tokens.py`)이 **둘 다** 잽니다.
SPA 쪽은 오래도록 아무도 안 재고 있었고, 레거시가 이미 겪고 고친 문제
(결측색이 글자 하한 아래 — 결함 117)가 **그대로 다시 있었습니다**(결함 177).
색을 고칠 때는 **두 파일을 같이 보세요.**

- SPA 에서 `--c-*-base` 는 **재료**입니다. 화면이 직접 쓰면 가드가 잡습니다 —
  화면이 쓰는 값은 잉크 쪽으로 민 파생값입니다
- ⛔ 규칙 안에 `color: #fff` 를 적지 마십시오. 다크에서 **채움만 뒤집히고**
  글자는 흰색으로 남습니다 (결함 60 · 179). `--c-ink-inverse` 를 쓰세요
- ⛔ **누를 수 있는 것을 `opacity` 로 흐리게 하지 마십시오.** 이 저장소의
  비활성 버튼은 `aria-disabled` 라 **초점도 받고 눌리기도 합니다** — WCAG
  1.4.3 의 비활성 예외를 쓸 수 없습니다. 흐림 대신 **모양**을 바꾸세요
  (`.btn--unmet` = 덜 채운 모양)
- ⛔ **`:hover`·`:active` 는 막힌 것을 비켜 가야 합니다.** `:not(:disabled)`
  만으로는 못 막습니다 — 이 저장소의 「아직 안 됨」은 `aria-disabled` 쪽이라
  그 방패는 **아무것도 안 막습니다.** 마우스를 올리면 `btn--unmet` 의 채움만
  primary 로 돌아와 글자가 사라졌습니다(1.16~1.27:1, 다섯 버튼·두 테마 —
  결함 239). ⚠️ **대비를 잴 때 마우스를 올려 보십시오** — 쉬고 있는 상태만
  재면 이 자리는 구조적으로 안 보입니다(236 과 같은 부류)

- Layer 1 원시값 → Layer 2 의미. **다크는 Layer 1 만 뒤집습니다.**
  ⚠️ Layer 2 에 날 hex 를 적으면 그 값은 영영 안 뒤집힙니다 (한 번 당했습니다)
- 껍데기(`--chrome-*`)는 **안 뒤집힙니다.** 왼쪽 열 전용
- 간격은 4px 격자. 아이콘↔텍스트만 6px 예외
- 줄 길이는 `ch` 가 아니라 `rem` 으로. `ch` 는 숫자 `0` 폭이라 한글에서
  너무 일찍 끊깁니다

⛔ **날짜·시각은 팀 달력(`Asia/Seoul`)입니다.** 화면에서
`new Date(iso).getMonth()` 같은 **브라우저 달력**을 쓰지 마십시오 — 같은
회의를 팀원마다 다른 날로 봅니다(결함 246). `@lib/time/calendar.ts` 의
`shortTeamDate`·`teamDateTime`·`teamDateOf` 를 쓰고, 서버 응답의 시각은
`UtcDatetime` 으로 **시간대를 글자로** 내보냅니다(SQLite 는 시간대를 안
돌려줍니다). ⚠️ 이걸 잴 때는 **자정을 넘는 순간**을 심으십시오 — 씨앗
회의는 전부 `10:00Z` 라 어느 시간대에서도 날짜가 안 넘어가, 세 시간대가
같은 값을 주고 「재현 안 됨」이 나옵니다.
**했습니다** — `2026-09-10T16:00Z`(서울 09-11 01:00 · 뉴욕 09-10 12:00)를
심고 서울·뉴욕·오클랜드로 걸으니 홈 둘과 일정 격자가 **셋 다 11일**이었
습니다(`docs/24`). 이제 가정이 아니라 잰 것입니다

---

## 화면 코드

- 판단은 `src/lib/**` 에, 화면(`src/demo/**`)은 그리기만. 화면 코드에는
  자동 테스트가 없으므로 판단이 거기 있으면 검증 밖으로 나갑니다
- ⛔ **모달을 손으로 짓지 마십시오.** `role="dialog" aria-modal="true"` 는
  **낭독기에게 하는 말**이고 키보드는 안 듣습니다 — 초점이 덮개 뒤로 새고,
  Escape 가 안 먹고, 닫으면 `body` 에 떨어집니다(결함 280).
  `@radix-ui/react-dialog` 를 쓰세요. ⚠️ 덮개와 상자는 **형제**라 상자가
  스스로 서고 덮개 **위** 층에 와야 합니다 — 안 그러면 마우스가 죽습니다
- 프런트 **런타임 의존성**은 React·React DOM·Radix 뿐입니다. 새로 들이기
  전에 정말 필요한지 보세요 — 졸업작품이 끝난 뒤에도 열려야 합니다
- 번들은 `public/` 에 커밋됩니다. `npm run build` 를 잊으면 테스트가
  "낡은 번들" 로 잡습니다
- 한국어 조사는 앞말에 붙여 씁니다 (`PR에`, `GitHub은`). 가드가 잡습니다.
  ⚠️ **`으로/로` 만 규칙이 다릅니다** — 받침이 있어도 그 받침이 **ㄹ** 이면
  `로` 입니다(`할 일로`·`서울로`·`PR로`). 이걸 몰라서 칸반 버튼에
  **"할 일으로"** 가 떠 있었습니다

### `npm run build` 가 **손대는 것**

빌드는 번들만 만들지 않습니다. 손으로 고치지 마십시오:

- `public/sw.js` 의 오프라인 목록 — `public/` 을 세어서 다시 씁니다.
  손으로 적던 시절 **두 번 어긋났고**, 어긋나도 온라인에서는 티가 안 났습니다
- `public/chunk-*.js` — 공용 조각. 빌드 전에 **옛것을 지웁니다.** 이름에
  해시가 붙어 덮이지 않고 하나 더 생기기 때문입니다

### 리디자인 「계측기」 (2026-08-18)

주 화면 아홉(로그인·홈·설정·로비·검토·칸반·기여도 + 녹음·통화)의 새
얼굴은 **`webapp/`(Vite SPA, `/app` 마운트)** 입니다. 결정 전부와 검수
결과는 `docs/22-리디자인-계측기.md`, 지시서 원본은 `design/redesign/`.

- 판단은 여전히 `frontend/src/lib` 한 벌 — webapp 은 `@lib` 별칭으로
  같은 파일을 씁니다. **복사 금지.**
- 녹음(`index.html`)·통화(`call.html`)는 SPA 밖 그대로입니다 (아래 참조).
- `frontend/src/demo` 의 화면들은 레거시입니다 — 라우트만 유지하고(R8)
  새 기능을 넣지 않습니다.

### React 로 옮긴 화면 · 안 옮기는 화면

`review`·`kanban`·`home`·`contributions`·`login`·`project`·`lobby`·
`reports`·`chat`·`calendar`·`notifications`·`activity`·`search` 가
React 입니다 (열셋). ⚠️ 이 숫자는 **화면을 더할 때마다 낡습니다** — 세는
자리는 `frontend/public/*.html` 옆에 같은 이름의 `src/demo/*.tsx` 가
있는가입니다.
**`index`(녹음)와 `call`(통화)은 안 옮깁니다** — 그 둘은 화면이 아니라
장치를 붙잡고 있는 것이고(`MediaRecorder`·`RTCPeerConnection`), 수명
관리를 `useEffect` 로 옮겨 적으면 같은 판단이 두 벌이 됩니다. 녹음이 한
번 끊기면 그 구간은 영영 못 잽니다. 자세한 것은 `docs/19` §24.9.

⚠️ **가드가 걷는 자리가 한쪽뿐인지 보십시오.** 결함 246 의 「브라우저
달력 금지」 가드는 `webapp/src` 만 걷고 있었고, **자동 테스트가 하나도
없는 레거시 화면 열셋**이 통째로 감시 밖이었습니다. 활동 화면에 심고
번들까지 다시 만들었더니 1708개가 전부 통과했습니다(결함 286).
가드를 쓸 때는 **뿌리마다 한 파일이라도 걸렸는지**를 같이 재십시오 —
안 보고 있는 상태 자체가 실패여야 합니다.

⚠️ **화면을 옮기면 가드가 눈을 감습니다.** 실제로 여덟 개가 그랬습니다 —
확장자를 `.ts` 로 하드코딩했거나, 명령형 헬퍼의 **이름**을 세고 있었거나,
마크업을 `.html` 에서만 찾았거나. 요구가 아니라 **찾는 자리**가 낡은
것이니, 옮긴 뒤 실패한 가드는 먼저 "이게 정말 요구를 재고 있나" 를
보십시오. 그중 하나는 **진짜 결함**이었습니다(로딩 깜빡임).

---

## 셸은 **둘뿐입니다** — PC 웹, PC 앱

모바일은 **범위에서 뺐습니다** (2026-08-13). 안드로이드 셸(`android/`)은
지웠습니다 — 실기기가 없어 한 번도 안 돌려 봤을 뿐 아니라 `gradlew` 를
커밋한 적이 없어 **빌드 자체가 불가능**했습니다.

⚠️ **"모바일을 뺐다" 가 "좁은 폭을 버렸다" 는 아닙니다.** 이 저장소는
좁은 폭이 **기본값**이고 PC 규칙이 `@media (min-width: 48rem)` 안에
있습니다 — 기본값을 걷어내면 PC 가 같이 무너집니다. 44px 손가락 표적과
입력 16px 도 남깁니다. 그건 폰 지원이 아니라 **창 반쪽·브라우저 확대
200%·터치 노트북**에서 필요한 것이고, 없앴다가 낸 결함이 기록에
있습니다(72·150).

⚠️ 새 안내 문구에 **"홈 화면에 추가"** 같은 폰 이야기를 쓰지 마십시오.
`platform/recording.test.ts` 가 잡습니다.

---

## 데스크톱 셸 (Electron)

**이유는 하나뿐입니다** — 창을 내리거나 화면이 잠기면 브라우저가 녹음을
끊습니다. 녹음이 한 번 끊기면 그 구간은 **영영 못 잽니다.** 그것 말고
다른 이유(트레이·자동 실행)로 이 셸을 늘리지 마십시오.

이 창은 **서버가 준 화면**을 띄웁니다(`/api/...` 를 same-origin 으로
부르기 때문에 `file://` 로는 로그인부터 안 됩니다). 즉 **원격 코드를
실행합니다.** 그래서:

- ⛔ `contextIsolation`·`sandbox`·`nodeIntegration:false`·`webSecurity`
  **넷 중 하나도 끄지 마십시오.** 끄면 서버의 XSS 가 곧 사용자 PC 의 코드
  실행입니다. 가드가 여섯 항목으로 잡습니다
- ⛔ **`--no-sandbox` 를 앱 코드에 넣지 마십시오.** 이 컨테이너가 root 라
  검사 하네스에서는 붙이는데, 그건 하네스 사정입니다
- ⛔ **`require.main === module` 로 진입을 감싸지 마십시오** — Electron 에서는
  언제나 거짓이라 **앱이 창을 하나도 안 엽니다**(결함 166)
- ⛔ 판단(허용 주소·바깥 링크)을 main 프로세스에 두지 마십시오. main 에는
  자동 검사가 안 붙습니다 → `src/lib/desktop/`
- ⛔ **electron-vite 를 들이지 마십시오.** 그 권고의 전제(Vite 사용)가 이
  저장소에 없습니다. main·preload 도 `build.mts` 의 esbuild 로 만듭니다

⚠️ **Phase 2 까지 왔습니다** — 창이 뜨고, 청크가 디스크에 앉고(`parked`),
**녹음 중에는 절전을 막습니다**(`powerSaveBlocker` + `backgroundThrottling`).
`keepsAwake` 는 이제 **참**이고 녹음 화면이 "화면을 꺼도 녹음이 이어집니다"
라고 말합니다. **녹음 생존율도 smoke 가 잽니다**(가짜 마이크로 실녹음,
창을 숨겨도 청크가 늘고 coverage 100% — 붙이자마자 결함 173 이 나왔습니다).
시스템 오디오(Phase 3)는 아직 없습니다. 자세한 것은 `docs/21`.
⚠️ 번들을 다시 만들었으면 smoke 전에 `rm -rf ~/.config/Electron` —
서비스 워커가 옛 번들을 물고 있어 **고치기 전 코드를 재게** 됩니다.
⚠️ **smoke 는 `npm test` 밖이라 화면이 바뀌면 조용히 낡습니다.** 결함 229
가 `#consent` 를 단추에서 링크로 바꾼 뒤, 생존율 측정은 **한 번도 안 돌고**
있었는데 이 문서는 계속 "잽니다" 라고 적혀 있었습니다(결함 238). 화면을
고쳤으면 **smoke 를 돌려 보십시오** — 가정이 어긋난 것은 `guards.test.ts`
가 서버 없이 잴 수 있는 만큼만 잡습니다.

- 경로 결정은 `src/lib/desktop/chunk-paths.ts`. renderer 는 **경로를 못
  줍니다** — 회의 id 만 주고 폴더·파일명은 여기서 만듭니다
- ⛔ **blocker 배선을 지우면 `keepsAwake` 도 같은 커밋에서 거짓으로.**
  값만 참으로 남으면 화면이 "화면을 꺼도 됩니다" 라고 **거짓말**을
  시작합니다 — `guards.test.ts` 짝 가드가 셋(main blocker ·
  `backgroundThrottling` · 화면 `shouldHoldAwake`)을 잽니다
- ⚠️ preload 가드는 이제 낱말이 아니라 **요구**를 잽니다 — `ipcRenderer`
  를 값으로 건네는가 · 채널이 글자로 박혀 있는가 (결함 170)

⚠️ **못 올린 조각은 디스크에 앉고, 「다시 올리기」로 되찾습니다.** 그
단추는 정지 뒤에만 나타나므로 **서버는 끝난 트랙에도 늦은 조각을 받아야**
합니다(결함 244) — 다만 회의가 처리에 들어간 뒤는 거절합니다. 그때는
소리가 이미 글이라 회의록과 갈라집니다. 되찾은 뒤에는 **판정도 다시
만듭니다**(`recomputeAfterRecovery`) — 안 그러면 소리는 돌아왔는데 기록은
계속 「사용 불가」입니다.

⚠️ **녹음 중에 창을 닫으면 묻습니다** (결함 342). 잠과 스로틀은 막아
뒀는데 **닫기 경로가 열려 있었습니다** — 녹음 중에 X 를 누르면 앱이 통째로
죽고 그 구간은 영영 사라졌습니다. 화면은 그때 「앱을 완전히 닫지만
마세요」라고 부탁만 하고 있었고, 사람은 X 를 「완전히 닫기」로 안 읽습니다.
판단은 `lib/desktop/closing.ts`(버튼 **순서**까지), 손은 main, 확인은
`smoke.mjs` 의 「닫기 잠금」 줄입니다. ⚠️ **Phase 5(트레이·자동 실행)는
그래도 안 만듭니다** — 위 금지 그대로이고, 이건 셸을 늘린 것이 아니라
하나뿐인 이유를 빠진 자리에서 지킨 것입니다(`docs/21` §5.2).

⛔ **절전 방지는 녹음 중에만 잡습니다** — 항상 켜면 배터리만 태우고
"이 앱은 켜 두면 배터리가 준다" 는 인상만 남깁니다. 어느 국면에서
잡는지는 `lib/platform/awake.ts` 의 `shouldHoldAwake` 가 정합니다
(`interrupted` 에서도 잡습니다 — 놓으면 기계가 잠들어 되살아날 기회가
사라집니다).

---

## 자세한 것

| 무엇 | 어디 |
|---|---|
| 제품 설명 (비개발자용) | `docs/00-이-프로그램은-무엇인가.md` |
| 기여도 설계·불변식 | `docs/05-기여도-산정-설계.md` |
| 법적·윤리 요구 | `docs/07-법적-윤리-요구사항.md` |
| 결함 기록 | `docs/17-결함-기록.md` |
| 페르소나 QA (사람인 척 끝까지 써 보기) | `docs/23-페르소나-QA.md` |
| 베타 체험 QA (버튼 하나하나 · 브라우저 오류 로그) | `docs/24-베타-체험-QA.md` |
| 셸·디자인 결정 전부 | `docs/19-메신저-셸-전환.md` |
| 데스크톱 셸(Electron) 결정 | `docs/21-데스크톱-셸-Electron.md` |

⚠️ 문서가 사실과 다른 곳이 여럿 있었습니다. **문서에 적힌 숫자를 그대로
믿지 말고 세어 보세요.**
