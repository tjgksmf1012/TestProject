"""채팅 — 팀원 간 실시간 의사소통 **만** (요구사항 정의서 §7).

## ⚠️ 이 패키지가 하지 않는 일

정의서 §7 머리말이 못 박습니다:

> 채팅 내용에 대한 AI 분석, 업무 자동 생성, 프로젝트 분석 등의 기능은
> 제공하지 않는다.

그냥 안 만든 게 아니라 **만들면 안 되는** 것입니다.

이 제품은 사람의 기여를 숫자로 말합니다. 그 숫자가 성적에 쓰일 수 있고,
그래서 `docs/05` §5 와 `test_anti_gaming.py` 가 "조작이 쉬운 양적 지표는
점수에 들어오지 않는다" 를 지키고 있습니다. **채팅 메시지는 그중에서도
가장 조작하기 쉬운 것**입니다 — 아무 때나, 아무나, 무한히 칠 수 있고,
회의 발언과 달리 트랙도 근거도 신뢰도도 붙지 않습니다.

메시지가 기여로 세어지는 순간 **도배가 기여도를 올리는 방법**이 됩니다.
그래서 이 패키지는 `contribution/` 을 import 하지 않고, `messages` 표는
`contribution_events` 와 닿지 않습니다. `test_chat_is_not_measured.py` 가
그 경계를 코드에서 다시 잽니다.

## 여기 있는 것

* `mentions` — 본문에서 `@이름` 을 뽑는 순수 함수. **서버만 판정합니다**
* `hub` — 지금 채널을 보고 있는 소켓들. **읽기 전용**입니다: 글은 HTTP 로
  쓰고, 저장된 뒤에 여기로 흘러갑니다
"""
