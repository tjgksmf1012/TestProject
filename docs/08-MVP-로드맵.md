# 08. MVP 범위와 16주 일정 조정

제안서 14·15장을 조정합니다. **핵심 변경: 범위 축소 + 0주차 신설.**

---

## 1. 왜 조정이 필요한가

제안서 12장은 스스로 이렇게 규정했습니다:
> "졸업작품의 직접 연구 범위는 발언 유형 분류, 비효율 구간 탐지, 코드 활동 분류 중 **1~2개에 집중**한다."

**옳은 판단입니다. 그런데 나머지 장이 이걸 안 지킵니다.**

- 12장 실험 표: **5개**
- 13장 평가 대상: **8개**
- 14장 MVP "반드시 구현" AI 기능: **6개** (STT · 화자 · 발언유형 · 반복 · 미결정 · 업무추출)
- 15장 일정: 이 6개를 **7~10주, 4주 안에**

그리고 15장에 **환경 구축 기간이 0주**입니다.
torch·vLLM·pyannote·transformers 버전 충돌, 모델 다운로드 30~50GB, HF gated 토큰 발급,
그리고 폰 마이크 권한(HTTPS) 확인까지 하면 며칠이 사라집니다.
`python3 scripts/check_env.py` 로 진단부터 하세요.

---

## 2. 조정된 MVP 범위

### 반드시 구현 (이게 안 되면 프로젝트가 성립하지 않음)

| 영역 | 범위 | 제안서 대비 |
|---|---|---|
| 협업 | 팀·프로젝트·칸반·마감일·역할 | 동일 |
| GitHub | GitHub **App** 연동, 커밋/PR/리뷰 수집, 업무 연결 | + App 명시, + 백필 |
| 회의 입력 | **멀티트랙 녹음(모드 A)** + 단일 파일 업로드(모드 B) | + 멀티트랙 |
| STT | Qwen3-ASR-1.7B + 타임스탬프 | Whisper → Qwen3-ASR |
| 화자 | 모드 A는 트랙=화자 / 모드 B는 pyannote + 수동 매핑 | 자동 식별은 후순위 |
| 발언 분류 | **KLUE-RoBERTa 파인튜닝** ← **유일한 직접 학습** | 5개 → 1개 |
| 업무 추출 | LLM + guided decoding (JSON 스키마 강제) | + 디코딩 강제 |
| **연동** | **회의 업무 후보 → 승인 → 칸반 등록** | ⭐ 최우선 |
| 기여도 | 이벤트 로그 기반 항목별 지표 + 근거 링크 | + 재계산 구조 |
| 보고서 | 회의록 · 주간 · 최종 | 동일 |
| 법적 | 3단계 동의 + 오디오 분리 보관 + 보존기간 | ⭐ 신규 |

### 일정이 남으면 (우선순위 순)

1. 반복 논의 구간 탐지 (문장 임베딩 + 클러스터링) — **차별점이라 여기가 1순위**
2. 미응답 질문 / 불완전 업무 탐지 (규칙 기반)
3. 이전 회의 결정 번복 탐지 (임베딩 대조)
4. 업무 편중·위험 경고 (규칙 기반)
5. 교수자 화면
6. 화자 자동 식별 (ECAPA-TDNN 임베딩 매칭)
7. 커밋 diff 작업 유형 딥러닝 분류
8. 프로젝트 지연 예측 모델

### 범위에서 제거

| 제거 | 이유 |
|---|---|
| **음성 변화 분석 (6.4)** | 평가 불가 (13장 평가표에 없음) + 감정 해석 위험 |
| **실시간 회의 (11장)** | 14장에서 이미 후순위. 화면 목록에서도 제거 |
| **실시간 AI 사회자** | 위 |
| **LoRA 파인튜닝** | 데이터 확보 비현실적 → 평가셋 구축으로 대체 |

---

## 3. 왜 "반복 논의 탐지"가 1순위 확장인가

제안서의 기능 중 **가장 독창적**입니다. 검색으로 확인한 결과 일반 협업툴에도,
회의록 서비스에도 이 기능은 없습니다.

그리고 **구현 난이도 대비 효과가 큽니다.**
문장 임베딩 → pgvector 유사도 → 시간축에서 떨어진 유사 클러스터 탐지.
딥러닝 학습 없이 사전학습 임베딩 모델만으로 됩니다.

평가도 가능합니다 — 합성 회의에 반복 구간을 심어두고 IoU로 측정.
([`04-회의-처리-파이프라인.md`](04-회의-처리-파이프라인.md) §5.2)

---

## 4. 조정된 16주 일정

| 주차 | 작업 | 산출물 | 변경 |
|---|---|---|---|
| **0주** | **환경 구축**: `scripts/check_env.py` 통과. torch·vLLM·pyannote 4.x·transformers 5.13+ 공존 확인. 모델 다운로드. HF 토큰. VRAM 실측. **폰 마이크 권한 확인** | 동작하는 개발 환경 + **VRAM 실측표** | ⭐ 신설 |
| 1~2 | 요구사항, 사용자 시나리오, 기능 우선순위, **법적 요구사항 정리** | 요구사항 명세, 화면 흐름 | +법적 |
| 3~4 | DB·API·GitHub·AI 아키텍처 설계. **동의/오디오 분리 스키마 포함** | 시스템 설계서, 스키마 | +스키마 |
| 5~6 | 프로젝트·팀원·칸반 + **GitHub App 연동 + 백필** | 협업 플랫폼 MVP | +App/백필 |
| **7** | **멀티트랙 녹음 클라이언트 + 트랙 정렬** | 다중 기기 녹음 동작 | ⭐ 신설 |
| 8 | STT (Qwen3-ASR) + 화자 처리 (모드 A/B) | 화자별 회의 대본 | |
| **9** | **발언 유형 데이터 라벨링 + KLUE-RoBERTa 파인튜닝** | 분류 모델 + 성능 지표 | 명시 |
| 10 | LLM 업무·결정 추출 (guided decoding) | 회의 분석 모듈 | |
| **11** | **회의 업무 후보 → 승인 → 칸반 연동** | ⭐ **핵심 통합 흐름** | 유지 |
| 12 | 기여도 이벤트 로그 + 항목별 지표 + 근거 링크 | 분석 대시보드 | |
| 13 | 주간·최종 보고서, 교수자 화면 | 보고서·관리 화면 | |
| 14 | **합성 회의 녹음 + 평가 실행** (CER/DER/F1/IoU) | 평가 결과 | 구체화 |
| 15 | 배포, 시연 데이터, 발표자료 | 배포본, 시연본 | |
| 16 | 오류 수정, 최종 보고서, 발표 | 최종 산출물 | |

**여유 확장 슬롯**: 11주까지 핵심 통합 흐름이 완성되면, 12~13주에 반복 논의 탐지를 끼워 넣습니다.
완성되지 않았으면 넣지 마세요. **11주차가 이 프로젝트의 생사를 가릅니다.**

---

## 5. 완성 기준

제안서 14장의 문장을 그대로 유지합니다. 이보다 나은 정의가 없습니다.

> "가장 중요한 완성 기준은 **'회의에서 나온 업무가 실제 칸반과 GitHub 활동으로 이어지는 흐름'**이다."

이 한 흐름이 끝까지 동작하면, 나머지 기능이 절반만 되어도 프로젝트는 성공입니다.
반대로 개별 AI 기능이 다 좋아도 이 연결이 없으면 "회의록 만드는 툴"에 그칩니다.

### 5.1 데모 성공 조건 (18장 시연 시나리오 기준)

```
[필수 경로]
회의 녹음 → 화자별 자막 → "민수가 금요일까지 로그인 API 구현" 업무 후보 생성
        → 승인 → 칸반에 등록 → 관련 PR 병합 → 업무 카드에 수행 근거 표시
        → 기여도 화면에 근거와 함께 반영
```

이 경로가 **끊김 없이** 돌아가는지가 유일한 성공 기준입니다.
시연 전에 이 경로만 20번 반복 테스트하세요.

---

## 6. 팀 규모별 조정

제안서 16장의 조언을 유지하고 구체화합니다.

| 팀 규모 | 조정 |
|---|---|
| 4인 | 제안서 16장 역할 분담 그대로. 직접 학습 1개(발언 유형) |
| 3인 | 백엔드 + GitHub 분석 통합. 교수자 화면 제거. 모드 B(단일 마이크) 제거하고 멀티트랙만 |
| 2인 | 직접 학습 제거하고 전부 사전학습 모델 + 규칙. 위험 예측·보고서 자동생성 제거. **핵심 통합 흐름에만 집중** |

2인이면 "AI 학습"을 포기하는 게 맞습니다.
**"오픈 웨이트 모델을 자체 서빙해 회의-업무-코드를 연결했다"**만으로도 충분한 졸업작품입니다.
어설픈 학습 결과를 붙이는 것보다 통합 흐름의 완성도가 높은 게 낫습니다.
</content>
</invoke>
