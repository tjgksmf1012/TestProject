# 요구사항 정의서 ↔ 저장소 대조

받은 「AI 기반 팀 프로젝트 협업 플랫폼 요구사항 정의서」의 요구 ID 를 지금
코드와 하나씩 대조한 것입니다.

## ⚠️ 이 문서를 만든 이유

**"얼마나 만들어졌나" 에 아무도 답할 수 없었습니다.** README 의 현황표는
저장소가 스스로 정한 항목으로 되어 있고, `docs/08` 로드맵은 요구사항
정의서보다 먼저 쓰였습니다. 요구 ID 로 물으면 코드를 다 읽어야 답이 나왔고,
그래서 **주석이 선언한 것을 구현으로 착각하는 일**이 실제로 있었습니다
(아래 §4 결함 122).

## ⚠️ 숫자를 손으로 적지 않았습니다

이 저장소는 문서가 사실과 다른 곳을 열세 군데 발견한 적이 있습니다. 그래서
**개수와 "있음" 주장은 테스트가 다시 셉니다** — `backend/tests/test_requirements_map.py`.
아래 표의 `근거` 칸에 적힌 경로가 없어지면 테스트가 터집니다.

---

## 1. 한눈에

| 영역 | 요구 | 상태 |
|---|---|---|
| §4 회원·사용자 | USER-001~005 | ✅ 다섯 다 (프로필 이미지는 업로드 통로 없이 — §5) |
| §5 프로젝트 | PROJECT-001~006 | 🟡 이메일 초대만 없음 (§2) |
| §6 채널 관리 | CHANNEL-001~005 | ✅ |
| §7 실시간 채팅 | CHAT-001~010 | 🟡 업로드 둘 빼고 (§5) |
| §8 음성 회의 | MEETING-001~007 | ✅ |
| §9 음성 인식·전처리 | AI-AUDIO-001~007 | 🟡 모델 미실행 (비중은 §14 검토 화면에) |
| §10 발언 유형 | AI-SPEECH-001~010 | 🟡 열 가지 다 있음 · 분류는 규칙 기준선 |
| §11 음성·억양 | AI-VOICE-001~006 | ⛔ **일부러 범위에서 뺌** (§2) |
| §12 비효율 회의 | AI-REVIEW-001~009 | 🟡 탐지기 다섯 + 발언 편중 · 규칙 기준선 (§4) |
| §13 회의 결과 생성 | AI-RESULT-001~007 | ✅ |
| §14 회의 리뷰 화면 | REVIEW-001~006 | 🟡 분석 마커만 규칙 기준선 — 나머지 다섯은 ✅ |
| §15 업무 관리 | TASK-001~009 | ✅ 전부 (담당자는 여럿 — `task_assignees` · 우선순위 4단계 2026-08-19) |
| §16 일정 관리 | CALENDAR-001~005 | ✅ **달력은 읽어서 만듭니다** (§5) |
| §17 GitHub | GITHUB-001~008 | 🟡 실측 미검증 |
| §18 프로젝트 분석 | ANALYTICS-001~006 | 🟡 신호 다섯 · 재배정 제안은 **일부러 뺌** (§2) |
| §19 알림 | NOTIFICATION-001~006 | ✅ 여섯 다 (파생 둘은 안 쌓음) |
| §20 검색 | SEARCH-001~005 | ✅ (GitHub 은 원문 제외) |
| §21 활동 기록 | ACTIVITY-001 | ✅ **쓰기 13곳 ↔ 읽는 화면 1곳** |
| §22 화면 구성 | UI-001~007 | ✅ |
| §25 비기능 | NFR-001~006 | 🟡 외부 도구 연동 없음 |

### ⚠️ 제일 먼저 알아야 할 것 — **이 진단은 해소됐습니다 (2026-08-13)**

이 문서를 처음 쓴 2026-08-12 에는 이렇게 적혀 있었습니다.

    1단계 — 회원가입 · 로그인 · 프로젝트 생성 · 팀원 초대 ·
            텍스트 채널 · 실시간 채팅 · 업무 관리 · Kanban Board

    이 중 **텍스트 채널과 실시간 채팅이 0%** 입니다. 표도, API 도, 화면도
    없습니다. 반면 3~5단계는 깊이 만들어져 있습니다. 즉 단계 순서와
    실제 구현 순서가 반대입니다.

**그날 안에 그 순서를 되돌렸습니다.** 지금 1단계 여덟 항목은 전부
구현돼 있습니다 — 표(`channels`·`messages`·`message_mentions`·
`message_reactions`) · 실시간 전송(`WS /api/channels/{id}/stream` +
`teamflow/chat/hub.py`) · 화면(`chat.html` + `chat.tsx`). §5 표가 근거입니다.

⚠️ **이 문단을 지우지 않은 이유** — 이 진단이 아래 §5 의 순서를 정했고,
무엇을 왜 먼저 했는지가 사라지면 다음 사람이 같은 판단을 다시 하게 됩니다.
다만 **지금을 주장하는 문장으로 읽히면 안 되므로** 인용으로 내렸습니다.

---

## 2. 다르게 만든 것 — 그리고 그 이유

요구사항 정의서와 저장소가 다른 곳입니다. **되돌리기 전에 이유를 보십시오.**
전부 근거가 문서에 남아 있고, 모르고 "고치면" 되살아나는 문제들이 있습니다.

| 요구 | 정의서 | 지금 | 왜 |
|---|---|---|---|
| STT | Whisper | **Qwen3-ASR-1.7B** | 한국어 성능·VRAM (`docs/02`) |
| 화자 구분 | pyannote | **멀티트랙(트랙=사람)** 우선 | pyannote DER 17~20% 가 그대로 기여도 오차가 됩니다 (`docs/04` §2). pyannote 는 단일 파일(모드 B)용으로 남습니다 |
| 음성 회의 | LiveKit | **WebRTC 메시 직접** | 비용 0원 제약. 5명까지 (`docs/15` §3.1) |
| 프런트 | Vite·Zustand·TanStack·shadcn·Recharts·dnd-kit | **esbuild + React + Radix + Tailwind** | 런타임 의존성 3개로 묶었습니다. 졸업작품이 끝난 뒤에도 열려야 합니다 (`docs/19` §24) |
| 상태 관리 | Zustand | **`useState` + lib 함수** | 판단은 `src/lib/**` 에 있고 화면은 그리기만 합니다 — 화면 코드에는 자동 테스트가 없습니다 |
| 차트 | Recharts | **없음 (글자와 점)** | ⚠️ 아래 §3 |

### ⛔ 일부러 범위에서 뺀 것 — §11 음성·억양 분석

`AI-VOICE-001~006`(말하기 속도·음량·음높이·억양·말버릇)은 `docs/08` §2 에서
**범위 제거**로 결정했습니다. 이유 둘:

1. **평가할 방법이 없습니다.** 정답 데이터가 없어 "맞았는지" 를 잴 수 없습니다
2. **감정 해석 위험.** 정의서 §11 머리말도 "감정이나 성격을 단정하는 용도로
   사용하지 않는다" 고 못 박고, NFR-005 도 같은 말을 합니다. 그런데 억양
   변화를 사람 옆에 적어 두면 **읽는 사람이 알아서 해석합니다** — 도구가
   금지해도 표는 이미 그 자리에 있습니다

되살리려면 평가셋부터 만들어야 합니다. 지금 켜면 근거 없는 숫자가 사람 옆에
붙습니다.

### ⛔ 일부러 범위에서 뺀 것 — 제안서 §4.5 의 **재배정·일정 조정 제안**

제안서 §4.5 는 위험 신호 다섯을 나열하고 마지막에 **"근거 기반 재배정 및
일정 조정 제안"** 을 넣었습니다. 앞의 넷은 만들었고 이것만 안 만들었습니다.
**2026-08-18 팀 결정으로 계획에서 뺐습니다** — 아래가 그 근거입니다.

`"김민수 님의 업무를 이하늘 님에게 넘기세요"` 는 **사람에 대한 판정**이고,
그중에서도 제일 무거운 쪽입니다 — 누가 못 하고 있다는 말이 되니까요.
불변식 4("시스템은 판정하지 않습니다")를 정면으로 어깁니다.

그래서 서버는 **사실만 보냅니다**: 누가 몇 건을 맡고 있고 팀 전체가 몇
건인지. 어떻게 할지는 팀이 정합니다. `backend/tests/test_project_analytics_api.py`
가 응답에 `재배정`·`권장`·`넘기` 같은 말이 섞이면 실패합니다.

⚠️ 같은 이유로 **적게 맡은 사람은 이름이 안 나갑니다.** 한 사람에게 몰린
것을 알리는 신호가 "덜 맡은 사람" 을 같이 실으면 그건 저성과자 표시가 됩니다.

---

## 3. ⚠️ 정의서 안에서 서로 어긋나는 곳 — 발언 비중

`AI-AUDIO-005` 의 예시는 이렇게 생겼습니다.

```
윤식 32%
민수 27%
지연 25%
철수 16%
```

**내림차순으로 늘어놓은 이 모양이 곧 리더보드입니다.** 그런데 같은 문서가
두 곳에서 반대로 말합니다.

* `AI-REVIEW-007` — "발언 비중은 사용자의 프로젝트 기여도를 판단하는 기준으로
  직접 사용하지 않는다"
* `NFR-005` — 단정적 평가를 금지하며 **올바른 예**로 "이번 회의에서 철수의
  발언 비중은 전체 발언의 8%였다" 를 듭니다

읽으면 뜻은 분명합니다. 요구는 **값을 만들라**는 것이지 **줄을 세우라**는
것이 아닙니다. 예시의 생김새만 리더보드였습니다.

그래서 이렇게 정했습니다 (`backend/teamflow/meeting/speaking.py`):

* 값은 **받은 순서 그대로** 돌려줍니다. 정렬하지 않습니다 — 정렬해서 주면
  부르는 쪽은 그게 뜻있는 순서라고 믿습니다
* 화면에 그릴 때는 **이름 순 · 값은 글자로**. 같은 축 위에 폭이나 위치로
  그리면 그 순간 순위표입니다 (이 저장소가 두 번 낸 결함)
* **기여도 점수에 넣지 않습니다.** `docs/05` §2.2 가 "총 발언 시간 = 점수 아님
  (참고 표시만)" 으로 이미 정해 뒀습니다

---

## 4. 이번 대조에서 나온 결함

### ⭐ 결함 121 — `speaking_ratio` 가 언제나 `0.0`

`TrackStats.speaking_ratio` 가 `return 0.0` 으로 박혀 있었고 부르는 곳은
0곳이었습니다.

**구조적으로 계산이 불가능한 자리였습니다** — 비중에는 분모(팀 전체 발언
시간)가 필요한데 그 dataclass 는 자기 트랙만 압니다. 스텁을 넣어 둔 것으로
보이는데, **`0.0` 은 "안 쟀다" 가 아니라 "한마디도 안 했다" 로 읽힙니다.**
이 저장소가 제일 하면 안 된다고 정한 것(측정 불가 ≠ 0점)을 정면으로
어기는 값입니다.

목록 전체를 받는 함수로 옮겼습니다(지금은 `speaking.shares()`). **아무도 말하지 않은
회의는 `None`** 입니다 — 분모가 0이면 비중이라는 것이 존재하지 않고, 그때
0.0 을 돌려주면 "다들 0% 말했다" 는 **잰 값**처럼 보입니다.

### ⭐ 결함 122 — 주석이 탐지기 다섯을 선언하는데 **넷은 코드가 0곳**

`meeting_events.event_type` 의 주석이 이랬습니다.

```
# repeated_discussion | unanswered_question | incomplete_task |
# topic_drift | decision_conflict
```

CHECK 제약은 없었고, **실제로 만들어지는 것은 `unanswered_question` 하나**
뿐이었습니다. 나머지 넷이 정의서의 `AI-REVIEW-001`(반복 논의)·`003`(주제
이탈)·`004`(미완성 업무)·`006`(결정 번복)이라, 이 주석이 **요구가 이미
구현된 것처럼** 보이게 만들고 있었습니다.

**같은 결함의 세 번째 사례입니다.** `speaker_source` 는 값이 다섯 곳에서
갈라져 있었고(결함 118), `report_type` 은 주석 한 줄이 전부였으며(결함 119),
이 열은 **둘 다**였습니다.

`db/vocab.py` 로 옮기고 `EVENT_PRODUCED` / `EVENT_NOT_PRODUCED_YET` 으로
갈랐습니다. ⭐ 그리고 테스트가 **"만들어진다고 적은 값에 진짜 코드가 있는지"**
를 소스에서 다시 셉니다 — 반대 방향(만든다고 적었는데 없음)이 바로 이
저장소의 대표 실패 ① 이고, 오래 안 들킨 이유는 **아무도 대조해 보지 않아서**
였습니다.

---

## 5. 요구별 대조

⚠️ `근거` 는 **그 요구를 실제로 하는 코드**입니다. 표만 있고 쓰는 코드가
없으면 ❌ 로 적습니다 — 그게 이 저장소의 대표 실패 ① 입니다.

### §4 회원 및 사용자 관리

| 요구 | 상태 | 근거 · 비고 |
|---|---|---|
| USER-001 회원가입 | ✅ | `services/auth_service.py` · scrypt |
| USER-002 로그인 | ✅ | 세션 쿠키. 구성원 확인은 모든 조회가 지납니다 |
| USER-003 로그아웃 | ✅ | `src/demo/logout.ts` |
| USER-004 프로필 관리 | ✅ | 이름·GitHub 계정 + 프로필 이미지·자기소개 (2026-08-17) — 홈 「내 정보」 에서 적고 팀원 목록에 보입니다. 이미지는 화면이 96×96 PNG 로 재부호화한 데이터 URI 를 **행에** 저장 — 파일 업로드 통로가 아니고, 원본의 EXIF(찍은 위치)도 안 남습니다. 검증은 `users/profile.py`(PNG 시그니처·치수·크기) |
| USER-005 사용자 상태 표시 | ✅ | 접속 중·자리 비움·오프라인·회의 중. ⚠️ **아무것도 저장하지 않습니다** — 세션의 마지막 요청 시각과 열린 회의 트랙에서 읽을 때 계산합니다(`backend/teamflow/users/presence.py`). 상태를 행으로 쌓으면 그 표는 곧 **출퇴근부**입니다. 시각도 화면에 안 보냅니다(`마지막 접속 3일 전` 은 상태가 아니라 근태 기록) |

### §5 프로젝트 및 워크스페이스

| 요구 | 상태 | 근거 · 비고 |
|---|---|---|
| PROJECT-001 생성 | ✅ | `public/home.html` |
| PROJECT-002 팀원 초대 | 🟡 | **초대 코드만.** 이메일 초대는 없음 (메일 발송 경로 자체가 없습니다) |
| PROJECT-003 팀원 관리 | ✅ | 조회·합류·**내보내기·권한 변경**. `DELETE /members/{id}` · `PATCH /members/{id}/role` |
| PROJECT-004 권한 3단계 | ✅ | `members.project_role` (CHECK 제약 포함). 판단은 `backend/teamflow/projects/permissions.py` **표 하나**에만. ⚠️ `role_shares` 는 여전히 **기여도 가중치**지 권한이 아닙니다 — 칸을 따로 뒀습니다 |
| PROJECT-005 정보 수정 | ✅ | `public/project.html` |
| PROJECT-006 나가기 | ✅ | `POST /members/me/leave`. ⚠️ **권한을 안 봅니다** — 나가는 것은 자기 일입니다. 다만 **마지막 소유자는 못 나갑니다**(소유자 0명이면 아무도 팀원을 못 다룹니다). 맡은 일은 그대로 남습니다 |

### §6 채널 관리 · §7 실시간 채팅

| 요구 | 상태 | 근거 |
|---|---|---|
| CHANNEL-001 텍스트 채널 생성 | ✅ | `channel_service.create_channel` · `public/chat.html` |
| CHANNEL-002 음성 채널 생성 | ✅ | 같은 곳. **방 이름**이고 회의는 그 안의 사건입니다 (`meetings.channel_id`) |
| CHANNEL-003 이름 변경 | ✅ | `rename_channel` |
| CHANNEL-004 삭제 | ✅ | `archive_channel` — **행을 안 지웁니다.** 채널을 지웠다고 남이 쓴 말이 사라지면 안 됩니다 |
| CHANNEL-005 순서 변경 | ✅ | `reorder_channels` — **전체 순서**를 받습니다 (하나 빠진 목록은 그 채널의 자리를 없앱니다) |
| CHAT-001 실시간 송수신 | ✅ | POST + `WS /api/channels/{id}/stream`. **소켓은 읽기 전용**입니다 |
| CHAT-002 수정 | ✅ | `edited_at` 이 **반드시** 남습니다 |
| CHAT-003 삭제 | ✅ | `deleted_at` — 답글이 가리킬 자리는 남습니다 |
| CHAT-004 답글 | ✅ | `reply_to_id`. 다른 채널의 글은 못 끌어옵니다 |
| CHAT-005 멘션 | ✅ | **서버가 본문에서 뽑습니다** (`chat/mentions.py`). 화면이 목록을 보내면 아무에게나 알림을 쏠 수 있습니다 |
| CHAT-006 이미지 업로드 | ❌ | **안 만들었습니다** — 아래 이유 |
| CHAT-007 파일 업로드 | ❌ | 같음 |
| CHAT-008 이모지 반응 | 🟡 | 있는데 **자유 이모지가 아니라 정해진 넷**입니다 — 아래 이유 |
| CHAT-009 히스토리 | ✅ | `before_id` 로 거슬러 올라갑니다. **번호**로 자릅니다(시각으로 자르면 같은 초의 메시지가 새거나 겹칩니다) |
| CHAT-010 검색 | ✅ | 프로젝트 안에서만. 지운 것은 안 나옵니다 |

#### CHAT-008 을 왜 이름으로 만들었는가

정의서는 "이모지 반응" 이라고 적었지만 저장하는 값은 `ok`·`agree`·
`question`·`thanks` 라는 **이름**이고, 그림은 화면이 SVG 로 그립니다
(`lib/nav/icons.ts`). 두 가지 이유이고 둘 다 이 저장소가 이미 겪은 것입니다.

1. **색 이모지는 화면에 못 나갑니다.** `guards.test.ts` 가 막습니다 — 기기
   마다 다른 그림이 나오고, 색이 박혀 있어 어두운 모드를 안 따라가고,
   베이스라인이 서체에 딸려 있어 세로 정렬이 틀어집니다. 칸반 카드의 `🗣`
   를 SVG 로 바꾼 것이 그 규칙이 생긴 자리입니다
2. **자유 입력이면 조롱 통로가 됩니다.** 반응은 **남이 쓴 글 위에** 붙고,
   이 제품은 성적에 쓰일 수 있는 기여도를 다룹니다(`docs/07`). 지금 이
   저장소에는 신고도 차단도 없습니다 — 없는 것을 전제로 통로부터 열지
   않습니다. 넷 다 중립이거나 긍정이고, **부정 반응은 일부러 없습니다**

#### CHAT-006·007 을 왜 미뤘는가

파일 업로드는 "저장 통로" 하나가 아니라 **저장 자리 + 인증이 붙은
내려받기 + MIME 판별 + 크기 상한 + 보존 기간**이 한 벌로 필요합니다.
이 중 하나라도 빠지면 남의 팀 파일이 주소만 알면 열리는 통로가 됩니다.
`audio/chunk_store.py` 가 이미 비슷한 문제를 풀고 있으므로 그것을 넓히는
쪽이 맞고, 그건 이 작업과 별개 크기입니다. **반쯤 만들어 두지 않습니다.**

⚠️ 왼쪽 열의 "회의 채널" 은 여전히 **회의 목록**입니다
(`lib/nav/channels.ts`) — 텍스트 채널은 `chat.html` 에 있습니다. 둘이 같은
말로 불리므로 여기 적어 둡니다.

### §8 음성 회의

| 요구 | 상태 | 근거 |
|---|---|---|
| MEETING-001~005 | ✅ | `teamflow/call/` · `public/call.html` · WebRTC 메시 5명 |
| MEETING-006 녹음 + 동의 | ✅ | 3단계 동의 (`docs/07` P3) · 동의 없이는 시작 못 함 |
| MEETING-007 회의 정보 저장 | ✅ | `meetings` · `meeting_tracks` |

### §9 음성 인식 및 전처리

| 요구 | 상태 | 비고 |
|---|---|---|
| AI-AUDIO-001 음성 인식 | 🟡 | 인터페이스·파이프라인 완성. **모델 미실행**(GPU 없음). `ASR_BACKEND=fake` 로 검증 |
| AI-AUDIO-002 화자 구분 | ✅ | 멀티트랙은 트랙=사람이라 **확정** |
| AI-AUDIO-003 화자↔사용자 연결 | ✅ | `speaker_source` 가 어떻게 정해졌는지까지 남깁니다 (결함 118) |
| AI-AUDIO-004 발언 시간 | 🟡 | `multitrack.track_stats` — 계산은 되는데 **저장·노출 안 됨** |
| AI-AUDIO-005 발언 비중 | ✅ | `GET /api/meetings/{id}/speaking` + 검토 화면. 발화 구간에서 **읽을 때 계산**합니다(저장 안 함 — 발화를 지우면 비중도 사라져야 합니다). §3 을 지킵니다: 이름 순 · 값은 글자 · 막대 없음 |
| AI-AUDIO-006 동시 발언 | ✅ | `multitrack` 겹침 구간 |
| AI-AUDIO-007 침묵 구간 | ✅ | `TrackAnalysis` |

### §10 발언 유형 분석

요구 열 가지가 **각각 제 라벨**을 가집니다. 값은 `db/vocab.py` 의
`UtteranceType` 한 곳에 있고 CHECK 제약이 그것을 그대로 씁니다.

| 요구 | 라벨 | |
|---|---|---|
| AI-SPEECH-001 질문 | `question` | ✅ |
| AI-SPEECH-002 제안 | `proposal` | ✅ |
| AI-SPEECH-003 정보 제공 | `answer` | ✅ |
| AI-SPEECH-004 동의 | `agreement` | ✅ |
| AI-SPEECH-005 반대 의견 | `objection` | ✅ |
| AI-SPEECH-006 보완 의견 | `refinement` | ✅ |
| AI-SPEECH-007 결정 | `decision` | ✅ |
| AI-SPEECH-008 업무 요청 | `request` | ✅ |
| AI-SPEECH-009 일정 약속 | `commitment` | ✅ |
| AI-SPEECH-010 확인 요청 | `confirmation` | ✅ |
| — | `opinion` · `social` · `other` | 요구에 없는 셋 |

#### ⭐ 가르되 **값은 같게** 뒀습니다

동의·반대·보완은 **점수가 똑같습니다**(`scoring.py` 1.0). 가른 것은
**세기 위해서**지 값을 매기기 위해서가 아닙니다.

> 반대에 더 주면 어깃장이 이득이 되고, 동의에 더 주면 반대가 손해가
> 됩니다. 어느 쪽이 더 값진가는 **시스템이 정할 일이 아닙니다.**

`test_taking_a_side_costs_nothing` 이 넷의 값이 갈라지는 것을 막습니다.

#### ⚠️ `opinion` 을 안 지웠습니다

요구가 열 개라고 열한 번째를 지우면, 지금 1.0 으로 세어지던 "어느 쪽도
아닌 의견" 이 `other`(0점)로 떨어집니다. **요구에 없다는 것과 없어도
된다는 것은 다릅니다.**

#### ⚠️ 맨몸 찬반은 0점입니다

`"동의합니다."` 하나만 있으면 `social`(0점)입니다 — `docs/05` §2.2 가
"단순 동의·맞장구는 0점" 이라고 정해 뒀기 때문입니다. **`"반대합니다."`
도 똑같이 0점입니다.** 동의만 0점으로 두면 반대가 이득이 되고, 그
순간 시스템이 어깃장에 값을 매기는 것이 됩니다.

⚠️ 그래서 `REVIEW-005` 가 세는 것은 **내용이 있는** 동의·반대입니다.
화면이 그렇게 말해야 합니다 — 안 그러면 "나는 동의했는데 왜 0이냐" 가
됩니다.

#### ⭐ `REVIEW-005` 를 **사람별로 안 만들었습니다**

정의서는 "발언 통계" 라고만 합니다. 회의 단위로 셉니다.

사람별로 세면 그 순간 **"누가 제일 많이 제안했나" 표**가 만들어집니다.
발언을 많이 한 것이 기여가 아니라는 것은 `docs/05` §2.2 가 원본 대화에서
그대로 가져온 경고이고, 순위표 금지는 이 저장소의 불변식 1 입니다.

⚠️ 막는 자리를 **화면이 아니라 API** 에 뒀습니다. 화면 코드에는 자동
테스트가 없으므로, 서버가 사람별 건수를 주면 언젠가 화면이 그걸로 표를
만듭니다. `GET /api/meetings/{id}/utterance-types` 는 `{labels, unclassified,
total}` 만 돌려주고, `test_the_counts_never_name_a_person` 이 응답을 통째로
훑어 사람 이름·id 가 섞이는 것을 막습니다.

같은 이유로 **막대를 안 그립니다.** 값을 같은 축 위에 세로로 늘어놓으면
그게 곧 순위표입니다 — 값은 글자로 적습니다.

#### 남은 것

분류는 여전히 **규칙 기준선**입니다(`CLASSIFIER_RULES`). KLUE-RoBERTa
파인튜닝은 GPU 가 있어야 하고, 지금 규칙이 그 실험의 비교 대상입니다.

⚠️ 그리고 규칙은 **틀립니다.** 그래서 검토 화면의 근거 발화마다 유형을
같이 보여 줍니다 — 잘못 매겨진 것을 사람이 볼 수 없으면 영영 안
고쳐집니다. 그 자리가 오랫동안 비어 있었습니다(서버는 `utterance_type` 을
보내고 있었는데 화면이 안 썼습니다 — 대표 실패 ①).

### §12 비효율적 회의 분석

| 요구 | 상태 | 비고 |
|---|---|---|
| AI-REVIEW-001 반복 논의 | 🟡 | `repeated_discussion` — 어휘 겹침 기준선. 떨어진 두 덩어리가 낱말 셋 이상 겹치면 |
| AI-REVIEW-002 장시간 미결정 | ⛔ | **계획에서 뺌 (2026-08-18 팀 결정)** — 관찰이 아니라 평가라서. 아래 |
| AI-REVIEW-003 주제 이탈 | 🟡 | `topic_drift` — 본줄기에서 **샜다가 돌아온** 구간만 |
| AI-REVIEW-004 미완성 업무 | 🟡 | `incomplete_task` — 약속이 **후보로 안 이어진** 것. 담당자·마감일이 빈 것은 승인 화면이 봅니다 (두 벌 방지) |
| AI-REVIEW-005 미응답 질문 | ✅ | `unanswered_question` — 유일하게 LLM 이 만드는 것 |
| AI-REVIEW-006 결정 번복 | 🟡 | `decision_conflict` — `supersedes_id` + 어휘 겹침. 그 칸은 **채워져도 읽는 곳이 0곳**이었습니다 |
| AI-REVIEW-007 발언 편중 | ✅ | 같은 엔드포인트의 `skewed`. ⚠️ **참/거짓 하나이고 누가인지는 안 보냅니다** — 이름을 실으면 화면이 적고, 그 순간 "이 회의를 독점한 사람" 표시가 됩니다. 화면 문구도 나무라지 않습니다(발제하는 자리였다면 자연스러운 모습입니다) |
| AI-REVIEW-008 동시 발언 증가 | ✅ | `overlap_surge` — 2분 구간으로 잘라 **바탕 대비 2배 이상**이고 겹침이 10초 이상인 구간만 (`meeting/speaking.py::overlap_windows`). 겹침 자체는 진작 탐지하고 있었고(`multitrack.is_overlap`) 없던 것은 **"늘었다" 는 판정**이었습니다. ⚠️ **누가 겹쳤는지는 안 보냅니다** — 이름을 실으면 화면이 적고 그 순간 "말 끊은 사람" 표시가 됩니다(`AI-REVIEW-007` 과 같은 원칙). ⚠️ 바탕이 이미 50% 이상이면 아무 말도 안 합니다 — 회의 내내 겹쳤으면 그건 구간의 문제가 아닙니다. `severity` 는 `info` — 격론이 벌어진 구간은 회의에서 제일 중요한 자리일 수 있습니다 |
| AI-REVIEW-009 핵심 구간 | ❌ | 없음 |

#### ⚠️ 다섯은 **규칙 기준선**입니다 — 임베딩이 아닙니다

계획(`docs/08` §9주차)은 문장 임베딩인데 GPU 가 없고 외부 생성형 API 도
못 씁니다. 그래서 **어휘 겹침**으로 만들었습니다. 임시방편이 아니라
논문의 비교 대상이기도 합니다 — "임베딩이 어휘 겹침 대비 얼마나 나은가"
를 말하려면 어차피 필요합니다.

⚠️ **그래서 틀립니다.** 좁게 잡고, 근거를 달고, 등급을 안 매깁니다.

#### ⭐ 회의를 빨갛게 칠하지 않습니다

`severity` 는 **전부 `info`** 이고 API 는 그 값을 아예 안 내보냅니다.
규칙 기반 추정에 등급을 매기는 순간 **팀에 대한 판정**으로 읽힙니다
(불변식 4). 화면도 빨강을 안 씁니다 — 이 저장소에서 빨강은 "네가 뭘
잘못했다" 입니다.

그리고 **사람을 안 싣습니다.** 결과에 화자가 없습니다. 있으면 화면이
그걸로 "누가 회의를 늘어지게 했는가" 를 만들 수 있습니다.

#### `AI-REVIEW-002`(장시간 미결정)를 왜 안 만들었나

**2026-08-18 팀 결정으로 계획에서 뺐습니다.** 아래는 그 결정의 근거입니다.

`meeting_events` 는 **구간**을 가리키는 표입니다. "오래 끌었는데 결론이
안 났다" 는 구간이 아니라 **회의 전체에 대한 서술**이고, 그걸 구간으로
욱여넣으면 "회의 시작부터 끝까지" 라는 무의미한 근거가 붙습니다.

⚠️ 그리고 이건 **판정에 가깝습니다.** "이 회의는 오래 끌었다" 를 시스템이
말하면 그건 관찰이 아니라 평가입니다. (`AI-REVIEW-009` 핵심 구간은
성격이 달라 — 판정이 아니라 선별이라 — 계획에서 빼지 않았고, 만든다면
회의 요약 쪽에서 다룹니다.)

### §13 회의 결과 생성

| 요구 | 상태 | 근거 |
|---|---|---|
| AI-RESULT-001 요약 | ✅ | `meetings.summary` |
| AI-RESULT-002 주요 안건 | ✅ | LLM + 검증 |
| AI-RESULT-003 결정 사항 | ✅ | `decisions` |
| AI-RESULT-004 미해결 안건 | ✅ | `meeting_events` |
| AI-RESULT-005 Action Item | ✅ | `meeting_task_candidates` |
| AI-RESULT-006 승인 후 등록 | ✅ | **자동 등록 안 함** — `public/review.html`. 이 제품의 대표 주장 |
| AI-RESULT-007 다음 회의 안건 | ✅ | `meetings.next_agenda` |

### §14 회의 리뷰 화면

| 요구 | 상태 | 비고 |
|---|---|---|
| REVIEW-001 기본 정보 | ✅ | |
| REVIEW-002 타임라인 | ✅ | 검토 화면 「타임라인」 (2026-08-15) — 발화 전체 시간 순 + 비효율 구간 머리말이 그 시각 자리에. **사람별 줄 없음** — 가르면 발언량 그림이 됩니다 |
| REVIEW-003 AI 분석 마커 | 🟡 | 미해결 사안 + §12 탐지기 다섯 — 타임라인에도 구간 머리말로 섭니다 |
| REVIEW-004 구간 재생 | ✅ | 발화 옆 듣기 버튼 (2026-08-15) — 트랙 원본(청크 이어붙임) 스트리밍 + **공백 보정 위치**(`audio/playback.py`). 소리가 보관된 회의만, 아니면 화면이 이유를 말함 |
| REVIEW-005 발언 통계 | ✅ | 검토 화면 "무슨 말이 오갔나". ⚠️ **회의 단위이고 사람별이 아닙니다** — 아래 이유 |
| REVIEW-006 회의 개선 추세 | ✅ | 프로젝트 화면 「회의에서 눈에 띈 것의 추세」 (2026-08-17) — 종류별 앞·뒤 절반 평균과 방향만. **회의별 값·제목·id 는 서버가 안 보냅니다** — 짚는 순간 회의 순위표가 됩니다. 분석된 회의 3개부터 재고, 그 전에는 흙빛으로 이유를 말합니다 |

### §15 업무 관리

| 요구 | 상태 | 비고 |
|---|---|---|
| TASK-001~003 생성·수정·삭제 | ✅ | `DELETE /tasks/{id}`. ⚠️ **행을 안 지웁니다** — 기여 이벤트·PR 연결·회의 후보가 가리키고 있어서 진짜로 지우면 `근거 업무 #7` 이 빈손이 됩니다. `deleted_at` 만 적고, 읽는 일곱 곳이 전부 `backend/teamflow/db/live.py` 를 거칩니다 |
| TASK-004 상태 4개 | ✅ | `todo · in_progress · review · done`. ⚠️ `review` 는 **완료로 안 셉니다**(`vocab.TASK_FINISHED`) — 검토 중인 일을 완료로 세면 진행률이 실제보다 높게 나옵니다 |
| TASK-005 Kanban + D&D | ✅ | 칸반 ✅ / 끌어 옮기기 ✅ (2026-08-15 — 버튼 경로는 **그대로 두고 더함**, 판단은 `lib/kanban/dnd.ts` 가 `nextStatuses` 에 위임) |
| TASK-006 담당자 | ✅ | `task_assignees` 표. **`tasks.assignee_id` 는 없앴습니다** — 칸과 표에 같은 사실이 두 벌로 남으면 갈라지고, 담당자는 기여 이벤트가 갈 사람을 정합니다. ⚠️ 화면 일이 아니라 **기여도 배분** 문제였습니다: 완료 점수를 **완료 이벤트 수**로 나눕니다(`contribution/sharing.py`) — 안 나누면 칸반 드롭다운이 곧 점수 발행기입니다. 마감 준수는 **안 나눕니다**(여부라서 나눌 것이 없음). 몫은 **저장 안 하고 읽을 때** 셉니다 |
| TASK-007 우선순위 4단계 | ✅ | `긴급·높음·보통·낮음` (`vocab.TaskPriority`, **작을수록 급함**). 칸반 카드 배지 + `⋯` 메뉴에서 바꿉니다. ⚠️ 오래 **칸만 있고 아무도 안 읽고 있었습니다** — 검색 API 는 이 값으로 거르기까지 했는데 사람이 정할 자리도 볼 자리도 없었습니다(실패 ①+③). 제약도 없어 `-1`·`99` 가 들어갔고, 그러면 화면이 라벨을 못 찾아 빈 칸을 그립니다 → `ck_task_priority`. ⛔ **기여도에 안 닿습니다** — 닿으면 드롭다운 하나가 점수 발행기가 됩니다(테스트가 지킵니다). `보통` 은 배지를 안 그립니다(넷 중 셋에 배지가 붙으면 배지가 배경이 됨) |
| TASK-008 지연 표시 | ✅ | |
| TASK-009 출처 회의 표시 | ✅ | 이 제품의 대표 주장이 보이는 자리 |

### §16 일정 관리 · §19 알림 · §20 검색

| 요구 | 상태 | 근거 |
|---|---|---|
| CALENDAR-001 프로젝트 캘린더 | ✅ | `public/calendar.html` — 월 격자 |
| CALENDAR-002 업무 일정 표시 | ✅ | `tasks.start_date` + `deadline` |
| CALENDAR-003 회의 일정 생성 | ✅ | `meetings.scheduled_at`. **잡아만 두는 것**이고 녹음은 안 열립니다 |
| CALENDAR-004 회의 일정 수정 | ✅ | **이미 연 회의의 시각은 못 고칩니다** — 녹음 트랙과 어긋납니다 |
| CALENDAR-005 프로젝트 마감일 | ✅ | `projects.deadline` |
| NOTIFICATION-001 채팅 멘션 | ✅ | 서버가 본문에서 뽑은 멘션에서 나옵니다 |
| NOTIFICATION-002 업무 배정 | ✅ | 후보 승인이 업무를 만드는 **유일한 자리**에서 |
| NOTIFICATION-003 마감 임박 | ✅ | **저장 안 함** — 3일 앞, 지금 상태에서 |
| NOTIFICATION-004 지연 업무 | ✅ | **저장 안 함** — 끝난 일은 안 나옵니다 |
| NOTIFICATION-005 회의 일정 | ✅ | 30분 전. 주기 작업이 5분마다 훑습니다 |
| NOTIFICATION-006 GitHub | ✅ | `task_link_service._tell_the_assignee` — PR 이 업무에 붙는 순간 **담당자에게만**. ⚠️ 자기가 올린 PR 은 자기에게 안 갑니다. 담당자가 없으면 조용합니다(전원에게 쏘면 PR 하나에 알림이 사람 수만큼 생기고, 그러면 알림을 통째로 무시하게 됩니다). `NOTIFICATION_NOT_PRODUCED_YET` 이 이제 비었고, 비어 있는지를 테스트가 지킵니다 |

#### ⚠️ 마감 알림을 **행으로 쌓지 않습니다**

`due_soon`·`overdue` 는 지금 상태에서 나옵니다. 저장하면 마감일을 미뤘을 때
"곧 마감" 이 남고, 업무를 끝냈을 때 "지연" 이 남습니다 — 알림이 사실이
아니라 **베낀 순간의 사실**을 가리키게 됩니다. CHECK 제약이 그 둘을
아예 거절하고(`test_notifications.py` 가 DB 가 막는 것까지 잽니다),
`test_moving_the_deadline_moves_the_notice` 가 "옮기면 따라오는가" 를
직접 잽니다.

문장도 저장하지 않습니다 — 가리키는 번호만 두고 읽을 때 만듭니다. 업무
이름을 고치면 알림 문장도 따라옵니다.
| SEARCH-001 채팅 검색 | ✅ | `GET /api/projects/{id}/messages/search` |
| SEARCH-002 업무 검색 | ✅ | 이름·담당자·상태·우선순위. 넷을 겹칠 수 있습니다 |
| SEARCH-003 회의 검색 | ✅ | 이름·날짜. **예정된 회의도** 나옵니다 — 이름으로 찾는 사람은 그것이 열렸는지 모릅니다 |
| SEARCH-004 회의 내용 검색 | ✅ | `utterances`. ⚠️ **동의의 산물**이라 지우면 검색에서도 사라집니다 (베껴 둔 색인이 없습니다) |
| SEARCH-005 GitHub 검색 | 🟡 | 저장소·이벤트 종류·참조까지. ⚠️ **원문(`payload`)은 안 훑습니다** — 저장소 설정과 이메일이 검색으로 새어 나갑니다 |

#### ⚠️ `calendar_events` 표를 **안 만들었습니다**

요구하는 다섯 중 넷은 이미 있는 행에서 나옵니다 — 업무 마감일·프로젝트
마감일·회의. 그걸 따로 베껴 담으면 **칸반에서 마감일을 고쳤을 때 달력만
옛 날짜를 말합니다.** 이 저장소가 반복해서 당한 실패 ② 입니다. 달력은
그때그때 읽어서 만들고, `test_calendar_api.py` 가 "마감일을 옮기면 달력이
따라오는가" 를 직접 잽니다 — 베낀 자료를 보고 있으면 그 검사가 터집니다.

예정된 회의도 **`meetings` 행**입니다. `scheduled_at` 이 있고
`started_at` 이 비어 있으면 아직 안 연 회의입니다. 예정을 별도 표에 두면
"그 예정이 이 회의가 됐다" 를 잇는 칸이 또 필요하고, 안 이어진 것들이
쌓입니다.

### §17 GitHub 연동

| 요구 | 상태 | 비고 |
|---|---|---|
| GITHUB-001 계정 연결 | ✅ | |
| GITHUB-002 Repository 연결 | ✅ | 소유권 검증 포함 (결함 34·35) |
| GITHUB-003~005 조회 | ✅ | 활동 화면 GitHub 갈래. ⚠️ **커밋 목록은 일부러 없음** — 커밋은 쪼개기 쉬워 안 세고(`docs/05` §2.1) 코드는 병합된 PR 로만. 화면이 그 이유를 말함 |
| GITHUB-006 업무 ↔ PR | ✅ | `task_github_links` |
| GITHUB-007 Merge 여부 | ✅ | |
| GITHUB-008 활동 통계 | ✅ | **프로젝트 단위** 종류별 건수(어휘 순·글자). ⚠️ 사람별은 일부러 없음 — 기여도 화면이 근거와 함께 담당(두 벌 금지), 사람별 건수는 곧 순위표 |

⚠️ 전부 **실제 GitHub 에 붙여 본 적이 없습니다** — 자격 증명이 없습니다.
코드와 테스트는 있고 멱등·잘림 처리까지 했지만, 실측은 못 했습니다.

### §18 프로젝트 분석 · §21 활동 기록

| 요구 | 상태 | 비고 |
|---|---|---|
| ANALYTICS-001 진행률 | ✅ | `GET /api/projects/{id}/analytics`. `backend/teamflow/projects/risk.py` 가 셉니다. ⚠️ 업무가 없으면 **`0%` 가 아니라 `null`** 입니다 — 측정 불가 ≠ 0점 |
| ANALYTICS-002 마감 대비 완료율 | ✅ | `behind_schedule`. 기간이 70% 지났는데 완료가 그보다 20%p 이상 뒤처지면 |
| ANALYTICS-003 업무 집중 | ✅ | `workload_skew`. ⚠️ **많이 맡은 사람만 이름이 나갑니다** — 적게 맡은 쪽을 지목하면 저성과자 표시가 됩니다 |
| ANALYTICS-004 활동 감소·장기 미완료 | ✅ | `activity_drop`(최근 7일 대 그 앞 7일) · `stale_tasks`(21일) |
| ANALYTICS-005 선행 지연 병목 | ✅ | `blocked_by_late`. **마감이 지난** 선행만 셉니다 |
| ANALYTICS-006 회의 통계 | 🟡 | 발언 유형 통계(`GET /api/meetings/{id}/utterance-types`)와 보고서가 나눠 맡습니다 |
| 제안서 §4.5 다섯째 — 재배정 제안 | ⛔ | **계획에서 뺌 (2026-08-18 팀 결정).** "A 의 업무를 B 에게 넘기세요" 는 사람에 대한 판정이고 불변식 4 를 정면으로 어깁니다. 사실만 내고 어떻게 할지는 팀이 정합니다 |
| ACTIVITY-001 활동 타임라인 | ✅ | `public/activity.html`. **쓰기는 열세 곳**인데 읽는 곳이 0곳이던 자리입니다 (대표 실패 ①) — 이제 읽습니다. `test_requirements_map.py` 가 **방향을 뒤집어** 이제는 "읽는 곳이 있는가" 를 지킵니다 |

### §22 화면 구성 · §25 비기능

| 요구 | 상태 | 비고 |
|---|---|---|
| UI-001~005 레이아웃 | ✅ | 4열 셸 (`docs/19`) |
| UI-006 Dark Mode | ✅ | Layer 1 만 뒤집는 3계층 토큰 |
| UI-007 반응형 | ✅ | PC 우선 + 폰까지 |
| NFR-001 실시간성 | ✅ | 통화는 WebRTC, 채팅은 WebSocket(`/api/channels/{id}/stream`) |
| NFR-002 접근 권한 | ✅ | 모든 조회가 구성원 확인을 지납니다 |
| NFR-003 데이터 보안 | ✅ | scrypt |
| NFR-004 녹음 고지 | ✅ | 3단계 동의 |
| NFR-005 AI 표현 | ✅ | 불변식 넷이 여기 대응합니다. 보고서는 생성기에 박았습니다 (`docs/17` F절) |
| NFR-006 확장성 | 🟡 | 화상·모바일은 길이 있음. Notion·Jira·Calendar 연동 없음 |

---

## 6. 다음에 할 만한 것 — 값어치 순

사람이 정할 일이라 **추천만** 적습니다.

### ✅ 2026-08-12 판 다섯은 전부 끝났습니다

발언 통계 화면 · 반복 논의 탐지 · 활동 기록 화면 · `TASK-004` `review` 열 ·
채팅·채널. **§5 표가 지금 상태의 근거이고, 이 절은 §5 를 따릅니다** — 둘이
어긋나면 §5 가 맞습니다(`test_requirements_map.py` 가 그것을 잽니다).

⚠️ 다섯째(채팅·채널)에는 **"시작하기 전에 이게 이 제품의 차별점인가를 먼저
답할 것"** 이라는 조건이 붙어 있었는데, **그 답을 적지 않고 3시간 45분 뒤에
착수했습니다.** 결과물이 나쁘지는 않지만 절차를 건너뛴 것이므로 남겨 둡니다 —
이 저장소에서 유일한 '치명·확률 높음' 위험이 "범위 과다" 입니다(`docs/09`).

### 지금 남은 것

**없습니다.** 2026-08-12 대조에서 나온 할 일이 여기서 다 끝났습니다.
남는 것은 「계획에서 뺀 것」과 「남겨 둔 선택지」(아래) · 규칙 기준선의
한계(모델은 GPU 가 있어야 — `docs/02`) · 데스크톱 Phase 3~6(`docs/21`) ·
실기기 실측(실제 절전·화면 잠금, 실제 GitHub 연결)입니다.

### ✅ USER-004 는 2026-08-17 에 끝났습니다

홈 「내 정보」 에서 사진과 자기소개를 적고, 각 프로젝트 설정 화면의
팀원 목록에 보입니다. 이미지는 **파일 업로드 통로 없이** 만들었습니다 —
화면이 캔버스로 96×96 PNG 로 재부호화한 것(원본의 EXIF — 찍은 위치 —
가 떨어져 나감)을 `data:image/png;base64,…` 글자로 받아 `users` 행에
저장하고, 서버(`users/profile.py`)가 그 재부호화를 믿지 않고 PNG
시그니처·치수·크기를 바이트로 다시 봅니다. SVG 는 형식부터 거절합니다
(문서로 열리는 순간 스크립트가 도는 유일한 이미지 형식). 나갈 때는
이미 인증이 걸린 JSON 응답(`MeOut`·`MemberOut`)에 실려 나가므로 안
잠긴 내려받기 문이 없습니다. `CHAT-006·007` 업로드를 안 만든 이유는
그대로 유효합니다 — 그쪽은 임의 파일이라 이 길이 없습니다.

### ✅ TASK-005 · REVIEW-002 · REVIEW-004 는 2026-08-15 에 끝났습니다

§5 표 참고. 드래그 앤 드롭은 버튼 경로를 지우지 않고 **더했고**(끌기는
마우스 지름길, 키보드·터치·낭독기의 길은 버튼), 그 짝을 `guards.test.ts`
가 잽니다 — 판단은 `lib/kanban/dnd.ts` 가 `nextStatuses` 에 위임.
타임라인·구간 재생은 발화 시각이 아니라 **공백을 보정한 소리 위 위치**로
틉니다(`audio/playback.py` — 이어 붙인 원본에는 공백이 없어 뒤가
앞당겨져 있습니다). 소리가 없는 회의는 화면이 이유를 말합니다.

### ✅ REVIEW-006 은 2026-08-17 에 끝났습니다

"이 회의가 저 회의보다 낫다" 가 아니라 "이 팀에서 이런 구간이 줄고 있다"
로 만들었습니다. 서버(`meeting/trends.py` · `trend_service`)는 종류별
앞쪽 절반·최근 절반 평균과 방향만 보내고 **회의별 값·제목·id 는 안
보냅니다** — 테스트가 눈에 띄는 제목을 심어 응답에 없는 것을 잽니다.
분석된(`needs_review`·`confirmed`) 회의만 세고 — 나머지를 넣으면 못 잰
회의가 "구간 0건" 으로 잡힙니다(측정 불가 ≠ 0) — 3개는 쌓여야 재며, 그
전에는 흙빛으로 이유를 말합니다. 문장은 화면 쪽 `lib/analytics/trends.ts`
가 만들고 "좋아졌다/나빠졌다" 는 쓰지 않습니다 — 줄었다·늘었다는 관찰이고
좋다·나쁘다는 판정입니다(불변식 4).

### 계획에서 뺀 것 — 2026-08-18 팀 결정으로 확정

만들면 제품이 나빠진다고 봐서 **계획에서 지웠습니다.** 되살리려면 이
결정부터 뒤집어야 합니다.

- `AI-REVIEW-002` 장시간 미결정 — 관찰이 아니라 평가 (§4)
- 제안서 §4.5 재배정 제안 — 사람에 대한 판정 (§2)
- **모바일 전부** — 2026-08-13 에 셸을 걷어냈고, 이번에 계획에서도
  지웠습니다 (§2)

### 남겨 둔 선택지 — 하려면 온전한 한 벌로

`CHAT-006·007`(업로드 — 저장·인증 내려받기·MIME·상한이 한 벌로 필요하고
하나만 빠져도 주소만 알면 열리는 통로) · `PROJECT-002` 이메일 초대(발송
경로 없음 + 사용자 열거 노출을 같이 설계해야 함). 팀이 하기로 정하면
만들 수 있는 것들이고, 반쯤 만들어 두지는 않습니다.
