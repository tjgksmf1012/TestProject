/**
 * 화면에 **글자 그대로** 나갈 한 줄로 (결함 262).
 *
 * ## ⛔ 마크다운 표시가 화면까지 새어 나왔습니다
 *
 * 이 저장소의 문구에는 강조(`**…**`)와 코드 표시(`` `…` ``)가 섞여 있습니다.
 * 그럴 만한 이유가 있습니다 — **같은 문장이 보고서로도 나갑니다.**
 * `reports/period.py` 의 「**확정된 기여도가 아닙니다.**」는 마크다운
 * 보고서에서 진짜 굵은 글씨가 됩니다. 그러니 문구에서 표시를 걷어내면
 * 보고서 쪽이 상합니다.
 *
 * 걷어낼 자리는 **화면**입니다. 그런데 그 일을 하는 코드가 세 벌 있었고
 * (`Why` · 레거시 프로젝트 · 레거시 검토) 셋 다 `**` 만 지우고 백틱은
 * 그대로 뒀습니다. 재 봤습니다 — 설정 화면에 이렇게 떠 있었습니다.
 *
 *     서버 관리자에게 `GITHUB_WEBHOOK_SECRET` 설정을 요청하세요.
 *     위 칸에 `owner/repo` 형식으로 적고 저장하세요.
 *
 * 한국어 문장 안의 백틱은 읽는 사람에게 아무 뜻이 없습니다. 그냥 **깨진
 * 글자**로 보입니다.
 *
 * ⚠️ 표시만 걷고 **낱말은 그대로 둡니다.** 요약하지 않습니다 — 이 저장소가
 * 「사유는 원문 그대로」를 지켜 온 이유와 같습니다.
 */
export function plainText(line: string): string {
  return line.replace(/\*\*/g, '').replace(/`/g, '');
}
